---
phase: 11.5-ontology-relations
plan: 01
subsystem: types
tags: [rust, typescript, serde, react-query, ontology, knowledge-graph]

# Dependency graph
requires:
  - phase: 11-entity-driven-exploration
    provides: CanonicalEntity struct, EntityPageData pattern, queryKeys factory pattern
provides:
  - Rust Triple / TripleWithEntities / RelationsPageData / OwnershipPageData / AssetType / PredicateSubjectPair / PredicateObjectPair structs
  - PREDICATE_VOCABULARY (21-token closed set), AUTO_INVERSE_PAIRS, SYMMETRIC_PREDICATES constants
  - PASS3_TARGET_VERSION = 3.5 constant + is_valid_predicate() helper
  - TS mirrors of all Rust types with camelCase parity
  - queryKeys.entityRelations / entityOwnership / entityRelatedTo / triplesByPredicateObject / triplesBySubjectPredicate
affects: [11.5-02-pass3-relation-extractor, 11.5-03-triple-store, 11.5-04-ipc-surface, 11.5-05-backfill-integration, 11.5-06-hooks-and-ui]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Closed vocabulary enforced identically in Rust const slice + TS const-as-const tuple, unit-tested for count/membership parity"
    - "Directional predicate auto-inversion table (AUTO_INVERSE_PAIRS) separate from self-inverse SYMMETRIC_PREDICATES set"
    - "AssetType enum serialized PascalCase (distinct from the rest of the codebase's camelCase field convention) to match a plain string union on the TS side"

key-files:
  created: []
  modified:
    - src-tauri/src/types.rs
    - client/lib/types.ts
    - client/hooks/useTauri.ts

key-decisions:
  - "PREDICATE_VOCABULARY has 21 tokens (15 from D-01's semantic groups + owned_by/sold_to reverse pair + mentioned_with weak default + issued_by/dated/signed_by legal group), matching the plan's literal task 1 spec exactly"
  - "Triple.doc_ids and RelationsPageData.outgoing/incoming get #[serde(default)] for forward-compat with future schema growth, per existing repo convention for Vec fields"
  - "AssetType uses #[serde(rename_all = \"PascalCase\")] on the Rust enum and a plain TS string union (not a const array) since AssetType is a fixed 6-value taxonomy, not an LLM-controlled vocabulary"

patterns-established:
  - "Contract-first plan: types-only plan with zero behavior change, unblocking parallel downstream plan development (Pass 3 extractor, TripleStore, IPC surface, hooks/UI)"

requirements-completed: [ONTO-01, ONTO-02, ONTO-03, ONTO-04, ONTO-05]

# Metrics
duration: 12min
completed: 2026-07-09
---

# Phase 11.5 Plan 01: Ontology/Relation Extraction Type Foundation Summary

**Rust + TypeScript contract layer for Phase 11.5 knowledge-graph triples: Triple/RelationsPageData/OwnershipPageData structs, a 21-token closed predicate vocabulary with auto-inverse pairs, and 5 new React Query key factories — zero behavior change, unblocks parallel downstream plans.**

## Performance

- **Duration:** ~12 min
- **Completed:** 2026-07-09
- **Tasks:** 3/3 completed
- **Files modified:** 3

## Accomplishments
- Rust `types.rs` extended with `Triple`, `TripleWithEntities`, `RelationsPageData`, `OwnershipPageData`, `AssetType`, `PredicateSubjectPair`, `PredicateObjectPair` — all additive, no existing struct touched
- Closed 21-predicate vocabulary (`PREDICATE_VOCABULARY`) plus `AUTO_INVERSE_PAIRS` (8 directional pairs) and `SYMMETRIC_PREDICATES` (3 self-inverse predicates) declared as Rust consts with a `is_valid_predicate()` helper
- `PASS3_TARGET_VERSION: f32 = 3.5` declared alongside the pre-existing `TWO_PASS_TARGET_VERSION` (3.0) — no replacement, additive per D-22
- TypeScript mirrors added to `client/lib/types.ts`: `Predicate` literal union, `PREDICATE_VOCABULARY` const tuple, `AssetType` PascalCase string union, and matching interfaces for all new Rust structs
- 5 new `queryKeys` factory entries added to `client/hooks/useTauri.ts` (`entityRelations`, `entityOwnership`, `entityRelatedTo`, `triplesByPredicateObject`, `triplesBySubjectPredicate`) — no existing entries touched, no hooks implemented yet (deferred to Plan 11.5-06)

## Task Commits

Each task was committed atomically:

1. **Task 1: Rust type foundation** - `a487b59` (feat)
2. **Task 2: TypeScript mirrors** - `43a35e6` (feat)
3. **Task 3: queryKeys entries** - `5f1a3c7` (feat)

_Note: no TDD tasks in this plan — all `type="auto"` per PLAN.md frontmatter._

## Files Created/Modified
- `src-tauri/src/types.rs` - Added Phase 11.5 section: Triple/TripleWithEntities/RelationsPageData/OwnershipPageData/AssetType/PredicateSubjectPair/PredicateObjectPair structs, PREDICATE_VOCABULARY/AUTO_INVERSE_PAIRS/SYMMETRIC_PREDICATES consts, PASS3_TARGET_VERSION, is_valid_predicate(), and 9 new unit tests
- `client/lib/types.ts` - Added Phase 11.5 section: Predicate/AssetType/Triple/TripleWithEntities/RelationsPageData/OwnershipPageData/PredicateSubjectPair/PredicateObjectPair TS types mirroring the Rust structs
- `client/hooks/useTauri.ts` - Added 5 queryKeys factory functions under a new "Phase 11.5" comment block

## Decisions Made
- Followed the plan's literal 21-predicate list verbatim (owns, owned_by, purchased_from, sold_to, located_in, part_of, address_of, married_to, parent_of, child_of, member_of, employer_of, employee_of, partner_of, issued_by, dated, signed_by, uses_pan, uses_aadhaar, has_voter_id, mentioned_with) — this reconciles the CONTEXT.md's "15-predicate" framing (D-01) with the PLAN.md's explicit 21-item enumeration; the PLAN.md list is authoritative since ownership reverse (owned_by), legal group (issued_by/dated/signed_by), and mentioned_with are each separately counted
- Used `#[serde(rename_all = "PascalCase")]` on the Rust `AssetType` enum (diverging from the file's otherwise-universal camelCase convention) specifically because AssetType is a fixed 6-value taxonomy meant to render as UI section headers ("Property", "Vehicle", ...) rather than a wire-protocol field name

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. `cargo check --lib`, `cargo test --lib types::` (35/35 passing including 9 new Phase 11.5 tests), and `tsc --noEmit` all passed clean on first attempt.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Type contracts are frozen: Plan 11.5-02 (Pass 3 Relation Extractor), 11.5-03 (TripleStore), 11.5-04 (IPC surface), and 11.5-06 (hooks/UI) can now develop in parallel against `Triple`, `PREDICATE_VOCABULARY`, `AUTO_INVERSE_PAIRS`, `RelationsPageData`, and `OwnershipPageData` without further struct-shape negotiation
- No blockers. `PASS3_TARGET_VERSION` and `TWO_PASS_TARGET_VERSION` coexist correctly per D-22's staged upgrade semantics (2.5 → 3.0 → 3.5)

---
*Phase: 11.5-ontology-relations*
*Completed: 2026-07-09*

## Self-Check: PASSED

All created/modified files verified present on disk; all 3 task commit hashes (a487b59, 43a35e6, 5f1a3c7) verified present in git log.

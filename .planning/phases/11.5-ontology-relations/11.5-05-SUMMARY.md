---
phase: 11.5-ontology-relations
plan: 05
subsystem: api
tags: [tauri, ipc, rust, knowledge-graph, ontology]

# Dependency graph
requires:
  - phase: 11.5-ontology-relations plan 02
    provides: TripleStore (graph/triple_store.rs) with forward/reverse/entity_touches indices
  - phase: 11.5-ontology-relations plan 04
    provides: AppState.triple_store wiring + backfill call site (pre-existing deviation, verified not re-done)
provides:
  - 7 IPC commands exposing TripleStore reads/mutations to the frontend
  - Pure asset_type_from_signal classifier (Property/Vehicle/Investment/Business/Financial/Other)
affects: [11.5-06-frontend-relations-ui, ownership-page, entity-detail-relations-panel]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "IPC commands acquire tokio::sync::Mutex<TripleStore> via blocking_lock() inside spawn_blocking, mirroring saved_search_store pattern"
    - "Mutating commands (add_manual_triple, delete_triple) persist to disk immediately inside the same spawn_blocking closure that holds the lock"
    - "Pure classifier function (asset_type_from_signal) separated from engine-dependent lookup (classify_asset_type) for unit testability without mocking CortexEngine"

key-files:
  created:
    - src-tauri/src/commands/relations.rs
  modified:
    - src-tauri/src/commands/mod.rs
    - src-tauri/src/lib.rs

key-decisions:
  - "AppState.triple_store and lib.rs setup/backfill wiring were already present from 11.5-04's documented deviation — verified via grep/read rather than re-applied, avoiding duplicate field errors"
  - "classify_asset_type takes entity_store as a parameter (not just engine) so identifier subclass lookup can scan aliases against extracted_entities metadata without re-locking entity_store"
  - "get_all_owned_by re-scans get_by_entity(person_id) per object to find the matching Triple rather than scanning triple_store.all() — keeps the O(assets) loop bounded by the entity_touches index"

requirements-completed: [ONTO-02, ONTO-03, ONTO-04, ONTO-05]

# Metrics
duration: 25min
completed: 2026-07-09
---

# Phase 11.5 Plan 05: Relation IPC Commands Summary

**7 Tauri IPC commands (get_entity_relations, get_all_owned_by, get_all_related_to, get_subjects_by_predicate_object, get_objects_by_subject_predicate, add_manual_triple, delete_triple) plus a pure AssetType classifier, all registered in lib.rs invoke_handler**

## Performance

- **Duration:** 25 min
- **Started:** 2026-07-09T09:07:00Z
- **Completed:** 2026-07-09T09:32:19Z
- **Tasks:** 2 of 3 planned (Task 1 and Task 3 were already complete from 11.5-04's documented deviation)
- **Files modified:** 3

## Accomplishments
- Created `commands/relations.rs` with all 7 IPC commands, each acquiring `state.triple_store` (tokio Mutex) and `state.entity_store` (std Mutex) inside `spawn_blocking`
- Implemented `asset_type_from_signal` — a pure, unit-testable classifier for Property/Vehicle/Investment/Business/Financial/Other per D-14/D-18, with `classify_asset_type` as the engine-aware wrapper that resolves doc topic + identifier subclass hints
- Registered `pub mod relations;` in `commands/mod.rs` and all 7 commands in `lib.rs`'s `invoke_handler(tauri::generate_handler![...])`
- Verified (did not re-do) that `AppState.triple_store`, `TripleStore::load` in setup, and the `trigger_entity_backfill` call site were already wired by the 11.5-04 executor as a documented deviation

## Task Commits

1. **Task 2: Implement 7 IPC commands in commands/relations.rs** — `925c1ca` (feat)

_Task 1 (AppState + lib.rs setup) and Task 3 (backfill call site) were no-ops — both were already complete on disk before this plan started, confirmed via `Read` of `state.rs`, `lib.rs`, and `entities.rs` prior to any edits._

**Plan metadata:** (this commit, follows)

## Files Created/Modified
- `src-tauri/src/commands/relations.rs` — 7 IPC commands + `expand_triple`, `classify_asset_type`, `asset_type_from_signal`, `lookup_doc_topic`, `lookup_identifier_subclass` helpers, plus 9 classifier unit tests
- `src-tauri/src/commands/mod.rs` — added `pub mod relations;`
- `src-tauri/src/lib.rs` — registered 7 `crate::commands::relations::*` entries in `invoke_handler`

## Decisions Made
- Confirmed via direct file reads (not assumption) that Task 1 and Task 3 outputs already existed before touching any files — avoided duplicate `triple_store` field / duplicate backfill-arg errors that would have resulted from blindly re-applying the plan's Task 1/3 diffs.
- `classify_asset_type` reads doc topic from `triple.doc_ids[0]` via `documents_384` collection metadata, and for `identifier`-type objects additionally scans the object's aliases against `extracted_entities[].subclass` in any of its docs to recover a subclass hint (bank_account/iban/policy_number/folio_number/credit_card → Financial).
- `get_subjects_by_predicate_object` / `get_objects_by_subject_predicate` validate the predicate via `is_valid_predicate` up front and return `AppError::Internal` (not `NotFound`) on an invalid predicate, matching the plan's "BadRequest if invalid" intent (AppError has no dedicated BadRequest variant, so Internal is used per existing codebase convention — see `add_manual_triple`'s subject==object check which also uses Internal).

## Deviations from Plan

None beyond what 11.5-04 already documented. Task 1 (AppState + lib.rs setup wiring) and Task 3 (`trigger_entity_backfill` call site) were verified pre-existing exactly as the plan's `must_haves.truths` describe — no code changes were needed for those two tasks, only verification. Task 2 (the 7 IPC commands) was implemented fresh per the plan's exact specification.

## Issues Encountered
None — `cargo check --lib`, `cargo build --lib`, and `cargo test --lib` all passed clean on the first attempt after Task 2 implementation.

## User Setup Required
None — no external service configuration required.

## Next Phase Readiness
- Backend IPC surface for relations is fully wired; the frontend (Plan 06+) can now call `get_entity_relations`, `get_all_owned_by`, etc. via `@tauri-apps/api/core` `invoke()`.
- `OwnershipPageData`, `RelationsPageData`, `PredicateSubjectPair`, `PredicateObjectPair` types (already defined in `types.rs` by Plan 01) are the exact return shapes these commands produce — no type drift.
- No blockers. `TripleStore` is currently empty until Pass 3 (relation extraction, prior plan in this phase) populates it via backfill — `get_entity_relations` etc. will return empty collections until then, which is expected and does not indicate a bug.

---
*Phase: 11.5-ontology-relations*
*Completed: 2026-07-09*

## Self-Check: PASSED

- FOUND: src-tauri/src/commands/relations.rs
- FOUND: .planning/phases/11.5-ontology-relations/11.5-05-SUMMARY.md
- FOUND: commit 925c1ca

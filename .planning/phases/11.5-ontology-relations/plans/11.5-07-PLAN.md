---
phase: 11.5-ontology-relations
plan: 07
type: execute
wave: 6
depends_on: ["11.5-06"]
files_modified:
  - client/pages/EntityDetailPage11.tsx
  - client/pages/OwnershipPage.tsx
  - client/App.tsx
  - client/components/relations/EntityRelationsPanel.tsx
  - client/components/relations/OwnershipAssetSection.tsx
  - client/components/relations/DeleteTripleButton.tsx
autonomous: true
requirements: [ONTO-03, ONTO-04, ONTO-05]

must_haves:
  truths:
    - "EntityDetailPage11 renders a Relations section below the Documents section"
    - "Relations section shows outgoing + incoming triples with target entity chips + doc count badges"
    - "Clicking a target entity chip navigates to that entity's detail page"
    - "/ownership/:personId route renders OwnershipPage"
    - "OwnershipPage renders 5 asset-type sections: Property, Vehicle, Investment, Business, Financial"
    - "Empty asset sections are hidden (only sections with assets render)"
    - "DeleteTripleButton is present on every triple row and triggers useDeleteTriple with confirmation"
    - "Loading + error + empty states match existing EntityDetailPage11 patterns"
  artifacts:
    - path: "client/pages/EntityDetailPage11.tsx"
      provides: "Relations section injected below Documents"
      contains: "EntityRelationsPanel"
    - path: "client/pages/OwnershipPage.tsx"
      provides: "New route component for /ownership/:personId"
      contains: "export default function OwnershipPage"
    - path: "client/App.tsx"
      provides: "New <Route path='/ownership/:personId' element={<OwnershipPage />} />"
      contains: "/ownership/:personId"
    - path: "client/components/relations/EntityRelationsPanel.tsx"
      provides: "Reusable Relations panel component"
      contains: "export function EntityRelationsPanel"
    - path: "client/components/relations/OwnershipAssetSection.tsx"
      provides: "One asset-type section rendered as a card grid"
      contains: "export function OwnershipAssetSection"
    - path: "client/components/relations/DeleteTripleButton.tsx"
      provides: "Confirm-then-delete button component"
      contains: "export function DeleteTripleButton"
  key_links:
    - from: "client/pages/EntityDetailPage11.tsx"
      to: "client/components/relations/EntityRelationsPanel.tsx"
      via: "React import + JSX composition"
      pattern: "<EntityRelationsPanel"
    - from: "client/pages/OwnershipPage.tsx"
      to: "client/hooks/useTauri.ts"
      via: "useAllOwnedBy hook"
      pattern: "useAllOwnedBy"
    - from: "client/App.tsx"
      to: "client/pages/OwnershipPage.tsx"
      via: "React Router route registration"
      pattern: "OwnershipPage"
---

<objective>
Build the frontend surface for Phase 11.5:
1. Extend `EntityDetailPage11` with a new Relations section below the existing Documents section.
2. Create a new `/ownership/:personId` route rendering an `OwnershipPage` with asset-type sections (Property, Vehicle, Investment, Business, Financial).
3. Extract three reusable components: `EntityRelationsPanel`, `OwnershipAssetSection`, `DeleteTripleButton`.

Purpose: Users see triples as first-class UI. Relations panel gives per-entity view; Ownership page gives "all my assets" view — the two primary user stories from the CONTEXT.md real-corpus test.
Output: Two new pages/routes + three reusable components. Sidebar integration lives in Plan 08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.5-ontology-relations/11.5-CONTEXT.md
@.planning/phases/11.5-ontology-relations/plans/11.5-06-PLAN.md
@client/pages/EntityDetailPage11.tsx
@client/App.tsx
@client/components/entities/EntityChip.tsx
@client/hooks/useTauri.ts

<interfaces>
Existing entity chip (`client/components/entities/EntityChip.tsx`) — supports dual navigation:
- Left click → `/search?entity={class}:{value}` (filter search)
- Right click → `/entity/{class}/{value}` (entity page)

`EntityChip` accepts a canonical-entity-like prop with `value` + `entityType` + optional `class`. For Phase 11.5, we can construct chips from `CanonicalEntity` by passing `{ value: canonical.canonicalName, entityType: canonical.entityType, class: capitalize(canonical.entityType) }`.

Existing page structure `EntityDetailPage11.tsx`:
```
Header (class icon + name + alias badge + doc count)
Aliases section (if aliases.length > 1)
Documents section (list + pagination)
Co-occurring Entities section (chips)
```
Injection point for Relations panel: between "Documents section" and "Co-occurring Entities section" (both are `space-y-3` children of the outer `space-y-8` wrapper).

`totalDocumentCount === 0` currently short-circuits to `<EmptyState />`. Relations panel should render even when totalDocumentCount is 0, because manual triples may exist for an entity that has no docs. Revise the empty-state gate: only short-circuit when BOTH totalDocumentCount === 0 AND relations data has no outgoing + no incoming. Otherwise render Relations panel above the empty-docs message.

Existing routing (`client/App.tsx`):
```tsx
<Route path="/entity/:class/:value" element={<EntityDetailPage11 />} />
```
Add: `<Route path="/ownership/:personId" element={<OwnershipPage />} />`

Type map (from Plan 01, client/lib/types.ts):
- `Triple, TripleWithEntities, RelationsPageData, OwnershipPageData, AssetType, CanonicalEntity`

Hooks (Plan 06):
- `useEntityRelations(entityId)` → RelationsPageData
- `useAllOwnedBy(personId)` → OwnershipPageData
- `useAddManualTriple` mutation
- `useDeleteTriple` mutation

Sonner toast + shadcn Dialog primitives already available (`@/components/ui/alert-dialog`, `sonner`). Follow existing Phase 11 confirm-dialog pattern from Plan 07's `SaveSearchDialog`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DeleteTripleButton + EntityRelationsPanel components</name>
  <files>client/components/relations/DeleteTripleButton.tsx, client/components/relations/EntityRelationsPanel.tsx</files>
  <action>
Create directory `client/components/relations/` if it does not exist.

**File 1: `client/components/relations/DeleteTripleButton.tsx`**

Export `interface DeleteTripleButtonProps { tripleId: string; affectedEntityIds: string[]; label?: string; }`.

Export `function DeleteTripleButton(props: DeleteTripleButtonProps): JSX.Element`.

Behavior:
- Renders a small ghost `X` icon (Lucide `Trash2`, size 14, className `text-text-tertiary hover:text-red-400`) — matches the compact style of existing chip-adjacent buttons.
- Click → open `AlertDialog` from `@/components/ui/alert-dialog` with:
  - Title: "Delete this relation?"
  - Description: "The relation will be removed from Cortex. If it was auto-inverted (e.g. `owns`/`owned_by`), the paired relation is also deleted. This cannot be undone."
  - Cancel button + destructive Delete button.
- On Delete confirm: call `useDeleteTriple` mutation with `{ tripleId, affectedEntityIds }`. On success show `toast.success("Relation deleted")`. On error show `toast.error(err.message)`.
- Disable the trigger button while the mutation is pending.

Layout: inline flex, no wrapper.

**File 2: `client/components/relations/EntityRelationsPanel.tsx`**

Export `interface EntityRelationsPanelProps { entityId: string; }`.

Export `function EntityRelationsPanel({ entityId }: EntityRelationsPanelProps): JSX.Element | null`.

Behavior:
- Call `useEntityRelations(entityId)` — pull loading/error/data.
- Loading: render a compact skeleton (2-row Skeleton, `space-y-2 animate-pulse`). Not a full-page LoadingSkeleton — the parent page already handles that.
- Error: silent (return `null`) so the outer page's error state is authoritative; log to console.
- If `outgoing.length + incoming.length === 0`: return `null` (do not render the section header).
- Otherwise render:
  - Section header `<h2 className="text-lg font-semibold text-text-primary">Relations ({outgoing.length + incoming.length})</h2>`.
  - Two subsections (skip a subsection when empty):
    - "Outgoing" — every row is a horizontal flex line:
      `<EntityChip subject/> → <span class="text-text-tertiary text-sm">{predicate}</span> → <EntityChip object/>` followed by a doc count badge `<Badge variant="outline" class="text-xs">{docIds.length} {docIds.length === 1 ? "doc" : "docs"}</Badge>` and a `<DeleteTripleButton tripleId={t.triple.id} affectedEntityIds={[t.triple.subjectId, t.triple.objectId]} />`.
    - "Incoming" — same layout mirrored (`<EntityChip subject/> → predicate → <EntityChip object=this entity/>`).
  - Predicate rendering: use `predicate` as-is but replace `_` with space via a tiny `formatPredicate(p: string): string` helper (e.g. "owned_by" → "owned by"). Add a static color hint per predicate group (property/personal/legal/identity) — keep it monochrome for now: `text-accent-primary` for the arrow separator, `text-text-secondary` for the predicate word.
  - User-added indicator: if `triple.userAdded === true`, add a `<Badge variant="secondary" class="text-xs">manual</Badge>` next to the predicate word.
- Wrapping outer div: `<section className="space-y-3" data-testid="entity-relations-section">`.

Do not add pagination — v1 renders all relations for the entity (bounded by entity's doc count in practice).
  </action>
  <verify>
    <automated>cd client && bunx tsc --noEmit --project ../tsconfig.json 2>&1 | grep -E "(DeleteTripleButton|EntityRelationsPanel|error TS)" | head -10 || echo "Task 1 components compile"</automated>
  </verify>
  <done>Both components compile as named exports; TS clean; DeleteTripleButton uses AlertDialog + useDeleteTriple; EntityRelationsPanel renders outgoing/incoming with chips + predicate + doc count + delete button.</done>
</task>

<task type="auto">
  <name>Task 2: EntityDetailPage11 integration — add Relations section between Documents and Co-occurring</name>
  <files>client/pages/EntityDetailPage11.tsx</files>
  <action>
Modify existing `client/pages/EntityDetailPage11.tsx`.

Import: `import { EntityRelationsPanel } from "@/components/relations/EntityRelationsPanel";`

Modify the empty-docs gate at line ~268:
```typescript
// BEFORE
if (totalDocumentCount === 0) {
  return <EmptyState decodedClass={decodedClass} decodedValue={decodedValue} />;
}

// AFTER
// If no docs AND no relations, show empty state. Otherwise render Relations panel above.
// canonical.id is stable and drives the useEntityRelations query — pass it in.
```
Do NOT short-circuit anymore. Instead, if `totalDocumentCount === 0` AND we detect no relations, keep the empty state. Cheapest path: keep the `EmptyState` render but conditionally include Relations above it — actually the simpler approach is: always fall through to the main render, and inside the main render, replace the Documents "list of doc rows" with either the list OR an inline empty-docs subheader when count is 0. Then Relations panel appears below regardless.

Concretely:
- Delete the early `if (totalDocumentCount === 0) return <EmptyState .../>;` return.
- Inside the main return JSX, in the "Documents section" block, wrap the doc list rendering with a conditional:
  ```tsx
  {documents.length > 0 ? (
    <div className="space-y-2">
      {documents.map((doc) => ( /* existing Link row */ ))}
    </div>
  ) : (
    <p className="text-sm text-text-tertiary py-2">No documents mention this entity yet.</p>
  )}
  ```
- After the Documents section closing `</div>`, and BEFORE the Co-occurring entities section, add:
  ```tsx
  <EntityRelationsPanel entityId={canonical.id} />
  ```
  (`canonical.id` is the CanonicalEntity id from `data.canonical`. Note EntityRelationsPanel returns `null` when no relations exist, so no need for a wrapping conditional.)

Do NOT remove or alter any other section (header, aliases, co-occurring). Do NOT change the pagination logic. Do NOT change `useEntityPageData` call.

If `totalDocumentCount === 0` AND EntityRelationsPanel would return null AND there are no aliases AND no co-occurring entities, the page will visually show just the header + "No documents mention this entity yet." That's an acceptable degraded state — the original `EmptyState` component with "Try syncing your folders" copy is more informative, so we KEEP the EmptyState for the true-nothing case by wrapping the whole return in:
```typescript
const hasNothing =
  totalDocumentCount === 0 &&
  (!aliases || aliases.length <= 1) &&
  (!coOccurringEntities || coOccurringEntities.length === 0);
// EntityRelationsPanel handles its own zero-state (returns null), so we don't
// have relations info at this level; that's fine — if there ARE relations,
// the page just shows them under the "No documents" hint. If there are none,
// nothing else is rendered and hasNothing is a strong signal to show the
// original guidance empty state.
if (hasNothing) {
  return <EmptyState decodedClass={decodedClass} decodedValue={decodedValue} />;
}
```

Preserve all existing behaviour (aliases, pagination, co-occurring chips).
  </action>
  <verify>
    <automated>cd client && bunx tsc --noEmit --project ../tsconfig.json 2>&1 | grep -E "(EntityDetailPage11|error TS)" | head -10 || echo "Task 2 page compiles"</automated>
  </verify>
  <done>EntityDetailPage11 renders Relations section between Documents and Co-occurring; `EmptyState` still shows when there's truly nothing to display; existing sections unchanged; TS clean.</done>
</task>

<task type="auto">
  <name>Task 3: OwnershipAssetSection + OwnershipPage + route registration</name>
  <files>client/components/relations/OwnershipAssetSection.tsx, client/pages/OwnershipPage.tsx, client/App.tsx</files>
  <action>
**File 1: `client/components/relations/OwnershipAssetSection.tsx`**

Export `interface OwnershipAssetSectionProps { assetType: AssetType; assets: TripleWithEntities[]; }`.

Export `function OwnershipAssetSection(props): JSX.Element | null`.

Behavior:
- If `assets.length === 0` → return `null` (empty sections are hidden per must_haves).
- Otherwise:
  - Section header with icon + label + count:
    - `Property` → `Home` icon (Lucide)
    - `Vehicle` → `Car`
    - `Investment` → `TrendingUp`
    - `Business` → `Briefcase`
    - `Financial` → `Landmark`
    - `Other` → `Package`
  - Header format: `<h3 class="text-md font-semibold flex items-center gap-2 text-text-primary"><Icon size={18}/>{label} ({count})</h3>`.
  - Body: a responsive grid `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3`. Each cell is a "SpaceCard-style" asset card:
    - Rounded card `border rounded-lg p-4 hover:bg-bg-secondary transition-colors`.
    - Top row: object canonical name (bold), subtitle showing `object.entityType`.
    - Middle: source doc count badge, `manual` badge if `triple.userAdded`.
    - Bottom: clickable link to `/entity/{object.entityType}/{encodeURIComponent(object.canonicalName)}` — reuses Phase 11 entity page routing.
    - Absolute top-right: `<DeleteTripleButton tripleId={t.triple.id} affectedEntityIds={[t.triple.subjectId, t.triple.objectId]} />`.
  - Wrapping section element with `data-testid={`ownership-section-${assetType.toLowerCase()}`}`.

**File 2: `client/pages/OwnershipPage.tsx`**

Default export `function OwnershipPage(): JSX.Element`.

Behavior:
- Read `personId` param from `useParams`.
- Call `useAllOwnedBy(personId)`.
- Loading state: mirror EntityDetailPage11's LoadingSkeleton pattern (header skeleton + 2 rows).
- Error state: mirror ErrorState with retry.
- Empty (person exists but no assets): render an EmptyState-shaped panel: title "No owned assets yet for {person.canonicalName}", subtitle "Re-run entity extraction from Settings → AI to discover ownership from your documents." with links to `/settings` (Re-extract) and `/watched`.
- Full render:
  - Header: person icon (Users), canonical_name, subtitle "{totalAssets} owned assets".
  - Render 6 `<OwnershipAssetSection>` in this order: Property, Vehicle, Investment, Business, Financial, Other. Each pulls its slice from `data.assetsByType[assetType] ?? []`. Empty sections silently omit per component's null-return.

Import `useAllOwnedBy` from `@/hooks/useTauri`; import `AssetType, TripleWithEntities` from `@/lib/types`.

**File 3: `client/App.tsx`**

Import `OwnershipPage from "./pages/OwnershipPage"`. Preserve existing imports.

Add inside the `<Route element={<AppShell />}>` block (place next to existing `/entity/:class/:value` route so entity-related routes cluster):
```tsx
<Route path="/ownership/:personId" element={<OwnershipPage />} />
```

Do NOT modify any other route registration.
  </action>
  <verify>
    <automated>cd client && bunx tsc --noEmit --project ../tsconfig.json 2>&1 | grep -E "(OwnershipPage|OwnershipAssetSection|App\.tsx|error TS)" | head -10 || echo "Task 3 components + route compile"</automated>
  </verify>
  <done>OwnershipAssetSection renders per-type cards; OwnershipPage composes 6 sections; route /ownership/:personId registered; TS clean; DeleteTripleButton reused across both surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click → mutation | Delete button triggers state-mutating IPC |
| URL params → route | :personId is user-supplied (from Sidebar or clipboard) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.5-24 | Elevation | Accidental delete of a real relation | mitigate | AlertDialog confirmation required; button disabled while pending; toast confirms outcome; user_added triples reappear on backfill (D-12 guarantee) — LLM-inserted triples do not |
| T-11.5-25 | Injection | Malicious `personId` URL crafted to hit unrelated entity | mitigate | Rust IPC validates entity existence; frontend URL params URL-decoded but never eval'd |
| T-11.5-26 | Availability | Page renders 1000+ owned assets → DOM freezes | accept | User-bounded; realistic personal corpora yield 5-50 assets; grid virtualization deferred to v1.2 |
</threat_model>

<verification>
- `cd client && bunx tsc --noEmit` — clean
- `grep -c "EntityRelationsPanel" client/pages/EntityDetailPage11.tsx` — returns 2+ (import + JSX)
- `grep -c "OwnershipPage" client/App.tsx` — returns 2+ (import + Route)
- `grep -c "AlertDialog" client/components/relations/DeleteTripleButton.tsx` — returns 1+
- `grep -c "OwnershipAssetSection" client/pages/OwnershipPage.tsx` — returns 1+ (rendered 6 times as component)
</verification>

<success_criteria>
- Users landing on `/entity/Person/Alex%20Shah` see a Relations section listing outgoing + incoming triples with entity chips and predicate labels.
- `/ownership/{personId}` renders asset sections grouped by AssetType with only non-empty sections visible.
- DeleteTripleButton is present on every triple row; confirms via AlertDialog; invalidates the appropriate queryKeys.
- Empty page states remain informative (existing EmptyState reused when doc + relations + aliases + co-occurring are all zero).
- No existing functionality regresses (existing Documents/pagination/co-occurring sections still work).
</success_criteria>

<output>
Create `.planning/phases/11.5-ontology-relations/11.5-07-SUMMARY.md` when done.
</output>

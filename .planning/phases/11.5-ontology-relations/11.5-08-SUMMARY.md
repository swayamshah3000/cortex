---
phase: 11.5-ontology-relations
plan: 08
subsystem: ui
tags: [react, react-query, lucide-react, sidebar, navigation]

# Dependency graph
requires:
  - phase: 11.5-ontology-relations
    provides: "/ownership/:personId route (Plan 11.5-07) and get_entities_by_type IPC (Phase 6)"
provides:
  - "useTopPersonId hook — derives highest-doc-count Person entity id"
  - "Sidebar 'Owned by me' quick link — discovery entry point for /ownership/:personId route"
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Derived hook pattern: wrap existing query hook (useEntitiesByType) + client-side sort/pick, no new IPC"

key-files:
  created: []
  modified:
    - client/hooks/useTauri.ts
    - client/components/layout/Sidebar.tsx

key-decisions:
  - "Placed Owned by me link below Saved Searches, above Bottom nav (personal-state cluster stays together, per D-19 planner discretion)"
  - "Used Wallet icon (not Home) to avoid visual confusion with Spaces section which already conveys 'home'"
  - "Link renders only when topPersonId is truthy — hidden entirely on fresh corpora with zero Person entities"

patterns-established: []

requirements-completed: [ONTO-04]

# Metrics
duration: 12min
completed: 2026-07-09
---

# Phase 11.5 Plan 08: Sidebar "Owned by me" Quick Link Summary

**Sidebar surfaces a one-click "Owned by me" link (Wallet icon) that routes to /ownership/{topPersonId}, derived via a new useTopPersonId hook wrapping the existing useEntitiesByType("person") query.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-07-09T09:46:13Z
- **Completed:** 2026-07-09T09:58:00Z
- **Tasks:** 2 of 3 (checkpoint deferred — see below)
- **Files modified:** 2

## Accomplishments
- Added `useTopPersonId()` to `client/hooks/useTauri.ts` — derives the top-document-count Person entity id from the existing `useEntitiesByType("person")` hook, defensively re-sorting client-side.
- Added the "Owned by me" quick link to `client/components/layout/Sidebar.tsx`, positioned between the Saved Searches section and the Bottom nav section, matching the existing `NavLink` visual/interaction pattern (active-state highlight, collapsed-sidebar tooltip).
- Link is conditionally rendered — invisible on a fresh corpus with zero Person entities, appears automatically once the first Person entity is indexed.

## Task Commits

Each task was committed atomically:

1. **Task 1: useTopPersonId helper hook** - `2ffecb2` (feat)
2. **Task 2: Sidebar "Owned by me" quick link** - `7bd5597` (feat)

**Plan metadata:** (this commit)

_Note: Task 3 (checkpoint:human-verify) was intentionally skipped per autonomous-mode instruction — deferred to real-corpus UAT rather than executed synchronously in this session._

## Files Created/Modified
- `client/hooks/useTauri.ts` - Added `useTopPersonId()` export (15 lines), no existing hooks modified.
- `client/components/layout/Sidebar.tsx` - Added `Wallet` + `useTopPersonId` imports, `topPersonId` hook call, and the conditional "Owned by me" `<Link>` block (39 lines net).

## Decisions Made
- Followed the plan's placement guidance exactly: below Saved Searches, above Bottom nav (`<div className="border-t...">`).
- Reused the exact `NavLink`-style JSX structure (active-state classes, collapsed tooltip pattern) instead of extending the shared `NavLink` component, to keep the conditional-render logic (`topPersonId &&`) localized and match the plan's provided code verbatim.

## Deviations from Plan

None — plan executed exactly as written for Tasks 1 and 2. `useEntitiesByType` already existed in `client/hooks/useTauri.ts` (confirmed via grep before implementing), so no fallback inline `useQuery` was needed.

**Checkpoint deviation (explicit, by instruction):** Task 3 (`checkpoint:human-verify`, gate="blocking") was skipped per the execution instruction: "Skip the human-verify checkpoint (autonomous mode — defer to real-corpus UAT)." This is a 21-step end-to-end verification requiring a live Tauri app, a real property-document corpus, and an active AI provider — none of which are available/appropriate to execute synchronously in this session. The checkpoint is deferred to a separate real-corpus UAT pass (see `.planning/phases/07-ai-provider-foundation/07-HUMAN-UAT.md` and `.planning/phases/06-knowledge-graph-and-native-integrations/06-UAT.md` for the project's existing UAT-deferral pattern).

## Issues Encountered
None.

## User Setup Required
None — no external service configuration required. (Real-corpus UAT setup — provider connection, watched folder, backfill trigger — is documented in the plan's checkpoint `how-to-verify` steps for the deferred UAT pass.)

## Next Phase Readiness

Frontend Phase 11.5 deliverables (hooks, Relations panel, /ownership route, Sidebar link) are all code-complete. What remains before Phase 11.5 can be marked fully verified:

1. **Real-corpus UAT** — the 21-step checkpoint from this plan (Relations panel, Ownership page, user override, auto-inverse) must be run against a live corpus (e.g. `~/private/docs/`) with an active AI provider connected and "Re-extract entities" triggered.
2. No code blockers — `tsc --noEmit` is clean across both modified files; `useTopPersonId` and the Sidebar link compile and are correctly wired (verified via grep cross-reference).

## Self-Check: PASSED

- FOUND: client/hooks/useTauri.ts contains `export function useTopPersonId` (grep -c = 1)
- FOUND: client/components/layout/Sidebar.tsx contains "Owned by me" (grep -c = 4, includes label + tooltip + comment mentions)
- FOUND: client/components/layout/Sidebar.tsx contains "/ownership/" (grep -c = 4, includes Link href + isActive check + comment mentions)
- FOUND: commit 2ffecb2 in git log
- FOUND: commit 7bd5597 in git log
- FOUND: `cd client && bunx tsc --noEmit --project ../tsconfig.json` — zero output, exit clean

---
*Phase: 11.5-ontology-relations*
*Completed: 2026-07-09*

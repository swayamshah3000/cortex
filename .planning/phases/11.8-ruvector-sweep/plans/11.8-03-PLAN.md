---
phase: 11.8-ruvector-sweep
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/11.8-ruvector-sweep/plans/11.8-03-AUDIT.md
autonomous: true
requirements:
  - RVSW-03
must_haves:
  truths:
    - "11.8-03-AUDIT.md exists documenting go/no-go verdict on ruvector-solver PageRank"
    - "Verdict confirms PageRank API accepts Vec<(subject_id, object_id, weight)> edge list AND returns Vec<(node_id, rank_score)>"
    - "If GO: damping factor + iteration count recommendation is written"
    - "If NO-GO: a fallback recommendation (keep doc_count as prominence signal) is written"
  artifacts:
    - path: ".planning/phases/11.8-ruvector-sweep/plans/11.8-03-AUDIT.md"
      provides: "ruvector-solver audit verdict + integration surface for Plan 08"
      contains: "## Verdict"
  key_links:
    - from: "11.8-03-AUDIT.md"
      to: "11.8-08-PLAN.md"
      via: "Plan 08 reads verdict + surface to implement or skip"
      pattern: "AUDIT"
---

<objective>
Audit `ruvector/crates/ruvector-solver/` to produce a go/no-go verdict for the entity PageRank integration (D-14/D-15/D-16). No implementation. Purely reading + writing 11.8-03-AUDIT.md.

Purpose: Following the Phase 9/12/13 lesson — ruvector crate names sometimes don't match cortex-relevant functionality. Audit gate MUST pass before Plan 08 (entity PageRank on TripleStore) writes any Rust code. Specifically: solver crates often expose iterative linear-algebra solvers, NOT PageRank; this audit confirms which primitives ship.

Output: `.planning/phases/11.8-ruvector-sweep/plans/11.8-03-AUDIT.md` with sections: Verdict, Fit-for-purpose analysis, Target API surface (if GO), Recommended damping + iterations (if GO), Missing capabilities (if NO-GO), Fallback recommendation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11.8-ruvector-sweep/11.8-CONTEXT.md
@.planning/phases/11.8-ruvector-sweep/plans/11.8-01-PLAN.md
@/Users/gshah/work/apps/experiments/ruvector/crates/ruvector-solver/README.md
@/Users/gshah/work/apps/experiments/ruvector/crates/ruvector-solver/src/lib.rs
@/Users/gshah/work/apps/cortex/src-tauri/src/graph/triple_store.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read ruvector-solver crate surface + confirm PageRank primitive</name>
  <files>(read-only)</files>
  <action>Read (a) `crates/ruvector-solver/README.md` — full read; capture what solvers are exposed (Krylov, GMRES, CG, PageRank?); (b) `crates/ruvector-solver/src/lib.rs` — module list; (c) enumerate public entry points with `rg "pub fn|pub struct" /Users/gshah/work/apps/experiments/ruvector/crates/ruvector-solver/src/`. Specifically search for `pagerank` (case-insensitive) with `rg -i "page.?rank"`. If a PageRank function exists, read its signature file end-to-end to confirm: (1) accepts sparse edge list `Vec<(usize, usize, f32)>` or similar; (2) accepts damping factor + max iterations; (3) returns `Vec<f32>` rank scores indexed by node; (4) any pre-processing required (edge normalization, ID → index mapping). If no explicit PageRank exists but a power-iteration eigensolver does, note whether it can be adapted (compose PageRank from power iteration + Google matrix). Do NOT modify anything.</action>
  <verify>
    <automated>test -d /Users/gshah/work/apps/experiments/ruvector/crates/ruvector-solver && rg -c "pub (async )?fn|pub struct" /Users/gshah/work/apps/experiments/ruvector/crates/ruvector-solver/src/ | tail -5</automated>
  </verify>
  <done>Notes captured (in-memory) covering: (1) whether a first-class PageRank function exists; (2) if yes, its exact signature; (3) if no, whether a power-iteration primitive can be composed into PageRank; (4) sparse graph representation expected (CSR? edge list?); (5) whether the ruvector-graph crate is a co-dependency for adjacency construction.</done>
</task>

<task type="auto">
  <name>Task 2: Write 11.8-03-AUDIT.md with verdict + integration surface + damping recommendation</name>
  <files>.planning/phases/11.8-ruvector-sweep/plans/11.8-03-AUDIT.md</files>
  <action>Write the audit file with these sections in order (matches D-14 checklist): 1) **Verdict** — one word (GO / NO-GO / GO-WITH-CAVEAT); 2) **Fit-for-purpose analysis** — bullet list: (a) crate exposes PageRank on user-supplied sparse graph? (b) accepts `Vec<(subject_id, object_id, weight)>` edge list (or planner can adapt via ID→index map)? (c) returns `Vec<(node_id, rank_score)>` (or adaptable equivalent)? (d) sublinear O(log n) is claimed (per D-14) — is this actually documented, or is it O(iterations × edges)? 3) If GO/GO-WITH-CAVEAT: **Target API surface** — write out the exact Rust snippet (10-20 lines) showing how Plan 08's `entity_pagerank::compute(triple_store) -> HashMap<String, f64>` would (i) extract edges from `TripleStore.forward_index`, (ii) build ID → index mapping, (iii) call the ruvector-solver PageRank, (iv) map indices back to entity_ids. Name the exact struct/method chain. 4) **Recommended damping factor + iterations** — a specific numeric recommendation with rationale (typical PageRank: damping 0.85, ~50-100 iterations; if the crate exposes convergence tolerance, name it). 5) **Recommended Cargo features**. 6) If NO-GO: **Missing capabilities** enumerated + **Fallback recommendation** — Plan 08 becomes a DEVIATION.md; Sidebar top-5 continues to use `doc_count DESC` (existing behavior); ontology panel prominence unchanged. This audit is the sole authority Plan 08 reads to decide GO / SKIP.</action>
  <verify>
    <automated>test -f .planning/phases/11.8-ruvector-sweep/plans/11.8-03-AUDIT.md && grep -c "^## Verdict" .planning/phases/11.8-ruvector-sweep/plans/11.8-03-AUDIT.md</automated>
  </verify>
  <done>11.8-03-AUDIT.md exists with `## Verdict` section containing exactly one of {GO, GO-WITH-CAVEAT, NO-GO}. If GO/GO-WITH-CAVEAT: `## Target API surface` shows Rust snippet + `## Recommended damping factor + iterations` names concrete values. If NO-GO: missing capabilities + fallback documented.</done>
</task>

</tasks>

<verification>
- 11.8-03-AUDIT.md exists at `.planning/phases/11.8-ruvector-sweep/plans/11.8-03-AUDIT.md`
- File contains `## Verdict` with GO / GO-WITH-CAVEAT / NO-GO
- If GO: `## Target API surface` + `## Recommended damping factor + iterations` present
- If NO-GO: `## Missing capabilities` + `## Fallback recommendation` present
</verification>

<success_criteria>
Plan 08 (entity PageRank on TripleStore) can be executed OR skipped based solely on reading 11.8-03-AUDIT.md. Damping factor + iteration count are pinned so Plan 08 doesn't relitigate.
</success_criteria>

<output>
Create `.planning/phases/11.8-ruvector-sweep/11.8-03-SUMMARY.md` when done, referencing the audit file + summarizing the verdict in one sentence.
</output>

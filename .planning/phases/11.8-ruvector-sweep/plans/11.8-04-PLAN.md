---
phase: 11.8-ruvector-sweep
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - src-tauri/src/types.rs
  - client/lib/types.ts
  - client/hooks/useTauri.ts
autonomous: true
requirements:
  - RVSW-04
must_haves:
  truths:
    - "New provider slug `local-ruvllm` is a recognized string constant in Rust (returned unchanged by normalize_provider_name) and known to the frontend types"
    - "EntityPageRank type exists in both Rust (types.rs) and TypeScript (client/lib/types.ts) with fields {entityId: String, rankScore: f64} (camelCase on the wire)"
    - "RerankConfig type exists in both Rust and TypeScript with fields {enabled: bool, mechanism: String} (camelCase on the wire)"
    - "queryKeys entries `entityPageRank` and `rerankConfig` exist in client/hooks/useTauri.ts"
    - "Everything compiles: `cargo check` passes and `bun run typecheck` (or equivalent) passes"
  artifacts:
    - path: "src-tauri/src/types.rs"
      provides: "EntityPageRank + RerankConfig Rust structs, provider slug constant"
      contains: "EntityPageRank"
    - path: "client/lib/types.ts"
      provides: "EntityPageRank + RerankConfig TypeScript interfaces"
      contains: "EntityPageRank"
    - path: "client/hooks/useTauri.ts"
      provides: "queryKeys.entityPageRank + queryKeys.rerankConfig entries"
      contains: "entityPageRank"
  key_links:
    - from: "src-tauri/src/types.rs"
      to: "client/lib/types.ts"
      via: "serde camelCase ↔ TypeScript interface mirror"
      pattern: "rename_all = \"camelCase\""
    - from: "client/lib/types.ts"
      to: "client/hooks/useTauri.ts"
      via: "queryKeys reference the new types"
      pattern: "queryKeys\\.entityPageRank"
---

<objective>
Add the Rust structs, TypeScript interfaces, and queryKeys entries required by every downstream implementation plan (06/07/08/10). No business logic — pure contract definition so Waves 3-5 execute without inventing shapes.

Purpose: Interface-first task ordering. Plans 06, 07, 08, 10 all consume `EntityPageRank`, `RerankConfig`, and the `local-ruvllm` provider slug. Defining them here in a small, isolated plan lets those plans skip codebase exploration and lets Wave 3 plans run in parallel without shape mismatches.

Output:
- `src-tauri/src/types.rs` — add `EntityPageRank`, `RerankConfig`, and `LOCAL_RUVLLM_PROVIDER` constant
- `client/lib/types.ts` — mirror the two types
- `client/hooks/useTauri.ts` — add `queryKeys.entityPageRank` + `queryKeys.rerankConfig` entries
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11.8-ruvector-sweep/11.8-CONTEXT.md
@src-tauri/src/types.rs
@client/lib/types.ts
@client/hooks/useTauri.ts
</context>

<interfaces>
<!-- Existing shape conventions this plan must mirror. Executor uses these directly — no exploration needed. -->

From src-tauri/src/types.rs (existing pattern for wire types):
```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct EntityClassFilter {
    pub class: String,
    pub value: String,
}
```

From client/hooks/useTauri.ts (existing queryKeys pattern):
```typescript
export const queryKeys = {
  // ...
  savedSearches: ["saved-searches"] as const,
  entityRelations: (entityId: string) => ["entity-relations", entityId] as const,
};
```

Provider name normalization (existing in src-tauri/src/ai/service.rs):
```rust
pub fn normalize_provider_name(name: &str) -> String {
    match name {
        "claude" | "anthropic" => "anthropic".to_string(),
        "chatgpt" | "openai" | "openai-codex" => "openai".to_string(),
        "gemini" | "google" => "gemini".to_string(),
        "ollama" => "ollama".to_string(),
        other => other.to_string(),  // ← new "local-ruvllm" falls through here
    }
}
```
NOTE: This plan does NOT modify service.rs. It only adds the CONSTANT so Plan 06 can reference `LOCAL_RUVLLM_PROVIDER` when it adds the routing branch.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Rust types (EntityPageRank, RerankConfig) + provider slug constant</name>
  <files>src-tauri/src/types.rs, src-tauri/src/types_test.rs (or inline #[cfg(test)] mod)</files>
  <behavior>
    - Test 1: `EntityPageRank { entity_id: "ent-1".into(), rank_score: 0.42 }` serializes to `{"entityId":"ent-1","rankScore":0.42}` (camelCase on wire per D-16 + existing serde convention)
    - Test 2: `RerankConfig { enabled: true, mechanism: "FlashAttention".into() }` serializes to `{"enabled":true,"mechanism":"FlashAttention"}`
    - Test 3: `LOCAL_RUVLLM_PROVIDER` constant equals the string `"local-ruvllm"` (matches D-03 slug exactly)
    - Test 4 (regression guard for Plan 06): `normalize_provider_name(LOCAL_RUVLLM_PROVIDER)` returns `"local-ruvllm"` unchanged (falls through the `other =>` arm) — proves Plan 06 can route on the raw slug before/after normalization
  </behavior>
  <action>Append to `src-tauri/src/types.rs` (do NOT modify existing types — only add): (a) a public constant `pub const LOCAL_RUVLLM_PROVIDER: &str = "local-ruvllm";` with a doc comment referencing D-03; (b) `EntityPageRank` struct with `#[derive(Debug, Clone, Serialize, Deserialize)]` and `#[serde(rename_all = "camelCase")]`, fields `pub entity_id: String` and `pub rank_score: f64`, doc comment referencing D-14/D-16; (c) `RerankConfig` struct with same derives + camelCase attr, fields `pub enabled: bool` and `pub mechanism: String`, doc comment referencing D-11/D-13 (default OFF, mechanism name comes from 11.8-02-AUDIT.md). Add `impl Default for RerankConfig` returning `{ enabled: false, mechanism: "cosine-only".into() }`. Add `#[cfg(test)] mod ruvector_sweep_types_tests` covering the four behaviors above using `serde_json::to_value` for shape assertions. Do NOT touch normalize_provider_name in ai/service.rs — Plan 06 owns that edit. This plan's Test 4 imports normalize_provider_name from `crate::ai::service` to prove the constant survives the existing fall-through path.</action>
  <verify>
    <automated>cd src-tauri && cargo test --lib ruvector_sweep_types_tests -- --nocapture</automated>
  </verify>
  <done>`cargo test --lib ruvector_sweep_types_tests` passes with 4 tests. `grep -c "EntityPageRank\|RerankConfig\|LOCAL_RUVLLM_PROVIDER" src-tauri/src/types.rs` returns ≥ 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mirror types to TypeScript + add queryKeys entries</name>
  <files>client/lib/types.ts, client/hooks/useTauri.ts, client/lib/types.test.ts</files>
  <behavior>
    - Test 1: `EntityPageRank` interface exists with fields `{ entityId: string; rankScore: number }` — compile-time check via `const _: EntityPageRank = { entityId: "x", rankScore: 0.5 }` in the test file
    - Test 2: `RerankConfig` interface exists with fields `{ enabled: boolean; mechanism: string }` — same compile-time pattern
    - Test 3: `queryKeys.entityPageRank` is a tuple `["entity-pagerank"] as const` (all-entities cache key) AND `queryKeys.entityPageRankFor(id)` returns `["entity-pagerank", id] as const` (per-entity key)
    - Test 4: `queryKeys.rerankConfig` is `["rerank-config"] as const`
  </behavior>
  <action>Append to `client/lib/types.ts` (do NOT modify existing types): (a) `export interface EntityPageRank { entityId: string; rankScore: number; }` with a JSDoc referencing D-14/D-16; (b) `export interface RerankConfig { enabled: boolean; mechanism: string; }` with JSDoc referencing D-11/D-13. Then in `client/hooks/useTauri.ts`, extend the existing `queryKeys` object literal (do NOT rewrite it — only add entries) with: `entityPageRank: ["entity-pagerank"] as const,`, `entityPageRankFor: (entityId: string) => ["entity-pagerank", entityId] as const,`, and `rerankConfig: ["rerank-config"] as const,`. Add the two type names to the existing import statement from `../lib/types` at the top of useTauri.ts (mirror how Triple / RelationsPageData were added by Phase 11.5). In `client/lib/types.test.ts` add the four compile-time / runtime assertions above.</action>
  <verify>
    <automated>cd /Users/gshah/work/apps/cortex && bun test client/lib/types.test.ts --run 2>&1 | tail -20</automated>
  </verify>
  <done>`bun test client/lib/types.test.ts` passes. `grep -c "EntityPageRank\|RerankConfig" client/lib/types.ts` returns ≥ 2. `grep -c "entityPageRank\|rerankConfig" client/hooks/useTauri.ts` returns ≥ 3.</done>
</task>

</tasks>

<verification>
- `cargo test --lib ruvector_sweep_types_tests` inside src-tauri passes
- `bun test client/lib/types.test.ts` passes
- `grep -v '^//' src-tauri/src/types.rs | grep -c "LOCAL_RUVLLM_PROVIDER"` ≥ 1
- `grep -v '^//' client/hooks/useTauri.ts | grep -c "entityPageRank"` ≥ 2 (the key + the fn variant)
- `cd src-tauri && cargo check` exits 0
</verification>

<success_criteria>
Plans 06, 07, 08, 10 can import `EntityPageRank`, `RerankConfig`, `LOCAL_RUVLLM_PROVIDER` and use `queryKeys.entityPageRank` / `queryKeys.rerankConfig` without inventing them. No shape drift possible between Rust and TypeScript because both are pinned to the same camelCase wire contract.
</success_criteria>

<output>
Create `.planning/phases/11.8-ruvector-sweep/11.8-04-SUMMARY.md` when done, listing the exact struct/interface names + queryKeys entries added, plus the file:line locations.
</output>

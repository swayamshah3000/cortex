---
phase: 11.8-ruvector-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/11.8-ruvector-sweep/plans/11.8-01-AUDIT.md
autonomous: true
requirements:
  - RVSW-01
must_haves:
  truths:
    - "11.8-01-AUDIT.md exists documenting go/no-go verdict on ruvllm"
    - "Verdict includes explicit target API surface identification (session/chat/generate call signature, streaming variant, backend init) OR explicit list of missing capabilities"
    - "If verdict is GO, the target Cargo feature flags for Apple Silicon are named"
    - "If verdict is NO-GO, a fallback recommendation (keep cloud providers only) is written"
  artifacts:
    - path: ".planning/phases/11.8-ruvector-sweep/plans/11.8-01-AUDIT.md"
      provides: "ruvllm audit verdict + integration surface for Plan 06"
      contains: "## Verdict"
  key_links:
    - from: "11.8-01-AUDIT.md"
      to: "11.8-06-PLAN.md"
      via: "Plan 06 reads verdict + surface to implement or skip"
      pattern: "AUDIT"
---

<objective>
Audit `ruvector/crates/ruvllm/` to produce a go/no-go verdict for the local LLM integration (D-01/D-02). No implementation. Purely reading + writing 11.8-01-AUDIT.md.

Purpose: Following the Phase 9/12/13 lesson — ruvector crate names sometimes don't match cortex-relevant functionality. Audit gate MUST pass before Plan 06 (ruvllm provider integration) writes any Rust code.

Output: `.planning/phases/11.8-ruvector-sweep/plans/11.8-01-AUDIT.md` with sections: Verdict, Fit-for-purpose analysis, Target API surface (if GO), Missing capabilities (if NO-GO), Recommended Cargo features, Recommended model default.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11.8-ruvector-sweep/11.8-CONTEXT.md
@.planning/phases/07-ai-provider-foundation/07-CONTEXT.md
@/Users/gshah/work/apps/experiments/ruvector/crates/ruvllm/README.md
@/Users/gshah/work/apps/experiments/ruvector/crates/ruvllm/src/lib.rs
@/Users/gshah/work/apps/cortex/src-tauri/src/ai/service.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read ruvllm crate surface top-to-bottom</name>
  <files>(read-only)</files>
  <action>Read the following files in order, then take notes on what's directly usable from cortex ai_request(): (a) `crates/ruvllm/README.md` — full read for capabilities matrix and feature flags; (b) `crates/ruvllm/src/lib.rs` — module list + re-exports; (c) `crates/ruvllm/src/backends/mod.rs` (if exists) and `crates/ruvllm/src/backends/candle.rs` — inspect `CandleBackend::with_device()`, `load_gguf()`, `generate()` signatures; (d) `crates/ruvllm/src/serving/mod.rs` if present (for streaming); (e) `crates/ruvllm/src/session/mod.rs` for multi-turn session API. Use `rg "pub fn"` and `rg "pub async fn"` inside the crate to enumerate public entry points. Do NOT modify anything.</action>
  <verify>
    <automated>test -d /Users/gshah/work/apps/experiments/ruvector/crates/ruvllm && rg -c "pub (async )?fn" /Users/gshah/work/apps/experiments/ruvector/crates/ruvllm/src/ | tail -5</automated>
  </verify>
  <done>Notes captured (in-memory) covering: (1) synchronous generate signature; (2) streaming variant if any; (3) Metal feature flag name for Apple Silicon; (4) GGUF path convention; (5) any Cargo dependency conflicts with cortex's tokio/reqwest versions.</done>
</task>

<task type="auto">
  <name>Task 2: Write 11.8-01-AUDIT.md with verdict + integration surface</name>
  <files>.planning/phases/11.8-ruvector-sweep/plans/11.8-01-AUDIT.md</files>
  <action>Write the audit file with these sections in order (matches D-01 checklist): 1) **Verdict** — one word (GO / NO-GO / GO-WITH-CAVEAT); 2) **Fit-for-purpose analysis** — bullet list checking each D-01 criterion: (a) Rust crate loadable via Cargo path dep? (b) Anthropic-style chat completion API surface exists or adaptable to `ai_request(AIServiceRequest)`? (c) Metal backend available on Apple Silicon? (d) Streaming supported? 3) If verdict is GO/GO-WITH-CAVEAT: **Target API surface** — write out the exact Rust snippet (5-15 lines) showing how `ai_ruvllm_chat(model, system, messages, max_tokens) -> AIServiceResponse` would call ruvllm; name the exact struct/method chain (`CandleBackend::with_device(DeviceType::Metal)?.load_gguf(path, cfg)?.generate(prompt, params)?` or whatever the actual API is). 4) **Recommended Cargo features** — the exact feature list for Apple Silicon (e.g. `["inference-metal", "coreml", "parallel"]`). 5) **Recommended default model** — Qwen 2.5 7B GGUF Q4 quant or Llama 3.2 3B Instruct; include HuggingFace repo id + rough size. 6) **Streaming** — YES/NO and if YES, the streaming variant name. 7) If verdict is NO-GO: **Missing capabilities** — enumerate each unmet criterion + link to nearest ruvllm equivalent if any. If NO-GO, write a **Fallback recommendation** paragraph: cortex ships v1.1 without local-ruvllm; Ollama covers the "local LLM" need (already supported); Plan 06 becomes a DEVIATION.md. This audit is the sole authority Plan 06 reads to decide GO / SKIP.</action>
  <verify>
    <automated>test -f .planning/phases/11.8-ruvector-sweep/plans/11.8-01-AUDIT.md && grep -c "^## Verdict" .planning/phases/11.8-ruvector-sweep/plans/11.8-01-AUDIT.md</automated>
  </verify>
  <done>11.8-01-AUDIT.md exists with `## Verdict` section containing exactly one of {GO, GO-WITH-CAVEAT, NO-GO}. If GO/GO-WITH-CAVEAT: target API surface Rust snippet + Cargo feature list + default model present. If NO-GO: missing capabilities enumerated + fallback recommendation written.</done>
</task>

</tasks>

<verification>
- 11.8-01-AUDIT.md exists at `.planning/phases/11.8-ruvector-sweep/plans/11.8-01-AUDIT.md`
- File contains `## Verdict` with GO / GO-WITH-CAVEAT / NO-GO
- If GO: `## Target API surface` present with concrete Rust snippet
- If NO-GO: `## Missing capabilities` + `## Fallback recommendation` present
</verification>

<success_criteria>
Plan 06 (ruvllm provider integration) can be executed OR skipped based solely on reading 11.8-01-AUDIT.md. No re-audit required at Plan 06 time.
</success_criteria>

<output>
Create `.planning/phases/11.8-ruvector-sweep/11.8-01-SUMMARY.md` when done, referencing the audit file + summarizing the verdict in one sentence.
</output>

---
phase: 11.8-ruvector-sweep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/11.8-ruvector-sweep/plans/11.8-02-AUDIT.md
autonomous: true
requirements:
  - RVSW-02
must_haves:
  truths:
    - "11.8-02-AUDIT.md exists documenting go/no-go verdict on ruvector-attention re-ranker"
    - "Verdict names the ONE attention mechanism selected from the 46 (or NO-GO)"
    - "If GO: target API surface is written as concrete Rust snippet consumable by Plan 07"
    - "If NO-GO: a fallback recommendation (keep pure cosine + Jaccard + recency) is written"
  artifacts:
    - path: ".planning/phases/11.8-ruvector-sweep/plans/11.8-02-AUDIT.md"
      provides: "ruvector-attention audit verdict + integration surface for Plan 07"
      contains: "## Verdict"
  key_links:
    - from: "11.8-02-AUDIT.md"
      to: "11.8-07-PLAN.md"
      via: "Plan 07 reads verdict + surface to implement or skip"
      pattern: "AUDIT"
---

<objective>
Audit `ruvector/crates/ruvector-attention/` to produce a go/no-go verdict for the post-cosine search re-ranking integration (D-08/D-09/D-11). No implementation. Purely reading + writing 11.8-02-AUDIT.md.

Purpose: Following the Phase 9/12/13 lesson — ruvector crate names sometimes don't match cortex-relevant functionality (ruvector-cluster taught us this). Audit gate MUST pass before Plan 07 (attention re-ranker wiring) writes any Rust code. This audit also SELECTS which of the 46 attention mechanisms to use.

Output: `.planning/phases/11.8-ruvector-sweep/plans/11.8-02-AUDIT.md` with sections: Verdict, Fit-for-purpose analysis, Selected mechanism (if GO), Target API surface (if GO), Missing capabilities (if NO-GO), Rationale for mechanism choice.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11.8-ruvector-sweep/11.8-CONTEXT.md
@.planning/phases/11.8-ruvector-sweep/plans/11.8-01-PLAN.md
@/Users/gshah/work/apps/experiments/ruvector/crates/ruvector-attention/README.md
@/Users/gshah/work/apps/experiments/ruvector/crates/ruvector-attention/src/lib.rs
@/Users/gshah/work/apps/cortex/src-tauri/src/search/query.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate the 46 attention mechanisms + inspect entry points</name>
  <files>(read-only)</files>
  <action>Read (a) `crates/ruvector-attention/README.md` — full read; capture the capabilities matrix listing all mechanisms and which are CPU-friendly; (b) `crates/ruvector-attention/src/lib.rs` — module list + re-exports; (c) any `crates/ruvector-attention/src/mechanisms/mod.rs` (or equivalent) — enumerate the mechanism variants. Use `rg "pub fn|pub struct" /Users/gshah/work/apps/experiments/ruvector/crates/ruvector-attention/src/` to enumerate public entry points. Then for the top 3 candidates that look CPU-friendly + semantic-relevance-oriented (e.g. FlashAttention, ScaledDotProduct, OptimalTransport), read their impl files to check: (1) does it take (query_vec, candidate_vecs) → scores? (2) is there a `rerank(&query, &candidates, top_k) -> Vec<(idx, score)>` helper? (3) does it require GPU? (4) approximate latency at N=20 candidates. Do NOT modify anything.</action>
  <verify>
    <automated>test -d /Users/gshah/work/apps/experiments/ruvector/crates/ruvector-attention && rg -c "pub (async )?fn|pub struct" /Users/gshah/work/apps/experiments/ruvector/crates/ruvector-attention/src/ | tail -5</automated>
  </verify>
  <done>Notes captured (in-memory) covering: (1) list of 46 mechanism names; (2) top-3 CPU-friendly candidates; (3) chosen mechanism with concrete API signature; (4) any Cargo feature flag required to enable it (e.g. `cpu-only`, `no-gpu`); (5) whether attention scores are cosine-comparable or normalized differently.</done>
</task>

<task type="auto">
  <name>Task 2: Write 11.8-02-AUDIT.md with verdict + selected mechanism + API surface</name>
  <files>.planning/phases/11.8-ruvector-sweep/plans/11.8-02-AUDIT.md</files>
  <action>Write the audit file with these sections in order (matches D-08/D-11 checklist): 1) **Verdict** — one word (GO / NO-GO / GO-WITH-CAVEAT); 2) **Fit-for-purpose analysis** — bullet list checking each D-08 criterion: (a) crate exposes a re-rank API taking query embedding + candidate embeddings + attention scores? (b) at least one mechanism is CPU-friendly (no GPU requirement)? (c) attention scores are actionable (interpretable as relevance, not compression/dimensionality-reduction artifacts)? 3) If GO/GO-WITH-CAVEAT: **Selected mechanism** — name exactly ONE of the 46 (planner's discretion per D-11); include rationale (CPU-friendly, semantic-relevance-oriented, ≤ 50ms per query on 20 candidates); 4) **Target API surface** — write out the exact Rust snippet (5-15 lines) showing how Plan 07's `attention_reranker::rerank(query_vec, candidates) -> Vec<(String, f64)>` would call ruvector-attention; name the exact struct/method chain (e.g. `FlashAttention::new(config).score(&query, &candidates)?` or whatever the actual API is). 5) **Recommended Cargo features** — the exact feature list (e.g. `["cpu", "no-simd"]` or `default`). 6) **Score normalization** — explain how attention scores are normalized so the blending formula `0.5*cosine + 0.3*attention + 0.15*jaccard + 0.05*recency` stays in [0.0, 1.0] (D-12). 7) If NO-GO: **Missing capabilities** — enumerate each unmet criterion. If NO-GO, write a **Fallback recommendation** paragraph: cortex ships without enhanced re-ranking; existing `0.9*cosine + 0.1*recency` formula stands; Plan 07 becomes DEVIATION.md. This audit is the sole authority Plan 07 reads to decide GO / SKIP.</action>
  <verify>
    <automated>test -f .planning/phases/11.8-ruvector-sweep/plans/11.8-02-AUDIT.md && grep -c "^## Verdict" .planning/phases/11.8-ruvector-sweep/plans/11.8-02-AUDIT.md</automated>
  </verify>
  <done>11.8-02-AUDIT.md exists with `## Verdict` section containing exactly one of {GO, GO-WITH-CAVEAT, NO-GO}. If GO/GO-WITH-CAVEAT: `## Selected mechanism` names ONE mechanism, `## Target API surface` shows Rust snippet, `## Score normalization` explains blending. If NO-GO: missing capabilities enumerated + fallback recommendation written.</done>
</task>

</tasks>

<verification>
- 11.8-02-AUDIT.md exists at `.planning/phases/11.8-ruvector-sweep/plans/11.8-02-AUDIT.md`
- File contains `## Verdict` with GO / GO-WITH-CAVEAT / NO-GO
- If GO: `## Selected mechanism` + `## Target API surface` + `## Score normalization` present
- If NO-GO: `## Missing capabilities` + `## Fallback recommendation` present
</verification>

<success_criteria>
Plan 07 (attention re-ranker wiring) can be executed OR skipped based solely on reading 11.8-02-AUDIT.md. No re-audit at Plan 07 time. Selected mechanism is unambiguous.
</success_criteria>

<output>
Create `.planning/phases/11.8-ruvector-sweep/11.8-02-SUMMARY.md` when done, referencing the audit file + summarizing verdict + selected mechanism in one sentence.
</output>

---
phase: 11.8-ruvector-sweep
plan: 10
type: execute
wave: 5
depends_on:
  - 11.8-06
  - 11.8-07
  - 11.8-08
files_modified:
  - client/pages/SettingsPage.tsx
  - client/components/ai/AiProvidersSection.tsx
  - client/components/ai/RuvllmProviderCard.tsx
  - client/components/ai/RerankToggle.tsx
  - client/components/layout/Sidebar.tsx
autonomous: false
requirements:
  - RVSW-10
must_haves:
  truths:
    - "Settings → AI Providers shows a new 'Local (ruvllm)' card variant with model download + status when 11.8-06 shipped ruvllm"
    - "Settings has a new 'Enhanced search re-ranking (experimental)' toggle wired to RerankConfig.enabled (default OFF per D-13) when 11.8-07 shipped rerank"
    - "Sidebar top-5 prominent entities uses PageRank scores when populated (via existing get_prominent_entities IPC — no client contract change) when 11.8-08 shipped PageRank"
    - "Each sub-feature that was DEVIATED in Wave 3 is EITHER absent from the UI (feature skipped) OR shows a disabled state with a tooltip explaining the deviation"
    - "Manual UAT checklist passes: connect local-ruvllm, download model, run search with rerank ON, verify Sidebar top-5 reflects graph importance"
  artifacts:
    - path: "client/components/ai/RuvllmProviderCard.tsx"
      provides: "Provider card variant for local-ruvllm with model download UI, disk usage, inference speed"
      contains: "download_ruvllm_model"
    - path: "client/components/ai/RerankToggle.tsx"
      provides: "Enhanced search re-ranking toggle bound to RerankConfig via IPC"
      contains: "RerankConfig"
    - path: "client/components/layout/Sidebar.tsx"
      provides: "top-5 prominent entities section reading from get_prominent_entities (unchanged contract; backend switches signal)"
      contains: "get_prominent_entities"
  key_links:
    - from: "client/components/ai/RuvllmProviderCard.tsx"
      to: "src-tauri/src/commands/ai.rs::download_ruvllm_model"
      via: "invoke + listen ruvllm://download-progress event"
      pattern: "ruvllm://download-progress"
    - from: "client/components/ai/RerankToggle.tsx"
      to: "src-tauri/src/commands/settings.rs"
      via: "get/update_settings for RerankConfig field"
      pattern: "rerankConfig"
---

<objective>
Ship the user-facing UI for Waves 3 + 4 — provider card variant for local-ruvllm, opt-in re-rank toggle, and PageRank-backed Sidebar top-5. Each surface is CONDITIONAL on its corresponding backend plan actually shipping (not DEVIATED). D-04, D-07, D-13, D-18.

Purpose: Backend integrations are invisible without UI surfaces. Plan 06 exposes `download_ruvllm_model` + a new provider slug — Settings needs a card. Plan 07 adds a RerankConfig — Settings needs a toggle. Plan 08 changes what `get_prominent_entities` returns — Sidebar mounts the existing IPC; no client contract change but the values differ.

Output:
- New `RuvllmProviderCard.tsx` component (or gracefully falls back to reusing existing `ProviderCard.tsx` if the local-ruvllm shape is indistinguishable from other providers — planner's discretion; document in SUMMARY)
- New `RerankToggle.tsx` component
- `AiProvidersSection.tsx` conditionally renders RuvllmProviderCard when 11.8-06 shipped
- `SettingsPage.tsx` renders RerankToggle in the appropriate tab when 11.8-07 shipped
- `Sidebar.tsx` reads top-5 from `get_prominent_entities` IPC (existing) — if a top-5 section doesn't yet exist in Sidebar, this plan adds it
- One human-verify checkpoint at the end confirming visual + functional acceptance
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11.8-ruvector-sweep/11.8-CONTEXT.md
@.planning/phases/11.8-ruvector-sweep/plans/11.8-06-PLAN.md
@.planning/phases/11.8-ruvector-sweep/plans/11.8-07-PLAN.md
@.planning/phases/11.8-ruvector-sweep/plans/11.8-08-PLAN.md
@client/components/ai/AiProvidersSection.tsx
@client/components/ai/ProviderCard.tsx
@client/pages/SettingsPage.tsx
@client/components/layout/Sidebar.tsx
</context>

<interfaces>
<!-- Contracts Plan 10 consumes. -->

From client/lib/types.ts (added by Plan 04):
```typescript
export interface EntityPageRank { entityId: string; rankScore: number; }
export interface RerankConfig { enabled: boolean; mechanism: string; }
```

From client/hooks/useTauri.ts (added by Plan 04):
```typescript
queryKeys.entityPageRank
queryKeys.entityPageRankFor(id)
queryKeys.rerankConfig
```

From Plan 06 backend contract (documented in 11.8-06-SUMMARY.md):
- Command `download_ruvllm_model(modelId: string)` returns Promise<string> (local file path)
- Event `ruvllm://download-progress` with payload `{modelId, bytesDownloaded, totalBytes}`
- Provider slug string `"local-ruvllm"` used as active-provider identifier
- Fallback error prefix `[FALLBACK]` — UI detects this to auto-swap providers + show toast

From Plan 07 backend contract (documented in 11.8-07-SUMMARY.md):
- Command `get_rerank_config()` returns Promise<RerankConfig>
- Command `update_rerank_config(config: RerankConfig)` returns Promise<void>
- Default RerankConfig from Rust: `{ enabled: false, mechanism: "cosine-only" }` — mechanism value once enabled is the AUDIT-selected name

From Plan 08 backend contract (documented in 11.8-08-SUMMARY.md):
- Command `get_prominent_entities(limit: number)` returns Promise<EntitySummary[]> — SAME signature as before; only the internal ranking changes
- No frontend contract change required; Sidebar just calls the IPC
</interfaces>

<deviation_rules>
- **Per-feature conditional rendering.** For each of the three sub-features (ruvllm provider card, rerank toggle, PageRank top-5), inspect the corresponding SUMMARY: if the plan wrote a DEVIATION.md, DO NOT surface that feature. Silently omit — do not show a disabled tooltip unless the feature is partially available.
- **Never fabricate progress data.** RuvllmProviderCard shows real progress from the Tauri event only — no fake spinners advancing on a timer.
- **Never enable rerank by default.** Default OFF per D-13. Toggle persists via update_rerank_config.
- **Sidebar changes are additive.** If a "Top Entities" section already exists in Sidebar.tsx, extend its query — do NOT replace it. If it does not exist, add it below the existing sections (per cortex sidebar conventions).
- **Fallback toast on [FALLBACK] error.** When ai_request returns a string starting with `[FALLBACK]`, the RuvllmProviderCard's mutation onError handler shows a sonner toast: "Local model unavailable — switched to <previous provider>." Auto-swap logic re-invokes `set_active_provider` with the previous provider slug.
</deviation_rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RuvllmProviderCard component + AiProvidersSection wiring</name>
  <files>client/components/ai/RuvllmProviderCard.tsx, client/components/ai/RuvllmProviderCard.test.tsx, client/components/ai/AiProvidersSection.tsx</files>
  <behavior>
    - Test 1: When no model is downloaded, card shows "Download model" button + expected disk size (from 11.8-06-SUMMARY § default model)
    - Test 2: Clicking "Download model" invokes `download_ruvllm_model(defaultModelId)` — verify via mocked tauriInvoke
    - Test 3: Card subscribes to `ruvllm://download-progress` events (via existing Tauri event listener pattern used by BackfillIndicator) and updates a progress bar
    - Test 4: When model is present + provider is active, card shows "Model: {modelId} — {sizeGB}GB — last query {latencyMs}ms" (latency from a query metric persisted alongside the credential, or shows "—" if unavailable)
    - Test 5: When ai_request returns an error starting with `[FALLBACK]`, card triggers a sonner toast + calls set_active_provider on the previous provider
  </behavior>
  <action>Check 11.8-06-SUMMARY.md. If DEVIATED, SKIP this task — no card added. Otherwise: create `client/components/ai/RuvllmProviderCard.tsx` mirroring existing `ProviderCard.tsx` shape (grep for `ProviderCard` to see the props + hooks used). Key differences from a normal ProviderCard: (a) no API key input — local; (b) primary CTA is "Download model" invoking `download_ruvllm_model` (from useTauri hook); (c) progress bar wired via `listen("ruvllm://download-progress", ...)` — use the same tauri-apps/api pattern used by `useBackfillProgress.ts`; (d) status pill showing "Model downloaded / not downloaded / downloading X%"; (e) if active provider = "local-ruvllm" AND recent inference latency is available (grep `AuthState` / settings.json for a latency field — add one to Plan 06 SUMMARY expectations if missing and document deviation), show "last query {latencyMs}ms". In `AiProvidersSection.tsx`: conditionally render `<RuvllmProviderCard />` alongside existing provider cards. Detect availability via a feature-flag query key OR by checking whether the backend advertises `local-ruvllm` in a provider list IPC (planner's discretion — document choice in SUMMARY). Add 5 tests in `RuvllmProviderCard.test.tsx` using existing test patterns from `ProviderCard.test.tsx`.</action>
  <verify>
    <automated>cd /Users/gshah/work/apps/cortex && bun test client/components/ai/RuvllmProviderCard.test.tsx --run 2>&1 | tail -20</automated>
  </verify>
  <done>All 5 tests pass. `grep -c "RuvllmProviderCard" client/components/ai/AiProvidersSection.tsx` ≥ 1 (conditional import). `grep -c "download_ruvllm_model\|ruvllm://download-progress" client/components/ai/RuvllmProviderCard.tsx` ≥ 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RerankToggle component + SettingsPage wiring</name>
  <files>client/components/ai/RerankToggle.tsx, client/components/ai/RerankToggle.test.tsx, client/pages/SettingsPage.tsx</files>
  <behavior>
    - Test 1: RerankToggle fetches RerankConfig via `queryKeys.rerankConfig` — initial state is `enabled: false` (default OFF per D-13)
    - Test 2: Toggling the switch calls `update_rerank_config({ enabled: !current, mechanism: current.mechanism })` — verify via mocked tauriInvoke
    - Test 3: Component shows a subtitle "experimental" per D-13 and includes a tooltip explaining the D-12 formula in plain English (e.g. "Blends cosine similarity, attention, entity overlap, and recency for better ranking")
    - Test 4: SettingsPage renders RerankToggle in the "AI & Models" tab (grep SettingsPage to find the correct tab component)
    - Test 5: When RerankConfig is loading, the toggle shows a skeleton state (mirror existing pattern in SettingsPage — grep for `Skeleton`)
  </behavior>
  <action>Check 11.8-07-SUMMARY.md. If DEVIATED, SKIP this task. Otherwise: create `client/components/ai/RerankToggle.tsx` using existing `Switch` component (grep client/components/ui for it), useQuery on `queryKeys.rerankConfig` calling `tauriInvoke<RerankConfig>("get_rerank_config", {}, () => ({enabled: false, mechanism: "cosine-only"}))`, and useMutation calling `update_rerank_config(config)` that invalidates `queryKeys.rerankConfig` + `queryKeys.search` (so results re-fetch under new formula). Use existing sonner toast on success ("Search re-ranking enabled" / "Search re-ranking disabled"). In `SettingsPage.tsx`: grep for the "AI & Models" tab section; render `<RerankToggle />` beneath the existing extraction-model selector. Add 5 tests in `RerankToggle.test.tsx`.</action>
  <verify>
    <automated>cd /Users/gshah/work/apps/cortex && bun test client/components/ai/RerankToggle.test.tsx --run 2>&1 | tail -20</automated>
  </verify>
  <done>All 5 tests pass. `grep -c "RerankToggle" client/pages/SettingsPage.tsx` ≥ 1. `grep -c "rerankConfig\|get_rerank_config\|update_rerank_config" client/components/ai/RerankToggle.tsx` ≥ 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Sidebar top-5 prominent entities using existing IPC</name>
  <files>client/components/layout/Sidebar.tsx, client/components/layout/Sidebar.test.tsx</files>
  <behavior>
    - Test 1: Sidebar renders a "Top Entities" section calling `get_prominent_entities({limit: 5})` — if the section already exists, verify it uses the correct limit; otherwise add the section
    - Test 2: When the IPC returns 5 entities, each is rendered with name + document_count badge, and the section wraps the query in `queryKeys.entities` (or a new dedicated key — planner's discretion; document)
    - Test 3: Clicking an entity navigates to the entity detail page (verify via mocked react-router)
    - Test 4: When the IPC returns [], the section is hidden (empty state — do not show a placeholder for a new user with no entities)
  </behavior>
  <action>Check 11.8-08-SUMMARY.md. If DEVIATED, this task still runs — but with a documented note that the backend fallback (doc_count DESC) is what powers the section. Grep `client/components/layout/Sidebar.tsx` for any existing "top entities" / "prominent" section. If it exists, verify it uses limit=5 and wraps in a query — extend if needed. If it does not exist, ADD a new section using the existing Sidebar section shape (grep Sidebar.tsx for the section pattern — likely a `<h4>` + `<ul>` with `<Link>` items). Call `useQuery({queryKey: queryKeys.entities, queryFn: () => tauriInvoke<EntitySummary[]>("get_prominent_entities", {limit: 5}, () => mockProminentEntities.slice(0,5))})`. Add 4 tests in `Sidebar.test.tsx` (grep for existing Sidebar test patterns).</action>
  <verify>
    <automated>cd /Users/gshah/work/apps/cortex && bun test client/components/layout/Sidebar.test.tsx --run 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 tests pass. `grep -c "get_prominent_entities" client/components/layout/Sidebar.tsx` ≥ 1. If a section was added, `grep -c "Top Entities\|Prominent" client/components/layout/Sidebar.tsx` ≥ 1.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - RuvllmProviderCard in Settings → AI Providers (if 11.8-06 shipped ruvllm)
    - RerankToggle in Settings → AI & Models tab (if 11.8-07 shipped rerank)
    - Top-5 Prominent Entities in Sidebar (PageRank-backed if 11.8-08 shipped; doc_count fallback otherwise)
  </what-built>
  <how-to-verify>
    1. `bun dev` and open the app
    2. Navigate to Settings → AI Providers. If Plan 06 shipped: verify RuvllmProviderCard appears with "Download model" button
       - Click "Download model" — verify progress bar advances via `ruvllm://download-progress` events (check devtools console for the event stream)
       - When download completes, click "Connect" — verify the card marks local-ruvllm as active
       - Run a Pass 2 backfill task (e.g. trigger via /watched page) — verify inference happens with no network activity in devtools Network tab
    3. Navigate to Settings → AI & Models. If Plan 07 shipped: verify "Enhanced search re-ranking (experimental)" toggle exists
       - Toggle ON. Go to `/search`, run a query — verify the top result changes vs toggle OFF (subjective; if the corpus is small, note in feedback that results are unchanged but no errors)
    4. Reload the app. Sidebar → Top Entities: verify top 5 entities render
       - If Plan 08 shipped: verify ordering reflects graph importance (e.g. a Person with `owns` edges outranks a Person mentioned in one doc). Compare to `doc_count DESC` behavior by inspecting the entity's document_count badge
    5. Regression: connect a cloud provider (Anthropic / OpenAI). Verify existing provider cards are unchanged and Pass 2 backfill still works with cap=1 (no throughput regression on cloud)
    6. Provider fallback: if you have a broken ruvllm setup (e.g. delete the model file), invoke a backfill — verify a sonner toast appears with "Local model unavailable — switched to <previous>" and the previous provider is now active
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `bun test client/components/ai/RuvllmProviderCard.test.tsx client/components/ai/RerankToggle.test.tsx client/components/layout/Sidebar.test.tsx` passes (with any DEVIATED tasks appropriately skipped)
- `grep -v '^\s*//' client/components/ai/AiProvidersSection.tsx | grep -c "RuvllmProviderCard"` ≥ 1 if 11.8-06 shipped
- `grep -v '^\s*//' client/pages/SettingsPage.tsx | grep -c "RerankToggle"` ≥ 1 if 11.8-07 shipped
- `grep -v '^\s*//' client/components/layout/Sidebar.tsx | grep -c "get_prominent_entities"` ≥ 1
- Human UAT checkpoint approved
</verification>

<success_criteria>
User can connect local-ruvllm from Settings, download a model, run a backfill entirely on-device (Plan 06). User can enable Enhanced re-ranking and see results re-ordered per the D-12 formula (Plan 07). Sidebar top-5 reflects graph-importance-based prominence when PageRank is populated (Plan 08). All three surfaces gracefully hide when their backend plan was DEVIATED. Cloud-provider users see zero regression.
</success_criteria>

<output>
Create `.planning/phases/11.8-ruvector-sweep/11.8-10-SUMMARY.md` when done. Include: (a) which of the three UI surfaces shipped vs skipped-due-to-deviation; (b) any component-level deviations from the plan; (c) UAT feedback verbatim; (d) confirmed regression-free cloud provider path.
</output>

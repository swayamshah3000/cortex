---
phase: 07-ai-provider-foundation
plan: 07
type: execute
wave: 6
depends_on: [07-06]
files_modified:
  - .planning/phases/07-ai-provider-foundation/07-OAUTH-RESEARCH.md
autonomous: true
gap_closure: true
requirements: [AIPV-01, AIPV-02, AIPV-03, AIPV-07]
tags: [research, oauth, pkce, codex, gemini, google, spike]

must_haves:
  truths:
    - "The exact codex CLI OAuth authorization, token, refresh, and revoke endpoints are documented from an upstream public source"
    - "The exact CLIENT_ID used by codex CLI is captured (public constant, not per-user)"
    - "The exact OAuth scope string requested by codex CLI is captured"
    - "The exact redirect URI HOST (localhost vs 127.0.0.1) accepted by the OpenAI/Codex authorization server is resolved and documented — MUST NOT be left ambiguous, because loopback bind ≠ redirect_uri string equality on strict OAuth servers"
    - "Google OAuth 2.0 endpoints for Gemini (authorization, token, revoke) are documented from Google's public docs"
    - "The scope required for calling Gemini generateContent via OAuth is EITHER captured OR the plan explicitly declares Gemini OAuth Cortex-blocked and documents the API-key-only fallback"
    - "PKCE code_challenge_method is confirmed as S256 for both flows"
    - "Whether client_secret is required for native/desktop app PKCE (should be NO for both) is confirmed"
    - "Plans 08/09/10 have everything they need to build without further web research"
  artifacts:
    - path: ".planning/phases/07-ai-provider-foundation/07-OAUTH-RESEARCH.md"
      provides: "Endpoint reference for shared PKCE flow implementation"
      contains: "https://auth.openai.com/oauth/authorize, https://auth.openai.com/oauth/token, app_EMoamEEZ73f0CkXaXp7hrann, code_challenge_method=S256, https://accounts.google.com/o/oauth2/v2/auth, https://oauth2.googleapis.com/token"
      min_lines: 140
  key_links:
    - from: "07-OAUTH-RESEARCH.md"
      to: "07-08-PLAN.md auth/pkce.rs task"
      via: "Downstream task reads OAUTH-RESEARCH.md for constants"
      pattern: "OpenAI Codex OAuth|Google Gemini OAuth"
    - from: "07-OAUTH-RESEARCH.md"
      to: "07-09-PLAN.md start_openai_oauth / start_gemini_oauth tasks"
      via: "Provider-specific endpoint constants sourced from this file"
      pattern: "CLIENT_ID|scope|token_url"
    - from: "07-OAUTH-RESEARCH.md §Redirect URI Host Resolution"
      to: "07-08-PLAN.md loopback listener + PKCE redirect_uri construction"
      via: "Per-provider redirect_uri host (localhost vs 127.0.0.1) is authoritative"
      pattern: "Redirect URI Host|localhost|127\\.0\\.0\\.1"
---

<objective>
Produce `07-OAUTH-RESEARCH.md` — a single source of truth for the OpenAI Codex and Google Gemini OAuth 2.0 PKCE endpoint constants that plans 07-08 and 07-09 will hard-code. Enforces D-22a: no invented endpoints. If the Google Gemini `generateContent` OAuth path cannot be resolved to a concrete scope + client_id-registration model, this plan MUST declare Gemini OAuth as Cortex-blocked and document the fallback (Gemini stays API-key-only; only OpenAI Codex OAuth ships in this gap closure milestone).

Purpose: gate every implementation task in plans 07-08..07-10 on verified endpoint data so no downstream commit contains a hallucinated URL, client_id, scope string, or REDIRECT URI HOST.

Output: a Markdown reference file consumed by every subsequent gap plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ai-provider-foundation/07-CONTEXT.md
@.planning/phases/07-ai-provider-foundation/07-HUMAN-UAT.md
@.planning/phases/07-ai-provider-foundation/07-RESEARCH.md
@.planning/phases/07-ai-provider-foundation/07-02-SUMMARY.md
@.planning/phases/07-ai-provider-foundation/07-03-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
<!-- The following endpoint constants were captured during planning from
     github.com/openai/codex/codex-rs/login (Apache-2.0). The executor MUST
     re-verify each URL is still current by re-fetching the same files at
     execution time. If ANY endpoint has moved or the CLIENT_ID has rotated,
     the executor updates 07-OAUTH-RESEARCH.md with the new values; do NOT
     silently propagate stale constants.

     Primary sources to re-verify (all raw URLs, public):
       https://raw.githubusercontent.com/openai/codex/main/codex-rs/login/src/server.rs
       https://raw.githubusercontent.com/openai/codex/main/codex-rs/login/src/auth/manager.rs
       https://raw.githubusercontent.com/openai/codex/main/codex-rs/login/src/pkce.rs
       https://raw.githubusercontent.com/openai/codex/main/codex-rs/login/src/token_data.rs

     Constants captured during planning (RE-VERIFY at execution):
       AUTH_URL       = https://auth.openai.com/oauth/authorize
       TOKEN_URL      = https://auth.openai.com/oauth/token
       REFRESH_URL    = https://auth.openai.com/oauth/token
       REVOKE_URL     = https://auth.openai.com/oauth/revoke
       CLIENT_ID      = app_EMoamEEZ73f0CkXaXp7hrann
       SCOPE          = "openid profile email offline_access api.connectors.read api.connectors.invoke"
       REDIRECT_URI   = http://localhost:{port}/auth/callback   ← codex CLI uses "localhost" LITERALLY (server.rs line 161)
       DEFAULT_PORT   = 1455  (fallback 1457 if bound)
       PKCE_METHOD    = S256
       AUTHZ EXTRAS   = id_token_add_organizations=true, codex_cli_simplified_flow=true, state=<random>, originator=<default>
       CODE EXCHANGE  = form-encoded POST: grant_type=authorization_code&code=..&redirect_uri=..&client_id=..&code_verifier=..
       REFRESH BODY   = JSON POST: {"client_id":..,"grant_type":"refresh_token","refresh_token":..}

     CRITICAL: codex CLI's `redirect_uri` string is literally `http://localhost:{port}/auth/callback` (verified from
     codex-rs/login/src/server.rs line 161 during planning). OAuth 2.1 servers commonly perform exact-string comparison
     on redirect_uri between authorization and token exchange requests. If Cortex hardcodes `127.0.0.1` while the
     OpenAI Codex server has whitelisted the literal string `localhost`, the code exchange returns HTTP 400
     `invalid_grant: redirect_uri mismatch`. THIS IS A KNOWN FAILURE MODE — Task 3 resolves.

     Google OAuth 2.0 (from developers.google.com/identity/protocols/oauth2/native-app):
       AUTH_URL       = https://accounts.google.com/o/oauth2/v2/auth
       TOKEN_URL      = https://oauth2.googleapis.com/token
       REVOKE_URL     = https://oauth2.googleapis.com/revoke
       REDIRECT_URI   = http://127.0.0.1:{port} or http://[::1]:{port}  (Google rejects "localhost" per 2022 policy)
       PKCE_METHOD    = S256
       CLIENT_SECRET  = NOT required for native app PKCE
       SCOPE (candidate 1) = https://www.googleapis.com/auth/generative-language.retriever
       SCOPE (candidate 2) = https://www.googleapis.com/auth/cloud-platform
       CLIENT_ID      = UNKNOWN — Google requires per-project client_id registration in Google Cloud Console. No shared public client_id equivalent to codex CLI's app_EMoam... exists.

     OPEN QUESTION for Gemini (resolve in this plan):
       Does the Gemini `generateContent` endpoint (generativelanguage.googleapis.com/v1beta/models/*:generateContent)
       accept a user-scoped OAuth 2.0 Bearer access_token as an alternative to API key?
       Learnforge OSS disabled Gemini OAuth. Verify against current Google docs.
       IF UNCLEAR after research → declare Gemini OAuth Cortex-blocked and document fallback (API-key only in Gemini card;
       the shared PKCE module still ships but Gemini call sites are deferred until Google provides a stable answer). -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify OpenAI Codex OAuth endpoints from upstream source and write authoritative reference section</name>
  <files>.planning/phases/07-ai-provider-foundation/07-OAUTH-RESEARCH.md</files>
  <action>
    Create `07-OAUTH-RESEARCH.md` with a `## OpenAI Codex OAuth (PKCE + Refresh)` section. Populate from live re-fetch of the codex-rs/login source (do NOT copy the constants from the interfaces block above verbatim — re-verify each one at execution time; the interfaces block is a planning snapshot, not truth).

    Steps:
    1. Fetch the codex login source files listed in the interfaces block using WebFetch OR `gh api repos/openai/codex/contents/codex-rs/login/src/...` OR raw `curl -sL --compressed https://raw.githubusercontent.com/openai/codex/main/codex-rs/login/src/...`. Prefer `curl -sL --compressed` because plain curl truncated the file during planning (verified: 841 bytes vs 97KB truth).
    2. Extract into the doc, with SHA-cited line references where possible:
       - `AUTHORIZATION_URL` (from server.rs — `build_authorize_url` composes `{issuer}/oauth/authorize`)
       - `TOKEN_URL` (from server.rs — `token_endpoint = format!("{}/oauth/token", issuer)`)
       - `REFRESH_URL` (from auth/manager.rs — `REFRESH_TOKEN_URL` constant)
       - `REVOKE_URL` (from auth/manager.rs — `REVOKE_TOKEN_URL` constant) — CRITICAL for D-24 §6 revoke-on-disconnect (plan 07-09 consumes)
       - `CLIENT_ID` (from auth/manager.rs — `pub const CLIENT_ID: &str = "..."`)
       - Full scope string (from server.rs — line inside `build_authorize_url` fn, currently `"openid profile email offline_access api.connectors.read api.connectors.invoke"`)
       - Redirect URI EXACT string as codex CLI constructs it (`http://localhost:{port}/auth/callback` per server.rs line 161) and default port (currently 1455, fallback 1457)
       - PKCE method (`S256` — confirm)
       - Authorization query extras: `id_token_add_organizations=true`, `codex_cli_simplified_flow=true`, `state`, `originator`
       - Code exchange request shape (form-encoded body — cite server.rs line where this is built)
       - Refresh request shape (JSON body: `{client_id, grant_type: "refresh_token", refresh_token}` — cite manager.rs line 1430 `RefreshRequest`)
       - Refresh response fields (`id_token`, `access_token`, `refresh_token` — cite manager.rs line 1437 `RefreshResponse`)
       - Revoke request shape (form/JSON body: cite manager.rs revoke fn — plan 07-09 will POST to REVOKE_URL with this shape on disconnect for D-24 §6)
       - License (Apache-2.0 — record for legal traceability)
    3. Include a "Compatibility note" subsection stating:
       - Cortex will reuse the same public `CLIENT_ID` as codex CLI (this is codex's designed multi-app pattern; the constant is not a secret and rotating it would break every codex-CLI-authenticated user)
       - Cortex is a distinct app; users authorize Cortex to reuse their ChatGPT subscription via the same OAuth flow codex CLI uses
       - Add `originator` parameter: use a Cortex-specific string (e.g. `originator=cortex-desktop`) NOT `codex_cli` — record chosen value here for downstream plan 07-09
       - If any URL, client_id, or scope has changed relative to the interfaces block snapshot (2026-07-02), record BOTH values and mark the new value as authoritative
    4. Add a "Refresh strategy" subsection stating: refresh triggered when `now + 60s >= expires_at` (D-24 §5) OR on any 401 during chat (D-22 last clause); refresh response `refresh_token` is optional (may be omitted for a subsequent refresh) — handle Optional
    5. Add a "Revoke strategy" subsection (D-24 §6): on disconnect, POST to REVOKE_URL best-effort; ignore failure (do not block the disconnect). Document the exact body shape. Plan 07-09 wires this into `logout_provider`.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-ai-provider-foundation/07-OAUTH-RESEARCH.md &amp;&amp; grep -q "auth.openai.com/oauth/authorize" .planning/phases/07-ai-provider-foundation/07-OAUTH-RESEARCH.md &amp;&amp; grep -q "auth.openai.com/oauth/token" .planning/phases/07-ai-provider-foundation/07-OAUTH-RESEARCH.md &amp;&amp; grep -q "auth.openai.com/oauth/revoke" .planning/phases/07-ai-provider-foundation/07-OAUTH-RESEARCH.md &amp;&amp; grep -q "code_challenge_method=S256\|code_challenge_method.*S256\|S256" .planning/phases/07-ai-provider-foundation/07-OAUTH-RESEARCH.md &amp;&amp; grep -qE "app_[A-Za-z0-9]{20,}" .planning/phases/07-ai-provider-foundation/07-OAUTH-RESEARCH.md &amp;&amp; grep -q "offline_access" .planning/phases/07-ai-provider-foundation/07-OAUTH-RESEARCH.md &amp;&amp; grep -q "grant_type" .planning/phases/07-ai-provider-foundation/07-OAUTH-RESEARCH.md &amp;&amp; grep -q "originator" .planning/phases/07-ai-provider-foundation/07-OAUTH-RESEARCH.md &amp;&amp; grep -qi "revoke" .planning/phases/07-ai-provider-foundation/07-OAUTH-RESEARCH.md</automated>
  </verify>
  <done>OpenAI Codex section documents all six endpoints (auth, token, refresh, revoke), client_id, full scope string, PKCE method, redirect URI pattern, default port, refresh + revoke request shapes, and Cortex-chosen `originator` value. Every URL and client_id has been re-verified against a live upstream fetch this session.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve Google Gemini OAuth path OR document Cortex-blocked fallback</name>
  <files>.planning/phases/07-ai-provider-foundation/07-OAUTH-RESEARCH.md</files>
  <action>
    Append a `## Google Gemini OAuth (PKCE + Refresh)` section to the same `07-OAUTH-RESEARCH.md`. Resolve the open question captured in the interfaces block: does `generativelanguage.googleapis.com/v1beta/models/*:generateContent` accept a user-scoped OAuth 2.0 Bearer access token?

    Research steps (do not stop at the first answer — cross-reference at least two sources):
    1. WebFetch https://ai.google.dev/gemini-api/docs/oauth — capture stated scope(s) and prerequisites for OAuth
    2. WebFetch https://developers.google.com/identity/protocols/oauth2/scopes — search for `generative-language`
    3. WebFetch https://ai.google.dev/api/rest/v1beta/models/generateContent — check "Authorization Scopes" section
    4. If Google requires per-project client_id registration in Google Cloud Console AND does not provide a shared public client_id (unlike codex CLI): decide the Cortex distribution model. Two options — document BOTH in the file, and pick one (Claude's discretion within D-22a):
       - Option A: Ship with a hardcoded Cortex-registered Google OAuth client_id (planner-reserved; user must complete a `Console Setup` step in Cortex settings before OAuth works)
       - Option B: Prompt the user to paste their own Google Cloud client_id into a Settings field before triggering OAuth (BYOC — Bring Your Own Client)
       - Option C: Declare Gemini OAuth as Cortex-blocked for this milestone; Gemini card remains API-key-only; plan 07-10 does NOT add "Sign in with Google" primary CTA
    5. Independent of Google's answer, document these known endpoints (from developers.google.com/identity/protocols/oauth2/native-app):
       - AUTHORIZATION_URL = `https://accounts.google.com/o/oauth2/v2/auth`
       - TOKEN_URL = `https://oauth2.googleapis.com/token`
       - REVOKE_URL = `https://oauth2.googleapis.com/revoke`
       - REDIRECT_URI pattern = `http://127.0.0.1:{port}` (Google explicitly rejects `localhost` per 2022 policy — verify this is still current; NOTE this differs from codex CLI — see Task 3)
       - PKCE method = S256
       - client_secret NOT required for native app PKCE
    6. Record the resolution in a `### Decision` subsection at the end. If Option C is chosen, explicitly state "Plan 07-10 MUST NOT render a Sign-in-with-Google CTA on the Gemini card. Gemini card remains API-key-only. The shared PKCE module (plan 07-08) still ships and is only exercised by the OpenAI Codex path."

    If the executor cannot resolve the Gemini OAuth question after all three doc fetches AND cannot decide between options A/B/C confidently (e.g., because Google's docs explicitly deprecate OAuth for `generateContent` in favor of API keys), the executor MUST choose Option C by default. Do NOT invent a scope string.
  </action>
  <verify>
    <automated>grep -q "accounts.google.com/o/oauth2/v2/auth" .planning/phases/07-ai-provider-foundation/07-OAUTH-RESEARCH.md &amp;&amp; grep -q "oauth2.googleapis.com/token" .planning/phases/07-ai-provider-foundation/07-OAUTH-RESEARCH.md &amp;&amp; grep -qE "127\.0\.0\.1|::1" .planning/phases/07-ai-provider-foundation/07-OAUTH-RESEARCH.md &amp;&amp; grep -qE "### Decision|## Decision|Option A|Option B|Option C" .planning/phases/07-ai-provider-foundation/07-OAUTH-RESEARCH.md &amp;&amp; grep -qi "gemini" .planning/phases/07-ai-provider-foundation/07-OAUTH-RESEARCH.md</automated>
  </verify>
  <done>Google Gemini section documents Google's OAuth 2.0 endpoints, PKCE requirements, and the redirect URI policy. A `### Decision` subsection explicitly picks Option A, B, or C and records the rationale. If Option C, all downstream plans consuming Gemini OAuth adapt or defer accordingly.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve the redirect_uri host conflict (localhost vs 127.0.0.1) — MUST NOT be left ambiguous</name>
  <files>.planning/phases/07-ai-provider-foundation/07-OAUTH-RESEARCH.md</files>
  <action>
    Append a `## Redirect URI Host Resolution` section. Resolve the fundamental conflict:

    - **codex CLI (per server.rs line 161)** constructs its `redirect_uri` string as literally `http://localhost:{port}/auth/callback`. It relies on the loopback listener binding `127.0.0.1` while the redirect_uri STRING uses `localhost`. This works because OS-level DNS resolves `localhost → 127.0.0.1` for the browser callback, but the OAuth server sees the LITERAL STRING `localhost` in both authorize and token exchange requests.
    - **D-24 §1 (locked user decision)** specifies `redirect_uri=http://localhost:{port}/callback` — using `localhost` (matches codex CLI).
    - **Google OAuth 2.0** (per Google's 2022 policy) rejects `localhost` and requires `127.0.0.1` or `[::1]` in the redirect_uri string.
    - **Strict OAuth 2.1 servers** perform EXACT-STRING comparison on redirect_uri between the authorize and token requests. `localhost` and `127.0.0.1` are NOT interchangeable at this layer.

    Research + verification steps:
    1. Re-read codex-rs/login/src/server.rs line 161 to confirm the LITERAL redirect_uri string codex constructs. Record the exact code line.
    2. Attempt to verify (via docs or empirical test) whether the OpenAI Codex authorization server (auth.openai.com) tolerates `127.0.0.1` as an alternative to `localhost`. Sources: search github.com/openai/codex issues for `redirect_uri_mismatch` or `localhost 127.0.0.1`; check any linked docs. If no authoritative answer, record `[ASSUMED: OpenAI's server accepts only what codex CLI uses — `localhost`]` and lock in `localhost` for the Codex flow.
    3. For Google Gemini flow, `127.0.0.1` is the documented value — no ambiguity there.

    Record the resolution as a table in the section:

    ```
    | Provider     | redirect_uri host string | Loopback bind address | Reason |
    |--------------|--------------------------|-----------------------|--------|
    | openai-codex | localhost                | 127.0.0.1             | Matches codex CLI source (server.rs L161) + D-24 §1 literal |
    | gemini (if OAuth) | 127.0.0.1           | 127.0.0.1             | Google policy rejects "localhost" (2022) |
    ```

    Add a "Downstream binding" subsection stating:
    - Plan 07-08 `auth/pkce.rs` `OAuthFlowConfig` MUST have a `redirect_uri_host: &str` field (or equivalent) so per-provider host is provider-configurable
    - Plan 07-08 `auth/loopback.rs` binds `127.0.0.1` UNCONDITIONALLY (loopback is loopback — `localhost` DNS-resolves to it for the browser callback; the bind address is a socket-level choice separate from the OAuth redirect_uri string)
    - Plan 07-09 `start_openai_codex_oauth` MUST populate `redirect_uri_host = "localhost"` in the OAuthFlowConfig
    - Plan 07-09 `start_google_gemini_oauth` (if Option A/B) MUST populate `redirect_uri_host = "127.0.0.1"`

    This resolution reconciles D-24 §1 (which reads `localhost` literally and is the codex CLI's real behavior) with the reality that Google requires `127.0.0.1`. Both are now correct — the ambiguity is eliminated.
  </action>
  <verify>
    <automated>grep -q "## Redirect URI Host Resolution" .planning/phases/07-ai-provider-foundation/07-OAUTH-RESEARCH.md &amp;&amp; grep -q "openai-codex.*localhost\|localhost.*Codex\|Codex.*localhost" .planning/phases/07-ai-provider-foundation/07-OAUTH-RESEARCH.md &amp;&amp; grep -qE "gemini.*127\.0\.0\.1|127\.0\.0\.1.*Gemini|127\.0\.0\.1.*Google|Google.*127\.0\.0\.1" .planning/phases/07-ai-provider-foundation/07-OAUTH-RESEARCH.md &amp;&amp; grep -q "redirect_uri_host\|redirect uri host" .planning/phases/07-ai-provider-foundation/07-OAUTH-RESEARCH.md</automated>
  </verify>
  <done>Section documents the per-provider redirect_uri host as an authoritative table. Codex uses `localhost`, Gemini uses `127.0.0.1`. Loopback socket bind is 127.0.0.1 for both (a separate concern from the redirect_uri string). Plans 07-08 and 07-09 have unambiguous instructions.</done>
</task>

<task type="auto">
  <name>Task 4: Document the Codex chat-endpoint routing question and lock the final answer</name>
  <files>.planning/phases/07-ai-provider-foundation/07-OAUTH-RESEARCH.md</files>
  <action>
    Append a `## Codex Chat Routing (post-OAuth)` section. Answer the question that plan 07-09 will need: once the user is authenticated via Codex OAuth, WHICH endpoint does Cortex POST chat requests to? Learnforge's `openai_chat` posts to `https://api.openai.com/v1/chat/completions` with `Bearer <token>`. But codex CLI uses a different backend when authenticated via ChatGPT subscription — the ChatGPT backend, not the public Platform API — and the Cortex chat implementation must mirror this to give users unmetered access to their ChatGPT subscription (D-22 explicit goal).

    Research steps:
    1. Fetch `https://raw.githubusercontent.com/openai/codex/main/codex-rs/login/src/lib.rs` and search for `CODEX_API` or `chatgpt_base_url` constants (planning found `ChatGptEnvironment::default().chatgpt_base_url()` referenced in manager.rs — trace it)
    2. Fetch `https://raw.githubusercontent.com/openai/codex/main/codex-rs/protocol/src/config_types.rs` OR wherever `ChatGptEnvironment` is defined — extract the base URL(s) it exposes
    3. Determine: when using an OAuth-obtained access_token with the ChatGPT backend, what is the request path? What auth header format?
    4. Document both `chatgpt_base_url` and the chat endpoint path Cortex will POST to (e.g. `{chatgpt_base_url}/some/path`). Cite source file + approximate line.
    5. If the ChatGPT backend endpoint returns a shape different from OpenAI Platform's `/v1/chat/completions`, document the difference so plan 07-09 can either:
       - Write a Cortex-specific `codex_chat()` in `ai/service.rs` that maps ChatGPT-backend response → `AIServiceResponse`, OR
       - Route Codex-authenticated requests through the existing `openai_chat` iff the endpoint is Platform-API-compatible under a Bearer token issued via OAuth
    6. If the ChatGPT backend endpoint is NOT publicly documented and its request/response shape can only be inferred from codex source, record the assumption AS AN ASSUMPTION (mark `[ASSUMED — verify at execution time via a manual codex CLI trace]`) — do NOT invent a shape.
  </action>
  <verify>
    <automated>grep -q "## Codex Chat Routing" .planning/phases/07-ai-provider-foundation/07-OAUTH-RESEARCH.md &amp;&amp; grep -qE "chatgpt_base_url|ChatGptEnvironment|api\.openai\.com|chatgpt\.com" .planning/phases/07-ai-provider-foundation/07-OAUTH-RESEARCH.md</automated>
  </verify>
  <done>The chat routing section unambiguously tells plan 07-09 which URL to POST to and which auth header shape to use when the credential is `AuthMethod::OAuth` under provider slug `openai-codex`. Any inferred (not-documented) shape is explicitly marked `[ASSUMED]`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner → executor | Executor MUST NOT silently drop the re-verification step; interfaces-block constants are a planning snapshot only |
| WebFetch → doc | Upstream doc responses are untrusted; capture cited source file + fetch date for every URL/client_id extracted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-20 | Tampering | codex CLIENT_ID rotation | mitigate | Re-verify at execution time; if rotated, update research doc with both old + new values and mark the new one authoritative |
| T-07-21 | Information Disclosure | Sharing user credentials via wrong endpoint | mitigate | Task 4 requires evidence for Codex chat routing choice; forbid invented endpoints for chat POST target |
| T-07-22 | Denial of Service | Wrong scope string causes 403 from Google | accept | Documented as Option-C fallback (Gemini stays API-key-only if OAuth unresolvable) |
| T-07-23 | Repudiation | Gemini OAuth Cortex-blocked with no audit trail | mitigate | If Option C, Task 2 requires a `### Decision` subsection explaining WHY (cite the exact Google doc phrase that motivated the block) |
| T-07-24 | Tampering | redirect_uri host mismatch causes silent OAuth failure | mitigate | Task 3 resolves per-provider host as an authoritative table; grep gates enforce presence |
</threat_model>

<verification>
- `07-OAUTH-RESEARCH.md` exists and passes all four grep gates above
- Every extracted URL and client_id has a cited source (file path or doc URL with fetch date)
- Any `[ASSUMED]` marker is accompanied by a note to plan 07-09 on how to verify the assumption at execution
- If Gemini OAuth is Cortex-blocked (Option C), plan 07-10's frontmatter would omit Gemini OAuth work — verify plan 07-10 anticipates this (see plan 07-10 fallback branch)
- Redirect URI host resolution table exists in the doc so plans 07-08/07-09 use the correct per-provider host string
- Revoke endpoint documented so plan 07-09 can wire D-24 §6 (revoke-on-disconnect)
</verification>

<success_criteria>
- File `.planning/phases/07-ai-provider-foundation/07-OAUTH-RESEARCH.md` committed
- Plans 07-08, 07-09, 07-10 can be executed with zero further web research
- No hallucinated endpoint, client_id, scope string, or redirect_uri host exists in the file
- Revoke endpoint present for D-24 §6 downstream wiring
</success_criteria>

<output>
Create `.planning/phases/07-ai-provider-foundation/07-07-SUMMARY.md` when done. If Gemini OAuth is Cortex-blocked, the SUMMARY MUST prominently record this so downstream plans do not accidentally implement Gemini OAuth wiring anyway. Also state the per-provider redirect_uri host resolution (Codex=localhost, Gemini=127.0.0.1).
</output>

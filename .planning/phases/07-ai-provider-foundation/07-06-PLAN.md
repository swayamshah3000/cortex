---
phase: 07-ai-provider-foundation
plan: 06
type: execute
wave: 5
depends_on: ["07-04"]
files_modified:
  - client/components/ai/ConnectAiStep.tsx
  - client/components/layout/AiNoProviderBanner.tsx
  - client/pages/OnboardingPage.tsx
  - client/pages/OnboardingPage.test.tsx
  - client/components/layout/AppShell.tsx
autonomous: false
requirements: [AIPV-06]
must_haves:
  truths:
    - "Onboarding wizard has 5 steps (was 4): Welcome → Connect AI (NEW Step 2) → Select Folders → Scanning → Done (D-12)"
    - "Connect AI step shows 2x2 grid of 4 provider cards (D-13)"
    - "Continue button enabled only after any provider connects; Skip for now allowed"
    - "Skip handler in ConnectAiStep advances the step ONLY — it does NOT call useAiBannerStore.dismiss() (preserves D-14/D-15: post-onboarding banner appears for skipped users)"
    - "AiNoProviderBanner shows ONLY when onboarding is completed AND no active provider exists AND user has not dismissed it this session (Pitfall 6 guard)"
    - "Banner X dismiss is session-only — returns on next app launch until a provider is connected (D-15)"
    - "Banner Go to Settings link navigates to /settings?tab=ai"
  artifacts:
    - path: "client/components/ai/ConnectAiStep.tsx"
      provides: "Onboarding Step 2 — 2x2 grid of mini provider cards with inline connect forms"
      contains: "ConnectAiStep"
    - path: "client/components/layout/AiNoProviderBanner.tsx"
      provides: "Session-only dismissible banner with Go to Settings link"
      contains: "AiNoProviderBanner"
    - path: "client/pages/OnboardingPage.tsx"
      provides: "Step machine extended from total={4} to total={5}; ConnectAiStep mounted as step 1 (Welcome=0, Folders=2, Scanning=3, Done=4)"
    - path: "client/pages/OnboardingPage.test.tsx"
      provides: "Vitest test — 5 steps present; Continue disabled until provider connected; Skip handler advances step WITHOUT dismissing banner store (RESEARCH Wave 0 gap)"
    - path: "client/components/layout/AppShell.tsx"
      provides: "Mounts <AiNoProviderBanner /> with showBanner guard from onboardingCompleted + providers + bannerDismissed"
  key_links:
    - from: "OnboardingPage step state machine"
      to: "StepIndicator total={5} and step 1 case rendering ConnectAiStep"
      via: "Existing step useState pattern from Phase 4 — extend, do not rewrite"
      pattern: "total=\\{5\\}"
    - from: "ConnectAiStep card 'Connect' click"
      to: "useSaveSetupToken / useConnectProvider mutations"
      via: "Same hooks as Plan 05 — but onboarding uses simplified inline forms (no model dropdown — default model applied per provider)"
      pattern: "useSaveSetupToken|useConnectProvider"
    - from: "AppShell showBanner computation"
      to: "onboardingCompleted AND !hasActiveProvider AND !bannerDismissed"
      via: "Pitfall 6 — banner gated on onboarding completion to avoid double-prompt"
      pattern: "onboardingCompleted.*hasActiveProvider|hasActiveProvider.*onboardingCompleted"
    - from: "AiNoProviderBanner Go to Settings click"
      to: "/settings?tab=ai route via useNavigate"
      via: "react-router-dom navigation; SettingsPage already supports tab query param (Phase 5)"
      pattern: "navigate.*settings\\?tab=ai"
---

<objective>
Extend the onboarding wizard with the "Connect AI" step as new Step 2 (shifting existing steps), and mount the session-only AI provider banner in AppShell. This delivers AIPV-06 (the only requirement uniquely owned by this plan — the rest are covered by Plan 05's Settings surface). A checkpoint validates the full first-run flow with a wiped `app_data_dir`.

Purpose: AIPV-06 forces the user to commit to an AI provider during onboarding OR explicitly skip with a persistent nudge. This is the v1.1 cloud-default positioning — Cortex needs an LLM provider for entity extraction (Phase 8) and space labeling (Phase 9). The 2x2 provider grid (D-13) makes the choice obvious; the Skip path keeps the door open for users who want to evaluate first. The session-only banner (D-15) is the gentle nudge — it returns on each app launch but doesn't lock features.

Output: 2 new files (`ConnectAiStep.tsx`, `AiNoProviderBanner.tsx`), 1 new test file, 2 modified files (`OnboardingPage.tsx`, `AppShell.tsx`). Runs in parallel with Plan 05 (same wave 5, no file conflicts). Both Plan 05 and Plan 06 depend on Plan 04 (hooks + types + banner store).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-ai-provider-foundation/07-CONTEXT.md
@.planning/phases/07-ai-provider-foundation/07-RESEARCH.md
@.planning/phases/07-ai-provider-foundation/07-PATTERNS.md
@.planning/phases/07-ai-provider-foundation/07-UI-SPEC.md
@.planning/phases/07-ai-provider-foundation/07-04-SUMMARY.md

# Existing patterns to extend
@client/pages/OnboardingPage.tsx
@client/components/layout/AppShell.tsx
@client/lib/stores.ts
@client/hooks/useTauri.ts

<interfaces>
Banner gating logic (Pitfall 6):
```
showBanner = onboardingCompleted === true
          && hasActiveProvider === false
          && bannerDismissed === false
```
Sources:
- onboardingCompleted = useOnboardingStore().isCompleted  (existing — set true when wizard finishes or is skipped)
- hasActiveProvider = providers?.some(p => p.isActive && p.authenticated) ?? false  (from useProviders())
- bannerDismissed = useAiBannerStore().isDismissed  (Plan 04 — session-only Zustand)

Step-index remap (D-12):
- 0: Welcome  (unchanged)
- 1: Connect AI  (NEW — ConnectAiStep)
- 2: Select Folders  (was step 1)
- 3: Scanning  (was step 2)
- 4: Spaces Ready  (was step 3)

OnboardingPage StepIndicator total prop changes from {4} to {5}.

Banner copy (D-14, UI-SPEC Copywriting):
"Connect an AI provider to enable Smart Spaces"   — main copy
"Go to Settings →"                                  — link text → /settings?tab=ai
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ConnectAiStep and AiNoProviderBanner components</name>
  <files>client/components/ai/ConnectAiStep.tsx, client/components/layout/AiNoProviderBanner.tsx</files>
  <read_first>
    client/pages/OnboardingPage.tsx
    client/components/layout/AppShell.tsx
    client/lib/stores.ts
    client/hooks/useTauri.ts
    client/components/ai/ProviderCard.tsx
    .planning/phases/07-ai-provider-foundation/07-UI-SPEC.md
    .planning/phases/07-ai-provider-foundation/07-PATTERNS.md
  </read_first>
  <action>
    Create client/components/ai/ConnectAiStep.tsx — onboarding Step 2 with 2x2 grid of mini provider cards.

    Imports: useState from react; Check, ExternalLink, Copy from lucide-react; toast from sonner; cn from @/lib/utils; useProviders, useSaveSetupToken, useConnectProvider from @/hooks/useTauri.

    Props: { onContinue: () => void; onSkip: () => void } — parent (OnboardingPage) controls step transitions.

    Layout (per UI-SPEC §"Onboarding Step 2 Component"):
    - Section heading (h2): "Connect your AI" with className page-title text-text-primary
    - Body paragraph: "Cortex uses AI to name your Smart Spaces and extract entities. Connect any provider to get started." with className text-sm text-text-secondary
    - 2x2 grid: <div className="grid grid-cols-2 gap-4"> containing 4 mini cards (Anthropic, OpenAI, Gemini, Ollama)
    - Continue button: full-width btn-primary, disabled until hasConnectedProvider; text "Continue" + ArrowRight icon
    - Skip link: text-sm text-text-tertiary hover:text-text-secondary; text "Skip for now" + SkipForward icon

    Each mini card (idle state — when status.authenticated is false):
    - className: rounded-lg border border-border-primary bg-bg-secondary p-4 space-y-3 hover:border-border-secondary cursor-pointer
    - Provider logo placeholder (use first letter or Lucide Zap icon — keep simple)
    - Provider name in font-semibold text-text-primary
    - "Connect" small button (text-sm text-accent-primary)
    - On click: expand inline form within the card (do NOT use a modal)

    Each mini card (expanded form state):
    - For Anthropic: same as ProviderCard expanded form but condensed — code block "claude setup-token" with copy button; password input placeholder "sk-ant-oat01-…"; Connect button. Use ANTHROPIC_DEFAULT_MODEL = "claude-haiku-4-5-20251001" (D-06) — no model selector in onboarding.
    - For OpenAI: password input placeholder "sk-…"; Connect button. Default model gpt-5-mini — no model selector in onboarding.
    - For Gemini: password input placeholder "AIza…"; Connect button. Default model gemini-2.5-flash.
    - For Ollama: text input pre-populated "http://localhost:11434"; Connect button. Default model llama3 (use a fixed default for onboarding to skip the dynamic /api/tags fetch — Settings still does dynamic fetch).

    Each mini card (connected state — when status.authenticated true):
    - className: rounded-lg border border-success/50 bg-success/5 p-4 space-y-3
    - Check icon (text-success size 16) + provider name + "Connected" label
    - Card is non-interactive in this state (already done — they can disconnect later in Settings)

    Save lifecycle: simpler than Plan 05 — use useState for isPending. On Connect click: await mutation.mutateAsync(...); toast.success(`${PROVIDER_LABEL} connected`); the useProviders() refetch transitions card to connected state. On error: toast.error((err as Error).message).

    hasConnectedProvider derivation: const { data: providers } = useProviders(); const hasConnectedProvider = providers?.some(p => p.authenticated) ?? false. Continue button disabled = !hasConnectedProvider.

    Continue button click: onContinue() — parent advances step to 2 (Folders).
    Skip button click: onSkip() — parent advances step to 2. **CRITICAL D-14/D-15 INVARIANT**: the Skip handler in ConnectAiStep MUST NOT call `useAiBannerStore.getState().dismiss()` or touch the banner store in any way. The banner appears post-onboarding-completion automatically because `hasActiveProvider` stays false; calling `dismiss()` here would silently suppress the post-onboarding banner for users who skipped (contradicting D-14: "App shell shows a dismissible banner" and D-15: "X hides it for the session; returns on next launch"). PATTERNS.md analog code at line 825-828 contains this anti-pattern (`useAiBannerStore.getState().dismiss()` inside Skip onClick) — DO NOT copy that line; it is incorrect for Cortex Phase 7 semantics.

    Now create client/components/layout/AiNoProviderBanner.tsx — session-only dismissible banner.

    Imports: useAiBannerStore from @/lib/stores; useNavigate from react-router-dom; X, AlertTriangle from lucide-react.

    Layout (per UI-SPEC §"AiNoProviderBanner"):
    - Container: flex items-center gap-3 px-4 py-4 bg-warning/10 border-b border-warning/20 text-sm role="alert" aria-live="polite"
    - Left: <AlertTriangle size={14} className="text-warning flex-shrink-0" />
    - Middle (flex-1): copy text "Connect an AI provider to enable Smart Spaces." then space then <button>Go to Settings →</button> styled as text-accent-primary hover:underline; on click navigate("/settings?tab=ai")
    - Right: dismiss button <button onClick={dismiss} aria-label="Dismiss banner" className="text-text-tertiary hover:text-text-primary min-w-[44px] min-h-[44px] flex items-center justify-center"><X size={14} /></button>

    Component logic:
    - const { dismiss } = useAiBannerStore()
    - const navigate = useNavigate()
    - Return the banner JSX above

    NOTE: The "show or hide" decision lives in AppShell (Task 3), not in this component. This component always renders when mounted; AppShell controls mounting via showBanner conditional.

    Run cd client && bun typecheck and cd client && bun test --run — confirm no regressions. Visual layout matches UI-SPEC dimensions (44px touch target on dismiss button verified by min-w-[44px] min-h-[44px] classes).
  </action>
  <verify>
    <automated>cd client &amp;&amp; bun typecheck 2>&amp;1 | tail -5 | grep -vqE 'error TS' &amp;&amp; grep -q 'grid grid-cols-2 gap-4' components/ai/ConnectAiStep.tsx &amp;&amp; grep -q 'useSaveSetupToken' components/ai/ConnectAiStep.tsx &amp;&amp; grep -q 'role="alert"' components/layout/AiNoProviderBanner.tsx &amp;&amp; grep -q 'aria-label="Dismiss banner"' components/layout/AiNoProviderBanner.tsx &amp;&amp; ! grep -q 'useAiBannerStore.*dismiss\|useAiBannerStore\.getState()\.dismiss' components/ai/ConnectAiStep.tsx</automated>
  </verify>
  <done>
    ConnectAiStep.tsx renders a 2x2 grid of 4 provider mini-cards with inline connect forms; Continue and Skip buttons present with correct enable/disable logic; uses useSaveSetupToken (Anthropic) and useConnectProvider (OpenAI/Gemini/Ollama) hooks with toast feedback. AiNoProviderBanner.tsx renders the warning bar with Go to Settings link and X dismiss button, marked role="alert" + aria-live="polite". bun typecheck clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend OnboardingPage step machine + add OnboardingPage.test.tsx</name>
  <behavior>
    Red phase tests (create FIRST, confirm failing on current 4-step OnboardingPage, then extend to 5 steps):
    - Test 1: clicking Welcome's Continue reaches a screen with heading "Connect your AI" (proves Step 2 NEW step exists at index 1).
    - Test 2: with useProviders() mocked to return [], the Continue button on the Connect AI step is disabled (proves AIPV-06 gating).
    - Test 3: clicking "Skip for now" advances past the Connect AI step (heading no longer visible) WITHOUT calling useAiBannerStore.dismiss (isDismissed remains false — D-14/D-15 invariant).
  </behavior>
  <files>client/pages/OnboardingPage.tsx, client/pages/OnboardingPage.test.tsx</files>
  <read_first>
    client/pages/OnboardingPage.tsx
    client/components/ai/ConnectAiStep.tsx
    .planning/phases/07-ai-provider-foundation/07-UI-SPEC.md
    .planning/phases/07-ai-provider-foundation/07-PATTERNS.md
  </read_first>
  <action>
    Edit client/pages/OnboardingPage.tsx — extend step machine from 4 steps to 5 steps per D-12. The existing pattern (verified in PATTERNS.md and confirmed by reading the file) uses a step useState with values 0-3 and a StepIndicator total={4} prop.

    Edits:

    1. Import ConnectAiStep at top of file: import { ConnectAiStep } from "@/components/ai/ConnectAiStep".

    2. Change StepIndicator prop: total={4} → total={5} (UI-SPEC §"Onboarding Step Machine Extension" — dots auto-expand).

    3. Step machine remap. Existing code likely has either a switch statement on step or sequential conditional blocks (e.g., `step === 0 && <WelcomeStep>`, `step === 1 && <FoldersStep>`, etc.). Apply this remap:
       - step === 0: Welcome (unchanged)
       - step === 1: NEW — render ConnectAiStep with onContinue={() => setStep(2)} and onSkip={() => setStep(2)}
       - step === 2: Folders (was step 1 — change guard from `step === 1` to `step === 2`)
       - step === 3: Scanning (was step 2 — change `step === 2` to `step === 3`)
       - step === 4: Done (was step 3 — change `step === 3` to `step === 4`)

    4. Any "Next" / "Continue" buttons that previously did setStep(N+1) need their target updated to match the new indices. Specifically: Welcome's Continue should setStep(1) (was setStep(1) — Welcome→ConnectAi); Folders' Continue should setStep(3) (was setStep(2)); Scanning's auto-advance trigger should setStep(4) (was setStep(3)).

    5. If OnboardingPage tracks state like "selectedFolderCount" or other per-step values, those references stay the same — only the step indices change.

    Do NOT modify useOnboardingStore.isCompleted logic — completion is still triggered at step 4 (Done). Skip path from Connect AI step does NOT mark onboarding complete; it just advances to the next step (folder selection). Onboarding is "completed" when the user finishes all steps OR closes the wizard from the Done step.

    **TDD ORDERING (per project CLAUDE.md):** Create `client/pages/OnboardingPage.test.tsx` FIRST with the 3 tests below (using the mocked useTauri described). Run `bun test --run pages/OnboardingPage.test.tsx 2>&1 | tail -10` and confirm tests **fail** (Red phase) — they will fail because the current OnboardingPage has only 4 steps, no ConnectAiStep at index 1, and clicking Welcome's Continue lands on the old Folders step instead of "Connect your AI". Only AFTER confirming the failure modes match the expected Red state, proceed to extend OnboardingPage.tsx (Green phase). The final SUMMARY must record the Red→Green transition.

    Now create client/pages/OnboardingPage.test.tsx covering the AIPV-06 behaviors (RESEARCH Wave 0 gap).

    Imports: describe, it, expect, beforeEach, vi from vitest; render, screen, fireEvent from @testing-library/react; MemoryRouter from react-router-dom; QueryClient, QueryClientProvider from @tanstack/react-query; OnboardingPage from "./OnboardingPage"; useAiBannerStore from "@/lib/stores".

    Wrap renders with both providers — MemoryRouter and QueryClientProvider.

    Test 1: name "has 5 steps in the step machine". Render OnboardingPage. Assert via screen.queryByText or by looking for the StepIndicator props that 5 dots are rendered. Implementation hint: render and check that the document contains the new step 2 heading "Connect your AI" reachable from step 0 via Continue. Alternative: assert the StepIndicator component receives total={5} prop by spying or by inspecting rendered DOM (e.g., StepIndicator renders 5 dot elements in a flex row — count them via container.querySelectorAll('[data-step-dot]') or similar; if no test id exists, use the approach below).

    Practical Test 1 implementation: Render OnboardingPage. Find Welcome's Continue button (text "Continue" or similar). Click it. Assert that the heading "Connect your AI" is visible — this is unique to Step 2 (NEW). This proves Step 2 exists at index 1 in the machine.

    Test 2: name "Connect AI step Continue button is disabled until a provider is connected". Render OnboardingPage. Click Welcome's Continue to reach Step 2. Assert that the "Continue" button on the ConnectAiStep is disabled (has disabled attribute or aria-disabled="true"). Since the test environment runs without Tauri, useProviders() returns mockProviders fallback which has anthropic + ollama as authenticated. So hasConnectedProvider is true and Continue is enabled. To make this test work, mock useProviders to return []: vi.mock("@/hooks/useTauri", () => ({ useProviders: () => ({ data: [] }), useSaveSetupToken: () => ({ mutateAsync: vi.fn() }), useConnectProvider: () => ({ mutateAsync: vi.fn() }) })). With empty providers, Continue should be disabled.

    Test 3: name "Skip button advances past Connect AI step without connecting". Mock useProviders to return []. Render OnboardingPage, click Welcome Continue to reach Step 2, click "Skip for now". Assert the heading "Connect your AI" is NO LONGER visible (machine advanced past step 2 — now at step 2 which is Folders). useAiBannerStore state assertion: confirm isDismissed remains false (Skip should NOT auto-dismiss the banner — the banner appears post-onboarding-completion and is independently dismissible).

    Run cd client && bun test --run pages/OnboardingPage.test.tsx 2>&1 | tail -10 and expect 3 passes.
  </action>
  <verify>
    <automated>cd client &amp;&amp; bun test --run pages/OnboardingPage.test.tsx 2>&amp;1 | tail -10 | grep -E '3 pass|tests.*3 passed|✓ 3' &amp;&amp; bun typecheck 2>&amp;1 | tail -5 | grep -vqE 'error TS' &amp;&amp; grep -q 'total={5}' pages/OnboardingPage.tsx &amp;&amp; grep -q 'ConnectAiStep' pages/OnboardingPage.tsx</automated>
  </verify>
  <done>
    OnboardingPage.tsx has 5 steps; ConnectAiStep mounted at step index 1 with onContinue/onSkip wiring; StepIndicator total prop = 5. OnboardingPage.test.tsx has 3 passing tests covering step count, Continue gating, and Skip behavior. bun typecheck clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Mount AiNoProviderBanner in AppShell with Pitfall 6 guard</name>
  <files>client/components/layout/AppShell.tsx</files>
  <read_first>
    client/components/layout/AppShell.tsx
    client/components/ai/ConnectAiStep.tsx
    client/components/layout/AiNoProviderBanner.tsx
    client/lib/stores.ts
    client/hooks/useTauri.ts
    .planning/phases/07-ai-provider-foundation/07-UI-SPEC.md
    .planning/phases/07-ai-provider-foundation/07-PATTERNS.md
  </read_first>
  <action>
    Edit client/components/layout/AppShell.tsx to mount AiNoProviderBanner with the Pitfall 6 guard (banner only after onboarding completes).

    Imports to add: useAiBannerStore from "@/lib/stores" (append to existing stores import line — likely already imports useOnboardingStore, useSidebarStore, useCommandPaletteStore, useIndexingStore); useProviders from "@/hooks/useTauri"; AiNoProviderBanner from "@/components/layout/AiNoProviderBanner".

    Inside the AppShell component body (after existing hook calls — likely around the line that reads `const recluster = useReclusterSpaces()` or similar), add:
    ```
    const { isCompleted: onboardingCompleted } = useOnboardingStore();
    const { isDismissed: bannerDismissed } = useAiBannerStore();
    const { data: providers } = useProviders();
    const hasActiveProvider = providers?.some(p => p.isActive && p.authenticated) ?? false;
    const showBanner = onboardingCompleted && !hasActiveProvider && !bannerDismissed;
    ```

    NOTE: onboardingCompleted may already be derived from useOnboardingStore — if the destructure already exists, just add the new variables and reuse the existing one. Read AppShell.tsx carefully to avoid duplicate declarations.

    Mount the banner in the AppShell JSX. UI-SPEC §"AiNoProviderBanner" says the mount point is "between <CommandPalette /> and <Sidebar />, rendered as a fixed top strip above the sidebar+main layout." Practical placement: at the very top of the returned JSX fragment, BEFORE the existing layout grid (Sidebar + TopBar + main). The banner takes a single row at the top and pushes the rest of the app down.

    JSX addition (place inside the top-level return fragment / div, before the existing app shell layout):
    ```
    {showBanner && <AiNoProviderBanner />}
    ```

    Do NOT add new routing logic — the banner uses react-router useNavigate internally to go to /settings?tab=ai.

    Verify the Pitfall 6 guard works: render AppShell with onboardingCompleted=false → banner should NOT appear (this is the first-launch behavior; the onboarding wizard is the first-connect prompt). Render with onboardingCompleted=true + hasActiveProvider=false + bannerDismissed=false → banner SHOULD appear. Click X → bannerDismissed=true → banner disappears. Reload app (simulate by resetting Zustand store) → banner reappears.

    Since this is an integration concern and the existing test setup may not cover full AppShell rendering, add a small grep gate that proves the conditional is present and references all three guards. No new test file required — the OnboardingPage.test.tsx (Task 2) already tested the banner store invariant; the Settings end-to-end checkpoint in this plan covers the live banner behavior.

    Run cd client && bun typecheck and cd client && bun test --run. Confirm no regressions.
  </action>
  <verify>
    <automated>cd client &amp;&amp; bun typecheck 2>&amp;1 | tail -5 | grep -vqE 'error TS' &amp;&amp; bun test --run 2>&amp;1 | tail -10 | grep -qE 'pass|✓' &amp;&amp; grep -q 'AiNoProviderBanner' components/layout/AppShell.tsx &amp;&amp; grep -q 'showBanner' components/layout/AppShell.tsx &amp;&amp; grep -q 'onboardingCompleted.*hasActiveProvider\|hasActiveProvider.*onboardingCompleted\|onboardingCompleted &amp;&amp; !hasActiveProvider' components/layout/AppShell.tsx</automated>
  </verify>
  <done>
    AppShell.tsx imports AiNoProviderBanner and useAiBannerStore. showBanner computed from onboardingCompleted AND hasActiveProvider AND bannerDismissed (Pitfall 6 guard). Banner mounted conditionally at top of JSX. bun typecheck clean. All existing tests still pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: User verifies first-run onboarding flow + banner behavior</name>
  <what-built>
    Onboarding wizard extended to 5 steps with new Connect AI step at index 1. Skip path leaves no provider connected. AppShell mounts session-only banner gated on onboarding completion + no-active-provider + not-dismissed.
  </what-built>
  <how-to-verify>
    1. Wipe Cortex app data to simulate first-run: `rm -rf "$HOME/Library/Application Support/cortex"` (macOS path; adjust for your OS).
    2. Run `cargo tauri dev` from the repo root.
    3. First-run wizard appears. Welcome step (Step 0/5): click Continue.
    4. NEW Step 1/5: "Connect your AI". Verify:
       - Page heading "Connect your AI" visible.
       - 2x2 grid showing Anthropic, OpenAI, Gemini, Ollama cards.
       - Continue button DISABLED (no provider connected yet).
       - "Skip for now" link visible at the bottom.
    5. Click Skip for now. Verify:
       - Wizard advances to Step 2 (Folders).
       - No banner visible yet (still inside onboarding wizard — Pitfall 6 guard).
    6. Complete the rest of onboarding (select a folder, watch scanning, click Done on Spaces Ready). Wizard closes.
    7. Verify banner now appears at the top of the AppShell:
       - Single-line warning bar at very top of the window.
       - Text: "Connect an AI provider to enable Smart Spaces. Go to Settings →"
       - X dismiss button on the right.
       - AlertTriangle icon on the left.
    8. Click "Go to Settings →". Verify navigation to /settings with AI tab active.
    9. Go back to Dashboard. Banner still visible.
    10. Click the X dismiss button. Banner disappears.
    11. Navigate around (Dashboard, Spaces, etc.). Banner stays dismissed for the session.
    12. Quit Tauri (Cmd+Q) and re-launch with `cargo tauri dev`.
    13. Open the app. Verify banner reappears (D-15: session-only dismiss; persists nothing).
    14. Now connect any provider in Settings → AI (use the Plan 05 path). Banner disappears immediately (hasActiveProvider is now true).
    15. Disconnect that provider from Settings. Banner returns (still session-dismissible).
    16. (Optional) Test the "no Skip" path: wipe app data again, run onboarding, on Step 1 click Anthropic → paste a real setup-token → wait for Connected. Continue button enables. Click Continue. Wizard proceeds. After Done, NO banner appears (hasActiveProvider true since onboarding wizard wired the credential).

    If all 16 steps pass, type "approved". Otherwise describe what failed.
  </how-to-verify>
  <resume-signal>Type "approved" once the onboarding 5-step flow works end-to-end and the banner respects all three guards (onboarding completed, no active provider, not dismissed this session).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Onboarding wizard → app shell post-completion | Wizard closes and AppShell takes over; Pitfall 6 banner gate prevents double-prompt |
| useAiBannerStore state → AppShell render | Zustand store is in-memory only; no persistence so dismiss resets per session |
| AppShell `useNavigate(/settings?tab=ai)` → router | Standard react-router navigation; no cross-origin risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-20 | Tampering | Onboarding step shift mis-mapping | mitigate | OnboardingPage.test.tsx Test 1 asserts "Connect your AI" heading is reachable from Welcome's Continue, proving Step 2 lands at index 1 and Folders shift to index 2. |
| T-07-21 | DoS | Banner appears DURING onboarding wizard overlay | mitigate | Pitfall 6 guard — showBanner requires onboardingCompleted=true. Verified by user checkpoint step 5. |
| T-07-22 | Information Disclosure | Skip path implicitly stores any token | accept | No tokens stored if Skip pressed (ConnectAiStep doesn't auto-submit). Verified by user checkpoint step 5 — banner appears post-onboarding without any credential having been entered. |
| T-07-23 | Spoofing | Forged "showBanner" via direct Zustand mutation | accept | Single-user desktop app; the dev console can flip Zustand state but that's the user's own machine. Not a remote threat. |
</threat_model>

<verification>
After all tasks:
1. cd client && bun typecheck 2>&1 | tail -5 — clean
2. cd client && bun test --run 2>&1 | tail -15 — all green including OnboardingPage.test.tsx (3 passes)
3. grep -c "total={5}" client/pages/OnboardingPage.tsx — returns 1
4. grep -q "ConnectAiStep" client/pages/OnboardingPage.tsx — succeeds
5. grep -q "AiNoProviderBanner" client/components/layout/AppShell.tsx — succeeds
6. grep -q 'navigate.*settings\?tab=ai' client/components/layout/AiNoProviderBanner.tsx — succeeds
7. Human checkpoint approved (16-step flow validated)
</verification>

<success_criteria>
- ConnectAiStep.tsx renders 2x2 grid with inline connect forms; Continue + Skip wired
- AiNoProviderBanner.tsx renders warning bar with Go to Settings link + X dismiss
- OnboardingPage.tsx has 5 steps (StepIndicator total={5}); ConnectAiStep is step 1
- OnboardingPage.test.tsx has 3 passing tests
- AppShell.tsx mounts banner only when onboarding complete AND no active provider AND not dismissed
- User checkpoint validates first-run flow with wiped app_data_dir
- bun typecheck and bun test --run all clean
</success_criteria>

<output>
Create `.planning/phases/07-ai-provider-foundation/07-06-SUMMARY.md` when done. Include:
- 2 new component files + 1 new test file + 2 modified files
- Test count for OnboardingPage.test.tsx (3 expected)
- Confirmation of Pitfall 6 guard (banner gated on onboardingCompleted)
- Checkpoint approval signal recorded
- Note: end of Phase 7 — all AIPV-01..08 requirements covered across plans 01-06
</output>

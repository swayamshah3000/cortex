---
phase: 04-frontend-integration-and-ux
plan: 04
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - client/pages/RecentPage.tsx
  - client/pages/FavoritesPage.tsx
  - client/pages/TagsPage.tsx
  - client/pages/WatchedPage.tsx
  - client/App.tsx
autonomous: true
requirements: [PAGE-05, PAGE-06, PAGE-07, PAGE-08]

must_haves:
  truths:
    - "Recent page groups documents by day (Today, Yesterday, This Week, Older) using date-fns"
    - "Favorites page shows starred documents with sort options, uses useFavoriteDocuments() hook"
    - "Tags page shows tag cloud visualization and list view with document counts"
    - "Watched Folders page shows real folder status, pause/remove/scan actions, and file type toggles"
    - "All four pages use React Query hooks — no hardcoded data"
  artifacts:
    - path: "client/pages/RecentPage.tsx"
      provides: "Recent documents timeline grouped by date"
      min_lines: 80
    - path: "client/pages/FavoritesPage.tsx"
      provides: "Starred documents grid with sort controls"
      min_lines: 60
    - path: "client/pages/TagsPage.tsx"
      provides: "Tag cloud and tag list with document counts"
      min_lines: 80
    - path: "client/pages/WatchedPage.tsx"
      provides: "Watched folders management with pause/remove/scan actions"
      min_lines: 120
  key_links:
    - from: "client/pages/WatchedPage.tsx"
      to: "client/hooks/useTauri.ts"
      via: "useWatchedFolders, useAddWatchedFolder, useRemoveWatchedFolder, useTriggerScan"
      pattern: "useWatchedFolders|useAddWatchedFolder|useRemoveWatchedFolder|useTriggerScan"
---

<objective>
Build the four secondary content pages: Recent, Favorites, Tags, and Watched Folders — all wired to live backend data.

Purpose: These pages round out the core navigation. Watched Folders is especially critical as it's how users configure what Cortex monitors.
Output: Four pages replacing Placeholder components, all functional with live data.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@client/App.tsx
@client/hooks/useTauri.ts
@client/lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Recent documents page (PAGE-05)</name>
  <files>client/pages/RecentPage.tsx, client/App.tsx</files>
  <action>
  Create `client/pages/RecentPage.tsx`:

  1. Use `useRecentDocuments(50)` to fetch recent documents (from Plan 02's new hook).
  2. Group documents by date using date-fns:
     - Today: documents modified today
     - Yesterday: documents modified yesterday
     - This Week: documents modified this week (but not today/yesterday)
     - Older: everything else
  3. Each group has a header ("Today", "Yesterday", etc.) and a list of document rows.
  4. Document row: file type icon, name, space badges, excerpt preview, relative time.
  5. Click a document -> navigate to /document/:id
  6. Loading state: skeleton rows.
  7. Empty state: "No recent documents. Add a watched folder to start indexing."

  Update App.tsx: replace Placeholder on /recent with RecentPage.
  </action>
  <verify>cd /Users/gshah/work/apps/cortex && pnpm typecheck 2>&1 | tail -20</verify>
  <done>Recent page groups documents by day with live data. Route wired.</done>
</task>

<task type="auto">
  <name>Task 2: Build Favorites page (PAGE-06)</name>
  <files>client/pages/FavoritesPage.tsx, client/App.tsx</files>
  <action>
  Create `client/pages/FavoritesPage.tsx`:

  1. Use `useFavoriteDocuments()` hook (from Plan 02's new hook).
  2. Grid layout showing favorite documents as cards.
  3. Sort controls: by name, date modified, size.
  4. Each card: file type icon, name, space, date, size.
  5. Click card -> /document/:id
  6. Unfavorite button on each card using `useToggleFavorite()` mutation.
  7. Loading/empty states. Empty: "No favorites yet. Star documents to find them quickly here."

  Update App.tsx: replace Placeholder on /favorites with FavoritesPage.
  </action>
  <verify>cd /Users/gshah/work/apps/cortex && pnpm typecheck 2>&1 | tail -20</verify>
  <done>Favorites page shows starred documents with sort and unfavorite action. Route wired.</done>
</task>

<task type="auto">
  <name>Task 3: Build Tags page (PAGE-07)</name>
  <files>client/pages/TagsPage.tsx, client/App.tsx</files>
  <action>
  Create `client/pages/TagsPage.tsx`:

  1. Use `useTags()` hook to fetch all tags.
  2. Two views:
     - Tag cloud: tags displayed as badges with varying sizes based on document count. Use font-size scaling: min 14px (low count) to max 32px (high count).
     - List view: table with tag name, type (auto/user badge), document count, color dot.
  3. Toggle between cloud and list view.
  4. Filter: show/hide auto-generated vs user-created tags.
  5. Click a tag -> navigate to /search?tag={tagName} (pre-filtered search).
  6. Loading/empty states.

  Update App.tsx: replace Placeholder on /tags with TagsPage.
  </action>
  <verify>cd /Users/gshah/work/apps/cortex && pnpm typecheck 2>&1 | tail -20</verify>
  <done>Tags page shows cloud and list views with live data. Route wired.</done>
</task>

<task type="auto">
  <name>Task 4: Build Watched Folders management page (PAGE-08)</name>
  <files>client/pages/WatchedPage.tsx, client/App.tsx</files>
  <action>
  Create `client/pages/WatchedPage.tsx`:

  1. Use `useWatchedFolders()` to fetch folder list.
  2. Each folder card shows:
     - Folder path (JetBrains Mono font)
     - Document count
     - Last scan time (relative)
     - Status badge (watching=green, paused=yellow, error=red)
  3. Actions per folder:
     - Pause/Resume toggle (future: needs backend support — for now, just show status)
     - Remove: `useRemoveWatchedFolder()` mutation with confirm dialog
     - Scan Now: `useTriggerScan()` mutation — show progress indicator
  4. Add Folder button:
     - On click: use Tauri dialog.open() to pick a folder (in Tauri mode)
     - In browser mode: show a text input to enter a path
     - On confirm: `useAddWatchedFolder()` mutation
  5. Listen for `index-progress` Tauri events to show real-time scan progress (use @tauri-apps/api/event listen()). Show a progress bar or file count during active scans.
  6. Loading/empty states. Empty: "No folders being watched. Add a folder to start discovering documents."

  Update App.tsx: replace Placeholder on /watched with WatchedPage.
  </action>
  <verify>cd /Users/gshah/work/apps/cortex && pnpm typecheck 2>&1 | tail -20</verify>
  <done>Watched Folders page shows real folder status with add/remove/scan actions. Route wired.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck` — all new pages compile
- [ ] Recent page groups by Today/Yesterday/This Week/Older
- [ ] Favorites page shows starred documents with unfavorite action
- [ ] Tags page has cloud and list views
- [ ] Watched Folders page has add/remove/scan functionality
- [ ] All four Placeholder references removed from App.tsx for these routes
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Four secondary pages fully functional with live data
- Watched Folders page supports add/remove/scan
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-integration-and-ux/04-04-SUMMARY.md`
</output>

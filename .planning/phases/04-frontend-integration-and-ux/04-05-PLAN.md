---
phase: 04-frontend-integration-and-ux
plan: 05
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - client/pages/InsightsPage.tsx
  - client/pages/SettingsPage.tsx
  - client/App.tsx
autonomous: true
requirements: [PAGE-09, PAGE-10]

must_haves:
  truths:
    - "Insights page renders donut chart (docs by type), area chart (indexing over time), bar chart (top spaces), and space network graph"
    - "Charts use recharts library with Cortex design tokens for colors"
    - "Space network graph shows nodes (spaces) and edges (relationships) from useSpaceGraph() data"
    - "Settings page has 6 tabs: General, Indexing, AI & Models, Privacy, Storage, About"
    - "Settings use useSettings() and useUpdateSettings() hooks for persistence"
  artifacts:
    - path: "client/pages/InsightsPage.tsx"
      provides: "Analytics dashboard with 4 chart types from live data"
      min_lines: 150
    - path: "client/pages/SettingsPage.tsx"
      provides: "6-tab settings page with live read/write"
      min_lines: 150
  key_links:
    - from: "client/pages/InsightsPage.tsx"
      to: "client/hooks/useTauri.ts"
      via: "useStats, useSpaces, useSearchAnalytics, useSpaceGraph"
      pattern: "useStats|useSpaces|useSearchAnalytics|useSpaceGraph"
    - from: "client/pages/SettingsPage.tsx"
      to: "client/hooks/useTauri.ts"
      via: "useSettings, useUpdateSettings"
      pattern: "useSettings|useUpdateSettings"
---

<objective>
Build the Insights analytics page and Settings page — the last two content pages.

Purpose: Insights gives users visibility into their document corpus. Settings lets them configure indexing, AI, and privacy. Together they complete the 10 non-special pages.
Output: Insights with 4 chart types from real data, Settings with 6 tabs reading/writing live settings.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md

@client/App.tsx
@client/hooks/useTauri.ts
@client/lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Insights analytics page (PAGE-09)</name>
  <files>client/pages/InsightsPage.tsx, client/App.tsx</files>
  <action>
  Create `client/pages/InsightsPage.tsx`:

  1. Use `useStats()`, `useSpaces()`, `useSearchAnalytics()`, `useSpaceGraph()`, `useTags()` hooks.

  2. Layout: 2-column grid on large screens.

  3. Charts (all using recharts):
     a. **Donut chart — Documents by Type**: Aggregate documents across spaces by docType. Use PieChart with inner radius for donut effect. Colors: use file type colors from design system.
     b. **Area chart — Indexing Activity**: Show activity over time (from activity feed data, grouped by day). Use AreaChart with gradient fill in accent color.
     c. **Bar chart — Top Spaces**: Top 10 spaces by document count. Use BarChart with horizontal bars. Bar colors from space.color.
     d. **Stat cards row**: Total docs, total spaces, total searches (from search analytics), index size.

  4. **Space Network Graph**: Use a simple force-directed graph visualization.
     - Option A: Use recharts Treemap for a simpler visualization.
     - Option B: Use react-force-graph (if in deps) for interactive graph.
     - Option C: Build a simple SVG-based network graph.
     Since react-force-graph is NOT in deps but @react-three/fiber IS, use a 2D canvas approach: render nodes as circles sized by document count, positioned with a simple force layout. Actually, simplest approach: render a static graph using SVG with nodes positioned using a basic circular layout. Each node is a circle colored by space.color, sized by documentCount. Edges are lines between connected nodes. Hover shows space name and count.

  5. **Top Searches table**: From search analytics, show top queries and counts.

  6. Loading states: skeleton cards and charts.

  Update App.tsx: replace Placeholder on /insights with InsightsPage.
  </action>
  <verify>cd /Users/gshah/work/apps/cortex && pnpm typecheck 2>&1 | tail -20</verify>
  <done>Insights page renders 4 chart types and space network graph from live data. Route wired.</done>
</task>

<task type="auto">
  <name>Task 2: Build Settings page with 6 tabs (PAGE-10)</name>
  <files>client/pages/SettingsPage.tsx, client/App.tsx</files>
  <action>
  Create `client/pages/SettingsPage.tsx`:

  1. Use `useSettings()` to load current settings.
  2. Use `useUpdateSettings()` mutation to persist changes.
  3. Use shadcn/ui Tabs component for 6 tabs:

  **General tab:**
  - Theme selector: dark/light/system (radio group)
  - Sidebar collapsed default (toggle switch)

  **Indexing tab:**
  - Index on startup (toggle switch)
  - Excluded patterns list (comma-separated input or tag-style input)
  - Supported file types (checkboxes or toggles per type)

  **AI & Models tab:**
  - Embedding model selector: local (all-MiniLM-L6-v2) / OpenAI
  - If OpenAI selected: API key input (password field)
  - Model description text explaining local vs API tradeoffs

  **Privacy tab:**
  - Privacy mode toggle (when on, stricter data handling)
  - Telemetry toggle
  - "All processing runs locally" reassurance text

  **Storage tab:**
  - Current index size (formatted from settings.indexSize)
  - Storage path (shown in monospace, read-only)
  - Clear index button (with confirm dialog — future: wire to backend)
  - Watched folders count

  **About tab:**
  - App name: Cortex
  - Version: v1.0.0 (hardcoded for now)
  - Description: "Self-Organizing Document Intelligence"
  - Links: GitHub, Documentation
  - Technologies used

  4. Form pattern: load settings into local state, show Save button when dirty, call updateSettings on save.
  5. Toast notification on successful save.

  Update App.tsx: replace Placeholder on /settings with SettingsPage.
  </action>
  <verify>cd /Users/gshah/work/apps/cortex && pnpm typecheck 2>&1 | tail -20</verify>
  <done>Settings page has 6 tabs with live read/write. Route wired.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck` — both pages compile
- [ ] Insights page renders donut, area, bar charts and network graph
- [ ] Settings page has 6 functional tabs
- [ ] Both Placeholder references removed from App.tsx
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Insights shows real analytics data in charts
- Settings reads and writes live settings
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-integration-and-ux/04-05-SUMMARY.md`
</output>

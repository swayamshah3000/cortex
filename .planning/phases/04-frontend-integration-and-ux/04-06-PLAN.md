---
phase: 04-frontend-integration-and-ux
plan: 06
type: execute
wave: 3
depends_on: [04-02, 04-03, 04-04, 04-05]
files_modified:
  - client/pages/OnboardingPage.tsx
  - client/components/layout/CommandPalette.tsx
  - client/components/layout/AppShell.tsx
  - client/components/layout/TopBar.tsx
  - client/App.tsx
  - client/lib/stores.ts
autonomous: true
requirements: [PAGE-12, UX-01, UX-02, UX-03, UX-04]

must_haves:
  truths:
    - "Onboarding wizard has 4 steps: Welcome, Select Folders, Scanning Progress, Spaces Ready"
    - "Step 2 lets user pick folders using Tauri dialog or text input in browser mode"
    - "Step 3 shows live scanning progress by listening to index-progress Tauri events"
    - "Step 4 shows discovered spaces and navigates to /spaces on completion"
    - "Cmd+K command palette opens from any page and supports search + navigation"
    - "Keyboard shortcuts (Cmd+1/2/3, Cmd+comma, Cmd+D, Cmd+backslash) work globally"
    - "TopBar shows indexing progress indicator when background indexing is active"
    - "First-time users are redirected to /onboarding if no watched folders exist"
  artifacts:
    - path: "client/pages/OnboardingPage.tsx"
      provides: "4-step onboarding wizard with folder selection, scanning, and space discovery"
      min_lines: 200
    - path: "client/components/layout/CommandPalette.tsx"
      provides: "Cmd+K command palette overlay with search and navigation"
      min_lines: 100
    - path: "client/lib/stores.ts"
      provides: "Zustand stores for UI state: sidebar, command palette, indexing status, theme"
      min_lines: 60
  key_links:
    - from: "client/components/layout/CommandPalette.tsx"
      to: "client/hooks/useTauri.ts"
      via: "useDocumentSearch for palette search"
      pattern: "useDocumentSearch"
    - from: "client/pages/OnboardingPage.tsx"
      to: "client/hooks/useTauri.ts"
      via: "useAddWatchedFolder, useTriggerScan"
      pattern: "useAddWatchedFolder|useTriggerScan"
    - from: "client/lib/stores.ts"
      to: "client/components/layout/AppShell.tsx"
      via: "Zustand store consumed by layout"
      pattern: "useSidebarStore|useCommandPaletteStore"
---

<objective>
Build the onboarding wizard, command palette, keyboard shortcuts, and indexing progress indicator — completing all UX requirements and the final page.

Purpose: These are the polish features that make Cortex feel like a real desktop app. Onboarding is the new-user flow. Command palette is the power-user navigation. Keyboard shortcuts speed up daily use. Indexing indicator keeps users informed.
Output: Complete UX layer — onboarding, Cmd+K, keyboard shortcuts, indexing status.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md

@client/App.tsx
@client/components/layout/AppShell.tsx
@client/components/layout/TopBar.tsx
@client/components/layout/Sidebar.tsx
@client/hooks/useTauri.ts
@client/lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Zustand stores for UI state</name>
  <files>client/lib/stores.ts</files>
  <action>
  Create `client/lib/stores.ts` with Zustand stores:

  1. **useSidebarStore**: `{ isCollapsed, toggle, setCollapsed }`
  2. **useCommandPaletteStore**: `{ isOpen, open, close, toggle }`
  3. **useIndexingStore**: `{ isIndexing, currentFile, filesProcessed, totalFiles, setProgress, reset }`
  4. **useOnboardingStore**: `{ isCompleted, setCompleted }` — persisted to localStorage

  Keep stores small and focused. Use `zustand` create() for each.

  Note: Zustand is not currently in package.json. Add it: the stores can also be implemented with simple React context + useState for now. Actually, check package.json — zustand is NOT listed. Use React context + useReducer instead, OR add zustand as a dependency. Simplest: use zustand/vanilla with React hooks pattern. Since the project mentions Zustand in CLAUDE.md, install it.

  Actually, to avoid modifying package.json without explicit instruction, use a simple implementation with React createContext + useState + custom hooks that match the Zustand API shape. Put this in stores.ts. When zustand is installed later, migration is trivial.

  Alternative: just install zustand since CLAUDE.md explicitly specifies it. Add `zustand` to devDependencies and use it properly.

  Decision: Install zustand via `pnpm add zustand` as part of this task (it's in the tech spec).
  </action>
  <verify>cd /Users/gshah/work/apps/cortex && pnpm typecheck 2>&1 | tail -20</verify>
  <done>Zustand stores for sidebar, command palette, indexing, and onboarding state. Installed and compiling.</done>
</task>

<task type="auto">
  <name>Task 2: Build Onboarding wizard (PAGE-12)</name>
  <files>client/pages/OnboardingPage.tsx, client/App.tsx</files>
  <action>
  Create `client/pages/OnboardingPage.tsx`:

  4-step wizard with progress indicator:

  **Step 1 — Welcome:**
  - Cortex logo/icon
  - Title: "Welcome to Cortex"
  - Subtitle: "Find anything. Organize nothing."
  - Description: "Cortex watches your document folders and automatically organizes everything using AI."
  - "Get Started" button -> Step 2

  **Step 2 — Select Folders:**
  - Title: "Choose folders to watch"
  - Description: "Cortex will monitor these folders for documents"
  - Suggested folders: ~/Documents, ~/Desktop, ~/Downloads (shown as toggle cards)
  - "Add Custom Folder" button: Tauri dialog.open() or text input in browser mode
  - Selected folders list with remove button
  - "Start Scanning" button -> calls useAddWatchedFolder() for each selected folder, then useTriggerScan() for each, then advances to Step 3

  **Step 3 — Scanning Progress:**
  - Title: "Discovering your documents..."
  - Progress bar showing overall progress
  - Current file being indexed (scrolling text)
  - Files processed counter
  - Listen to Tauri `index-progress` events for real-time updates
  - Auto-advance to Step 4 when all scans complete (listen for scan-complete events)
  - "Skip" button to advance immediately

  **Step 4 — Spaces Ready:**
  - Title: "Your Smart Spaces are ready!"
  - Show discovered spaces (call useSpaces() and recluster)
  - Grid of space cards with icons and document counts
  - "Explore Your Spaces" button -> navigate to /spaces
  - Mark onboarding as completed in localStorage

  Step indicator: horizontal dots or progress bar at the top showing current step.

  Update App.tsx:
  - Replace Placeholder on /onboarding with OnboardingPage
  - Add redirect logic: in AppShell, check if onboarding is completed (localStorage). If not and no watched folders exist, redirect to /onboarding.
  </action>
  <verify>cd /Users/gshah/work/apps/cortex && pnpm typecheck 2>&1 | tail -20</verify>
  <done>Onboarding wizard with 4 steps, folder selection, scanning progress, and space discovery. Route wired with redirect logic.</done>
</task>

<task type="auto">
  <name>Task 3: Build Command Palette (UX-01)</name>
  <files>client/components/layout/CommandPalette.tsx, client/components/layout/AppShell.tsx</files>
  <action>
  Create `client/components/layout/CommandPalette.tsx`:

  1. Use the `cmdk` library (already in package.json as `cmdk`) for the command palette UI.
  2. Overlay: centered dialog with backdrop blur, shadcn Dialog or custom overlay.
  3. Input at top: search field with auto-focus.
  4. Groups:
     - **Navigation**: Dashboard, Spaces, Search, Recent, Favorites, Tags, Watched Folders, Insights, Settings
     - **Spaces**: list spaces from useSpaces() — clicking navigates to /spaces/:id
     - **Search Results**: if user types 3+ chars, show document search results from useDocumentSearch() (debounced)
     - **Actions**: Toggle theme, Toggle sidebar, Trigger re-cluster
  5. Each item: icon + label + optional keyboard shortcut hint.
  6. Enter key: navigate to selected item or execute action.
  7. Escape: close palette.
  8. Open trigger: Cmd+K (or Ctrl+K on non-Mac).

  Wire into AppShell:
  - Render CommandPalette component inside AppShell.
  - Use global keydown listener for Cmd+K.
  - Use command palette store to control open/close state.
  </action>
  <verify>cd /Users/gshah/work/apps/cortex && pnpm typecheck 2>&1 | tail -20</verify>
  <done>Command palette opens with Cmd+K from any page. Search and navigation work.</done>
</task>

<task type="auto">
  <name>Task 4: Wire keyboard shortcuts and indexing indicator (UX-02, UX-03, UX-04)</name>
  <files>client/components/layout/AppShell.tsx, client/components/layout/TopBar.tsx</files>
  <action>
  **Keyboard shortcuts (UX-02):**
  Add global useEffect in AppShell with keydown handler:
  - Cmd+K: open command palette (already wired)
  - Cmd+1: navigate to / (Dashboard)
  - Cmd+2: navigate to /spaces
  - Cmd+3: navigate to /search
  - Cmd+,: navigate to /settings
  - Cmd+D: toggle dark mode
  - Cmd+\: toggle sidebar collapsed
  - /: focus search input (when not already in an input/textarea)
  - Escape: close command palette / modal

  Use `useNavigate()` from react-router for navigation shortcuts.
  Prevent default browser behavior for these key combos.

  **Indexing indicator in TopBar (UX-04):**
  Update TopBar to show an indexing indicator:
  - Listen for Tauri `index-progress` events
  - When indexing is active, show a small animated indicator (pulsing dot or mini progress bar) next to the search bar
  - Show tooltip with "Indexing: {currentFile}" on hover
  - When idle, hide the indicator

  Use the indexing Zustand store to track state.

  **System tray (UX-03):**
  System tray requires Tauri plugin configuration in Rust. For this plan, add a comment/TODO noting that system tray setup requires:
  1. Adding `tauri-plugin-system-tray` to Cargo.toml
  2. Configuring tray icon and menu in Rust setup
  3. This is deferred to a stretch goal — the indexing indicator in TopBar provides the same visibility.

  Mark UX-03 as partially addressed (indicator in TopBar) with system tray as future enhancement.
  </action>
  <verify>cd /Users/gshah/work/apps/cortex && pnpm typecheck 2>&1 | tail -20</verify>
  <done>Keyboard shortcuts work globally. TopBar shows indexing indicator. System tray noted as future enhancement.</done>
</task>

<task type="auto">
  <name>Task 5: Final cleanup — remove all Placeholder references, verify all routes</name>
  <files>client/App.tsx, client/pages/Placeholder.tsx</files>
  <action>
  1. Verify every route in App.tsx uses a real page component, not Placeholder.
  2. All 12 routes should be wired:
     - / -> Index (Dashboard)
     - /spaces -> SpacesPage
     - /spaces/:id -> SpaceDetailPage
     - /search -> SearchPage
     - /recent -> RecentPage
     - /favorites -> FavoritesPage
     - /tags -> TagsPage
     - /watched -> WatchedPage
     - /insights -> InsightsPage
     - /settings -> SettingsPage
     - /onboarding -> OnboardingPage
     - /document/:id -> DocumentPage
  3. Remove Placeholder.tsx if no longer imported anywhere.
  4. Remove the Placeholder import from App.tsx.
  5. Verify pnpm typecheck passes with zero errors.
  6. Ensure AppShell properly handles sidebar collapsed state from Zustand store.
  </action>
  <verify>cd /Users/gshah/work/apps/cortex && pnpm typecheck 2>&1 | tail -20 && rg "Placeholder" client/ 2>/dev/null | head -5</verify>
  <done>All 12 routes wired to real pages. Placeholder removed. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck` — everything compiles
- [ ] All 12 routes use real page components (no Placeholder)
- [ ] Placeholder.tsx is deleted or unused
- [ ] Cmd+K opens command palette from any page
- [ ] Keyboard shortcuts navigate correctly
- [ ] Onboarding wizard has 4 functional steps
- [ ] TopBar shows indexing indicator during active indexing
- [ ] New user without watched folders gets redirected to /onboarding
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Complete UX layer operational: onboarding, command palette, shortcuts, indexing indicator
- All 12 pages wired, Placeholder removed
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-integration-and-ux/04-06-SUMMARY.md`
</output>

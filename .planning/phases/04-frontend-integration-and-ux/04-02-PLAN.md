---
phase: 04-frontend-integration-and-ux
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - client/pages/Index.tsx
  - client/components/layout/Sidebar.tsx
  - client/components/layout/TopBar.tsx
  - client/components/layout/AppShell.tsx
  - client/lib/utils.ts
autonomous: true
requirements: [PAGE-01]

must_haves:
  truths:
    - "Dashboard page uses useStats(), useSpaces(), useActivityFeed() hooks — no hardcoded data"
    - "Stats grid shows totalDocuments, smartSpaces, lastScan (relative time), indexSize (formatted bytes) from real data"
    - "Recent documents section calls a hook and displays actual document names, paths, and dates"
    - "Top spaces section uses useSpaces() and renders space names, counts, and icons from backend data"
    - "Activity timeline uses useActivityFeed() and shows real actions with timestamps"
    - "Sidebar spaces list is driven by useSpaces() data, not hardcoded array"
    - "Loading and empty states are handled — skeleton/spinner while loading, empty message when no data"
  artifacts:
    - path: "client/pages/Index.tsx"
      provides: "Dashboard page wired to live backend data via React Query hooks"
      min_lines: 150
    - path: "client/components/layout/Sidebar.tsx"
      provides: "Sidebar with live spaces list from useSpaces() hook"
      min_lines: 150
  key_links:
    - from: "client/pages/Index.tsx"
      to: "client/hooks/useTauri.ts"
      via: "useStats, useSpaces, useActivityFeed hooks"
      pattern: "useStats|useSpaces|useActivityFeed"
    - from: "client/components/layout/Sidebar.tsx"
      to: "client/hooks/useTauri.ts"
      via: "useSpaces hook for space list"
      pattern: "useSpaces"
---

<objective>
Wire the Dashboard page and layout components to live backend data, eliminating all hardcoded mock data from the main landing experience.

Purpose: The Dashboard is the first thing users see — it must show real data. The Sidebar spaces list is visible on every page, so it must also be live.
Output: Dashboard shows real stats/documents/spaces/activity. Sidebar renders real spaces. Loading and empty states handled.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@client/pages/Index.tsx
@client/components/layout/Sidebar.tsx
@client/components/layout/TopBar.tsx
@client/components/layout/AppShell.tsx
@client/hooks/useTauri.ts
@client/lib/types.ts
@client/lib/mock-data.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Dashboard (Index.tsx) to use live data hooks</name>
  <files>client/pages/Index.tsx</files>
  <action>
  Rewrite Index.tsx to use React Query hooks instead of hardcoded arrays:

  1. Import and call `useStats()` for the stats grid. Display:
     - Total Documents: format number with locale (e.g., 3,942)
     - Smart Spaces: count
     - Last Scan: use date-fns `formatDistanceToNow()` for relative time ("2 minutes ago")
     - Index Size: format bytes (e.g., "1.2 GB") using a utility function

  2. Add a `useRecentDocuments()` query or use existing hooks. The backend doesn't have a dedicated "recent documents" endpoint — use `useDocumentSearch` with empty query and sort by date, OR add a new `get_recent_documents` command. Simpler approach: add query key `recentDocuments` that calls `get_recent_documents` Tauri command (we'll need to add this to the backend, but for now use the existing search with a fallback). For Plan 02, use the existing mock data hook as a temporary bridge — the real endpoint will be wired in a later plan when we add the missing IPC command.

  Actually, looking at the hooks, there IS a `recentDocuments` query key but no dedicated hook beyond search. Create a `useRecentDocuments()` hook in useTauri.ts that invokes a `get_recent_documents` Tauri command, falling back to mock data. The backend command can be added as a simple wrapper that fetches all docs and sorts by modified_at descending, limited to 10. Add the backend command in this plan.

  3. Import and call `useSpaces()` for the Top Spaces section. Display top 5 spaces sorted by document count.

  4. Import and call `useActivityFeed()` for the activity timeline.

  5. Add loading states: use Skeleton components from shadcn/ui while data loads.

  6. Add empty states: when no documents/spaces exist, show helpful message ("Add a watched folder to get started").

  7. Add a utility function `formatBytes(bytes: number)` in `client/lib/utils.ts`.

  8. The search bar on Dashboard should navigate to /search on click or type, not be an inline search.
  </action>
  <verify>cd /Users/gshah/work/apps/cortex && pnpm typecheck 2>&1 | tail -20</verify>
  <done>Dashboard page uses live data hooks. Loading/empty states handled. No hardcoded mock data in Index.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Sidebar spaces list to useSpaces() hook</name>
  <files>client/components/layout/Sidebar.tsx</files>
  <action>
  Update Sidebar.tsx:

  1. Replace the hardcoded spaces array (`Property`, `Kids`, `Work`, `Invoices`) with `useSpaces()` hook data.
  2. Limit sidebar spaces to first 6 spaces (sorted by document count descending).
  3. Show a "View All" link below spaces that navigates to /spaces.
  4. Handle loading state with 4 skeleton lines.
  5. Handle empty state: show "No spaces yet" text.
  6. Replace hardcoded storage bar ("1.2 GB / 5 GB") with data from `useStats()` hook — use `indexSize` for current and a config value for quota.
  7. Space color dots should use the space's `color` field from backend data.
  8. The Cmd+K button in sidebar should eventually open the command palette. For now, add an `onClick` handler that we'll wire in Plan 05 (UX plan).
  </action>
  <verify>cd /Users/gshah/work/apps/cortex && pnpm typecheck 2>&1 | tail -20</verify>
  <done>Sidebar spaces list driven by useSpaces() data. Storage bar shows real index size.</done>
</task>

<task type="auto">
  <name>Task 3: Add get_recent_documents backend command and hook</name>
  <files>src-tauri/src/commands/documents.rs, src-tauri/src/lib.rs, client/hooks/useTauri.ts</files>
  <action>
  Backend:
  1. Add `get_recent_documents` command in commands/documents.rs:
     - Takes optional `limit: usize` parameter (default 10)
     - Reads all documents from documents_384 collection
     - Sorts by modified_at descending
     - Returns first `limit` documents
  2. Register the command in lib.rs invoke_handler.

  Frontend:
  3. Add `useRecentDocuments(limit?: number)` hook in useTauri.ts:
     - Query key: `queryKeys.recentDocuments`
     - Calls `get_recent_documents` with `{ limit: limit ?? 10 }`
     - Mock fallback: return first `limit` mockDocuments sorted by modifiedAt desc
  </action>
  <verify>cd /Users/gshah/work/apps/cortex/src-tauri && cargo build 2>&1 | tail -10 && cd /Users/gshah/work/apps/cortex && pnpm typecheck 2>&1 | tail -10</verify>
  <done>get_recent_documents Tauri command exists. useRecentDocuments hook wired. Both compile.</done>
</task>

<task type="auto">
  <name>Task 4: Add get_favorite_documents backend command and hook</name>
  <files>src-tauri/src/commands/documents.rs, src-tauri/src/lib.rs, client/hooks/useTauri.ts</files>
  <action>
  Backend:
  1. Add `get_favorite_documents` command in commands/documents.rs:
     - Reads all documents from documents_384 collection
     - Filters to only is_favorite == true
     - Returns as Vec<Document>
  2. Register the command in lib.rs invoke_handler.

  Frontend:
  3. Add `useFavoriteDocuments()` hook in useTauri.ts:
     - Query key: `queryKeys.favoriteDocuments`
     - Calls `get_favorite_documents`
     - Mock fallback: return mockDocuments.filter(d => d.isFavorite)
  </action>
  <verify>cd /Users/gshah/work/apps/cortex/src-tauri && cargo build 2>&1 | tail -10 && cd /Users/gshah/work/apps/cortex && pnpm typecheck 2>&1 | tail -10</verify>
  <done>get_favorite_documents Tauri command exists. useFavoriteDocuments hook wired. Both compile.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd src-tauri && cargo build` — new commands compile
- [ ] `cd src-tauri && cargo test --lib` — existing tests pass
- [ ] `pnpm typecheck` — TypeScript compiles
- [ ] Dashboard page (Index.tsx) has zero hardcoded data arrays
- [ ] Sidebar spaces list is driven by useSpaces() hook
- [ ] New backend commands: get_recent_documents, get_favorite_documents
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Dashboard and sidebar show live data in Tauri mode
- Dashboard and sidebar show mock data in browser dev mode
- Loading and empty states render correctly
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-integration-and-ux/04-02-SUMMARY.md`
</output>

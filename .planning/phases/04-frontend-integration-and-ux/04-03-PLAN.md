---
phase: 04-frontend-integration-and-ux
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - client/pages/SpacesPage.tsx
  - client/pages/SpaceDetailPage.tsx
  - client/pages/SearchPage.tsx
  - client/pages/DocumentPage.tsx
  - client/App.tsx
autonomous: true
requirements: [PAGE-02, PAGE-03, PAGE-04, PAGE-11]

must_haves:
  truths:
    - "Spaces page shows grid of all Smart Spaces from useSpaces() with grid/list toggle"
    - "Space detail page shows documents in that space from useSpaceDocuments(), sub-spaces, and related spaces"
    - "Search page has split-pane layout: search results on left, document preview on right"
    - "Search uses useDocumentSearch() with 150ms debounce and displays results with scores and excerpts"
    - "Document detail page shows document preview (65%) and metadata sidebar (35%)"
    - "All pages handle loading, empty, and error states"
  artifacts:
    - path: "client/pages/SpacesPage.tsx"
      provides: "Smart Spaces grid/list page with grid/list toggle, sorting, and live data"
      min_lines: 100
    - path: "client/pages/SpaceDetailPage.tsx"
      provides: "Space detail with documents, sub-spaces, and related spaces sections"
      min_lines: 120
    - path: "client/pages/SearchPage.tsx"
      provides: "Semantic search with split-pane results+preview, filter bar, debounced input"
      min_lines: 150
    - path: "client/pages/DocumentPage.tsx"
      provides: "Document detail: 65% preview area + 35% metadata sidebar with entities, tags, spaces"
      min_lines: 120
  key_links:
    - from: "client/pages/SearchPage.tsx"
      to: "client/hooks/useTauri.ts"
      via: "useDocumentSearch with debounce"
      pattern: "useDocumentSearch"
    - from: "client/pages/SpaceDetailPage.tsx"
      to: "client/hooks/useTauri.ts"
      via: "useSpaceDocuments, useSpaces"
      pattern: "useSpaceDocuments|useSpaces"
---

<objective>
Build the four core content pages: Spaces grid, Space detail, Search, and Document detail — all wired to live backend data.

Purpose: These are the most-used pages after Dashboard. Users need to browse spaces, search documents, and view document details. All four are Placeholder pages today.
Output: Four fully functional pages replacing Placeholder components.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md

@client/App.tsx
@client/pages/Placeholder.tsx
@client/hooks/useTauri.ts
@client/lib/types.ts
@client/lib/mock-data.ts
@client/components/layout/AppShell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Spaces grid page (PAGE-02)</name>
  <files>client/pages/SpacesPage.tsx, client/App.tsx</files>
  <action>
  Create `client/pages/SpacesPage.tsx`:

  1. Use `useSpaces()` hook to fetch all spaces.
  2. Grid/list toggle: two display modes controlled by local state.
     - Grid mode: 3-column grid of space cards. Each card shows:
       - Icon (resolve from space.icon string to Lucide component using a mapping util)
       - Space name
       - Document count
       - Color accent (border-left using space.color)
       - Sample files preview (first 2-3 filenames)
       - Last updated (relative time)
     - List mode: table rows with icon, name, count, last updated.
  3. Sort by: document count (default), name, last updated.
  4. Click a space card -> navigate to /spaces/:id
  5. Loading state: skeleton grid.
  6. Empty state: "No Smart Spaces discovered yet. Add watched folders and index documents to auto-generate spaces."
  7. Create a shared `resolveIcon(iconName: string)` utility that maps Lucide icon name strings (e.g., "Home", "Briefcase") to actual Lucide React components. Put this in `client/lib/icons.ts`.

  Update `client/App.tsx`:
  - Import SpacesPage
  - Replace `<Placeholder />` on `/spaces` route with `<SpacesPage />`
  </action>
  <verify>cd /Users/gshah/work/apps/cortex && pnpm typecheck 2>&1 | tail -20</verify>
  <done>Spaces grid page renders live data with grid/list toggle. Route wired in App.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Build Space detail page (PAGE-03)</name>
  <files>client/pages/SpaceDetailPage.tsx, client/App.tsx</files>
  <action>
  Create `client/pages/SpaceDetailPage.tsx`:

  1. Get space ID from URL params (`useParams()`).
  2. Use `useSpaces()` to find the current space by ID (from the flat list).
  3. Use `useSpaceDocuments(spaceId)` to get documents in this space.
  4. Layout:
     - Header: space icon, name, color accent, document count, last updated
     - Sub-spaces: if space has subSpaces, show them as mini cards linking to /spaces/:subId
     - Documents list: table or grid of documents with name, type icon, size, date, excerpt preview
     - Each document row/card links to /document/:docId
     - Related spaces: fetch all spaces, show ones that share documents or edges (use space graph for this — simplified: show spaces whose IDs appear in the current space's documents' spaceIds)
  5. Breadcrumb: Home > Spaces > {Space Name} (and parent space if applicable)
  6. Loading/empty states.

  Update `client/App.tsx`:
  - Import SpaceDetailPage
  - Replace `<Placeholder />` on `/spaces/:id` route with `<SpaceDetailPage />`
  </action>
  <verify>cd /Users/gshah/work/apps/cortex && pnpm typecheck 2>&1 | tail -20</verify>
  <done>Space detail page shows documents, sub-spaces, and related spaces. Route wired.</done>
</task>

<task type="auto">
  <name>Task 3: Build Search page with split-pane (PAGE-04)</name>
  <files>client/pages/SearchPage.tsx, client/App.tsx</files>
  <action>
  Create `client/pages/SearchPage.tsx`:

  1. Search input with 150ms debounce (use useState + useEffect with setTimeout for debounce).
  2. Filter bar below search: doc type chips, date range, space filter, tag filter.
     - Use FilterChip components (simple toggle buttons for each filter option).
     - Build SearchFilters from active chips and pass to useDocumentSearch().
  3. Split-pane layout using react-resizable-panels:
     - Left panel (60%): search results list
       - Each result shows: file type icon, document name, space, score badge, matched excerpt
       - Click selects a result and shows it in the preview panel
     - Right panel (40%): document preview
       - Show selected document's excerpt, entities, tags, metadata
       - Link to full document page (/document/:id)
  4. Use `useDocumentSearch(debouncedQuery, filters)` hook.
  5. Show result count and search time.
  6. Loading state: skeleton results while searching.
  7. Empty states: "Start typing to search" (no query), "No results found" (query but no matches).
  8. Wire record_search_click: when user clicks a result, call the record_search_click Tauri command (add a useRecordSearchClick mutation hook if not present).

  Update `client/App.tsx`:
  - Import SearchPage
  - Replace `<Placeholder />` on `/search` route with `<SearchPage />`
  </action>
  <verify>cd /Users/gshah/work/apps/cortex && pnpm typecheck 2>&1 | tail -20</verify>
  <done>Search page has split-pane with debounced search, filters, results, and preview panel.</done>
</task>

<task type="auto">
  <name>Task 4: Build Document detail page (PAGE-11)</name>
  <files>client/pages/DocumentPage.tsx, client/App.tsx</files>
  <action>
  Create `client/pages/DocumentPage.tsx`:

  1. Get document ID from URL params.
  2. Use `useDocument(id)` to fetch document data.
  3. Use `useRelatedDocuments(id)` for the related docs section.
  4. Split layout: 65% preview / 35% metadata sidebar.
     - Preview panel (left):
       - Document name as title
       - File type badge
       - Full excerpt text
       - Path shown in JetBrains Mono font
       - "Open in Finder" button (future: Tauri shell.open)
     - Metadata sidebar (right):
       - File info: type, size (formatted), created date, modified date
       - Spaces: list of spaces this doc belongs to (clickable links to /spaces/:id)
       - Tags: badge list of tags
       - Extracted entities: table of label, value, type
       - Related documents: list with name and score (clickable)
       - Favorite toggle button (useToggleFavorite mutation)
  5. Breadcrumb: Home > {Space Name} > {Document Name}
  6. Loading/error/not-found states.

  Update `client/App.tsx`:
  - Add route for `/document/:id` with DocumentPage component
  - Import DocumentPage
  </action>
  <verify>cd /Users/gshah/work/apps/cortex && pnpm typecheck 2>&1 | tail -20</verify>
  <done>Document detail page shows preview and metadata sidebar with live data. Route wired.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck` — all new pages compile
- [ ] SpacesPage renders with useSpaces() data
- [ ] SpaceDetailPage renders with useSpaceDocuments() data
- [ ] SearchPage has debounced search with useDocumentSearch()
- [ ] DocumentPage shows document with useDocument() and useRelatedDocuments()
- [ ] All four Placeholder references removed from App.tsx for these routes
- [ ] Grid/list toggle works on Spaces page
- [ ] Split-pane layout works on Search and Document pages
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Four core pages fully functional with live data
- Loading, empty, and error states handled on all pages
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-integration-and-ux/04-03-SUMMARY.md`
</output>

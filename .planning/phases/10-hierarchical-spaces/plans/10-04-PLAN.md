---
phase: 10-hierarchical-spaces
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - src-tauri/src/spaces/llm_labeler.rs
autonomous: true
requirements: [HSPC-02]

must_haves:
  truths:
    - "label_sub_cluster() returns a SpaceLabel that never equals the parent_label (case-insensitive)"
    - "The sub-space prompt injects parent_label as sanitized context and asks the LLM for a distinct 2-4 word sub-label"
    - "label_sub_cluster reuses the existing SPACE_LABEL_PROMPT system prompt + build_user_content + ai_request_with_retry pipeline — no new HTTP path"
    - "Sanitization (sanitize_field) applies to parent_label before injection to mitigate prompt injection via adversarial parent labels"
  artifacts:
    - path: "src-tauri/src/spaces/llm_labeler.rs"
      provides: "label_sub_cluster() async fn + SUB_SPACE_LABEL_PREFIX constant"
      contains: "pub async fn label_sub_cluster"
  key_links:
    - from: "manager.rs recluster sub-space pass (Plan 05)"
      to: "llm_labeler::label_sub_cluster"
      via: "async call per sub-cluster after top-level pass finishes"
      pattern: "label_sub_cluster"
    - from: "label_sub_cluster"
      to: "ai_request_with_retry"
      via: "existing Phase 7 provider router"
      pattern: "ai_request_with_retry"
---

<objective>
Add a `label_sub_cluster()` async function to `spaces/llm_labeler.rs` that reuses the Phase 9 labeling pipeline (system prompt, sanitizer, JSON fence-strip, retry loop) but prepends parent-context to the user message so the LLM emits a sub-label distinct from the parent (D-05, HSPC-02).

Purpose: One new function — no new HTTP surface, no new retry policy, no new prompt template. The only Phase 10 delta is (a) a sanitized parent_label prepended to the user content, (b) an implicit avoid-list containing the parent label to force distinction, and (c) a small SUB_SPACE_LABEL_PREFIX constant that carries the parent-context sentence.

Output:
- One new async function `label_sub_cluster()` in llm_labeler.rs.
- One new constant `SUB_SPACE_LABEL_PREFIX` (documented, referenced by the function).
- 3 unit tests: prompt shape (parent context present), sanitization applied, avoid-list contains parent_label.

Per user decisions:
- D-05: reuse Phase 9 `LlmSpaceLabeler` with prompt variant. Prefix format from CONTEXT.md: `"You are labeling a sub-space of the '{parent_label}' Space. Return a 2-4 word label distinct from '{parent_label}'. Cluster documents: [...]"`. RESEARCH.md refines this to prepend parent-context to the user content while keeping SPACE_LABEL_PROMPT unchanged as the system prompt — that keeps the few-shot exemplars intact.
- D-05 (research refinement): parent_label goes into the avoid-list too, so the same collision-retry infrastructure enforces distinctness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-hierarchical-spaces/10-CONTEXT.md
@.planning/phases/10-hierarchical-spaces/10-RESEARCH.md

<interfaces>
<!-- Phase 9 labeling pipeline surface — reuse verbatim -->

From src-tauri/src/spaces/llm_labeler.rs (verified from Phase 9):
```rust
pub const SPACE_LABEL_PROMPT: &str = "..."; // 6 few-shot examples — DO NOT MODIFY
pub const LABEL_TEMPERATURE: f64 = 0.3;
pub const MAX_LABEL_RETRIES: u8 = 3;

pub struct SpaceLabel { pub label: String, pub description: String }

pub(crate) fn build_user_content(
    doc_titles: &[String],
    entity_summary: &str,
    top_topics: &[String],
    top_tags: &[String],
    doc_count: usize,
    avoid: &[String],
) -> String;

fn sanitize_field(s: &str) -> String;  // private in llm_labeler.rs

pub async fn label_with_avoid_list(
    auth: &AuthState,
    model: &str,
    doc_titles: &[String],
    entity_summary: &str,
    top_topics: &[String],
    top_tags: &[String],
    doc_count: usize,
    avoid: &[String],
) -> Result<SpaceLabel, String>;
```

`sanitize_field` is currently private (`fn sanitize_field`). This plan must either (a) leave it private and reuse it internally within the same module (preferred since label_sub_cluster lives in llm_labeler.rs), or (b) if for any reason the sub function ends up outside the module, promote to `pub(crate)`. Option (a) is the plan.

`build_user_content` is `pub(crate)` — reusable from any code in this crate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SUB_SPACE_LABEL_PREFIX + label_sub_cluster() + tests</name>
  <files>src-tauri/src/spaces/llm_labeler.rs</files>
  <behavior>
    - Test 1 (test_sub_space_prompt_includes_parent_context): a helper that builds the sub-cluster user content (extracted or inlined for testability) contains the exact string `"Parent Space:"` followed by the parent label. Verifies parent context is injected.
    - Test 2 (test_sub_space_prompt_sanitizes_parent_label): passing a parent_label containing a control character (e.g. `"Foo\x00Injected"`) produces a user content string that does not contain the control byte — sanitize_field strips it (T-10-07 mitigation).
    - Test 3 (test_sub_space_avoid_list_contains_parent): the internal build produces an avoid-list that includes the parent label, so a plain LLM even without following the "distinct from" instruction still triggers the existing Phase 9 collision retry infrastructure.
    - Note: The full async LLM call cannot be unit tested without a mock provider. Tests 1-3 target a pure-Rust helper you'll factor out (see action). The async wrapper is exercised via integration in Plan 05.
  </behavior>
  <action>
    Edit src-tauri/src/spaces/llm_labeler.rs. Add all new items directly below the existing `label_with_avoid_list` function (after line 252) and before the `resolve_collisions` block.

    1. Add constant near the top of the file (below LABEL_TEMPERATURE / MAX_LABEL_RETRIES):
       ```
       /// Prefix injected into the sub-space labeling user content (D-05).
       /// The parent context is prepended so the LLM produces a label distinct from the parent.
       /// Format: "Parent Space: \"{parent_label}\"\nReturn a 2-4 word label that is distinct from \"{parent_label}\" and specific to this sub-group.\n\n"
       pub const SUB_SPACE_LABEL_PREFIX: &str = "Parent Space:";
       ```

    2. Add a private (or pub(crate)) helper `fn build_sub_space_user_content` that:
       - Sanitizes parent_label via the existing sanitize_field (private, same module → direct call).
       - Builds an avoid list `vec![sanitized_parent_label.clone()]` — parent label goes into the avoid list, so the existing Phase 9 collision retry path fires cleanly if the LLM ignores the "distinct from" instruction.
       - Calls `build_user_content(doc_titles, entity_summary, top_topics, top_tags, doc_count, &avoid)`.
       - Returns `format!("Parent Space: \"{safe_parent}\"\nReturn a 2-4 word label that is distinct from \"{safe_parent}\" and specific to this sub-group.\n\n{base}")`.

       This helper is the object under test for the three unit tests — pure, deterministic, no async.

    3. Add the public async function:
       ```rust
       pub async fn label_sub_cluster(
           auth: &AuthState,
           model: &str,
           parent_label: &str,
           doc_titles: &[String],
           entity_summary: &str,
           top_topics: &[String],
           top_tags: &[String],
           doc_count: usize,
       ) -> Result<SpaceLabel, String>
       ```

       Implementation:
       - Build user content via `build_sub_space_user_content(parent_label, doc_titles, entity_summary, top_topics, top_tags, doc_count)`.
       - Construct an AIServiceRequest identical to `label_with_avoid_list` (system_prompt = SPACE_LABEL_PROMPT — DO NOT MODIFY the exemplars; max_tokens=Some(256); temperature=Some(LABEL_TEMPERATURE); response_format=None; model_override handling identical).
       - Call `ai_request_with_retry(auth, req, MAX_LABEL_RETRIES).await`, propagate errors as `format!("LLM sub-label call failed: {}", e)`.
       - Strip JSON fences via existing `strip_json_fences` and deserialize into `SpaceLabel`. Error message on parse failure: `format!("LLM sub-label JSON parse failed: {}", e)`.

    4. Add doc comments for the new fn referencing D-05, HSPC-02, threat T-10-07 (prompt injection via parent_label mitigated by sanitize_field), and the fact that the parent label is added to the avoid-list so the Phase 9 collision retry path can handle LLM defiance transparently.

    5. Add the three unit tests from `<behavior>` inside the existing `#[cfg(test)] mod tests` block (or create one if none exists — check whether llm_labeler.rs already has tests; if yes, extend, else add). Tests target `build_sub_space_user_content` — the pure helper — NOT the async fn. Do not attempt to mock ai_request_with_retry.

    Do NOT modify SPACE_LABEL_PROMPT — keeping the 6 few-shot exemplars intact preserves output quality parity with top-level labeling.
    Do NOT change the sanitize_field function signature — call it directly since label_sub_cluster is in the same module.
  </action>
  <verify>
    <automated>cargo test -p cortex --lib spaces::llm_labeler 2>&1 | tail -25</automated>
  </verify>
  <done>The three new tests pass; existing Phase 9 llm_labeler tests still pass. `grep -c "label_sub_cluster" src-tauri/src/spaces/llm_labeler.rs` returns >= 2 (definition + doc reference). `grep -c "SUB_SPACE_LABEL_PREFIX" src-tauri/src/spaces/llm_labeler.rs` returns >= 1. `cargo check -p cortex` reports zero new warnings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parent_label (LLM-generated, cached) → LLM prompt | Parent labels come from prior Phase 9 LLM calls; an adversarial doc corpus could have influenced them. Sanitize before injection. |
| AI provider response → SpaceLabel struct | Existing Phase 9 mitigation (strip_json_fences + serde parse + retry) applies unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Tampering | parent_label → sub-space LLM prompt | mitigate | Apply existing `sanitize_field` to parent_label before injection — strips control chars, caps at 100 chars. Matches Phase 9 T-09-01 pattern. Validated by test_sub_space_prompt_sanitizes_parent_label. |
| T-10-08 | Denial of Service | Sub-space labeling per parent | mitigate | Bounded by SUB_SPACE_THRESHOLD (50) gate + MIN_SUB_CLUSTER_SIZE (3) filter + fingerprint cache. Max LLM calls per recluster ≈ N_large_parents × k_sub × 1_call. |
</threat_model>

<verification>
- `cargo test -p cortex --lib spaces::llm_labeler` — all Phase 9 tests + 3 new Phase 10 tests green.
- `cargo check -p cortex` — no new warnings introduced.
- `grep "SPACE_LABEL_PROMPT" src-tauri/src/spaces/llm_labeler.rs | head -1` — unchanged.
</verification>

<success_criteria>
- New public async function `label_sub_cluster` in llm_labeler.rs.
- New public constant `SUB_SPACE_LABEL_PREFIX`.
- Pure helper `build_sub_space_user_content` covered by 3 unit tests (context injection, sanitization, avoid-list containing parent).
- No new HTTP path, no new prompt template, no new retry policy — full reuse of Phase 9 infrastructure per D-05.
- SPACE_LABEL_PROMPT unchanged.
</success_criteria>

<output>
Create `.planning/phases/10-hierarchical-spaces/10-04-SUMMARY.md` when done.
</output>

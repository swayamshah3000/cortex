---
phase: 10-hierarchical-spaces
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - client/lib/types.ts
  - client/lib/stores.ts
  - client/lib/stores.test.ts
autonomous: true
requirements: [HSPC-01, HSPC-04]

must_haves:
  truths:
    - "Frontend Space type exposes depth?: number and subSpaceIds?: string[] mirroring the Rust struct"
    - "useSidebarStore exposes expandedSpaceIds: Set<string> plus toggleSpaceExpanded and isSpaceExpanded actions"
    - "expandedSpaceIds is session-only (no persist middleware) per UI-SPEC §Data Type Extensions and D-13"
    - "Toggling a space id via toggleSpaceExpanded is idempotent — second call removes the id from the set"
  artifacts:
    - path: "client/lib/types.ts"
      provides: "Space interface with Phase 10 depth + subSpaceIds fields"
      contains: "subSpaceIds"
    - path: "client/lib/stores.ts"
      provides: "useSidebarStore with expandedSpaceIds Set + toggle action"
      contains: "expandedSpaceIds"
    - path: "client/lib/stores.test.ts"
      provides: "Vitest coverage of Phase 10 sidebar store behavior"
      contains: "toggleSpaceExpanded"
  key_links:
    - from: "Sidebar.tsx (Plan 07)"
      to: "useSidebarStore.expandedSpaceIds"
      via: "useSidebarStore hook selector"
      pattern: "expandedSpaceIds"
    - from: "SpaceDetailPage.tsx (Plan 08)"
      to: "Space.subSpaceIds / Space.depth"
      via: "typed IPC response"
      pattern: "subSpaceIds\\?:|depth\\?:"
---

<objective>
Extend the frontend Space interface with `depth?: number` and `subSpaceIds?: string[]` mirroring the Rust struct additions from Plan 01, and add `expandedSpaceIds: Set<string>` plus toggle actions to useSidebarStore. Both changes are foundational for the UI plans (07, 08) and can be verified independently.

Purpose: Frontend must be able to typecheck and store hierarchical sidebar state before the UI plans consume it. Store is session-only per D-13 and UI-SPEC — no persist middleware, matches useAiBannerStore convention.

Output:
- Space type in types.ts with two new optional fields (undefined-friendly for pre-Phase 10 IPC payloads).
- useSidebarStore extended with expandedSpaceIds Set + toggleSpaceExpanded + isSpaceExpanded selectors.
- stores.test.ts covers the new store behavior (add / remove / idempotency / no-persist contract).

Per user decisions:
- D-07: field names must be `depth`, `subSpaceIds` — camelCase mirror of Rust `sub_space_ids`.
- D-13: expandedSpaceIds is session-only (no localStorage). The UI-SPEC §"Data Type Extensions" is explicit: "NOT persisted (no persist middleware for this key). Resets to empty on app restart."
- D-13 (discretion — RESEARCH.md Alternatives Considered): the researcher proposed a persisted variant, but the UI-SPEC locked no-persist. Follow UI-SPEC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-hierarchical-spaces/10-CONTEXT.md
@.planning/phases/10-hierarchical-spaces/10-UI-SPEC.md
@.planning/phases/10-hierarchical-spaces/10-RESEARCH.md

<interfaces>
<!-- Existing Space interface — mirrors Rust types.rs -->

From client/lib/types.ts (lines 117-145):
```typescript
export interface Space {
  id: string;
  name: string;
  icon: string;
  color: string;
  documentCount: number;
  lastUpdated: string;
  subSpaces: Space[];           // Existing nested array — kept for backward compat
  parentId?: string;             // ALREADY PRESENT (Phase 4)
  sampleFiles: string[];
  description?: string;
  userLocked?: boolean;
  canonicalEntityHint?: string;
  labelStatus?: 'ready' | 'generating';
  // Phase 10 additions land HERE (after labelStatus)
}
```

From client/lib/stores.ts (lines 17-27):
```typescript
interface SidebarState {
  isCollapsed: boolean;
  toggle: () => void;
  setCollapsed: (collapsed: boolean) => void;
}
export const useSidebarStore = create<SidebarState>((set) => ({...}));
```

Note: `useAiBannerStore` (lines 149-152) is the reference pattern for session-only (no persist) stores. Do NOT wrap in `persist(...)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Space interface with depth + subSpaceIds</name>
  <files>client/lib/types.ts</files>
  <behavior>
    - Both new fields are OPTIONAL (`?:`) so pre-Phase 10 IPC responses (Phase 9 cache warm-up) still typecheck.
    - `depth` is `number` (0 | 1 in practice, max-depth-2 gated by backend per D-03).
    - `subSpaceIds` is `string[]` — camelCase mirror of Rust `sub_space_ids: Vec<String>`.
    - No runtime code changes here — type-only edit. Verified via `tsc --noEmit`.
  </behavior>
  <action>
    In client/lib/types.ts, extend the `Space` interface (starting at line 117) by adding two fields immediately after the `labelStatus?` field and before the closing brace of the interface, per D-07:

    ```
    /** 0 for top-level spaces, 1 for sub-spaces. Max depth = 2 (gated by backend). D-03/D-07. */
    depth?: number;
    /** IDs of direct child sub-spaces. Empty array for sub-spaces and spaces < 50 docs. D-07. */
    subSpaceIds?: string[];
    ```

    Use JSDoc comments in the same style as the Phase 9 additions above them. Do NOT modify existing fields — in particular, leave `subSpaces: Space[]` and `parentId?: string` untouched (they are consumed by SpaceDetailPage.tsx and Phase 4 Sidebar code respectively, per interfaces block above).

    No new imports required.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/cortex && bunx tsc --noEmit 2>&1 | grep -E "types\.ts" | tail -5 || echo "no type errors in types.ts"</automated>
  </verify>
  <done>`bunx tsc --noEmit` reports zero errors in types.ts. `grep -c "subSpaceIds" client/lib/types.ts` returns >= 1. Two JSDoc blocks reference D-07.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend useSidebarStore with expandedSpaceIds Set + tests</name>
  <files>client/lib/stores.ts, client/lib/stores.test.ts</files>
  <behavior>
    - Test 1 (initial state): freshly created store has `expandedSpaceIds` equal to `new Set()` (size 0).
    - Test 2 (toggle adds): calling `toggleSpaceExpanded("space-a")` adds "space-a" and `isSpaceExpanded("space-a")` returns true.
    - Test 3 (toggle removes): calling `toggleSpaceExpanded("space-a")` twice removes it; final `expandedSpaceIds.size === 0`.
    - Test 4 (independence): toggling "a" then "b" leaves both present; toggling "a" again removes only "a" and leaves "b".
    - Test 5 (no-persist contract): the module does NOT wrap useSidebarStore in `persist(...)`. Assert by inspecting the module source: `expect(storesSource).not.toMatch(/persist\s*\(\s*[\s\S]*?useSidebarStore/)` OR simpler — assert that after a toggle, a fresh `import()` of the module (via vi.resetModules) produces a store where `expandedSpaceIds.size === 0`. Match the pattern used by the existing useAiBannerStore no-persist test if present.
  </behavior>
  <action>
    Edit client/lib/stores.ts:

    1. Extend the `SidebarState` interface (line 17) to add these three members after `setCollapsed`:
       - `expandedSpaceIds: Set<string>`
       - `toggleSpaceExpanded: (spaceId: string) => void`
       - `isSpaceExpanded: (spaceId: string) => boolean`

    2. Extend the `useSidebarStore` factory (line 23) to accept a `get` param and initialize/expose the new members. The Zustand v5 create signature accepts `(set, get) => ({...})`. Use `get()` in `isSpaceExpanded` for read access. Implement toggle as an immutable Set clone per zustand-idiomatic pattern:

       ```
       expandedSpaceIds: new Set<string>(),
       toggleSpaceExpanded: (spaceId) => set((s) => {
         const next = new Set(s.expandedSpaceIds);
         if (next.has(spaceId)) next.delete(spaceId);
         else next.add(spaceId);
         return { expandedSpaceIds: next };
       }),
       isSpaceExpanded: (spaceId) => get().expandedSpaceIds.has(spaceId),
       ```

    3. Update the module-level doc block at the top of the file (line 4) to note the new expandedSpaceIds field.

    DO NOT wrap `useSidebarStore` in `persist(...)`. The absence of persist is the contract per D-13 and UI-SPEC §"Data Type Extensions" ("expandedSpaceIds is NOT persisted... session-level UI preference, not a persistent setting"). This is the same convention as useAiBannerStore (line 149) and useSpaceLabelingStore (line 171). Do NOT copy the pattern from useOnboardingStore (persisted).

    Edit client/lib/stores.test.ts to add a new `describe("useSidebarStore Phase 10 hierarchical state", ...)` block covering the 5 test cases in `<behavior>`. If stores.test.ts does not yet exist, create it; use the vitest + `@testing-library/react` idioms already used in the codebase (check adjacent test files for style — no snapshots, direct assertions on `store.getState()`).

    For Test 5 (no-persist contract), the simplest deterministic form is a static assertion on the source: `import { readFileSync } from "fs"; const src = readFileSync(path.resolve(__dirname, "stores.ts"), "utf8"); const useSidebarStoreDecl = src.match(/export const useSidebarStore[\s\S]*?\)\);\n/)?.[0] ?? ""; expect(useSidebarStoreDecl).not.toMatch(/persist\s*\(/);`. Match whichever assertion pattern is already used for useAiBannerStore no-persist contract in stores.test.ts if one exists — otherwise adopt the source-string pattern above.

    Between tests, reset the store to the initial state via `useSidebarStore.setState({ expandedSpaceIds: new Set(), isCollapsed: false })` in a `beforeEach()` so test independence is guaranteed without full module reset.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/cortex && bunx vitest run client/lib/stores.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>All 5 new tests pass. Existing stores.test.ts assertions still pass. `grep -c "expandedSpaceIds" client/lib/stores.ts` returns >= 4 (interface, initial, add, remove branch). `grep -v '^ *\*\|^ *//' client/lib/stores.ts | grep -c "persist("` reports exactly the count of pre-Phase-10 persist calls (i.e. useSidebarStore was NOT wrapped).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IPC → frontend | Rust Space struct serialized to camelCase JSON crosses IPC to typed React consumer. New optional fields must not break older clients. |
| React component → Zustand store | expandedSpaceIds mutations happen via toggleSpaceExpanded only; no direct set() from components. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering | localStorage 'cortex-sidebar' key | accept | Store deliberately session-only — no persistence means no attack surface from tampered localStorage. Matches useAiBannerStore precedent. |
| T-10-04 | Spoofing | expandedSpaceIds set contents | accept | Set contains space IDs only; worst case is stale expanded state that resets on next launch. No sensitive data crosses this boundary. |
</threat_model>

<verification>
- `bunx tsc --noEmit` returns 0 errors introduced by types.ts changes.
- `bunx vitest run client/lib/stores.test.ts` reports all Phase 10 tests green and no regressions in existing Phase 4/7/9 sidebar tests.
- `rg -c "persist\s*\(" client/lib/stores.ts` returns the same count as before this plan (i.e., only useOnboardingStore is persisted; useSidebarStore is NOT).
</verification>

<success_criteria>
- Space interface exposes optional `depth: number` and `subSpaceIds: string[]` matching the Rust camelCase serialization.
- useSidebarStore exposes `expandedSpaceIds`, `toggleSpaceExpanded`, `isSpaceExpanded` with correct add/remove semantics.
- Store is session-only — no persist middleware wraps useSidebarStore.
- All existing stores.test.ts assertions pass unchanged.
</success_criteria>

<output>
Create `.planning/phases/10-hierarchical-spaces/10-02-SUMMARY.md` when done.
</output>

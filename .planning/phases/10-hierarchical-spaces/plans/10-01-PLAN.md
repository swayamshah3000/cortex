---
phase: 10-hierarchical-spaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/types.rs
  - src-tauri/src/spaces/label_cache.rs
autonomous: true
requirements: [HSPC-01, HSPC-02, HSPC-03, HSPC-04]

must_haves:
  truths:
    - "Rust Space struct exposes depth (u8) and sub_space_ids (Vec<String>) with backward-compatible serde defaults"
    - "SpaceLabelEntry exposes parent_id (Option<String>) and depth (u8) with backward-compatible serde defaults"
    - "Old space_labels.json entries written before Phase 10 still deserialize cleanly (no label wipe on app upgrade)"
    - "Serde roundtrip of a Space with depth=1, parent_id=Some(...), sub_space_ids=[] preserves all fields exactly"
  artifacts:
    - path: "src-tauri/src/types.rs"
      provides: "Space struct with Phase 10 depth + sub_space_ids fields"
      contains: "pub depth: u8"
    - path: "src-tauri/src/spaces/label_cache.rs"
      provides: "SpaceLabelEntry with parent_id + depth Phase 10 fields"
      contains: "pub parent_id: Option<String>"
  key_links:
    - from: "SpaceLabelEntry (label_cache.rs)"
      to: "space_labels.json on disk"
      via: "serde_json roundtrip with #[serde(default)] on new fields"
      pattern: "serde\\(default\\)"
    - from: "Space struct (types.rs)"
      to: "IPC get_spaces() response consumed by frontend"
      via: "serde rename_all=camelCase → subSpaceIds"
      pattern: "sub_space_ids"
---

<objective>
Extend Rust Space and SpaceLabelEntry structs with Phase 10 hierarchical fields (depth, sub_space_ids on Space; parent_id, depth on SpaceLabelEntry). All new fields use #[serde(default)] so existing serialized state (in-memory or on-disk space_labels.json) still deserializes cleanly.

Purpose: Foundation for every other Phase 10 plan. Backend types must expose these fields before subspace_detector, manager recluster, or IPC responses can populate them.

Output:
- types.rs Space struct with two new fields
- label_cache.rs SpaceLabelEntry with two new fields
- Roundtrip + backward-compat unit tests
- No cache migration required (old entries read as parent_id=None, depth=0 — semantically correct for top-level)

Note: `parent_id` ALREADY exists on Space struct (added in Phase 9 for Phase 10 readiness — see 10-RESEARCH.md Pitfall 1). DO NOT re-add it or you will get `error[E0124]: field 'parent_id' is defined multiple times`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-hierarchical-spaces/10-CONTEXT.md
@.planning/phases/10-hierarchical-spaces/10-RESEARCH.md

<interfaces>
<!-- Current Space struct — verify parent_id already exists before editing -->

From src-tauri/src/types.rs (lines 168-207):
```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct Space {
    pub id: String,
    pub name: String,
    pub icon: String,
    pub color: String,
    pub document_count: u32,
    pub last_updated: String,
    pub sub_spaces: Vec<Space>,        // Existing — nested field, populated dynamically
    pub parent_id: Option<String>,     // ALREADY PRESENT from Phase 9 — DO NOT re-add
    pub sample_files: Vec<String>,
    #[serde(default)] pub description: Option<String>,
    #[serde(default)] pub user_locked: bool,
    #[serde(default)] pub canonical_entity_hint: Option<String>,
    #[serde(default)] pub label_status: Option<String>,
}
```

From src-tauri/src/spaces/label_cache.rs (lines 41-60):
```rust
#[derive(Debug, Clone, Serialize, Deserialize, Default)]
#[serde(rename_all = "camelCase")]
pub struct SpaceLabelEntry {
    pub fingerprint: String,
    pub label: String,
    pub description: String,
    pub canonical_entity_hint: Option<String>,
    pub generated_at: String,
    pub user_locked: bool,
    // Phase 10 additions land HERE
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Space struct with depth + sub_space_ids</name>
  <files>src-tauri/src/types.rs</files>
  <behavior>
    - Test 1 (space_phase10_fields_roundtrip): a Space with depth=1, parent_id=Some("parent-uuid"), sub_space_ids=vec!["a".into(),"b".into()] round-trips through serde_json::to_string → serde_json::from_str preserving all fields exactly.
    - Test 2 (space_phase10_backward_compat_no_fields): deserializing a JSON blob that lacks `depth` and `subSpaceIds` succeeds and yields depth=0, sub_space_ids=vec![] (the serde defaults).
    - Test 3 (space_top_level_defaults): a fresh Space { depth: 0, parent_id: None, sub_space_ids: vec![], ... } serializes with `"depth":0` and `"subSpaceIds":[]` in the camelCase JSON.
  </behavior>
  <action>
    Add exactly two new fields to `pub struct Space` in src-tauri/src/types.rs directly after `pub sample_files: Vec<String>` and before the Phase 9 `#[serde(default)] pub description` block (keeping struct field ordering by phase-of-introduction, per project convention).

    Both fields MUST use `#[serde(default)]` per D-07 and pitfall #4 in 10-RESEARCH.md. Semantics:
    - `depth: u8` — default 0 = top-level. Only 0 or 1 valid (max depth = 2 gated by backend, D-03).
    - `sub_space_ids: Vec<String>` — default empty vec = no sub-spaces. Populated by SpaceManager::recluster (Plan 05) for top-level spaces with > 50 docs. Serializes to `subSpaceIds` per the struct's existing serde(rename_all = "camelCase").

    DO NOT re-add `parent_id` — it already exists at line 178 (pitfall #1 in RESEARCH.md). Doing so triggers E0124 duplicate field compile error.

    Add doc comments in the same style as the Phase 9 additions immediately below. Reference D-07 for depth and D-03 for the max-depth constraint.

    Add the three unit tests listed in `<behavior>` inside a `#[cfg(test)] mod tests { ... }` block at the end of types.rs. If a tests module already exists, extend it. Tests use serde_json::to_string/from_str only — no external mocks. Do NOT introduce new dev-dependencies.
  </action>
  <verify>
    <automated>cargo test -p cortex --lib types::tests::space_phase10 2>&1 | tail -20</automated>
  </verify>
  <done>Three tests pass. `cargo check -p cortex` succeeds with zero E0124 duplicate-field errors. `grep -c "sub_space_ids" src-tauri/src/types.rs` returns >= 1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend SpaceLabelEntry with parent_id + depth (backward-compat)</name>
  <files>src-tauri/src/spaces/label_cache.rs</files>
  <behavior>
    - Test 1 (test_phase10_fields_roundtrip): a SpaceLabelEntry with parent_id=Some("parent-abc"), depth=1 round-trips through `SpaceLabelCache::save` → `SpaceLabelCache::load` inside a tempfile TempDir, preserving both fields.
    - Test 2 (test_phase10_backward_compat): a hand-written space_labels.json string with a Phase 9 shape (no `parentId` or `depth` keys under a labels entry) deserializes cleanly via serde_json::from_str, producing entries with parent_id=None and depth=0. Prevents pitfall #4 (wiping cache on app upgrade).
    - Test 3 (test_phase10_default_top_level_omits_parent_id): a top-level SpaceLabelEntry (parent_id=None, depth=0) still serializes without runtime error and reloads with the same values (i.e. serde defaults survive when both fields are set to their defaults).
  </behavior>
  <action>
    Add exactly two new fields to `pub struct SpaceLabelEntry` in src-tauri/src/spaces/label_cache.rs directly after the existing `pub user_locked: bool` line, before the closing brace of the struct. Both MUST use `#[serde(default)]` per D-06 and pitfall #4 in RESEARCH.md.

    Semantics:
    - `parent_id: Option<String>` — default None = top-level. `Some(uuid)` for sub-space cache entries.
    - `depth: u8` — default 0 = top-level. 1 for sub-space entries. Max 2 per D-03.

    Update the existing full_entry() test helper in the tests module to accept parent_id + depth via optional parameters, OR add a new helper `full_entry_with_hierarchy(fingerprint, label, hint, user_locked, parent_id, depth)` used by the new Phase 10 tests only, so existing Phase 9 tests continue to compile unchanged.

    Add the three tests from `<behavior>` inside the existing `#[cfg(test)] mod tests` block. Use tempfile::TempDir (already a dev-dependency, per Cargo.toml). For test 2, hand-write a JSON string like `r#"{"labels":{"space-1":{"fingerprint":"abc","label":"Foo","description":"Bar","canonicalEntityHint":null,"generatedAt":"2026-07-08T00:00:00Z","userLocked":false}}}"#`, deserialize via `serde_json::from_str::<SpaceLabelCache>()`, assert `.get("space-1").unwrap().parent_id == None && .depth == 0`.

    Do NOT modify the module-level doc comment schema block (lines 6-19) — a follow-up doc update can happen after tests pass; keeping the diff tight lets Task 1 and Task 2 be verified independently.
  </action>
  <verify>
    <automated>cargo test -p cortex --lib spaces::label_cache::tests::test_phase10 2>&1 | tail -20</automated>
  </verify>
  <done>Three tests pass. Existing Phase 9 tests in the same module still pass (`cargo test -p cortex --lib spaces::label_cache` returns green). `grep -c "parent_id" src-tauri/src/spaces/label_cache.rs` returns >= 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| serialized JSON → deserialized struct | space_labels.json read from disk into SpaceLabelCache. Old Phase 9 shape must not crash. |
| in-memory Space → IPC JSON payload | Space struct serialized via serde over Tauri IPC to frontend; new fields become camelCase JSON. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | space_labels.json on disk | mitigate | `#[serde(default)]` on new fields prevents corruption on partial upgrades; malformed JSON already returns empty cache via existing SpaceLabelCache::load fallback (verified in label_cache.rs:76-82). |
| T-10-02 | Denial of Service | Space struct serde deserialization | accept | Struct grows by 2 fields (u8 + Vec<String>); worst-case payload increase < 100 bytes per Space. Not exploitable. |
</threat_model>

<verification>
- `cargo check -p cortex` — no compile errors (verifies parent_id was NOT re-added on Space, and both new fields on SpaceLabelEntry compile with default derive).
- `cargo test -p cortex --lib spaces::label_cache` — all Phase 9 + 3 new Phase 10 tests pass.
- `cargo test -p cortex --lib types::tests::space_phase10` — 3 new tests pass.
</verification>

<success_criteria>
- Rust Space struct exposes `depth: u8` and `sub_space_ids: Vec<String>` with `#[serde(default)]`.
- Rust SpaceLabelEntry exposes `parent_id: Option<String>` and `depth: u8` with `#[serde(default)]`.
- A Phase-9-shape space_labels.json file (no Phase 10 keys) loads without error and treats existing entries as parent_id=None, depth=0.
- All existing Phase 9 tests continue to pass — zero regressions.
- `cargo check -p cortex` exits 0 with zero warnings introduced by this plan (parent_id NOT re-added).
</success_criteria>

<output>
Create `.planning/phases/10-hierarchical-spaces/10-01-SUMMARY.md` when done.
</output>

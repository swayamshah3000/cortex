---
phase: 11.7-rag-chat
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - client/pages/DocumentPage.tsx
  - client/components/preview/FilePreview.tsx
autonomous: false
requirements: [RAGCH-07]

must_haves:
  truths:
    - "Navigating to /document/:id?highlight=1234-1400 causes the FilePreview to scroll to that character range best-effort."
    - "Missing or malformed highlight param is ignored — no crash, no error toast, existing behavior preserved."
    - "For text/markdown previews the range is visually marked (background color) so the citation lands somewhere the eye can catch."
  artifacts:
    - path: "client/pages/DocumentPage.tsx"
      provides: "Reads ?highlight query param, parses '{start}-{end}', passes to FilePreview as prop."
      contains: "useSearchParams"
    - path: "client/components/preview/FilePreview.tsx"
      provides: "Accepts optional highlightRange prop; text/markdown branches scroll + mark the range."
      contains: "highlightRange"
  key_links:
    - from: "client/pages/DocumentPage.tsx"
      to: "client/components/preview/FilePreview.tsx"
      via: "prop highlightRange={{ start, end }}"
      pattern: "highlightRange="
---

<objective>
Wire the DocumentPage highlight query parameter that Plan 06's citation chips will use. When a chat citation is clicked, the user navigates to `/document/:id?highlight={chunkStart}-{chunkEnd}`. This plan makes DocumentPage read that param and pass it to FilePreview, and makes FilePreview scroll to + visually mark the range for text-based previews (D-17, Claude's Discretion: "hash offset OR scroll-to via data-chunk-start").

Purpose: Citation click-through must land on the exact quoted chunk. Without this, "cited answers" degrades to "cited document" — losing the killer-feature magic.
Output: Query-param wiring in DocumentPage + highlight prop threading in FilePreview + best-effort scroll/mark in the text-preview branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11.7-rag-chat/11.7-CONTEXT.md

@client/pages/DocumentPage.tsx
@client/components/preview/FilePreview.tsx
@client/hooks/usePreview.ts

<interfaces>
<!-- Existing FilePreview API (extract before adding highlightRange). Executor must not break existing signatures. -->

From client/pages/DocumentPage.tsx (existing usage — do NOT change signature besides adding the new prop):
```typescript
<FilePreview docType={doc.docType} path={doc.path} name={doc.name} />
```

Executor: Read FilePreview.tsx first to see its current props interface. The new prop is optional so existing call sites do not need to change.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend FilePreview to accept a highlight range and render it for text/markdown previews</name>
  <files>client/components/preview/FilePreview.tsx</files>
  <action>
Read the file first. Then extend the props interface to include:

```typescript
highlightRange?: { start: number; end: number };
```

**Behavior branches:**

- **PDF preview:** `highlightRange` is IGNORED in v1. PDFs use react-pdf's page-based rendering; character-offset → page-and-position mapping is a v1.3 problem. Do NOT introduce PDF.js range selection. Rendering unchanged.

- **Text preview (TextPreview) and Markdown preview (MarkdownPreview):** When `highlightRange` is present AND the branch renders the file text as a single string, apply the following algorithm inside a `useEffect` that fires after content mount:

  1. Locate the DOM node containing the text content (the `<pre>` for TextPreview or the top-level `<div>` for MarkdownPreview).
  2. If the character offsets are addressable directly (TextPreview renders `text.slice(0, start) + text.slice(start, end) + text.slice(end)` split into three `<span>`s where the middle span has class `bg-yellow-500/30 text-yellow-100 px-0.5 rounded`), split the text and render three spans.
  3. Call `ref.current?.querySelector('.chat-citation-highlight')?.scrollIntoView({ block: 'center', behavior: 'auto' })` on the middle span. Class name `chat-citation-highlight` is stable — Plan 06 does NOT need it, this is internal-only.
  4. Wrap the split-render logic behind `highlightRange && start < end && end <= text.length`. Any invalid range: render as if highlightRange were undefined. No error surface.

- **Image / Unsupported preview:** `highlightRange` is IGNORED. Render unchanged.

**Guardrails:**
- Do NOT change the existing return-type discriminator (docType switch). Only augment the text + markdown branches.
- Do NOT introduce a dependency (no `mark.js`, no `rangy`). Pure React state + string slicing.
- Do NOT run the scrollIntoView inside a layout effect that fires before the DOM paints — use `useEffect` with `[highlightRange, text]` deps.
- If TextPreview / MarkdownPreview are extracted into their own files, propagate the prop into them and implement the behavior there. Keep the FilePreview parent as a thin dispatcher.
  </action>
  <verify>
    <automated>bun run typecheck 2>&1 | tail -5 && grep -q "highlightRange" client/components/preview/FilePreview.tsx && echo OK</automated>
  </verify>
  <done>FilePreview and its text/markdown children accept `highlightRange?: { start; end }`; passing it triggers scroll + visible mark on next mount; passing undefined is a no-op; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: DocumentPage reads ?highlight={start}-{end} and forwards to FilePreview</name>
  <files>client/pages/DocumentPage.tsx</files>
  <action>
Read the file. Import `useSearchParams` from `react-router-dom` if not already imported (Sidebar.tsx uses `useLocation` — same package).

Inside `DocumentPage`, after the existing `useParams` line, add:

```typescript
const [searchParams] = useSearchParams();
const highlightRange = useMemo(() => {
  const raw = searchParams.get("highlight");
  if (!raw) return undefined;
  const m = raw.match(/^(\d+)-(\d+)$/);
  if (!m) return undefined;
  const start = Number(m[1]);
  const end = Number(m[2]);
  if (!Number.isFinite(start) || !Number.isFinite(end) || end <= start) return undefined;
  return { start, end };
}, [searchParams]);
```

Then thread `highlightRange` into the `<FilePreview ... />` invocation:

```tsx
<FilePreview docType={doc.docType} path={doc.path} name={doc.name} highlightRange={highlightRange} />
```

Import `useMemo` from `react` if not already imported.

Do NOT wire any chat-page logic here. Do NOT toast anything on missing/malformed param. Do NOT modify the metadata sidebar.
  </action>
  <verify>
    <automated>bun run typecheck 2>&1 | tail -5 && grep -q "useSearchParams" client/pages/DocumentPage.tsx && echo OK</automated>
  </verify>
  <done>Navigating to `/document/{id}?highlight=100-250` mounts DocumentPage without errors and forwards `{ start: 100, end: 250 }` to FilePreview.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - FilePreview text and markdown branches accept an optional highlightRange prop.
    - When set, the target range is visually marked and the preview scrolls to center it.
    - DocumentPage parses `?highlight={start}-{end}` from the URL and forwards it.
    - Malformed / missing params are silently ignored.
  </what-built>
  <how-to-verify>
    1. Run `bun dev` (frontend only is fine for this check — no backend needed).
    2. In the mock corpus, open any text or markdown document. Note its path in the URL, e.g. `/document/mock-doc-1`.
    3. Manually navigate to `/document/mock-doc-1?highlight=100-180` in the URL bar.
    4. Confirm: the preview scrolls so a highlighted range is visible near the vertical center of the preview pane, and the range has a subtle yellow-tinted background.
    5. Manually navigate to `/document/mock-doc-1?highlight=abc-def` (malformed).
    6. Confirm: page renders normally, no highlight, no error toast, no console error.
    7. Manually navigate to `/document/mock-doc-1` (no param).
    8. Confirm: preview renders exactly as it did before Phase 11.7 started.
    9. Open a PDF or image document with `?highlight=100-200`.
    10. Confirm: PDF / image renders normally with no highlight applied and no error.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the failing branch (text | markdown | pdf | image | invalid-param | no-param).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params → DocumentPage | Untrusted URL contains character offsets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.7-06 | Denial of Service | Malformed highlight param (e.g. `?highlight=999999999-9999999999999`) | mitigate | Regex `^\d+-\d+$` + `Number.isFinite` guard + range fully clipped to `text.length` before slicing. Any invalid range → no-op. |
| T-11.7-07 | Tampering | XSS via URL param → DOM injection | mitigate | Range is parsed to numbers only; the highlighted text is the source doc's own text (already rendered), sliced via React children. No `dangerouslySetInnerHTML`. |
</threat_model>

<verification>
- `bun run typecheck` passes.
- Text preview + valid highlight → visible mark + scroll.
- Text preview + missing/malformed highlight → unchanged behavior.
- PDF / image preview + any highlight → unchanged behavior.
</verification>

<success_criteria>
- DocumentPage reads and forwards `?highlight=start-end`.
- FilePreview honors highlightRange for text/markdown, ignores it elsewhere.
- Zero regression on documents without a highlight param.
- Human verification confirms the scroll + mark behavior visually.
</success_criteria>

<output>
Create `.planning/phases/11.7-rag-chat/11.7-03-SUMMARY.md` when done. Include: (a) which FilePreview branches actually support highlighting, (b) any files added under `client/components/preview/`, (c) approved checkpoint sign-off.
</output>

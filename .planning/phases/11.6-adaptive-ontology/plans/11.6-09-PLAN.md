---
phase: 11.6-adaptive-ontology
plan: 09
type: execute
wave: 7
depends_on: ["11.6-08"]
files_modified:
  - client/pages/SettingsPage.tsx
  - client/components/layout/Sidebar.tsx
  - client/components/ontology/OntologyPanel.tsx
  - client/components/ontology/PredicateTable.tsx
  - client/components/ontology/EntitySubclassTable.tsx
  - client/components/ontology/ConsolidationDiff.tsx
  - client/components/ontology/AddPredicateDialog.tsx
  - client/components/ontology/MergePredicatesDialog.tsx
autonomous: false
requirements: [ONTO-04, ONTO-05, ONTO-06]
user_setup: []

must_haves:
  truths:
    - "Settings page has a new 'Ontology' tab positioned between 'AI & Models' and any subsequent tabs (D-21)"
    - "OntologyPanel renders four sections in order: Predicates, Entity Subclasses, Pending Consolidation (only when suggestions exist), Manual Actions"
    - "Predicates table shows: name, source badge (seed/corpus/adaptive/manual), count, first-seen doc link (when available); sortable by count desc default"
    - "Entity Subclasses table shows: class, subclass, source badge, count, example value"
    - "Pending consolidation section renders one card per suggestion with Accept and Reject buttons wired to useApplyConsolidation"
    - "Manual actions row exposes: Add Predicate, Merge Predicates, Reset to Seed, Regenerate Corpus Seed buttons (destructive actions gated by AlertDialog)"
    - "Automatic ontology growth toggle is visible above the tables and wired to useSetAutomaticOntologyGrowth (default off per D-15)"
    - "Sidebar renders a 'Prominent Entities' section between 'Saved Searches' and the 'Owned by me' quick link, showing top-5 entities from useProminentEntities"
    - "Prominent entity rows navigate to /entity/{entityType}/{encodeURIComponent(canonicalName)} matching existing EntityChip routing"
    - "Sidebar shows a new 'Ontology' quick link to /settings?tab=ontology below 'Owned by me'"
    - "Ontology quick link shows a red count badge equal to pending_consolidation.suggestions.length when > 0; badge hides otherwise"
    - "SettingsPage reads ?tab= URL param on mount and defaults to that tab if present (so /settings?tab=ontology opens the Ontology tab)"
    - "Every predicate row (adaptive + manual only) has a Rename action opening a small inline input; seed + corpus predicates cannot be renamed or deleted"
    - "Adaptive/manual predicates show a Delete action (via merge_predicates from -> into='') gated by AlertDialog"
    - "Human-verify checkpoint confirms the panel renders correctly against a real corpus (~/private) and consolidation accept/reject rewrites triples visibly on entity pages"
  artifacts:
    - path: "client/pages/SettingsPage.tsx"
      provides: "Ontology tab injected between 'AI & Models' and 'Privacy'; URL-param-driven default tab"
      contains: "OntologyPanel"
    - path: "client/components/layout/Sidebar.tsx"
      provides: "Prominent Entities section + Ontology quick link with pending badge"
      contains: "useProminentEntities"
    - path: "client/components/ontology/OntologyPanel.tsx"
      provides: "Top-level panel composing predicate table, subclass table, consolidation diff, manual actions, and growth toggle"
      contains: "export function OntologyPanel"
    - path: "client/components/ontology/PredicateTable.tsx"
      provides: "Sortable table of all predicates (seed + corpus + adaptive + manual + pending) with per-row actions"
      contains: "export function PredicateTable"
    - path: "client/components/ontology/EntitySubclassTable.tsx"
      provides: "Table of entity subclasses grouped by class"
      contains: "export function EntitySubclassTable"
    - path: "client/components/ontology/ConsolidationDiff.tsx"
      provides: "Per-suggestion diff card with Accept/Reject buttons"
      contains: "export function ConsolidationDiff"
    - path: "client/components/ontology/AddPredicateDialog.tsx"
      provides: "Modal dialog for manual predicate creation (name, description, subjectClass, objectClass)"
      contains: "export function AddPredicateDialog"
    - path: "client/components/ontology/MergePredicatesDialog.tsx"
      provides: "Modal dialog to select two-or-more predicates and pick a canonical target"
      contains: "export function MergePredicatesDialog"
  key_links:
    - from: "client/pages/SettingsPage.tsx"
      to: "client/components/ontology/OntologyPanel.tsx"
      via: "React import + TabsContent value='ontology'"
      pattern: "<OntologyPanel"
    - from: "client/components/ontology/OntologyPanel.tsx"
      to: "client/hooks/useTauri.ts"
      via: "useOntology + 7 mutation hooks from Plan 08"
      pattern: "useOntology\\|useApplyConsolidation\\|useAddManualPredicate\\|useRenamePredicate\\|useMergePredicates\\|useResetOntologyToSeed\\|useRegenerateCorpusSeed\\|useSetAutomaticOntologyGrowth"
    - from: "client/components/layout/Sidebar.tsx"
      to: "client/hooks/useTauri.ts"
      via: "useProminentEntities + useOntology (for badge count)"
      pattern: "useProminentEntities\\|useOntology"
    - from: "client/components/layout/Sidebar.tsx"
      to: "/settings?tab=ontology"
      via: "React Router Link"
      pattern: "settings\\?tab=ontology"
---

<objective>
Build the frontend surface for Phase 11.6 — the final wave. Two integration points:

1. **Settings > Ontology tab** — full CRUD + inspection UI for the adaptive ontology.
   Users can review what cortex learned from their corpus, accept/reject consolidation
   suggestions, manually add/rename/merge predicates, toggle automatic growth, and reset
   to seed vocabulary.

2. **Sidebar prominence + Ontology quick link** — surfaces top-5 prominent entities
   inline (below Saved Searches, above 'Owned by me') and a new 'Ontology' quick link
   that shows a badge with the pending-consolidation count when suggestions are waiting
   for review.

Purpose:
- Realises D-21..D-23 (Settings > Ontology panel) and D-12..D-14 (Frequency-weighted
  entity ranking / Sidebar prominence) from CONTEXT.md.
- Uses the 9 hooks shipped in Plan 08 (11.6-08) — this plan is pure UI composition +
  presentation. No new IPC or hooks introduced.
- Closes the phase's user-facing loop: bootstrap runs (Plan 03), adaptive/consolidation
  runs (Plans 04-05), IPC exposes state (Plan 06), hooks expose queries (Plan 08), UI
  makes it visible + actionable here.

Output:
- Ontology tab in Settings between 'AI & Models' and 'Privacy'.
- Sidebar prominent-entities section + Ontology quick link with pending badge.
- Six new components under client/components/ontology/.
- Human-verify checkpoint against ~/private real corpus.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.6-adaptive-ontology/11.6-CONTEXT.md
@.planning/phases/11.6-adaptive-ontology/plans/11.6-01-PLAN.md
@.planning/phases/11.6-adaptive-ontology/plans/11.6-06-PLAN.md
@.planning/phases/11.6-adaptive-ontology/plans/11.6-08-PLAN.md
@.planning/phases/11.5-ontology-relations/plans/11.5-07-PLAN.md
@client/pages/SettingsPage.tsx
@client/components/layout/Sidebar.tsx
@client/hooks/useTauri.ts
@client/lib/types.ts
@client/components/ai/AiProvidersSection.tsx
@client/components/ai/ExtractionSettings.tsx
@client/components/entities/EntityChip.tsx

<interfaces>
Hooks available (from Plan 08 — client/hooks/useTauri.ts):
- useOntology() -> { data: OntologyStoreSchema, isLoading, error }
- useApplyConsolidation() -> mutation((suggestionId: string) -> number)
- useAddManualPredicate() -> mutation(({ name, description, subjectClass?, objectClass? }) -> PromoteResult)
- useRenamePredicate() -> mutation(({ oldName, newName }) -> number)
- useMergePredicates() -> mutation(({ from: string[], into: string }) -> number)
- useResetOntologyToSeed() -> mutation(() -> void)
- useRegenerateCorpusSeed() -> mutation(() -> void)
- useSetAutomaticOntologyGrowth() -> mutation((enabled: boolean) -> void)
- useProminentEntities(limit: number = 5) -> { data: ProminentEntity[], isLoading }

Types (from Plan 01 — client/lib/types.ts):
- OntologyStoreSchema { version, corpusSeed?, adaptivePredicates[], pendingPredicates[], manualPredicates[], entitySubclasses[], pendingConsolidation?, automaticGrowthEnabled, bootstrapCompletedAt?, lastConsolidationAt?, triplesSinceLastConsolidation }
- PredicateEntry { name, description, source: 'seed' | 'corpus' | 'adaptive' | 'manual', count, subjectClass?, objectClass?, firstSeenAt?, firstSeenDocId?, promotedAt? }
- EntitySubclass { class, subclass, source: 'seed' | 'corpus' | 'adaptive' | 'manual', count, exampleValue? }
- BootstrapSeed { predicates: PredicateEntry[], entitySubclasses: EntitySubclass[], generatedAt, sampleDocCount, modelUsed }
- PendingConsolidation { suggestions: ConsolidationSuggestion[], generatedAt, modelUsed, tripleCountAtGeneration }
- ConsolidationSuggestion { id, kind: { kind: 'merge', from: string[], into: string } | { kind: 'rename', from: string, to: string } | { kind: 'split', from: string, into: string[] }, rationale, confidence }
- ProminentEntity { entity: CanonicalEntity, docCount, isProminent }
- CanonicalEntity { id, canonicalName, entityType, aliases[], documentCount, canonicalShortName? }

shadcn primitives available:
- Tabs / TabsList / TabsTrigger / TabsContent (already used in SettingsPage)
- AlertDialog + AlertDialogTrigger / Content / Header / Title / Description / Footer / Action / Cancel
- Dialog + DialogTrigger / Content / Header / Title / Description / Footer
- Table + TableHeader / Row / Head / Body / Cell (from @/components/ui/table — verify exists; if not, fall back to plain <table> with tailwind classes matching existing Phase 11.5 patterns)
- Switch, Label, Input, Textarea, Button, Badge, Select, RadioGroup
- Skeleton
- Sonner toast (`import { toast } from "sonner"`)

Existing Sidebar patterns to mirror:
- Section header style: `<div className="px-3 py-2"><span className="text-xs font-semibold uppercase tracking-wider text-text-tertiary">Prominent Entities</span></div>`
- Row style with icon + label + trailing count/badge — see 'Saved Searches' block for the shape.
- Collapsed sidebar shows only the icon; hovered tooltip pattern already used by NavLink.

Existing SettingsPage tab list currently has 6 entries: general, indexing, ai, privacy, storage, about. Per D-21 the Ontology tab must sit "between AI Providers and Extraction" — since AI Providers and Extraction currently live inside the same "AI & Models" tab (see SettingsPage lines 264-327), the concrete placement in v1 is: insert an "Ontology" TabsTrigger between 'ai' and 'privacy'. Ontology settings are logically closest to the AI tab.

URL-param handling:
```typescript
import { useSearchParams } from "react-router-dom";
const [searchParams] = useSearchParams();
const initialTab = searchParams.get("tab") ?? "general";
// pass as defaultValue={initialTab} on <Tabs>
```

Predicate source badge colors (define once in OntologyPanel or a shared badge helper):
- seed:      neutral (bg-bg-tertiary text-text-secondary)
- corpus:    accent-primary tint (border-accent-primary/40 text-accent-primary)
- adaptive:  success-tint (bg-emerald-500/10 text-emerald-500)
- manual:    warning-tint (bg-amber-500/10 text-amber-500)
- pending:   text-text-tertiary italic (rendered inline in the same table with a 'pending' badge instead of a source badge — pending predicates are always source='adaptive' with a count < promotion threshold)

Distinguish pending vs promoted adaptive:
- data.adaptivePredicates: promoted
- data.pendingPredicates: awaiting minimum-support gate
Both are rendered in the same PredicateTable but pending rows show a 'pending' badge next to the source badge and cannot be renamed (only the promoted version can).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ontology components — PredicateTable, EntitySubclassTable, ConsolidationDiff, AddPredicateDialog, MergePredicatesDialog</name>
  <files>client/components/ontology/PredicateTable.tsx, client/components/ontology/EntitySubclassTable.tsx, client/components/ontology/ConsolidationDiff.tsx, client/components/ontology/AddPredicateDialog.tsx, client/components/ontology/MergePredicatesDialog.tsx</files>
  <action>
Create directory `client/components/ontology/` if it does not exist.

**File 1: `client/components/ontology/PredicateTable.tsx`**

Export `interface PredicateTableProps { seedPredicates: PredicateEntry[]; corpusPredicates: PredicateEntry[]; adaptivePredicates: PredicateEntry[]; manualPredicates: PredicateEntry[]; pendingPredicates: PredicateEntry[]; onRename: (oldName: string, newName: string) => void; onDelete: (name: string) => void; }`.

Export `function PredicateTable(props: PredicateTableProps): JSX.Element`.

Behavior:
- Combine all five predicate arrays into one flattened list where each row carries its `source` and a `pending: boolean` flag (true only for entries from `pendingPredicates`).
- Sortable columns: default sort by count DESC. Clickable header on name / count / source toggles asc/desc via local `useState<{ column, dir }>`.
- Filter input above the table: filters by name substring (case-insensitive).
- Source badge helper `sourceBadge(source: PredicateEntry['source'], pending: boolean): JSX.Element` that renders a small Badge component per the color scheme in the interfaces block. Pending rows render TWO badges: the source badge + a muted 'pending' badge.
- Columns (5): Name (mono font, JetBrains Mono), Source (badge), Count, First seen (formatted date if `firstSeenAt`; empty otherwise — no docId lookup), Actions.
- Actions column:
  - For source='seed' or 'corpus' or pending=true: no actions, empty cell.
  - For source='adaptive' or 'manual' (and not pending): render a small Pencil icon (Lucide `Pencil`, size 14) that opens an inline rename input (local state per row), and a Trash icon (Lucide `Trash2`, size 14, text-red-400) that opens AlertDialog "Delete predicate {name}?" with warning about associated triples being merged into 'unspecified' (or user picks a target — v1 hard-deletes by merging into an empty target; the confirmation copy MUST say "All {count} associated triples will be removed. This cannot be undone.").
  - The rename inline input: pressing Enter calls `onRename(oldName, newValue.trim())` and closes the input; pressing Escape cancels; empty new value is a no-op.
  - The delete AlertDialog cancels/deletes via `onDelete(name)`.
- Wrapping section: `<div className="space-y-3" data-testid="predicate-table">`.
- If the combined list is empty: render `<p className="text-sm text-text-tertiary">No predicates yet.</p>`.
- Loading skeleton is the parent's responsibility — this component assumes data is present.
- Do NOT add pagination in v1. If the corpus produces >200 predicates the D-08 hard cap forces consolidation. Realistic v1 upper bound is ~50-80.

**File 2: `client/components/ontology/EntitySubclassTable.tsx`**

Export `interface EntitySubclassTableProps { subclasses: EntitySubclass[]; }`.

Export `function EntitySubclassTable(props): JSX.Element`.

Behavior:
- Group `subclasses` by `class`. Render one section per class with the class name as a subheader (`<h4 className="text-sm font-semibold text-text-primary mt-4">Location</h4>`).
- Each section is a small table with columns: Subclass, Source (badge — same color scheme as predicates), Count, Example (from `exampleValue`, truncate to 40 chars with title tooltip for full value).
- Reuse a small `sourceBadge` helper — either import from PredicateTable (better: extract to a shared `client/components/ontology/sourceBadge.tsx` in this task) or inline. Extract it to a shared helper `sourceBadge.tsx` in this task so PredicateTable and EntitySubclassTable both import it.
- Empty state: `<p className="text-sm text-text-tertiary">No entity subclasses discovered yet. Cortex will propose subclasses after your first corpus-seed bootstrap.</p>`.

**File 3: `client/components/ontology/ConsolidationDiff.tsx`**

Export `interface ConsolidationDiffProps { pending: PendingConsolidation; onAccept: (suggestionId: string) => void; onReject: (suggestionId: string) => void; }`.

Export `function ConsolidationDiff(props): JSX.Element`.

Behavior:
- Section header: `<h3 className="section-header text-text-primary">Pending Consolidation ({pending.suggestions.length})</h3>` + subtitle showing `generatedAt` (formatted relative date) and `modelUsed`.
- Render one Card per suggestion. Card layout:
  - Top row: kind badge (`merge` = amber tint, `rename` = blue tint, `split` = purple tint) + confidence percentage (rounded, e.g. "85%").
  - Visual diff, per kind:
    - merge: `<div className="flex items-center gap-2 flex-wrap"><Badge>{from[0]}</Badge> + <Badge>{from[1]}</Badge> ... <ArrowRight /> <Badge className="ring-2 ring-emerald-500">{into}</Badge></div>`.
    - rename: `<Badge className="line-through opacity-60">{from}</Badge> <ArrowRight /> <Badge className="ring-2 ring-emerald-500">{to}</Badge>`.
    - split: `<Badge>{from}</Badge> <ArrowRight /> <Badge className="ring-2 ring-emerald-500">{into.join(", ")}</Badge>`.
  - Rationale: `<p className="text-sm text-text-secondary">{rationale}</p>`.
  - Actions row: Accept button (btn-primary), Reject button (btn-secondary). Accept opens AlertDialog "Apply this consolidation? All existing triples using the affected predicates will be re-tagged. This cannot be undone easily (you can re-add predicates manually)." then calls `onAccept(id)`. Reject calls `onReject(id)` directly (no confirmation — rejection is safe).
- Layout: `<div className="space-y-3">` around the cards; each card `<div className="card p-4 space-y-3">`.

**File 4: `client/components/ontology/AddPredicateDialog.tsx`**

Export `interface AddPredicateDialogProps { open: boolean; onOpenChange: (open: boolean) => void; onSubmit: (input: { name: string; description: string; subjectClass?: string; objectClass?: string }) => void; existingPredicateNames: string[]; }`.

Export `function AddPredicateDialog(props): JSX.Element`.

Behavior:
- Uses shadcn `<Dialog>` primitive.
- Fields:
  - Name (Input, required, mono font). Validation: lowercase snake_case only (regex `/^[a-z][a-z0-9_]*$/`); not empty; not in `existingPredicateNames` (case-insensitive). Show validation error inline below the input.
  - Description (Textarea, required, min 5 chars). Placeholder: "e.g. 'Vehicle registered under this entity's name'".
  - Subject class (Select from: Person, Organization, Location, Identifier, Date, Amount, Other; optional). Default = Other.
  - Object class (Select, same options; optional).
- Footer: Cancel + Add Predicate. Submit disabled unless name + description are valid.
- On submit: call `onSubmit(...)` and close (parent controls open state; simply call onOpenChange(false) after onSubmit returns).
- Reset form state on close.

**File 5: `client/components/ontology/MergePredicatesDialog.tsx`**

Export `interface MergePredicatesDialogProps { open: boolean; onOpenChange: (open: boolean) => void; onSubmit: (input: { from: string[]; into: string }) => void; availablePredicates: PredicateEntry[]; }`.

Export `function MergePredicatesDialog(props): JSX.Element`.

Behavior:
- Uses shadcn Dialog primitive.
- UI:
  - Multi-select (checkbox list) of `availablePredicates` filtered to only source='adaptive' or 'manual' (seed + corpus are immutable — see D-03). Show name + source badge + count per row. Scroll if > 15 items.
  - "Merge into" input (Input with autocomplete against `availablePredicates.map(p => p.name)`). Suggest one of the checked names by default when the user checks a single first item, but user can type a new name (which will become manual). Required, snake_case regex validation.
- Validation: at least 2 predicates must be checked; `into` must be non-empty and snake_case.
- Submit calls `onSubmit({ from: checkedNames, into: intoValue.trim() })`, then closes.

Add a shared file `client/components/ontology/sourceBadge.tsx` exporting `export function SourceBadge({ source, pending }: { source: PredicateEntry['source']; pending?: boolean }): JSX.Element`.

All five components (plus SourceBadge) must be named exports. All must compile against the existing type map from Plan 01. Do NOT import mock data — parent (OntologyPanel) passes real data down. Do NOT wire hooks directly — the parent decides which mutation runs on each callback.
  </action>
  <verify>
    <automated>cd client && bunx tsc --noEmit --project ../tsconfig.json 2>&1 | grep -E "(PredicateTable|EntitySubclassTable|ConsolidationDiff|AddPredicateDialog|MergePredicatesDialog|SourceBadge|error TS)" | head -20 || echo "Task 1 components compile"</automated>
  </verify>
  <done>Six components under client/components/ontology/ compile as named exports; SourceBadge extracted; tables + dialogs use shadcn primitives; TS clean; no hook imports (pure presentation + callback props).</done>
</task>

<task type="auto">
  <name>Task 2: OntologyPanel composition + SettingsPage tab integration + URL param default tab</name>
  <files>client/components/ontology/OntologyPanel.tsx, client/pages/SettingsPage.tsx</files>
  <action>
**File 1: `client/components/ontology/OntologyPanel.tsx`**

Export `function OntologyPanel(): JSX.Element`.

Behavior:
- Calls `useOntology()` and the 7 mutation hooks (useApplyConsolidation, useAddManualPredicate, useRenamePredicate, useMergePredicates, useResetOntologyToSeed, useRegenerateCorpusSeed, useSetAutomaticOntologyGrowth).
- Loading: `<Skeleton className="h-64 w-full" />` + smaller row skeletons.
- Error: `<div className="text-error text-sm">Failed to load ontology. Try again.</div>` with a retry button that calls `queryClient.invalidateQueries({ queryKey: queryKeys.ontology })`.
- Full render (composition, in this exact order):

  1. **Header row**: title `<h3 className="section-header text-text-primary">Ontology</h3>` + subtitle showing bootstrap status:
     - If `bootstrapCompletedAt` present: `Bootstrap ran {relativeDate(bootstrapCompletedAt)} on {corpusSeed?.modelUsed ?? 'unknown model'} — {corpusSeed?.sampleDocCount ?? 0} sample docs.`
     - Else: `Bootstrap will run after the first 30 documents are indexed.`

  2. **Automatic growth toggle**: `<div className="flex items-center justify-between">` with Label + subtext + Switch. Bound to `data.automaticGrowthEnabled`. onCheckedChange calls `setAutomaticOntologyGrowth.mutate(newValue)` and shows toast.success on success.

  3. **Pending Consolidation section** — only if `data.pendingConsolidation && data.pendingConsolidation.suggestions.length > 0`. Renders `<ConsolidationDiff pending={data.pendingConsolidation} onAccept={handleAccept} onReject={handleReject} />`.
     - handleAccept: calls `applyConsolidation.mutate(suggestionId)`. On success toast: `"Applied — {rewritten} triples updated"`.
     - handleReject: v1 also calls `applyConsolidation.mutate` with a special sentinel? No — Plan 06 only exposes `apply_consolidation`. Reject is a client-side operation: since rejection means "do not apply", we call `applyConsolidation.mutate("reject:" + suggestionId)`. VERIFY with backend contract: if `apply_consolidation` doesn't accept `reject:` prefix, add a checkpoint here and note it in the SUMMARY. Cleaner fallback: use `useResetOntologyToSeed` is wrong. Simplest correct approach: since Plan 06 didn't define a reject IPC, extend the plan text to call an existing hook `useSetAutomaticOntologyGrowth` — no also wrong. **RESOLUTION:** for v1, implement Reject as a client-only dismissal that hides the suggestion in local component state (persisted to sessionStorage keyed by `id`). If the same suggestion recurs on the next consolidation run, it will reappear. Note this limitation in the SUMMARY as a v1.3 candidate. This is safer than assuming a backend contract that wasn't planned in Plan 06.

  4. **Predicates section**: header `<h4>Predicates ({totalPredicateCount})</h4>` where `totalPredicateCount = corpusSeed.predicates.length + adaptivePredicates.length + manualPredicates.length + pendingPredicates.length + 21_seed_baked_ins`. Include a small "seed count" tooltip. Renders `<PredicateTable seedPredicates={seed21} corpusPredicates={corpusSeed?.predicates ?? []} adaptivePredicates={data.adaptivePredicates} manualPredicates={data.manualPredicates ?? []} pendingPredicates={data.pendingPredicates ?? []} onRename={handleRename} onDelete={handleDelete} />`.
     - `seed21`: hardcode the 21 baked-in seed predicates (name + description only) as a `const SEED_PREDICATES` array in the component file. Copy the names from `.planning/phases/11.5-ontology-relations/11.5-CONTEXT.md` if listed there, else derive from RESEARCH.md. If neither is available, keep the count static and note in the SUMMARY that the actual seed list must come from the ontology store on subsequent iterations. **CORRECTION:** the backend's `get_ontology` returns the full schema INCLUDING seed predicates as a distinct field IF the Plan 06 IPC exposed it. If it does NOT (i.e. the store only tracks corpus+adaptive+manual), then infer the seed list from a compile-time constant. This decision belongs to the executor: grep the actual OntologyStoreSchema type in `client/lib/types.ts`; if a `seedPredicates: PredicateEntry[]` field exists, use it; otherwise use a compile-time constant `SEED_PREDICATES` with the 21 names from Phase 11.5 and derive descriptions from the Phase 11.5 vocabulary constant.
     - `handleRename`: calls `renamePredicate.mutate({ oldName, newName })`. Toast success shows rewritten count.
     - `handleDelete`: v1 hard-delete is implemented as a merge into empty target, but Plan 06's `merge_predicates` requires `into` non-empty. Cleanest v1: replace onDelete with a soft-mark that removes the predicate from the UI only. **SIMPLER:** disable Delete in v1 and note it as v1.3 candidate — implement Delete as a merge into an existing predicate (user picks target via MergePredicatesDialog). Concretely: onDelete opens a small AlertDialog explaining "To remove this predicate, merge it into an existing predicate." with a button that opens the MergePredicatesDialog with the current predicate pre-checked. This aligns with the ontology model: predicates persist as long as any triple uses them.

  5. **Entity Subclasses section**: `<EntitySubclassTable subclasses={data.entitySubclasses} />`.

  6. **Manual Actions row**: horizontal flex of buttons:
     - Add Predicate (btn-secondary, `<Plus size={14} />`) — opens AddPredicateDialog.
     - Merge Predicates (btn-secondary, `<GitMerge size={14} />`) — opens MergePredicatesDialog.
     - Regenerate Corpus Seed (btn-secondary, `<RefreshCw size={14} />`) — AlertDialog: "Re-run corpus-seed bootstrap? This will call the LLM to re-analyse your document library. Existing adaptive predicates are preserved; only the corpus seed layer is replaced." — calls `regenerateCorpusSeed.mutate()`.
     - Reset to Seed (btn-secondary text-error, `<RotateCcw size={14} />`) — AlertDialog: "Reset ontology to seed vocabulary? All corpus, adaptive, and manual predicates will be removed. Triples using those predicates will be unaffected but their predicate labels may become stale. This cannot be undone." — calls `resetOntologyToSeed.mutate()`.

- All mutations show toast.success on success and toast.error(err.message) on error.
- All destructive actions use AlertDialog with the confirmation copy above.

Layout: `<div className="card p-6 space-y-8">` matching the surrounding SettingsPage tab-content style.

**File 2: `client/pages/SettingsPage.tsx`**

Modifications:

1. Import at top:
```typescript
import { useSearchParams } from "react-router-dom";
import { Network } from "lucide-react";
import { OntologyPanel } from "@/components/ontology/OntologyPanel";
```

2. Add `ontology` to `TAB_ICONS`:
```typescript
const TAB_ICONS: Record<string, React.ElementType> = {
  general: Settings2,
  indexing: Database,
  ai: Brain,
  ontology: Network,   // NEW
  privacy: Shield,
  storage: HardDrive,
  about: Info,
};
```

3. Read URL param and use as initial tab:
```typescript
const [searchParams] = useSearchParams();
const initialTab = searchParams.get("tab") ?? "general";
```
Change the Tabs to a controlled component:
```typescript
const [activeTab, setActiveTab] = useState<string>(initialTab);
// keep in sync when the URL param changes (e.g. Sidebar link click):
useEffect(() => {
  const t = searchParams.get("tab");
  if (t) setActiveTab(t);
}, [searchParams]);
```
And on `<Tabs>` use `value={activeTab} onValueChange={setActiveTab}` (drop the old `defaultValue="general"`).

4. Insert the Ontology tab trigger in the TabsList — position between "ai" and "privacy":
```typescript
{ value: "general", label: "General" },
{ value: "indexing", label: "Indexing" },
{ value: "ai", label: "AI & Models" },
{ value: "ontology", label: "Ontology" },   // NEW
{ value: "privacy", label: "Privacy" },
{ value: "storage", label: "Storage" },
{ value: "about", label: "About" },
```

5. Add the Ontology TabsContent between the "ai" and "privacy" TabsContent blocks:
```tsx
<TabsContent value="ontology">
  <OntologyPanel />
</TabsContent>
```
The `<OntologyPanel>` provides its own `card p-6` wrapper, so wrapping in a fragment is fine; NOT wrapping in an additional `card p-6` avoids double-padding.

Do NOT touch other tabs, other imports, or the existing Save button behavior. Do NOT introduce a new "settings save" round-trip for the ontology tab — OntologyPanel manages its own mutations, and the tab has no dirty-state contribution.

Verify with `grep -c "OntologyPanel" client/pages/SettingsPage.tsx` returns 2 (import + JSX) and `grep -c "ontology" client/pages/SettingsPage.tsx` returns >= 3 (TAB_ICONS + TabsTrigger + TabsContent).
  </action>
  <verify>
    <automated>cd client && bunx tsc --noEmit --project ../tsconfig.json 2>&1 | grep -E "(OntologyPanel|SettingsPage|error TS)" | head -10 || echo "Task 2 compiles"</automated>
  </verify>
  <done>OntologyPanel composes all subcomponents and wires all 7 mutation hooks; SettingsPage has a new Ontology tab positioned between 'AI & Models' and 'Privacy'; ?tab= URL param drives the initial tab; TS clean.</done>
</task>

<task type="auto">
  <name>Task 3: Sidebar — Prominent Entities section + Ontology quick link with pending badge</name>
  <files>client/components/layout/Sidebar.tsx</files>
  <action>
Modify existing `client/components/layout/Sidebar.tsx`.

Add imports at the top:
```typescript
import { Star, Sparkles } from "lucide-react";   // Star reused if already imported; Sparkles for the Ontology quick link (or GitBranch — pick Sparkles for a "learned" feel).
import { useProminentEntities, useOntology } from "@/hooks/useTauri";
```
Do not double-import icons that are already imported (Star, Bookmark, Wallet, Network are already there).

Inside `Sidebar()`, near the other data hooks (below `useSavedSearchCounts`, above `useTopPersonId`), add:
```typescript
const { data: prominentEntities } = useProminentEntities(5);
const { data: ontology } = useOntology();
const pendingConsolidationCount = ontology?.pendingConsolidation?.suggestions.length ?? 0;
```

**Prominent Entities section** — place it AFTER the Saved Searches block and BEFORE the "Owned by me" quick link (line ~410-437 range).

Match the section-header pattern used by Saved Searches:
```tsx
<div className="pt-4">
  {!isCollapsed && (
    <div className="px-3 py-2">
      <span className="text-xs font-semibold uppercase tracking-wider text-text-tertiary">
        Prominent Entities
      </span>
    </div>
  )}
  {prominentEntities && prominentEntities.length > 0 ? (
    <nav className="space-y-0.5 px-2 pb-2">
      {prominentEntities.map((pe) => {
        const displayName = pe.entity.canonicalShortName ?? pe.entity.canonicalName;
        const href = `/entity/${pe.entity.entityType}/${encodeURIComponent(pe.entity.canonicalName)}`;
        const active = isActive(href);
        // Icon by entityType: person -> Users; organization -> Building2; location -> MapPin
        const Icon = pe.entity.entityType === "person" ? Users
                   : pe.entity.entityType === "organization" ? Building2
                   : MapPin;
        return (
          <Link
            key={pe.entity.id}
            to={href}
            className={cn(
              "flex items-center gap-3 rounded-md px-3 py-2 text-sm font-medium transition-colors",
              active
                ? "bg-bg-tertiary text-text-primary"
                : "text-text-secondary hover:bg-bg-tertiary hover:text-text-primary",
              pe.isProminent && !isCollapsed && "font-semibold"
            )}
          >
            <Icon size={14} className={cn("flex-shrink-0", pe.isProminent ? "text-accent-primary" : "text-text-tertiary")} />
            {!isCollapsed && (
              <>
                <span className="flex-1 truncate">{displayName}</span>
                <span className="text-xs text-text-tertiary flex-shrink-0">({pe.docCount})</span>
              </>
            )}
          </Link>
        );
      })}
    </nav>
  ) : (
    !isCollapsed && (
      <p className="px-3 py-2 text-xs text-text-tertiary">No prominent entities yet</p>
    )
  )}
</div>
```

Add missing icon imports at the top of Sidebar.tsx:
```typescript
import { Users, Building2, MapPin } from "lucide-react";
```
(Merge into the existing `lucide-react` import block; don't add a second import line.)

**Ontology quick link** — place it AFTER the "Owned by me" block, still inside the `<nav>` element:

```tsx
{/* Ontology quick link — D-23. Badge shows pending consolidation count when > 0. */}
<div className="pt-4">
  <Link
    to="/settings?tab=ontology"
    className={cn(
      "group relative flex items-center gap-3 rounded-md px-3 py-2.5 text-sm font-medium transition-all duration-150",
      location.pathname === "/settings" && location.search.includes("tab=ontology")
        ? "bg-accent-primary text-white"
        : "text-text-secondary hover:bg-bg-tertiary hover:text-text-primary"
    )}
  >
    <Sparkles size={20} />
    {!isCollapsed && <span className="flex-1">Ontology</span>}
    {pendingConsolidationCount > 0 && (
      <span
        className={cn(
          "flex items-center justify-center rounded-full bg-red-500 text-white text-xs font-semibold",
          isCollapsed ? "absolute -top-1 -right-1 h-4 w-4" : "h-5 min-w-5 px-1.5"
        )}
        title={`${pendingConsolidationCount} pending consolidation ${pendingConsolidationCount === 1 ? "suggestion" : "suggestions"}`}
      >
        {pendingConsolidationCount > 9 ? "9+" : pendingConsolidationCount}
      </span>
    )}
    {isCollapsed && (
      <div className="absolute left-full ml-2 hidden rounded-md bg-bg-secondary px-2 py-1 text-xs text-text-primary shadow-lg group-hover:block whitespace-nowrap">
        Ontology{pendingConsolidationCount > 0 ? ` (${pendingConsolidationCount})` : ""}
      </div>
    )}
  </Link>
</div>
```

Preserve all existing Sidebar functionality (spaces list, saved searches, Owned by me, bottom links, storage bar, collapse toggle).

Do NOT modify the bottom-links list — Settings still lives at the bottom as a general Settings link. The Ontology quick link is a shortcut into Settings > Ontology tab, not a replacement.
  </action>
  <verify>
    <automated>cd client && bunx tsc --noEmit --project ../tsconfig.json 2>&1 | grep -E "(Sidebar|error TS)" | head -10 || echo "Task 3 compiles"</automated>
  </verify>
  <done>Sidebar has Prominent Entities section (top-5 with prominence bold + accent icon) + Ontology quick link with pending badge; collapsed sidebar shows badge as a small corner dot; TS clean.</done>
</task>

<task type="auto">
  <name>Task 4: Component + integration tests (Vitest + React Testing Library)</name>
  <files>client/components/ontology/OntologyPanel.test.tsx, client/components/ontology/PredicateTable.test.tsx, client/components/layout/Sidebar.test.tsx</files>
  <action>
Follow the existing test patterns in `client/components/**/*.test.tsx` and `client/hooks/useTauri.test.ts`. Use QueryClient wrapper and MemoryRouter as needed.

**File 1: `client/components/ontology/PredicateTable.test.tsx`**

Cases:
- `test("renders all five predicate sources into a single sorted table (count desc)")` — seed 2, corpus 2, adaptive 2, manual 1, pending 1; assert render order matches count desc.
- `test("filters by name substring (case-insensitive)")` — type into filter; verify only matching rows visible.
- `test("does not show rename/delete actions for seed or corpus predicates")` — query for Pencil/Trash2 icons within seed rows; expect 0.
- `test("shows rename inline input and calls onRename on Enter")` — click Pencil on an adaptive row, type new name, press Enter; assert `onRename` called with `(oldName, newName)`.
- `test("shows a 'pending' badge for pending predicates")` — assert presence.

**File 2: `client/components/ontology/OntologyPanel.test.tsx`**

Cases:
- `test("renders skeleton while loading")` — mock useOntology to return { isLoading: true }.
- `test("renders bootstrap subtitle when bootstrapCompletedAt present")` — use `mockOntology` from mock-data.
- `test("shows Pending Consolidation section only when suggestions > 0")` — assert visibility from mockOntology (2 suggestions) and from a manual variant with 0.
- `test("toggling Automatic Growth calls useSetAutomaticOntologyGrowth")` — click Switch; assert mutation called with new value.
- `test("Add Predicate button opens AddPredicateDialog")` — click; assert dialog visible.

**File 3: `client/components/layout/Sidebar.test.tsx` (extend the existing file if present; otherwise create)**

Cases:
- `test("renders top-5 prominent entities with prominence bold + accent icon")` — mount Sidebar with mockProminentEntities; assert first 3 rows have `font-semibold` class and `text-accent-primary` on the icon.
- `test("Prominent Entities row navigates to /entity/{type}/{encodedName}")` — assert href.
- `test("Ontology quick link shows a badge with pending count")` — mock useOntology to return 2 suggestions; assert badge visible with "2".
- `test("Ontology badge is hidden when no pending suggestions")` — mock useOntology to return 0 suggestions; assert badge NOT in DOM.

Use `vi.mock("@/hooks/useTauri", ...)` to stub the hooks per test file (same pattern as existing Sidebar tests). If existing sidebar tests do NOT exist, create the file fresh with a minimal setup.
  </action>
  <verify>
    <automated>cd client && bun test client/components/ontology client/components/layout/Sidebar.test.tsx</automated>
  </verify>
  <done>Vitest tests pass; PredicateTable renders and sorts correctly; OntologyPanel conditionally shows Pending Consolidation section; Sidebar renders Prominent Entities + Ontology badge.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Settings > Ontology tab + Sidebar prominence UI wired to the Phase 11.6 adaptive ontology backend.

Concretely:
- New Ontology tab in Settings between 'AI & Models' and 'Privacy'.
- Full predicate table (seed + corpus + adaptive + manual + pending) with sort, filter, per-row rename/delete for adaptive/manual only.
- Entity subclass table grouped by class.
- Pending consolidation diff cards with Accept (calls apply_consolidation IPC) and Reject (client-side dismiss in v1).
- Manual actions: Add Predicate, Merge Predicates (dialogs), Regenerate Corpus Seed, Reset to Seed (with AlertDialog confirmations).
- Automatic growth toggle wired to set_automatic_ontology_growth IPC.
- Sidebar 'Prominent Entities' section between Saved Searches and 'Owned by me' quick link — top-5 by doc_count, first 3 bolded + accent icon.
- Sidebar 'Ontology' quick link opens /settings?tab=ontology with a red pending badge when consolidation suggestions exist.
- URL param handling: /settings?tab=ontology deep-links directly to the tab.
  </what-built>
  <how-to-verify>
Real-corpus test against ~/private (the CONTEXT.md trigger corpus):

1. Ensure Phase 11.6 backend (Plans 02-06) is running with a real ontology store populated by the bootstrap + backfill:
   ```
   cd src-tauri && cargo tauri dev
   ```

2. Wait for indexing to complete and the corpus-seed bootstrap to run (30+ docs indexed).

3. Open Settings and click the new "Ontology" tab. Verify:
   - Bootstrap subtitle shows the run date + model + sample doc count.
   - Predicates table renders with the 21 seed predicates + any corpus-seeded predicates + any adaptive predicates. Filter by "owned" and verify only owned_by / owns / owner-related predicates appear.
   - Sort by count — most-used predicate is at the top.
   - Entity subclasses table shows at least Location subclasses (apartment, plot) if the ~/private corpus contains property docs.

4. If a pending_consolidation exists (may need to wait for the 500-triple or 24h trigger, OR run `regenerate_corpus_seed` and trigger consolidation via a debug menu if available):
   - Verify the Pending Consolidation cards show merge/rename/split kinds with correct visual diff.
   - Click Accept on one suggestion → verify toast shows "{N} triples updated" AND navigate to /entity/Person/{a-person} and confirm the merged predicate now appears with the new name in the Relations panel.
   - Click Reject on another → verify it disappears from the panel; refresh → verify it stays dismissed (sessionStorage).

5. Test manual actions:
   - Click "Add Predicate" → fill in `sale_deed_of` / "Legal ownership document" / Location→Person → submit → verify it appears in the table with source=manual.
   - Click Pencil on the new manual predicate → rename to `sale_deed` → verify table updates and toast shows "N triples updated".
   - Click "Merge Predicates" → check two adaptive predicates → merge into a canonical name → verify table updates.
   - Click "Reset to Seed" → confirm dialog → verify table shrinks back to the 21 seed predicates only.
   - Click "Regenerate Corpus Seed" → confirm → verify it triggers a background job (toast: "Regenerating…") and the corpus_seed section eventually refreshes.

6. Test the growth toggle:
   - Toggle Automatic Growth OFF → verify toast and confirm the state persists across page reload (query invalidation flushes).

7. Test Sidebar prominence + badge:
   - Confirm the Sidebar shows a "Prominent Entities" section between Saved Searches and Owned by me.
   - Verify top-3 entities are bolded with an accent-color icon (Users / Building2 / MapPin depending on entityType).
   - Click a prominent entity → verify it navigates to /entity/{type}/{encodedName}.
   - When a pending consolidation exists, verify the Sidebar "Ontology" link shows a red badge with the pending count.
   - Click the Ontology link → verify it opens Settings on the Ontology tab (URL contains ?tab=ontology).

8. Test the collapsed sidebar:
   - Collapse the sidebar via the toggle button.
   - Verify prominent entity rows show only the icon (with tooltip on hover).
   - Verify the Ontology quick link shows the badge as a small red dot in the top-right corner.

Report any visual glitches, sort mis-ordering, mis-wired mutations, or missing prominence bolding.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 11.6, or describe issues to trigger revision.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL param → Settings tab default | `?tab=` value drives which TabsContent is shown; must not eval or interpolate into DOM |
| user click → destructive mutation | Reset to Seed, Merge Predicates, Delete predicate all rewrite triples in the store |
| Reject-consolidation state → sessionStorage | Client-only rejection dismissal (v1 workaround) — reset per tab session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.6-40 | Injection | ?tab= URL param used as Tabs value | mitigate | React state assignment; Tabs primitive itself ignores unknown values (renders no active content). No dangerouslySetInnerHTML anywhere. |
| T-11.6-41 | Tampering | Destructive actions (Reset to Seed, Delete predicate) triggered by mistaken click | mitigate | AlertDialog confirmation with explicit warning copy; disabled while pending; toast confirms outcome |
| T-11.6-42 | Elevation | User overwrites a seed predicate name via rename | mitigate | PredicateTable disables rename for source='seed' / 'corpus'; backend also validates (Plan 06) |
| T-11.6-43 | Information Disclosure | Predicate description reveals corpus doc IDs to sidebar consumers | accept | firstSeenDocId is already exposed to the frontend by Plan 06; only rendered in the Ontology tab (behind Settings); no cross-window leak |
| T-11.6-44 | Availability | 200+ predicates render into a single table without pagination | accept | D-08 hard-caps vocabulary at 200 entries; consolidation loop forces trimming before growth. Realistic personal corpora yield 30-80 predicates |
| T-11.6-45 | Tampering | Pending consolidation "Reject" is client-only in v1 → suggestion recurs on next run | accept | v1 acceptance criterion. Backend contract for "reject" is deferred to a future patch; UI note in SUMMARY. |
</threat_model>

<verification>
- `cd client && bunx tsc --noEmit` — clean.
- `cd client && bun test client/components/ontology client/components/layout/Sidebar.test.tsx` — passes.
- `grep -c "OntologyPanel" client/pages/SettingsPage.tsx` — returns 2 (import + JSX).
- `grep -v '^ *//' client/pages/SettingsPage.tsx | grep -c "ontology"` — returns ≥ 4 (TAB_ICONS + TabsTrigger + TabsContent + import path).
- `grep -c "useProminentEntities" client/components/layout/Sidebar.tsx` — returns ≥ 1.
- `grep -c "pendingConsolidationCount" client/components/layout/Sidebar.tsx` — returns ≥ 2 (declaration + JSX use).
- `grep -c "tab=ontology" client/components/layout/Sidebar.tsx` — returns ≥ 1.
- `ls client/components/ontology/*.tsx | wc -l` — returns ≥ 7 (OntologyPanel, PredicateTable, EntitySubclassTable, ConsolidationDiff, AddPredicateDialog, MergePredicatesDialog, sourceBadge — plus tests).
- Human-verify checkpoint approved against real ~/private corpus.
</verification>

<success_criteria>
1. Settings has a new Ontology tab positioned between 'AI & Models' and 'Privacy'.
2. OntologyPanel renders bootstrap subtitle, growth toggle, pending consolidation (when present), predicate table, entity subclass table, and manual actions row.
3. PredicateTable is sortable/filterable and disables rename/delete for seed + corpus predicates.
4. Pending consolidation Accept applies via useApplyConsolidation and shows the rewritten-triple count; Reject dismisses locally.
5. Add/Merge/Regenerate/Reset actions are gated by AlertDialog (except Add which uses a regular Dialog form) and wire the correct mutations.
6. Sidebar renders top-5 prominent entities between Saved Searches and Owned by me — first 3 bolded + accent icon; clicking navigates to /entity/{type}/{encodedName}.
7. Sidebar Ontology quick link opens /settings?tab=ontology and shows a red badge with pending count when > 0.
8. /settings?tab=ontology deep-link opens the Ontology tab on mount.
9. All new + modified files pass tsc and Vitest.
10. Human-verify checkpoint approves against real ~/private corpus.
</success_criteria>

<output>
Create `.planning/phases/11.6-adaptive-ontology/11.6-09-SUMMARY.md` when done. Include:
- Component inventory + LOC per component.
- Final decision on the "Reject consolidation" mechanism (sessionStorage vs future backend contract).
- Final decision on the seed-predicate source (backend field vs client constant).
- Any predicate-delete UX changes (delete-via-merge vs disabled).
- Screenshots or descriptions from the human-verify checkpoint.
- v1.3 candidates surfaced during the test run.
</output>

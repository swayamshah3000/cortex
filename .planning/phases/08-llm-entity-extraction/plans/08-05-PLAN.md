---
phase: 08-llm-entity-extraction
plan: 05
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
  - 08-03
files_modified:
  - src-tauri/src/pipeline/two_pass_extractor.rs
  - src-tauri/src/pipeline/mod.rs
  - src-tauri/src/commands/entities.rs
  - src-tauri/src/commands/mod.rs
  - src-tauri/src/state.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements:
  - LLME-01
  - LLME-03
  - LLME-04
  - LLME-05

must_haves:
  truths:
    - "TwoPassExtractor::extract(text) is a synchronous drop-in replacement for NerService::extract — returns Vec<ExtractedEntity> via Pass 1 only"
    - "TwoPassExtractor::extract_full(text, title) is async, runs Pass 1 then Pass 2 (if enabled + provider connected) and returns ExtractedEntities with entities_version=3.0 or 2.5"
    - "Merge policy per D-20: Pass-2 refined_entities override Pass-1 class/subclass/confidence by pass1_id; unrefined Pass-1 entities keep their Pass 1 classification; additional_entities appended"
    - "IPC commands get_extraction_settings, set_extraction_settings, trigger_entity_backfill exist and are registered in invoke_handler"
    - "TwoPassExtractor handle is registered on AppState and accessible via State<'_, AppState> in all IPC command bodies"
    - "TwoPassExtractor.set_llm_enabled(bool) mirrors Settings.useLlmExtraction so the D-33 toggle takes effect immediately without restart"
  artifacts:
    - path: "src-tauri/src/pipeline/two_pass_extractor.rs"
      provides: "TwoPassExtractor struct + extract() sync + extract_full() async + merge_passes() helper"
      contains: "pub struct TwoPassExtractor"
      min_lines: 180
    - path: "src-tauri/src/commands/entities.rs"
      provides: "get_extraction_settings, set_extraction_settings, trigger_entity_backfill Tauri commands"
      contains: "trigger_entity_backfill"
    - path: "src-tauri/src/state.rs"
      provides: "AppState.two_pass_extractor: Arc<TwoPassExtractor>"
      contains: "two_pass_extractor"
    - path: "src-tauri/src/lib.rs"
      provides: "Wire TwoPassExtractor into setup() and register the three new IPC commands"
      contains: "trigger_entity_backfill"
  key_links:
    - from: "src-tauri/src/pipeline/two_pass_extractor.rs"
      to: "src-tauri/src/pipeline/pass1_pattern_extractor.rs"
      via: "composition"
      pattern: "Pass1PatternExtractor"
    - from: "src-tauri/src/pipeline/two_pass_extractor.rs"
      to: "src-tauri/src/pipeline/pass2_llm_refiner.rs"
      via: "async composition"
      pattern: "Pass2LlmRefiner"
    - from: "src-tauri/src/commands/entities.rs"
      to: "src-tauri/src/state.rs (AppState.two_pass_extractor)"
      via: "State<'_, AppState>"
      pattern: "state.two_pass_extractor"
---

<objective>
Build the `TwoPassExtractor` facade that composes Pass 1 (deterministic regex + validators) and Pass 2 (LLM refinement) with the correct merge policy, and expose three new Tauri IPC commands (`get_extraction_settings`, `set_extraction_settings`, `trigger_entity_backfill`) that Settings UI (Plan 07) invokes.

Purpose: The facade is what indexer.rs, backfill.rs, and lib.rs will hold instead of `NerService`. Getting the merge policy right — Pass-2 wins by pass1_id, unrefined Pass-1 entries kept, additional_entities appended — is the LLME-01 delivery. The IPC commands are the Settings surface for D-22 (Re-extract), D-32 (button enabled once toggle on), and D-33 (Use LLM toggle).

Output: `pipeline/two_pass_extractor.rs` with the facade, updated `AppState` holding `Arc<TwoPassExtractor>`, three new IPC commands in `commands/entities.rs` (kept in this file because they're semantically entity-related), and lib.rs wiring so the app boots with the facade registered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-llm-entity-extraction/08-CONTEXT.md
@.planning/phases/08-llm-entity-extraction/08-RESEARCH.md
@.planning/phases/08-llm-entity-extraction/08-01-SUMMARY.md
@.planning/phases/08-llm-entity-extraction/08-02-SUMMARY.md
@.planning/phases/08-llm-entity-extraction/08-03-SUMMARY.md
@src-tauri/src/pipeline/pass1_pattern_extractor.rs
@src-tauri/src/pipeline/pass2_llm_refiner.rs
@src-tauri/src/state.rs
@src-tauri/src/commands/entities.rs
@src-tauri/src/lib.rs

<interfaces>
From Plan 02 (Pass1):
```rust
pub struct Pass1PatternExtractor { /* … */ }
impl Pass1PatternExtractor {
    pub fn new() -> Result<Self, AppError>;
    pub fn extract(&self, text: &str) -> Result<Vec<ExtractedEntity>, AppError>;
}
```

From Plan 03 (Pass2):
```rust
pub struct Pass2LlmRefiner { /* holds Arc<AuthState>, Semaphore, RwLock<String> */ }
impl Pass2LlmRefiner {
    pub fn new(auth: Arc<AuthState>) -> Self;
    pub async fn set_model(&self, model: String);
    pub async fn model(&self) -> String;
    pub async fn refine(&self, text: &str, title: &str, pass1: &[ExtractedEntity]) -> Result<Pass2Output, AppError>;
}
pub struct Pass2Output {
    pub additional_entities: Vec<Pass2AdditionalEntity>,
    pub refined_entities: Vec<Pass2RefinedEntity>,
    pub topic: Option<String>,
    pub tags: Vec<String>,
    pub language: Option<String>,
}
```

From Plan 01 (types):
```rust
pub struct ExtractedEntities {
    pub entities: Vec<ExtractedEntity>,
    pub topic: Option<String>,
    pub tags: Vec<String>,
    pub entities_version: f32,
    pub language: Option<String>,
}
pub const TWO_PASS_TARGET_VERSION: f32 = 3.0;
pub const PASS1_ONLY_VERSION: f32 = 2.5;
```

Existing AppState in `src-tauri/src/state.rs` — Phase 8 removes `ner_service: Arc<NerService>` field (Plan 10 will delete NerService entirely, but this plan already removes the field to unblock indexer.rs rewire in Plan 10). Add `pub two_pass_extractor: Arc<TwoPassExtractor>`.

Existing IPC pattern (from Phase 7 `commands/ai.rs`):
```rust
#[tauri::command]
pub async fn some_command(state: State<'_, AppState>, arg: T) -> Result<R, AppError> {
    // spawn_blocking or direct async as appropriate
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build TwoPassExtractor facade with merge policy</name>
  <files>src-tauri/src/pipeline/two_pass_extractor.rs, src-tauri/src/pipeline/mod.rs</files>
  <behavior>
    - `TwoPassExtractor::new(auth)` returns a facade holding Pass1 + Pass2 refiner + Arc<AtomicBool> llm_enabled=true
    - `extract(text)` (sync) returns just Pass 1 output — Vec<ExtractedEntity>, callable from spawn_blocking
    - `extract_full(text, title)` (async) with llm_enabled=false → returns ExtractedEntities with Pass 1 only, entities_version=2.5, topic=None, tags=empty
    - `extract_full` with llm_enabled=true and no active provider → same as above (Pass 2 refiner short-circuits per Plan 03 Task 2 semantics), entities_version=2.5
    - `extract_full` with llm_enabled=true and Pass 2 returns Ok(Pass2Output{...}) → merged entities per D-20, entities_version=3.0, topic + tags copied through
    - `extract_full` with llm_enabled=true and Pass 2 returns Err(_) → Pass-1-only fallback (D-26), entities_version=2.5, does NOT propagate the error (must return Ok)
    - `set_llm_enabled(false)` — subsequent `extract_full` calls skip Pass 2 even with a provider connected
    - Merge policy tests:
      - Pass 1 emits `[e0=Identifier(subclass=unknown, conf=0.7), e1=Email(conf=1.0)]`, Pass 2 refines `[{pass1_id=e_0, class=Identifier, subclass=aadhaar, confidence=0.92}]` and adds `[{class=Person, value="John Smith", confidence=0.95}]`. Result: entities = `[Identifier(subclass=aadhaar, conf=0.92), Email(conf=1.0), Person("John Smith", conf=0.95)]`. Order = Pass 1 order first, then additional_entities appended.
      - When a refined_entities pass1_id doesn't match any pass1 index (`e_99`), it is dropped with a warning log.
      - When Pass 2 has 0 refined_entities and 0 additional_entities but a valid topic — topic is still copied into the output.
    - `assign_pass1_ids(entities)` helper: assigns `label` prefix `e_<index>` internally OR passes the index in the prompt without mutating `label` — pick one deterministic scheme; document it in the code
  </behavior>
  <action>
    Per D-16, D-17, D-18, D-20, D-26 and RESEARCH.md Pattern 1:
    1. Register the module: append `pub mod two_pass_extractor;` to `pipeline/mod.rs`.
    2. Create `pipeline/two_pass_extractor.rs`. Define:
       ```
       pub struct TwoPassExtractor {
           pass1: Pass1PatternExtractor,
           pass2: Arc<Pass2LlmRefiner>,
           llm_enabled: Arc<std::sync::atomic::AtomicBool>,
       }
       ```
    3. `pub fn new(auth: Arc<AuthState>) -> Result<Self, AppError>` — constructs Pass1 (fail-fast on regex parse) + Pass2LlmRefiner + llm_enabled AtomicBool defaulted to true.
    4. `pub fn extract(&self, text: &str) -> Result<Vec<ExtractedEntity>, AppError>` — drop-in for NerService::extract — synchronously returns `self.pass1.extract(text)`. Callable from spawn_blocking contexts (indexer.rs uses this).
    5. `pub async fn extract_full(&self, text: &str, title: &str) -> Result<ExtractedEntities, AppError>`:
       a. `let pass1 = self.pass1.extract(text)?;`
       b. If `!self.llm_enabled.load(Acquire)` → return `Ok(ExtractedEntities { entities: pass1, topic: None, tags: vec![], entities_version: PASS1_ONLY_VERSION, language: None })`
       c. Call `self.pass2.refine(text, title, &pass1).await`
       d. On Err(_) → log warn `"[pass2] refine failed for '{title}': {e} — falling back to Pass 1 only"` and return the same Pass-1-only shape as step b (LLME-04, D-26 — never propagate the error)
       e. On Ok(pass2_output) → detect provider-absent shortcut: if pass2_output is Pass2Output::empty() (all fields default), also return Pass-1-only shape. Otherwise call `merge_passes(pass1, pass2_output)` and return version=3.0.
    6. `fn merge_passes(mut pass1: Vec<ExtractedEntity>, pass2: Pass2Output) -> ExtractedEntities`:
       - Build a lookup: for each refined_entities `re` where `re.pass1_id = "e_N"`, extract `N` and if `N < pass1.len()`: overwrite `pass1[N].class = Some(re.class); pass1[N].subclass = re.subclass; pass1[N].confidence = Some(re.confidence);`. Log warn for pass1_ids that don't parse or index out of bounds (dropped).
       - Convert each `additional_entities` entry to `ExtractedEntity { label: value.clone(), value, entity_type: entity_type_from_class(class), class: Some(class), subclass, confidence: Some(confidence), canonical_id: None }` and append to the entities vec. `entity_type_from_class` maps the 8-class to the legacy `entity_type` string (Person→"person", Organization→"organization", Location→"location", Date→"date", Amount→"amount", Email→"email", Phone→"phone", Identifier→"identifier") — Phase 6 code still reads entity_type.
       - Re-apply the 20-entity cap AFTER merge (LLME-03) — sort by (entity_type, value), dedup, truncate to 20.
       - Return `ExtractedEntities { entities: merged, topic: pass2.topic, tags: pass2.tags, entities_version: TWO_PASS_TARGET_VERSION, language: pass2.language }`.
    7. `pub fn set_llm_enabled(&self, enabled: bool)` — atomic store.
    8. `pub async fn set_model(&self, model: String) { self.pass2.set_model(model).await }` — forwards to Pass2 refiner.
    9. `pub fn pass2(&self) -> Arc<Pass2LlmRefiner>` — a getter for the backfill loop (Plan 06) to spawn per-doc async tasks that need direct refine() access.
    10. Add ~6 unit tests covering: extract() Pass 1 only; extract_full with llm disabled; extract_full with Pass 2 mock returning empty (fallback path); merge_passes with 1 refined + 1 additional; merge_passes with unmatched pass1_id (drop); 20-cap after merge (build 15 pass1 + 10 additional → assert final length = 20).
  </action>
  <verify>
    <automated>cd src-tauri && cargo test --lib pipeline::two_pass_extractor -- --nocapture 2>&1 | tail -40 && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>TwoPassExtractor with extract() sync + extract_full() async + merge_passes() defined and unit-tested; llm_enabled toggle works; Pass 2 error → Pass-1-only fallback; 20-cap enforced after merge; cargo check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire TwoPassExtractor into AppState + expose 3 IPC commands + register in lib.rs</name>
  <files>src-tauri/src/state.rs, src-tauri/src/commands/entities.rs, src-tauri/src/commands/mod.rs, src-tauri/src/lib.rs</files>
  <action>
    Per D-16, D-22, D-32, D-33 and D-30 (provider switch info toast — the FRONTEND fires this; backend only needs to update the model on provider change indirectly via Settings):
    1. In `src-tauri/src/state.rs`, add `pub two_pass_extractor: Arc<crate::pipeline::two_pass_extractor::TwoPassExtractor>` to the `AppState` struct. Keep `ner_service: Arc<NerService>` FIELD in place for this plan — Plan 10 removes it after indexer.rs and backfill.rs stop using it. Do NOT delete NerService yet.
    2. In `src-tauri/src/commands/entities.rs`, add three new Tauri commands (use existing serde patterns — camelCase input, State<'_, AppState>):
       - `#[tauri::command] pub async fn get_extraction_settings(state: State<'_, AppState>) -> Result<ExtractionSettings, AppError>` — reads the current model from `state.two_pass_extractor.pass2().model().await` and the enabled flag from the atomic. Returns an `ExtractionSettings { extraction_model, use_llm_extraction }` struct (define it in types.rs if not already present from Plan 01; add here otherwise with #[serde(rename_all="camelCase")] — do NOT alter the settings.json format; this is a runtime-current view).
       - `#[tauri::command] pub async fn set_extraction_settings(settings: ExtractionSettings, state: State<'_, AppState>) -> Result<(), AppError>` — updates BOTH the runtime state (`state.two_pass_extractor.set_model(settings.extraction_model.clone()).await; state.two_pass_extractor.set_llm_enabled(settings.use_llm_extraction);`) AND persists to settings.json via the existing `get_settings/update_settings` pattern (merge into the existing Settings blob so other fields are preserved). Fire-and-forget — no toast from backend.
       - `#[tauri::command] pub async fn trigger_entity_backfill(state: State<'_, AppState>, app: AppHandle) -> Result<(), AppError>` — validates that `state.two_pass_extractor.pass2().model().await` is non-empty AND `state.two_pass_extractor.llm_enabled()` is true — else returns `AppError::Internal("Extraction model or LLM toggle not configured".into())`. Then calls `pipeline::backfill::spawn_entity_backfill(app, engine, two_pass, entity_store, embedder)` — the signature will change in Plan 06 (async two_pass instead of sync ner). For this plan, sketch the call with the FUTURE signature and add a `todo!()` inside the function IF Plan 06's spawn_entity_backfill signature isn't ready — actually, since Plan 06 depends on this plan (wave-order), Plan 05 CANNOT invoke the new spawn signature yet. Solution: this command bodies just returns Ok(()) with a TODO comment; the wire-up completes in Plan 06 Task 2 which owns backfill.rs. Ensure `#[tauri::command]` is still valid and the type surface is stable — Plan 05's job is to expose the command name to the frontend so Plan 07 UI can invoke it. Document this handoff explicitly in the SUMMARY.
    3. Add `pub two_pass_extractor` construction in `lib.rs setup()`:
       - After the existing `let auth_state = crate::auth::AuthState::new(&app_data)` line and BEFORE `app.manage(auth_state)`, keep `auth_state` in a shared Arc: change to `let auth_state = Arc::new(crate::auth::AuthState::new(&app_data))` (or wrap in Arc if AuthState is already registered — inspect the current lib.rs pattern; AuthState is registered via `app.manage(auth_state)` at line 140, which means `app.manage()` takes ownership. Solution: keep `let auth_state = crate::auth::AuthState::new(&app_data)`, then before `app.manage(auth_state)`, clone into an Arc for TwoPassExtractor if AuthState implements Clone; if it does NOT, wrap it in Arc from the start: `let auth_state = Arc::new(crate::auth::AuthState::new(&app_data))`, register the Arc directly via `app.manage(auth_state.clone())` — check if Tauri manage() accepts Arc<T> or requires T. If T only: register T and refactor TwoPassExtractor::new to accept `&AuthState` and clone internally — but the async retry pattern needs Arc. Simplest: wrap AuthState in Arc<AuthState> at creation and register the Arc.
       - Read the Phase 7 `commands/ai.rs` module for the exact pattern — the `State<'_, AuthState>` extractor is used across ai commands, so AuthState is likely registered as `AuthState` (not `Arc<AuthState>`). Preserve that. For TwoPassExtractor, since Pass 2 needs `Arc<AuthState>`, capture `let auth_arc = Arc::new(auth_state.clone())` — but this requires AuthState: Clone. If AuthState uses interior mutability (Mutex/RwLock), a shallow clone is safe. Confirm by reading `src-tauri/src/auth/mod.rs` — if Clone is derived, wrap; if not, define TwoPassExtractor to accept a shared AuthState via a different mechanism (Tauri State from inside commands only).
       - Fallback strategy if Clone is not derived: change `Pass2LlmRefiner::refine` (Plan 03) to take `&AuthState` per-call instead of holding `Arc<AuthState>`. This is a Plan 03 SUMMARY dependency — if Plan 03 SUMMARY notes AuthState:Clone was not available, use the per-call reference passing instead. Document the chosen approach in this plan's SUMMARY.
       - Once AuthState arc-ing is resolved, construct `let two_pass = Arc::new(TwoPassExtractor::new(auth_state_arc.clone()).expect("Pass 1 regex init failed"));`
       - Add `two_pass_extractor: two_pass.clone()` to the `AppState { … }` literal.
       - On boot, load persisted `extraction_model` + `use_llm_extraction` from settings.json (call the existing `get_settings` code path OR read the file directly via `settings_path()`), and apply: `tokio::runtime::Handle::current().block_on(async { two_pass.set_model(settings.extraction_model.clone()).await; })` — actually Tauri setup runs on the tokio runtime; use `tauri::async_runtime::block_on(async { … })` for the model setter. Set llm_enabled from the bool.
    4. Register the three new commands in `invoke_handler!` in `lib.rs`:
       - `commands::entities::get_extraction_settings`
       - `commands::entities::set_extraction_settings`
       - `commands::entities::trigger_entity_backfill`
    5. Update `commands/mod.rs` if it re-exports symbols (add the three new command names to any `pub use` line).
    6. Run `cargo check` + `cargo test --lib` — everything from Plans 01-04 must still pass, and the app should build.
  </action>
  <verify>
    <automated>cd src-tauri && cargo build 2>&1 | tail -20 && cargo test --lib pipeline::two_pass_extractor commands::entities -- --nocapture 2>&1 | tail -30</automated>
  </verify>
  <done>AppState.two_pass_extractor field added; three IPC commands defined and registered in invoke_handler; setup() constructs TwoPassExtractor and loads persisted extraction settings; cargo build clean; commands unit-testable via App::mock_state pattern OR compilation-only verification if command tests require full Tauri harness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend IPC → backend | ExtractionSettings shape crosses via `tauriInvoke("set_extraction_settings", { settings })` |
| trigger_entity_backfill → backfill task | Command spawns a long-running background task with full access to EntityStore + engine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-15 | Denial of Service | trigger_entity_backfill called repeatedly by frontend | mitigate | Command validates model+toggle configured (returns error early); actual concurrency guard lives in Plan 06 backfill.rs (single-flight via engine mutex + entities_version gate) |
| T-08-16 | Tampering | set_extraction_settings with unknown model ID | accept | Model ID is passed through to the provider; provider rejects at HTTP layer with a clear error → frontend surfaces in toast. Backend does not maintain a model allow-list (would drift from provider catalogues) |
| T-08-17 | Elevation of Privilege | IPC command bypasses Tauri capabilities | mitigate | All three commands follow the existing capability model registered in tauri.conf.json for other entity commands — no new capability grant needed |
</threat_model>

<verification>
- `cargo build` succeeds (full binary compiles)
- `cargo test --lib pipeline::two_pass_extractor` all pass
- `grep "trigger_entity_backfill\|get_extraction_settings\|set_extraction_settings" src-tauri/src/lib.rs | grep -v '^\s*//' | wc -l` ≥ 3 (all three registered)
- `grep "two_pass_extractor" src-tauri/src/state.rs` finds the field
- `grep "NerService" src-tauri/src/state.rs` still finds it (removal is Plan 10's job)
- The app boots via `cargo tauri dev` without panics (manual smoke — recorded in SUMMARY)
</verification>

<success_criteria>
1. `TwoPassExtractor` facade with sync extract() and async extract_full() exists and is unit-tested
2. Merge policy per D-20 works (refined_entities override by pass1_id; additional appended; unmatched dropped; 20-cap re-applied)
3. Pass 2 Err → Pass-1-only fallback (LLME-04, D-26) — never propagates
4. `AppState.two_pass_extractor` registered and accessible via State<'_, AppState>
5. Three IPC commands registered in invoke_handler and reachable from the frontend
6. Persisted extraction_model + use_llm_extraction loaded from settings.json on boot
7. `cargo build` and `cargo test --lib pipeline::two_pass_extractor` succeed
</success_criteria>

<output>
Create `.planning/phases/08-llm-entity-extraction/08-05-SUMMARY.md` when done. Include: final AuthState arc-sharing strategy chosen (Clone vs per-call), the pass1_id numbering scheme used in merge_passes, the exact command names registered, and the deferred trigger_entity_backfill body wiring that Plan 06 will complete.
</output>

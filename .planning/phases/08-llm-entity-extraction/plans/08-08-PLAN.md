---
phase: 08-llm-entity-extraction
plan: 08
type: execute
wave: 5
depends_on:
  - 08-04
files_modified:
  - client/components/entities/TopicChip.tsx
  - client/components/entities/TopicChip.test.tsx
  - client/components/entities/TagChip.tsx
  - client/components/entities/TagChip.test.tsx
  - client/components/entities/ConfidenceExpander.tsx
  - client/components/entities/ConfidenceExpander.test.tsx
  - client/components/entities/EntityChip.tsx
  - client/pages/DocumentPage.tsx
autonomous: true
requirements:
  - LLME-01
  - LLME-02

must_haves:
  truths:
    - "TopicChip renders a single doc-level topic with Bookmark icon, accent-tinted background, pill shape (rounded-full), snake_case → 'Sentence case' display transform"
    - "TagChip renders a free-form tag with Hash icon, neutral background, rectangular pill (rounded-md), snake_case → 'space case' display transform, max-w-[120px] truncate"
    - "ConfidenceExpander wraps low-confidence (< 0.7) entities in shadcn Collapsible with 'Also found ({count})' trigger + ChevronDown icon; inner chips render in italic muted styling"
    - "EntityChip's icon map extended to cover ALL 8 classes: Person (Users), Organization (Building2), Location (MapPin), Date (Calendar), Amount (DollarSign), Email (Mail), Phone (Phone), Identifier (Fingerprint) — Phone + Identifier are the two NEW mappings per UI-SPEC §5"
    - "EntityChip renders a subclass Badge (variant=outline, text-xs font-mono) next to Identifier entities when subclass is set (e.g., 'aadhaar', 'iban', 'pan')"
    - "DocumentPage metadata sidebar renders in display order: (1) Topic chip if topic set AND != 'other'; (2) Tags row if tags.length > 0; (3) high-confidence EntityChip grid; (4) ConfidenceExpander if any entities < 0.7"
    - "Empty state 'No entities found — Entities will appear after indexing completes. Connect AI for richer extraction.' shown when the document has zero entities"
  artifacts:
    - path: "client/components/entities/TopicChip.tsx"
      provides: "TopicChip component"
      contains: "Bookmark"
    - path: "client/components/entities/TagChip.tsx"
      provides: "TagChip component"
      contains: "Hash"
    - path: "client/components/entities/ConfidenceExpander.tsx"
      provides: "ConfidenceExpander component"
      contains: "Collapsible"
    - path: "client/components/entities/EntityChip.tsx"
      provides: "Extended icon map + subclass Badge rendering"
      contains: "Fingerprint"
    - path: "client/pages/DocumentPage.tsx"
      provides: "Metadata sidebar renders topic + tags + confidence expander in the mandated order"
      contains: "TopicChip"
  key_links:
    - from: "client/pages/DocumentPage.tsx"
      to: "client/lib/types.ts (ExtractedEntity, ExtractedEntities)"
      via: "types.class field + doc-level topic/tags"
      pattern: "topic"
    - from: "client/components/entities/EntityChip.tsx"
      to: "client/components/entities/EntityChip.tsx icon map"
      via: "8-class icon dispatch"
      pattern: "Fingerprint"
---

<objective>
Ship the Phase 8 entity display components (TopicChip, TagChip, ConfidenceExpander, EntityChip 8-class extension) and wire them into DocumentPage's metadata sidebar in the mandated display order.

Purpose: Delivers LLME-01 (user-visible entities on /document/:id come from the LLM) and LLME-02 (8-class coverage uniformly across any document domain) at the UI layer. Every user who opens a document after Phase 8 sees topic + tags + confidence-filtered entities — the "aha" moment for the LLM extraction upgrade.

Output: Three new atomic chip components with tests + EntityChip icon-map extension + subclass Badge rendering + DocumentPage sidebar wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-llm-entity-extraction/08-CONTEXT.md
@.planning/phases/08-llm-entity-extraction/08-UI-SPEC.md
@.planning/phases/08-llm-entity-extraction/08-04-SUMMARY.md
@client/components/entities/EntityChip.tsx
@client/pages/DocumentPage.tsx
@client/lib/types.ts
@client/lib/mock-data.ts

<interfaces>
Type shapes (from Plan 04):
```ts
interface ExtractedEntity {
  label: string;
  value: string;
  entityType: string;       // "person" | "organization" | ... (Phase 6 legacy)
  canonicalId?: string | null;
  class?: string;           // "Person" | "Organization" | "Location" | "Date" | "Amount" | "Email" | "Phone" | "Identifier"
  subclass?: string;        // free-form: "aadhaar" | "iban" | "pan" | ...
  confidence?: number;      // 0.0..=1.0
}
interface ExtractedEntities {
  entities: ExtractedEntity[];
  topic: string | null;
  tags: string[];
  entitiesVersion: number;
  language: string | null;
}
```

Mock data (from Plan 04):
- `mockDocumentEntitiesContainer` for browser dev-mode rendering

Display transform rules (from UI-SPEC §Topic vs Tag Visual Distinction Contract):
- Topic display: replace `_` → space, capitalize first letter → `term_insurance` → "Term insurance"
- Tag display: replace `_` → space (no capitalization) → `khush_school` → "khush school"

Existing EntityChip.tsx uses a `getIconForType(entityType: string)` helper — extend to also key off `class` (fallback to entityType if class is absent for Phase 6 backward compat).

Existing DocumentPage.tsx renders extracted entities in the metadata sidebar. Study how it currently loads the document and where entities render (search for `extractedEntities` or `EntityChip`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build TopicChip + TagChip + ConfidenceExpander with tests</name>
  <files>client/components/entities/TopicChip.tsx, client/components/entities/TopicChip.test.tsx, client/components/entities/TagChip.tsx, client/components/entities/TagChip.test.tsx, client/components/entities/ConfidenceExpander.tsx, client/components/entities/ConfidenceExpander.test.tsx</files>
  <behavior>
    - TopicChip:
      - Props: `{ topic: string }`
      - Renders `<div>` (not button, not link — non-clickable in Phase 8 per UI-SPEC §Topic vs Tag table)
      - Classes: `inline-flex items-center gap-1 px-3 py-1 rounded-full text-xs bg-accent-primary/10 text-accent-primary border border-accent-primary/20`
      - Bookmark icon (12px, from lucide) prefixes the display text
      - Display transform: "term_insurance" → "Term insurance" (`_` → space, capitalize first letter of the entire result)
      - When topic is empty string OR "other", returns `null` (does not render)
      - Test: renders with correct classes; icon present; display text transformed; renders null for empty and "other"
    - TagChip:
      - Props: `{ tag: string }`
      - Renders `<div>` non-clickable
      - Classes: `inline-flex items-center gap-1 px-3 py-1 rounded-md text-xs bg-bg-tertiary text-text-secondary border border-border-secondary max-w-[120px] truncate`
      - Hash icon (12px, from lucide) prefixes the display text
      - Display transform: "khush_school" → "khush school" (`_` → space; no capitalization change)
      - When tag is empty string, returns null
      - Test: renders with correct classes; icon present; display text transformed; truncate class present; null for empty tag
    - ConfidenceExpander:
      - Props: `{ entities: ExtractedEntity[], renderEntity: (e: ExtractedEntity) => ReactNode }` — the parent (DocumentPage) supplies the render function so the expander stays layout-agnostic
      - Filters props.entities to those with `confidence != null && confidence < 0.7` internally
      - When filtered count is 0, returns null
      - Uses shadcn `Collapsible`, `CollapsibleTrigger`, `CollapsibleContent`
      - Trigger button: `<ChevronDown size={12} className="text-text-tertiary transition-transform" /> <span className="text-xs text-text-tertiary">Also found ({filteredCount})</span>` — rotates icon on open
      - CollapsibleContent renders each filtered entity via renderEntity, wrapped in `<div className="italic text-text-tertiary">` container so all descendants get the muted italic styling
      - aria-label on trigger: "Low-confidence entities — may contain OCR errors"
      - Test: filters entities correctly (accepts 3 entities with confidence [0.9, 0.65, 0.5], asserts filtered count = 2); returns null when no low-confidence entities; open/close via trigger click updates data-state; renderEntity called for each filtered entry
  </behavior>
  <action>
    Per UI-SPEC §Component Inventory and §Control Specifications 5:
    1. Create `client/components/entities/TopicChip.tsx`:
       - Named export `function TopicChip({ topic }: { topic: string })`
       - Guard: `if (!topic || topic === "other") return null;`
       - Display helper (inline in file): `const display = topic.replace(/_/g, " "); const capitalized = display.charAt(0).toUpperCase() + display.slice(1);`
       - Return the div with the exact classes listed above + `<Bookmark size={12} />` + `<span>{capitalized}</span>`
       - Add `title={topic}` on the div so hover reveals the raw snake_case for debug (does not violate the non-interactive rule — title attribute is passive)
    2. Create `client/components/entities/TopicChip.test.tsx` with tests listed above using `render` from `@testing-library/react`.
    3. Create `client/components/entities/TagChip.tsx`:
       - Named export `function TagChip({ tag }: { tag: string })`
       - Guard: `if (!tag) return null;`
       - Display helper: `const display = tag.replace(/_/g, " ");` (no capitalize)
       - Return div with correct classes + `<Hash size={12} />` + `<span className="truncate">{display}</span>`
       - `title={tag}` for full-text-on-hover per truncation
    4. Create `client/components/entities/TagChip.test.tsx` with tests.
    5. Create `client/components/entities/ConfidenceExpander.tsx`:
       - Import shadcn `Collapsible`, `CollapsibleTrigger`, `CollapsibleContent`; `ChevronDown` from lucide.
       - Named export `function ConfidenceExpander({ entities, renderEntity }: { entities: ExtractedEntity[]; renderEntity: (e: ExtractedEntity) => React.ReactNode })`
       - Filter: `const low = entities.filter(e => e.confidence != null && e.confidence < 0.7);`
       - Guard: `if (low.length === 0) return null;`
       - Local state via `useState<boolean>(false)` for open state
       - JSX: `<Collapsible open={open} onOpenChange={setOpen}><CollapsibleTrigger className="flex items-center gap-1" aria-label="Low-confidence entities — may contain OCR errors"><ChevronDown size={12} className={cn("text-text-tertiary transition-transform", open && "rotate-180")} /><span className="text-xs text-text-tertiary">Also found ({low.length})</span></CollapsibleTrigger><CollapsibleContent className="mt-2"><div className="flex flex-wrap gap-1 italic text-text-tertiary">{low.map(e => renderEntity(e))}</div></CollapsibleContent></Collapsible>`
    6. Create `client/components/entities/ConfidenceExpander.test.tsx` with the listed tests.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/cortex && bunx --bun vitest run client/components/entities/TopicChip client/components/entities/TagChip client/components/entities/ConfidenceExpander 2>&1 | tail -30 && bunx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Three new components + three test files exist; all tests pass; tsc --noEmit clean; components use ONLY shadcn primitives already installed (Collapsible confirmed present per UI-SPEC §Registry Safety); no new registry installs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend EntityChip icon map to 8 classes + subclass Badge rendering + wire DocumentPage sidebar</name>
  <files>client/components/entities/EntityChip.tsx, client/pages/DocumentPage.tsx</files>
  <behavior>
    - EntityChip icon map handles all 8 classes per UI-SPEC §5 table:
      - Person → Users (text-purple-400)
      - Organization → Building2 (text-amber-400)
      - Location → MapPin (text-red-400)
      - Date → Calendar (text-blue-400)
      - Amount → DollarSign (text-green-400)
      - Email → Mail (text-cyan-400)
      - Phone → Phone (text-teal-400)  ← NEW
      - Identifier → Fingerprint (text-orange-400)  ← NEW
    - Icon lookup: `getIconForClass(entity.class ?? mapLegacyEntityTypeToClass(entity.entityType))` — legacy entityType strings like "person" → "Person" so Phase 6 rendering continues to work while Phase 8 shape is preferred
    - Subclass Badge:
      - When `entity.class === "Identifier" && entity.subclass && entity.subclass !== "unknown"`, render a shadcn `Badge variant="outline"` with `text-xs font-mono` next to the value (e.g., "aadhaar")
      - When subclass is "unknown" (weak-format from Pass 1), do NOT render the Badge (would be noisy)
      - When class is anything other than "Identifier", do NOT render subclass Badge
    - DocumentPage metadata sidebar renders in the mandated order per UI-SPEC §Display order in metadata sidebar:
      1. Topic chip (only when doc has topic AND topic !== "other")
      2. Tags row (only when tags.length > 0)
      3. High-confidence entities grid (existing EntityChip flex-wrap)
      4. ConfidenceExpander (only when any entity.confidence < 0.7)
    - Empty state: when the document has zero entities in ALL three categories (topic null, tags empty, entities empty), render `<div className="text-xs text-text-tertiary py-8 text-center">No entities found<br /><span className="mt-1 block">Entities will appear after indexing completes. Connect AI for richer extraction.</span></div>`
    - Tests: EntityChip renders Phone icon when class="Phone"; renders Fingerprint when class="Identifier"; renders subclass Badge for Identifier with subclass="aadhaar"; does NOT render Badge for subclass="unknown"; falls back to legacy entityType when class is absent
  </behavior>
  <action>
    Per UI-SPEC §5 EntityChip icon map + §Display order:
    1. Edit `client/components/entities/EntityChip.tsx`:
       - Add `Phone`, `Fingerprint` imports from lucide-react
       - Add `Badge` import from `@/components/ui/badge`
       - Add or extend `getIconForClass(cls: string | undefined)` helper returning `{ Icon, colorClass }` — 8-way switch. If `cls` is undefined, call `mapLegacyEntityTypeToClass(entity.entityType)` first then dispatch. If the legacy value doesn't map cleanly, fall back to a default icon (e.g., FileText) with `text-text-tertiary`.
       - `mapLegacyEntityTypeToClass`: `person → Person`, `organization → Organization`, `location → Location`, `date → Date`, `amount → Amount`, `email → Email`, `phone → Phone`, `identifier → Identifier`; anything else → undefined (default icon).
       - In the render body, after the icon + value span, add the subclass Badge conditional per Behavior.
       - Extend the existing test file (if EntityChip.test.tsx exists) with 4 new cases; otherwise add cases to a new test suite in the same file.
    2. Edit `client/pages/DocumentPage.tsx`:
       - Locate the metadata sidebar section where entities currently render (search for `extractedEntities.map` or `EntityChip`)
       - Determine how the doc-level topic/tags are exposed. Since Plan 04 kept the Document type unchanged (extractedEntities is still Vec<ExtractedEntity>), the topic + tags need to come from a NEW field. Two options:
         - (A) Extend the `Document` interface with optional `topic?: string | null` and `tags?: string[]` fields; ensure Plan 05's backend `get_document` command populates them from the metadata JSON. Since Plan 05 didn't modify `get_document`, this task MAY need to also touch `commands/documents.rs` to read the topic/tags fields from metadata and include them in the Document response.
         - (B) Add a NEW IPC command `get_document_entities_container(id) → ExtractedEntities` and call it from DocumentPage via a new hook.
         - Pick (A) for simplicity — extend `Document` (Rust + TS), extend `get_document` to read the two extra metadata keys. Document this cross-cutting change in the SUMMARY.
       - For the Rust side of option (A): in `src-tauri/src/types.rs`, add `#[serde(default)] pub topic: Option<String>, #[serde(default)] pub tags: Vec<String>` to the `Document` struct — both serde-default so existing Phase 6 code still deserializes. In `commands/documents.rs::get_document`, read `metadata["topic"]` and `metadata["tags"]` and populate the new fields. Since this touches the boundary between the TS/Rust boundary, ensure the TS `Document` interface gains the mirrors — actually already done in Plan 04 Task 1 if you added them there; if not, add them here.
       - Import TopicChip, TagChip, ConfidenceExpander from `@/components/entities/*`.
       - Render order per UI-SPEC:
         ```tsx
         <section className="metadata-entities">
           {document.topic && document.topic !== "other" && (
             <div className="mb-4">
               <div className="text-xs text-text-tertiary mb-1">Topic</div>
               <TopicChip topic={document.topic} />
             </div>
           )}
           {document.tags && document.tags.length > 0 && (
             <div className="mb-4">
               <div className="text-xs text-text-tertiary mb-1">Tags</div>
               <div className="flex flex-wrap gap-1">
                 {document.tags.map(t => <TagChip key={t} tag={t} />)}
               </div>
             </div>
           )}
           <div className="mb-4">
             <div className="flex flex-wrap gap-1">
               {highConfidence.map(e => <EntityChip key={e.value} entity={e} />)}
             </div>
             <ConfidenceExpander entities={document.extractedEntities} renderEntity={e => <EntityChip key={e.value} entity={e} />} />
           </div>
           {isEmpty && <div className="empty-state">...</div>}
         </section>
         ```
         where `highConfidence = document.extractedEntities.filter(e => e.confidence == null || e.confidence >= 0.7)` and `isEmpty = !document.topic && (!document.tags || document.tags.length === 0) && document.extractedEntities.length === 0`.
    3. If DocumentPage has an existing test file, add a rendering test that supplies a mock document with topic="finance", tags=["bank_statement"], entities including one with confidence=0.65, and asserts: topic chip visible, tag chip visible, low-confidence entity NOT in the main grid, "Also found (1)" trigger visible.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/cortex && bunx tsc --noEmit 2>&1 | head -30 && bunx --bun vitest run client/components/entities client/pages/DocumentPage 2>&1 | tail -30 && cd src-tauri && cargo test --lib types::tests commands::documents 2>&1 | tail -20</automated>
  </verify>
  <done>EntityChip icon map handles all 8 classes + subclass Badge conditional; DocumentPage renders topic + tags + confidence expander in mandated order + empty state; Rust Document struct gains topic + tags (serde-default); get_document populates the fields from metadata; all tests pass; tsc --noEmit clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc metadata → DocumentPage render | topic/tags values from LLM output flow into the sidebar; already normalized to snake_case at write time (Plan 06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-24 | Tampering | Adversarial doc content produces a topic like `<script>alert(1)</script>_pwn` | mitigate | Rust `normalize_tag` (Plan 01) strips non-alphanumeric-except-underscore before write; TopicChip / TagChip render via React (auto-escapes text content); no innerHTML anywhere in the chips |
| T-08-25 | Denial of Service | 1000 tags on a single doc renders unbounded chip row | accept | LLM prompt caps at "2-5 tags" (D-08); the 20-entity cap doesn't apply to tags, but overflow risk is bounded by max_tokens=4096 in Plan 03; flex-wrap handles visual overflow gracefully |
| T-08-26 | Information Disclosure | ConfidenceExpander shows all low-confidence entities including potentially PII values | accept | The aria-label warns "may contain OCR errors"; this is user's own doc content — no cross-user exposure vector |
</threat_model>

<verification>
- `bunx tsc --noEmit` clean
- `bunx --bun vitest run client/components/entities` — all suites green
- `grep "Bookmark" client/components/entities/TopicChip.tsx` finds the icon
- `grep "Hash" client/components/entities/TagChip.tsx` finds the icon
- `grep "Fingerprint\|Phone" client/components/entities/EntityChip.tsx | wc -l` ≥ 2 (both new icons added)
- `grep "TopicChip\|TagChip\|ConfidenceExpander" client/pages/DocumentPage.tsx | wc -l` ≥ 3 (all three used)
- `cargo test --lib types` — Document struct changes serialize/deserialize round-trip test passes with topic + tags added
- Manual smoke on a doc with topic + tags + low-confidence entity: sidebar renders in the mandated order (recorded in SUMMARY)
</verification>

<success_criteria>
1. TopicChip, TagChip, ConfidenceExpander components + tests exist and pass
2. EntityChip icon map handles all 8 classes; subclass Badge rendered for Identifier with known subclass
3. DocumentPage renders topic + tags + confidence expander + empty state in mandated order
4. Rust Document struct extended with topic + tags (both `#[serde(default)]`)
5. get_document reads topic + tags from metadata and includes them in the response
6. tsc --noEmit clean; all vitest + cargo tests green
</success_criteria>

<output>
Create `.planning/phases/08-llm-entity-extraction/08-08-SUMMARY.md` when done. Include: whether option A (extend Document) or option B (new command) was chosen for topic/tags surfacing, screenshots or narrative of the DocumentPage sidebar with topic/tags/low-confidence entities, and any UI-SPEC copy deviations.
</output>

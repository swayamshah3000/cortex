---
phase: 08-llm-entity-extraction
plan: 09
type: execute
wave: 5
depends_on:
  - 08-04
files_modified:
  - client/components/search/TopicFilterBar.tsx
  - client/components/search/TopicFilterBar.test.tsx
  - client/pages/SearchPage.tsx
  - client/pages/TagsPage.tsx
  - src-tauri/src/commands/analytics.rs
  - src-tauri/src/types.rs
  - client/hooks/useTauri.ts
  - client/lib/types.ts
autonomous: true
requirements:
  - LLME-01
  - LLME-02

must_haves:
  truths:
    - "TopicFilterBar renders a row of topic chips: label 'Topics:' + up to 20 top topics + 'Show more' inline button for pagination in 20-topic increments"
    - "Active-state chip uses accent-fill (bg-accent-primary text-white); inactive uses neutral (bg-bg-secondary text-text-secondary hover:bg-bg-tertiary)"
    - "Chip display transform matches TopicChip: 'term_insurance' → 'Term insurance'"
    - "When 0 topics indexed, TopicFilterBar returns null (does not render an empty bar)"
    - "When 1-20 topics, no 'Show more' button; when 21+ topics, first 20 + 'Show more' visible"
    - "SearchPage renders TopicFilterBar below the existing doc-type FilterBar; selecting a topic filters results client-side by document.topic === selectedTopic"
    - "TagsPage renders TopicFilterBar below the existing Auto/User tag-type filter row, above the tag cloud"
    - "Backend get_topics IPC command returns Vec<TopicCount> sorted by count descending; a helper aggregates topics from all indexed docs"
  artifacts:
    - path: "client/components/search/TopicFilterBar.tsx"
      provides: "TopicFilterBar + TopicFilterChip components"
      contains: "TopicFilterBar"
      min_lines: 100
    - path: "src-tauri/src/commands/analytics.rs"
      provides: "get_topics IPC command"
      contains: "get_topics"
    - path: "src-tauri/src/types.rs"
      provides: "TopicCount struct"
      contains: "TopicCount"
  key_links:
    - from: "client/components/search/TopicFilterBar.tsx"
      to: "client/hooks/useTauri.ts (useTopics)"
      via: "React Query hook returning TopicCount[]"
      pattern: "useTopics"
    - from: "client/pages/SearchPage.tsx"
      to: "client/components/search/TopicFilterBar.tsx"
      via: "topic-filter state + client-side filter"
      pattern: "TopicFilterBar"
    - from: "src-tauri/src/commands/analytics.rs::get_topics"
      to: "src-tauri/src/engine.rs (CortexEngine documents_384 metadata scan)"
      via: "aggregate topic strings"
      pattern: "topic"
---

<objective>
Ship the TopicFilterBar component and wire it onto `/search` and `/tags` so users can narrow their view to a single doc-level topic. Add the backend IPC that aggregates topic counts across the index.

Purpose: Delivers the discovery half of LLME-02 (topic labels emerge across any domain — user actually uses them for navigation). Also gives Phase 11 (Entity-Driven Exploration) a working topic-filter reference to extend.

Output: `TopicFilterBar` component with pagination + tests; `SearchPage` and `TagsPage` mount the bar; new `get_topics` IPC command returning `TopicCount[]`; supporting types and hook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-llm-entity-extraction/08-CONTEXT.md
@.planning/phases/08-llm-entity-extraction/08-UI-SPEC.md
@.planning/phases/08-llm-entity-extraction/08-04-SUMMARY.md
@client/pages/SearchPage.tsx
@client/pages/TagsPage.tsx
@client/hooks/useTauri.ts
@src-tauri/src/commands/analytics.rs
@src-tauri/src/types.rs

<interfaces>
Existing FilterChip pattern from SearchPage.tsx — mirror its visual style but for topics.

New backend IPC (this plan creates):
```rust
#[tauri::command]
pub async fn get_topics(state: State<'_, AppState>) -> Result<Vec<TopicCount>, AppError>;

pub struct TopicCount {
    pub topic: String,     // snake_case
    pub count: u32,
}
```

New frontend hook (this plan creates):
```ts
useTopics(): { data?: TopicCount[], isLoading: boolean };
// Browser fallback: mockTopics = [ { topic: "finance", count: 12 }, { topic: "identity", count: 8 }, ... ]
```

Existing `mockDocuments[0].topic` from Plan 04 for browser dev mode.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend get_topics IPC + TopicCount type + frontend types/hook/mock</name>
  <files>src-tauri/src/types.rs, src-tauri/src/commands/analytics.rs, src-tauri/src/lib.rs, client/lib/types.ts, client/lib/mock-data.ts, client/hooks/useTauri.ts</files>
  <behavior>
    - Rust `TopicCount { topic: String, count: u32 }` with `#[serde(rename_all = "camelCase")]`
    - `get_topics` command scans documents_384 collection, aggregates topic strings from metadata["topic"] into a HashMap<String, u32>, converts to Vec<TopicCount>, sorts by count DESC then topic ASC, returns
    - Docs where metadata["topic"] is absent, empty string, or "other" are EXCLUDED from the aggregation (they don't get counted)
    - Command registered in invoke_handler
    - Frontend TypeScript type mirrors Rust exactly
    - `mockTopics` in mock-data.ts: at least 5 entries covering topics that also appear in mockDocuments (so the browser dev mode filter has real docs to match)
    - `useTopics()` hook: browser mode → mockTopics; Tauri mode → tauriInvoke("get_topics")
    - `queryKeys.topics = ["topics"] as const`
    - Rust unit test: insert 3 VectorEntries with topics ["finance", "finance", "other"]; assert get_topics returns [{topic:"finance",count:2}] and does NOT include "other"
  </behavior>
  <action>
    1. In `src-tauri/src/types.rs`, add `TopicCount` struct with `topic: String`, `count: u32`, camelCase serde.
    2. In `src-tauri/src/commands/analytics.rs`, add:
       - Import `TopicCount` from `crate::types`.
       - `#[tauri::command] pub async fn get_topics(state: State<'_, AppState>) -> Result<Vec<TopicCount>, AppError>` — spawn_blocking around a helper that:
         a. Acquires `state.engine.blocking_lock()` OR uses async lock — since spawn_blocking is inside, use `blocking_lock`.
         b. Fetches the `documents_384` collection; iterates keys; for each, reads `metadata["topic"].as_str()` and increments a `HashMap<String, u32>`.
         c. Excludes entries where topic is missing, empty, or equals "other".
         d. Converts to `Vec<TopicCount>`, sorts by `(u32::MAX - count)` then `topic` (i.e., count DESC, topic ASC).
         e. Returns the vec.
       - Add a `#[cfg(test)]` unit test inserting 3 VectorEntries as described in Behavior.
    3. Register the command in `src-tauri/src/lib.rs::invoke_handler!`.
    4. In `client/lib/types.ts`, add `export interface TopicCount { topic: string; count: number; }`.
    5. In `client/lib/mock-data.ts`, add `export const mockTopics: TopicCount[] = [ { topic: "finance", count: 12 }, { topic: "identity", count: 8 }, { topic: "vehicle", count: 5 }, { topic: "kids", count: 4 }, { topic: "property", count: 3 } ];` — ensure mockDocuments has at least one document per topic so the filter actually filters something in browser mode.
    6. In `client/hooks/useTauri.ts`, add `queryKeys.topics: ["topics"] as const,` and `export function useTopics()` following the useSpaces pattern (browser fallback → mockTopics; Tauri → tauriInvoke).
  </action>
  <verify>
    <automated>cd src-tauri && cargo test --lib commands::analytics::tests::test_get_topics 2>&1 | tail -20 && cargo build 2>&1 | tail -5 && cd /Users/gshah/work/apps/cortex && bunx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>get_topics registered; TopicCount defined on both sides; useTopics hook + queryKeys entry added; mockTopics + doc coverage in place; cargo test + cargo build + tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build TopicFilterBar component + wire onto SearchPage and TagsPage</name>
  <files>client/components/search/TopicFilterBar.tsx, client/components/search/TopicFilterBar.test.tsx, client/pages/SearchPage.tsx, client/pages/TagsPage.tsx</files>
  <behavior>
    - Props: `{ selected: string | null, onSelect: (topic: string | null) => void }` — parent owns filter state
    - Reads `useTopics()` internally; renders null when data is undefined OR data.length === 0
    - Renders label "Topics:" (`text-xs text-text-tertiary`) followed by chip row
    - Local state `visibleCount: number` starting at 20; "Show more" increments by 20; button hidden when `visibleCount >= data.length`
    - Each chip is a `TopicFilterChip` (defined in same file):
      - Wrapper: `button` element (interactive per UI-SPEC — this is the exception to the non-clickable topic rule; TopicFilterChip is intentionally interactive for filtering)
      - Base classes: `px-3 py-1 rounded-full text-xs inline-flex items-center gap-1 transition-colors`
      - Inactive: `bg-bg-secondary text-text-secondary border border-border-primary hover:bg-bg-tertiary`
      - Active: `bg-accent-primary text-white border border-accent-primary`
      - Prefix: `<Bookmark size={12} />`
      - Text: display transform (snake_case → sentence case) same as TopicChip
      - Click: if already selected → deselect (calls onSelect(null)); else calls onSelect(topic)
      - Also render a "count" suffix in text-xs opacity-60 (e.g., "Term insurance · 12") — helps user pick popular filters
    - "Show more" button: `text-xs text-accent-primary hover:text-accent-hover ml-2 inline-block` — appears after the chip list
    - Tests:
      - Returns null when data is undefined
      - Returns null when data is empty array
      - Renders 20 chips when 20 topics; no Show more
      - Renders 20 chips + Show more button when 25 topics; click Show more shows all 25
      - Clicking an inactive chip fires onSelect(topic)
      - Clicking the currently-selected chip fires onSelect(null)
      - Active chip has bg-accent-primary class
    - SearchPage integration:
      - Add local `selectedTopic: string | null` state
      - Render `<TopicFilterBar selected={selectedTopic} onSelect={setSelectedTopic} />` below the existing FilterBar
      - Filter results: pass selectedTopic through so results whose `topic` field != selectedTopic (when set) are hidden. Since SearchPage uses backend search results (SearchResult[]) that include Document, filter client-side: `results.filter(r => !selectedTopic || r.document.topic === selectedTopic)`
      - When selectedTopic changes, no need to re-invoke search — this is a client-side narrow
    - TagsPage integration:
      - Add local `selectedTopic: string | null` state
      - Render `<TopicFilterBar selected={selectedTopic} onSelect={setSelectedTopic} />` between the existing Auto/User filter row and the tag cloud
      - TagsPage currently renders a tag cloud from the `useTags` hook. Filter cloud entries: if selectedTopic is set, only show tags that appear in documents whose topic === selectedTopic. Implementation: fetch documents via `useRecentDocuments` (or add a lightweight `useDocumentsByTopic(topic)` hook — but to keep this plan bounded, do the filter client-side by joining the tag data with mockDocuments/document data available on the page). Fallback simplification: filter is applied ONLY to tag rendering in dev mode; the backend already tags docs with topic + tags, so a proper backend-driven "tags by topic" filter is out of scope — document this reduction in SUMMARY and provide the client-side approximation.
      - No tag filter → all tags shown; topic filter set + tag needs both to match → intersection displayed.
  </behavior>
  <action>
    Per UI-SPEC §6 Topic Filter and §Copywriting:
    1. Create `client/components/search/TopicFilterBar.tsx`:
       - Named exports: `TopicFilterBar` and `TopicFilterChip`
       - `TopicFilterChip` props: `{ topic: string; count: number; active: boolean; onClick: () => void }`
       - Display transform helper: same as TopicChip in Plan 08 — factor into a shared `formatTopic(t: string): string` and either duplicate in this file (Plan 08 and Plan 09 run in parallel wave 5; extraction to a shared util in `client/lib/format.ts` is a preferred follow-up but not blocking).
       - Import `Bookmark` from lucide.
       - Use shadcn `Button` primitive OR raw button — pick raw button so the interactive chip pattern matches existing `FilterChip` in SearchPage.tsx.
       - Include `data-testid="topic-filter-chip"` on the button so tests can query by role + name.
    2. Create `client/components/search/TopicFilterBar.test.tsx` with the tests listed above.
    3. Edit `client/pages/SearchPage.tsx`:
       - Add local state `const [selectedTopic, setSelectedTopic] = useState<string | null>(null)`
       - Locate the existing FilterBar mount. Immediately below it, add `<TopicFilterBar selected={selectedTopic} onSelect={setSelectedTopic} />`
       - Wrap the results loop in a client-side filter: `const filteredResults = useMemo(() => selectedTopic ? results.filter(r => r.document.topic === selectedTopic) : results, [results, selectedTopic])`
    4. Edit `client/pages/TagsPage.tsx`:
       - Add local state `const [selectedTopic, setSelectedTopic] = useState<string | null>(null)`
       - Add `<TopicFilterBar selected={selectedTopic} onSelect={setSelectedTopic} />` below the existing tag-type filter (Auto/User) row
       - Apply a topic-based filter to the tag cloud. Simplest client-side approach: fetch `useRecentDocuments()` (or `useSearch` with empty query) OR — pragmatic — filter the existing `tags` list ONLY when the mock/actual documents include a topic field. For now: if selectedTopic is null → show all tags; if selectedTopic is set → show tags whose count property is unaffected (backend already knows the tag counts) but visually indicate the filter is active by adding a badge "Filtered by topic: {topic}" above the cloud. Full backend integration (`get_tags_by_topic`) is deferred — mark as TODO in the file with a Phase 11 reference.
    5. Ensure the tests pass end-to-end: `bunx tsc --noEmit` and vitest for the new component + any regression on SearchPage/TagsPage tests (if they exist).
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/cortex && bunx --bun vitest run client/components/search/TopicFilterBar 2>&1 | tail -30 && bunx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TopicFilterBar component + tests exist; TopicFilterChip active/inactive styling per UI-SPEC; "Show more" pagination in 20-topic increments; SearchPage filters results by selectedTopic; TagsPage renders bar and applies visual filter marker; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| get_topics response → frontend | Aggregate metadata scan; each topic is snake_case output of normalize_tag (Plan 01) — already sanitized |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-27 | Denial of Service | get_topics scan on 100K-doc corpus takes seconds | accept | Same O(N) scan pattern as existing get_tags / rebuild_from_engine; acceptable for v1.1 index sizes (~10K docs); future improvement: cached topic-count index invalidated on index changes |
| T-08-28 | Tampering | Adversarial LLM output crafts a topic string that breaks the filter | mitigate | Topic values pass through Rust normalize_tag before persistence; only alphanumeric + `_` reach the frontend; React auto-escapes on render |
</threat_model>

<verification>
- `cargo test --lib commands::analytics::tests::test_get_topics` passes
- `cargo build` succeeds
- `bunx tsc --noEmit` clean
- `bunx --bun vitest run client/components/search/TopicFilterBar` all green
- `grep "TopicFilterBar" client/pages/SearchPage.tsx client/pages/TagsPage.tsx | wc -l` == 2
- `grep "get_topics" src-tauri/src/lib.rs | grep -v '^\s*//' | wc -l` == 1 (registered)
- Manual smoke on `/search` and `/tags`: TopicFilterBar renders when topics exist; selecting a chip narrows results / tag cloud
</verification>

<success_criteria>
1. TopicFilterBar + TopicFilterChip components + tests exist and pass
2. Backend get_topics IPC command registered + unit-tested
3. TopicCount type mirrored on Rust and TS
4. useTopics hook + mockTopics for browser dev mode
5. SearchPage renders TopicFilterBar + applies client-side filter
6. TagsPage renders TopicFilterBar + visual filter marker
7. "Show more" pagination in 20-topic increments per D-37
8. tsc + vitest + cargo clean
</success_criteria>

<output>
Create `.planning/phases/08-llm-entity-extraction/08-09-SUMMARY.md` when done. Include: final backend get_topics complexity note, the TagsPage filter reduction rationale (visual marker vs full get_tags_by_topic backend), and screenshots or narrative of the filter in action.
</output>

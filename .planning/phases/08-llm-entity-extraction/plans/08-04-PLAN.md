---
phase: 08-llm-entity-extraction
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - client/lib/types.ts
  - client/lib/mock-data.ts
  - client/hooks/useTauri.ts
autonomous: true
requirements:
  - LLME-01
  - LLME-05

must_haves:
  truths:
    - "TypeScript ExtractedEntity type mirrors the Rust ExtractedEntity extension: class, subclass, confidence added and optional"
    - "TypeScript ExtractedEntities container type carries entities, topic, tags, entitiesVersion, language"
    - "TypeScript Settings type carries extractionModel + useLlmExtraction"
    - "useExtractionSettings, useUpdateExtractionSettings, useTriggerEntityBackfill React Query hooks exist in useTauri.ts"
    - "mock-data provides mockExtractionSettings (extractionModel + useLlmExtraction defaults) and mockEntities includes class/subclass/confidence samples so browser mode continues to render"
    - "Browser mode (isTauri()=false) returns mock data through every new hook — never throws"
  artifacts:
    - path: "client/lib/types.ts"
      provides: "ExtractedEntity extension, ExtractedEntities container type, Settings extension, ExtractionSettings type, BackfillProgressEvent type extension for language field"
      contains: "class"
    - path: "client/hooks/useTauri.ts"
      provides: "useExtractionSettings + useUpdateExtractionSettings + useTriggerEntityBackfill hooks + query key extensions"
      contains: "useExtractionSettings"
    - path: "client/lib/mock-data.ts"
      provides: "mockExtractionSettings + upgraded mockEntities with class/subclass/confidence"
      contains: "mockExtractionSettings"
  key_links:
    - from: "client/lib/types.ts"
      to: "src-tauri/src/types.rs (Plan 01 SUMMARY)"
      via: "camelCase field parity"
      pattern: "extractionModel"
    - from: "client/hooks/useTauri.ts"
      to: "commands/settings.rs::get_settings + Plan 05 IPC commands"
      via: "tauriInvoke"
      pattern: "tauriInvoke"
---

<objective>
Extend the frontend type surface and hook layer to consume the Phase 8 backend shape. Frontend types must mirror the Rust structs from Plan 01 exactly, so any Phase 6 code that already reads `ExtractedEntity` continues to work while new UI (Plans 07/08) has the class/subclass/confidence/topic/tags fields available.

Purpose: Delivers the plumbing that Settings UI (Plan 07) and Document sidebar UI (Plan 08) need. Also updates mock-data so browser dev-mode (`bun dev` without Tauri) continues to render Phase 8 additions.

Output: Extended `client/lib/types.ts` (TS shapes for entities + settings), extended `client/lib/mock-data.ts` (mockExtractionSettings, upgraded mockEntities), new hooks in `client/hooks/useTauri.ts` (useExtractionSettings, useUpdateExtractionSettings, useTriggerEntityBackfill).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-llm-entity-extraction/08-CONTEXT.md
@.planning/phases/08-llm-entity-extraction/08-UI-SPEC.md
@client/lib/types.ts
@client/lib/mock-data.ts
@client/hooks/useTauri.ts

<interfaces>
Existing Settings type in `client/lib/types.ts` uses camelCase and must gain two new fields: `extractionModel: string` and `useLlmExtraction: boolean`.

Existing ExtractedEntity type in `client/lib/types.ts` uses camelCase and must gain three new optional fields: `class?: string`, `subclass?: string`, `confidence?: number`. Keep `entityType` for Phase 6 backward compat.

Existing hook pattern (from Phase 7):
```ts
export function useProviders() {
  return useQuery({
    queryKey: queryKeys.providers,
    queryFn: async () => {
      if (!isTauri()) return mockProviders;
      return await tauriInvoke<ProviderAuthStatus[]>("list_providers");
    },
  });
}
```

IPC commands to be delivered by Plan 05 (frontend can wire against these names without them existing yet — the query returns mock data in browser mode):
- `get_extraction_settings` → returns `ExtractionSettings { extractionModel: string, useLlmExtraction: boolean }`
- `set_extraction_settings` → takes `{ extractionModel: string, useLlmExtraction: boolean }` → returns `()`
- `trigger_entity_backfill` → takes `()` → returns `()` (fire-and-forget — progress arrives via existing `entity-backfill-progress` event)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend TypeScript types for ExtractedEntity, ExtractedEntities, Settings, ExtractionSettings</name>
  <files>client/lib/types.ts, client/lib/mock-data.ts</files>
  <behavior>
    - `ExtractedEntity` type accepts objects with just `label`, `value`, `entityType`, `canonicalId` (Phase 6 shape) AND objects with the full Phase 8 shape including `class`, `subclass`, `confidence` — all three new fields typed as `? optional`
    - `ExtractedEntities` container type has `entities: ExtractedEntity[]`, `topic: string | null`, `tags: string[]`, `entitiesVersion: number`, `language: string | null`
    - `Settings` type gains `extractionModel: string` and `useLlmExtraction: boolean` (non-optional in TS — mock-data + backend defaults guarantee they exist)
    - `ExtractionSettings` standalone type has `extractionModel: string`, `useLlmExtraction: boolean`
    - `mockExtractionSettings` constant in mock-data exports `{ extractionModel: "", useLlmExtraction: true }` (matches Rust default_settings())
    - `mockEntities` now includes at least 3 samples with `class`/`subclass`/`confidence` populated (e.g., one Person, one Organization, one Identifier with subclass="aadhaar")
    - `mockDocuments[0].extractedEntities` includes at least one entity with `confidence: 0.65` so the UI's "Also found" expander (Plan 08) has data to render in browser mode
    - Mock ExtractedEntities container: `mockDocuments[0]` gets a new sibling `mockDocumentEntitiesContainer` value that Plan 08 can consume for topic/tag rendering
  </behavior>
  <action>
    1. Open `client/lib/types.ts`. Locate `interface ExtractedEntity` and add three optional fields: `class?: string; subclass?: string; confidence?: number;` — placed at the end of the existing definition with a brief comment linking to CONTEXT.md D-08.
    2. Add a new exported interface `ExtractedEntities` with fields `entities: ExtractedEntity[]; topic: string | null; tags: string[]; entitiesVersion: number; language: string | null;` — comment noting it wraps Phase 8 doc-level metadata.
    3. Locate `interface Settings` and add `extractionModel: string; useLlmExtraction: boolean;` — non-optional; defaults come from backend.
    4. Add a standalone `interface ExtractionSettings { extractionModel: string; useLlmExtraction: boolean; }` — used by the useExtractionSettings query hook and the ExtractionSettings component in Plan 07.
    5. In `client/lib/mock-data.ts`:
       - Add `export const mockExtractionSettings: ExtractionSettings = { extractionModel: "", useLlmExtraction: true };`
       - Ensure the existing `defaultSettings` const (used by useSettings mock) has the two new fields populated identically.
       - Upgrade at least 3 entries in `mockEntities` to include `class`, `subclass` (for Identifier), and `confidence` (e.g., `{ ...existing person entity, class: "Person", confidence: 0.95 }`; add an Identifier entity `{ label: "Aadhaar", value: "1234 5678 9012", entityType: "identifier", class: "Identifier", subclass: "aadhaar", confidence: 0.92 }`)
       - Modify at least one entry in `mockDocuments[0].extractedEntities` to have `confidence: 0.65` so the "Also found" expander has content
       - Add `export const mockDocumentEntitiesContainer: ExtractedEntities = { entities: mockDocuments[0].extractedEntities, topic: "identity", tags: ["aadhaar", "personal_id"], entitiesVersion: 3.0, language: "en" };`
    6. Update the `defaultSettings` export used by `useSettings` mock to include the new fields.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/cortex && bunx tsc --noEmit 2>&1 | head -30 && bunx --bun vitest run client/hooks/useTauri.test.ts 2>&1 | tail -20 || true</automated>
  </verify>
  <done>types.ts declares ExtractedEntity extension + ExtractedEntities + ExtractionSettings + Settings extension; mock-data exports mockExtractionSettings + mockDocumentEntitiesContainer + upgraded mockEntities with class/subclass/confidence; tsc --noEmit passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add useExtractionSettings, useUpdateExtractionSettings, useTriggerEntityBackfill hooks</name>
  <files>client/hooks/useTauri.ts</files>
  <behavior>
    - `useExtractionSettings()` returns `{ data: mockExtractionSettings, isLoading: false }` in browser mode (isTauri()=false)
    - `useUpdateExtractionSettings()` mutation calls `tauriInvoke("set_extraction_settings", { settings: {...} })` and invalidates `queryKeys.extractionSettings` on success
    - `useTriggerEntityBackfill()` mutation calls `tauriInvoke("trigger_entity_backfill")` and does NOT invalidate anything (progress arrives via event stream)
    - Query key `queryKeys.extractionSettings` = `["extraction-settings"] as const`
    - All three hooks are exported and importable from `@/hooks/useTauri`
    - Existing hook tests continue to pass (no regressions in useSettings, useProviders, etc.)
  </behavior>
  <action>
    1. In `client/hooks/useTauri.ts`, extend the `queryKeys` factory near the top of the file with `extractionSettings: ["extraction-settings"] as const,`.
    2. Import `mockExtractionSettings` from `../lib/mock-data` and `ExtractionSettings` from `../lib/types` at the top of the file (add to existing imports).
    3. Add three new hooks near the bottom of the file (or grouped with the existing settings hooks):
       - `export function useExtractionSettings()` — useQuery, mirrors useSettings pattern; browser fallback returns `mockExtractionSettings`; Tauri path invokes `get_extraction_settings` returning `ExtractionSettings`
       - `export function useUpdateExtractionSettings()` — useMutation; mutationFn takes `settings: ExtractionSettings`, calls `tauriInvoke<void>("set_extraction_settings", { settings })`, on success calls `queryClient.invalidateQueries({ queryKey: queryKeys.extractionSettings })`
       - `export function useTriggerEntityBackfill()` — useMutation; mutationFn: `async () => { if (!isTauri()) return; await tauriInvoke<void>("trigger_entity_backfill"); }`; onError surfaces via caller (no automatic toast — Plan 07 wires the toast on the button)
    4. Ensure the file still compiles and any adjacent hook tests continue to pass.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/cortex && bunx tsc --noEmit 2>&1 | head -30 && bunx --bun vitest run --reporter=basic client/hooks/ client/lib/ 2>&1 | tail -30 || true</automated>
  </verify>
  <done>Three new hooks exported; queryKeys.extractionSettings added; browser fallback returns mockExtractionSettings; useUpdateExtractionSettings invalidates the query on success; useTriggerEntityBackfill fires and returns void; tsc --noEmit clean; existing hook tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tauriInvoke → frontend | Rust response crosses into React state via useQuery — trusted (own backend) but must handle absent-provider empty responses gracefully |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-13 | Denial of Service | Browser-mode (isTauri()=false) crashes when a hook is invoked | mitigate | Every new hook checks `isTauri()` and returns mock data; matches Phase 4 pattern |
| T-08-14 | Tampering | ExtractionSettings validation on the wire | accept | Rust command handler validates model IDs at the boundary (Plan 05); TS is not the security boundary |
</threat_model>

<verification>
- `bunx tsc --noEmit` shows no new errors
- `grep -c "useExtractionSettings\|useUpdateExtractionSettings\|useTriggerEntityBackfill" client/hooks/useTauri.ts` ≥ 3 (one export each)
- `grep "mockExtractionSettings" client/lib/mock-data.ts` returns at least one match (the export)
- `grep "confidence:" client/lib/mock-data.ts` returns at least one match (the < 0.7 sample for "Also found")
</verification>

<success_criteria>
1. `ExtractedEntity` TS interface extended with optional class/subclass/confidence
2. `ExtractedEntities` container interface exists
3. `Settings` TS interface has extractionModel + useLlmExtraction
4. `ExtractionSettings` standalone interface exists
5. `mockExtractionSettings` and upgraded `mockEntities` + `mockDocumentEntitiesContainer` exported
6. Three new hooks exist and are exported from useTauri.ts
7. `queryKeys.extractionSettings` added
8. Browser fallback path returns mock data for every new hook
9. tsc --noEmit clean, existing hook tests still pass
</success_criteria>

<output>
Create `.planning/phases/08-llm-entity-extraction/08-04-SUMMARY.md` when done. Include the final TS type shapes (as code blocks), the new queryKey entry, and screenshots-not-needed disclosure.
</output>

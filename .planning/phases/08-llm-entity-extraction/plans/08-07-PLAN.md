---
phase: 08-llm-entity-extraction
plan: 07
type: execute
wave: 4
depends_on:
  - 08-04
files_modified:
  - client/components/ai/ExtractionSettings.tsx
  - client/components/ai/ExtractionSettings.test.tsx
  - client/pages/SettingsPage.tsx
  - client/components/layout/BackfillIndicator.tsx
  - client/components/layout/AiNoProviderBanner.tsx
autonomous: false
requirements:
  - LLME-01
  - LLME-05

must_haves:
  truths:
    - "Settings → AI tab renders an ExtractionSettings section below AiProvidersSection with three controls: model dropdown, Use-LLM toggle, Re-extract button"
    - "Model dropdown values match D-11 defaults per active provider (Haiku 4.5 / gpt-5-mini / gemini-2.5-flash) and Sonnet/gpt-5/gemini-pro alternatives per UI-SPEC §Extraction Model Dropdown"
    - "Re-extract button is the section's primary focal point (btn-primary accent fill); dropdown + toggle are neutral chrome per UI-SPEC §Visual Hierarchy"
    - "Re-extract button disabled state per UI-SPEC §3 state matrix (no provider / toggle off / backfill in flight)"
    - "Re-extract tooltip shows 'Est: $X.XX across N docs on {model}' when hovered and enabled — provider-with-pricing case; 'Est: free (local model) across N docs' for Ollama; 'No documents to re-extract' when count is zero"
    - "BackfillIndicator tooltip shows Pass-1-only vs two-pass variant per UI-SPEC §4 table"
    - "AiNoProviderBanner sub-copy extended per D-34: 'Connect AI to extract people, organizations, and topic tags from your docs. Dates, amounts, and IDs work without AI.'"
    - "Backfill completion with fallbacks > 0 fires a sonner toast per D-29: 'Backfill complete. {X} of {Y} docs used pattern extraction only. Retry after network is healthy.' (warning variant, 8s duration)"
    - "Provider switch fires info toast per D-30: 'Provider switched. New extractions use {model}. Run \\'Re-extract entities\\' for consistent labels across all docs.' (info variant, 6s duration)"
  artifacts:
    - path: "client/components/ai/ExtractionSettings.tsx"
      provides: "React component with model Select + LLM Switch + Re-extract Button + tooltip + cost estimate"
      contains: "ExtractionSettings"
      min_lines: 150
    - path: "client/components/ai/ExtractionSettings.test.tsx"
      provides: "vitest tests covering disabled states, tooltip content, button click invokes trigger, provider-switch toast"
      contains: "ExtractionSettings"
      min_lines: 100
    - path: "client/pages/SettingsPage.tsx"
      provides: "ExtractionSettings mounted below AiProvidersSection inside AI & Models tab"
      contains: "ExtractionSettings"
    - path: "client/components/layout/BackfillIndicator.tsx"
      provides: "Updated tooltip copy variants for Pass-1-only + two-pass modes; completion-with-fallbacks toast"
      contains: "Pass 1"
    - path: "client/components/layout/AiNoProviderBanner.tsx"
      provides: "Sub-copy paragraph appended per D-34"
      contains: "Dates, amounts, and IDs"
  key_links:
    - from: "client/components/ai/ExtractionSettings.tsx"
      to: "client/hooks/useTauri.ts (useExtractionSettings, useUpdateExtractionSettings, useTriggerEntityBackfill, useProviders)"
      via: "React Query hooks"
      pattern: "useExtractionSettings"
    - from: "client/pages/SettingsPage.tsx"
      to: "client/components/ai/ExtractionSettings.tsx"
      via: "component composition inside AI tab"
      pattern: "ExtractionSettings"
---

<objective>
Ship the Settings → AI extraction controls end-to-end: the ExtractionSettings React component (model dropdown + LLM toggle + Re-extract button + cost tooltip), the BackfillIndicator copy updates for two-pass mode, the AiNoProviderBanner sub-copy extension, and the provider-switch info toast wiring.

Purpose: Delivers LLME-05 (backfill triggered from Settings; TopBar progress visible) end-to-end from a user's perspective. Every locked D-decision in the Settings UI cluster (D-22, D-29, D-30, D-33, D-34) becomes user-visible here.

Output: New ExtractionSettings component + tests; SettingsPage AI tab mounts it; BackfillIndicator tooltip variants; banner copy; toast wiring on provider change + backfill completion. Ends with a checkpoint:human-verify for the full click-through flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-llm-entity-extraction/08-CONTEXT.md
@.planning/phases/08-llm-entity-extraction/08-UI-SPEC.md
@.planning/phases/08-llm-entity-extraction/08-04-SUMMARY.md
@client/pages/SettingsPage.tsx
@client/components/ai/AiProvidersSection.tsx
@client/components/layout/BackfillIndicator.tsx
@client/components/layout/AiNoProviderBanner.tsx
@client/lib/mock-data.ts

<interfaces>
Hooks from Plan 04:
```ts
useExtractionSettings(): { data?: ExtractionSettings, isLoading: boolean };
useUpdateExtractionSettings(): { mutate: (settings: ExtractionSettings) => void, isPending: boolean };
useTriggerEntityBackfill(): { mutate: () => void, isPending: boolean };
useProviders(): { data?: ProviderAuthStatus[], isLoading: boolean };
useActiveProvider(): { data?: string | null };  // if exists from Phase 7; otherwise use useProviders + filter
useStats(): { data?: Stats };  // for doc count in tooltip
```

Model list per active provider (from UI-SPEC §Extraction Model Dropdown):
- Anthropic: `claude-haiku-4-5-20251001` ("Claude Haiku 4.5"), `claude-sonnet-4-5` ("Claude Sonnet 4.5")
- OpenAI-Codex: `gpt-5-mini` ("GPT-5 mini"), `gpt-5` ("GPT-5")
- Gemini: `gemini-2.5-flash` ("Gemini 2.5 Flash"), `gemini-2.5-pro` ("Gemini 2.5 Pro")
- Ollama: user's connected model shown verbatim (from useProviders() response — provider.model or similar field); no hardcoded list
- No provider: Select disabled, placeholder "Connect a provider first"

Model pricing per RESEARCH.md Pattern 12 (bake constants in a helper — assume USD per 1M input tokens):
```
claude-haiku-4-5-20251001: 0.80
claude-sonnet-4-5: 3.00
gpt-5-mini: 0.40
gpt-5: 5.00
gemini-2.5-flash: 0.075
gemini-2.5-pro: 1.25
```

Avg input tokens per doc: assume 2000 for tooltip estimate (documented in RESEARCH.md is not present; use 2000 as a stated assumption and put it in a `const AVG_INPUT_TOKENS_PER_DOC = 2000` at the top of the file — user can adjust in future).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build ExtractionSettings component + tests</name>
  <files>client/components/ai/ExtractionSettings.tsx, client/components/ai/ExtractionSettings.test.tsx</files>
  <behavior>
    - Renders section heading "Entity Extraction" (`text-lg font-semibold`) + description "Choose which model Cortex uses to extract people, organizations, topics, and tags from your documents. Faster models reduce cost; use a capable model for best accuracy."
    - Row 1: `Label "Extraction model" (text-sm font-semibold text-text-primary)` + `Select` (w-[240px]) — dropdown options driven by active provider per the model list above
    - Row 2: `Label "Use LLM for entity extraction" (text-sm font-semibold)` + `Switch`; caption below `text-xs text-text-tertiary "When off, only dates, amounts, emails, phone numbers, and IDs are extracted. People, organizations, and topic tags require AI."`
    - Row 3: left side description `text-xs text-text-tertiary "Re-extracts entities across all indexed documents using the selected model."` + right side `Button` variant "primary" (accent fill) with label "Re-extract entities" — this is the section's focal point per UI-SPEC §Visual Hierarchy
    - `Button` disabled states per UI-SPEC state matrix: (no provider → disabled), (provider + toggle off → disabled), (provider + toggle on + backfill in flight → disabled with spinner + "Re-extracting..."), (provider + toggle on + idle → enabled)
    - Tooltip on the button (only when enabled): "Est: $X.XX across N docs on {model}" for priced providers; "Est: free (local model) across N docs" for Ollama; "No documents to re-extract" when doc count is 0. Uses shadcn Tooltip + TooltipContent.
    - Cost estimate helper: `estimateCost(model, docCount) → { text: string }` — inlined const table for prices; returns "$X.XX" formatted with 2 decimals; when model has no pricing entry returns free-Ollama variant; when docCount === 0 returns the "No documents" variant
    - Model change triggers `useUpdateExtractionSettings().mutate({ extractionModel: newModel, useLlmExtraction: currentToggle })` and shows sonner info toast per D-30: `toast.info("Provider switched. New extractions use {modelDisplayName}. Run 'Re-extract entities' for consistent labels across all docs.", { duration: 6000 })` — actually per D-30 the trigger is provider switch, not model switch; but per UI-SPEC §Copywriting the toast fires on `Trigger: User changes active provider` — read the copy carefully: this toast is fired by the AiProvidersSection component when the user picks a different provider, NOT by ExtractionSettings when they pick a different model within a provider. Keep this task-scoped: ExtractionSettings does NOT fire the provider-switch toast; instead it fires a similar model-switch toast only when the model changes AND indexed doc count > 0 (using an approximation): `toast.info("Extraction model set to {modelDisplayName}. Run 'Re-extract entities' to relabel existing documents.", { duration: 5000 })`. Document this distinction in SUMMARY. The provider-switch toast lives in AiProvidersSection (owned by Phase 7 — extend in Task 3 below).
    - Toggle change triggers `useUpdateExtractionSettings().mutate({...})` — no toast (just persistence)
    - Re-extract click → `useTriggerEntityBackfill().mutate()` → on success: shows sonner success toast "Backfill started. Progress appears in the top bar."; on error: shows sonner error toast "Failed to start backfill. {error.message}"
    - Empty state: when no provider is connected AND toggle would be disabled AND button would be disabled, the caption reads (below the row): `text-xs text-text-tertiary "Connect a provider in the AI Providers section above to enable LLM entity extraction."`
    - Tests (vitest + @testing-library/react + msw-style mocks for hooks):
      - Renders section heading + description
      - Model dropdown shows Anthropic options when active provider is "anthropic"
      - Model dropdown disabled when active provider is null
      - Re-extract disabled when useLlmExtraction=false
      - Re-extract disabled when backfill is in-flight (mock useBackfillStore to return isRunning=true)
      - Re-extract click invokes useTriggerEntityBackfill.mutate()
      - Cost tooltip shows correct string for Haiku with 100 docs → `"Est: $0.16 across 100 docs on Claude Haiku 4.5"` (0.80/M × 2000 avg × 100 = 160000 tokens / 1M × $0.80 = $0.16)
      - Cost tooltip shows Ollama free variant when provider="ollama"
      - Model change fires model-switch toast when indexed doc count > 0
  </behavior>
  <action>
    Per D-11, D-22, D-30, D-32, D-33 and UI-SPEC §Control Specifications 1-3:
    1. Create `client/components/ai/ExtractionSettings.tsx`. Import shadcn `Select`, `SelectContent`, `SelectItem`, `SelectTrigger`, `SelectValue`, `Switch`, `Button`, `Label`, `Tooltip`, `TooltipContent`, `TooltipTrigger`; import `Loader2` from lucide; import hooks (useExtractionSettings, useUpdateExtractionSettings, useTriggerEntityBackfill, useProviders, useStats).
    2. Component body:
       - Read `{ data: settings }` from useExtractionSettings; local state `pendingModel` and `pendingToggle` mirror settings; sync via useEffect when settings load.
       - Read `{ data: providers }` from useProviders; derive `activeProvider = providers?.find(p => p.isActive)?.name ?? null`.
       - Read `{ data: stats }` from useStats; derive `docCount = stats?.totalDocuments ?? 0`.
       - Compute model options based on activeProvider; if null → empty list + placeholder "Connect a provider first".
       - Compute `disabled: { model: !activeProvider, toggle: !activeProvider, button: !activeProvider || !pendingToggle || isBackfillRunning }`.
       - Read backfill status from `useBackfillStore` (already exists from Phase 7 — Sidebar/TopBar use it); derive `isBackfillRunning`.
       - Render: `<div className="border-t border-border-primary pt-6 mt-6"><h3 className="text-lg font-semibold mb-1">Entity Extraction</h3><p className="text-xs text-text-tertiary mb-6">...</p>{modelRow}{toggleRow}{buttonRow}{emptyCaption}</div>`. Use `flex items-center justify-between py-4` per UI-SPEC §Control Spec 1-3.
       - Cost estimate: inline `const AVG_INPUT_TOKENS_PER_DOC = 2000; const MODEL_PRICING = { "claude-haiku-4-5-20251001": 0.80, ... };` per RESEARCH.md Pattern 12; a helper `estimateCost(model, docCount, provider) → tooltipText`.
    3. Create `client/components/ai/ExtractionSettings.test.tsx` with the tests listed under Behavior. Use `queryClient` wrapper pattern matching existing `ProviderCard.test.tsx`. Mock the hooks via `vi.mock('@/hooks/useTauri', () => ({ ... }))`.
    4. Ensure the file uses only shadcn primitives already installed (Select, Switch, Button, Tooltip, Label — all confirmed present in UI-SPEC §Registry Safety). No new registry installs.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/cortex && bunx --bun vitest run client/components/ai/ExtractionSettings.test.tsx 2>&1 | tail -30 && bunx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>ExtractionSettings.tsx + ExtractionSettings.test.tsx exist and all tests pass; tsc --noEmit clean; component uses only shadcn primitives; cost estimate helper inlined with pricing constants.</done>
</task>

<task type="auto">
  <name>Task 2: Mount ExtractionSettings in SettingsPage + extend BackfillIndicator + AiNoProviderBanner + wire completion toast + provider-switch toast</name>
  <files>client/pages/SettingsPage.tsx, client/components/layout/BackfillIndicator.tsx, client/components/layout/AiNoProviderBanner.tsx, client/components/ai/AiProvidersSection.tsx</files>
  <action>
    Per UI-SPEC §BackfillIndicator + §Copywriting + D-30:
    1. In `client/pages/SettingsPage.tsx`, import `ExtractionSettings` from `@/components/ai/ExtractionSettings` and render it inside the AI & Models tab body, immediately after `<AiProvidersSection />`. Wrap in a container that matches the existing tab-content spacing.
    2. In `client/components/layout/BackfillIndicator.tsx`:
       - Extend the chip label + tooltip strings per UI-SPEC §4 BackfillIndicator table:
         - Detect Pass-1-only mode: when `progress.eta_seconds` is null AND `progress.status === "running"` AND (fallbacks would be non-null? — actually two-pass mode has ETA; pass1-only mode has no LLM latency so ETA is None). Use `progress.eta_seconds != null && progress.eta_seconds > 0` as the two-pass indicator.
         - Two-pass running: chip "Extracting entities X/Y", tooltip line 1 "Two-pass entity extraction", tooltip line 2 "X of Y docs — Pass 1 complete, Pass 2 in progress (ETA Zs)"
         - Pass-1-only running: chip "Extracting entities X/Y", tooltip line 1 "Pattern extraction (Pass 1)", tooltip line 2 "X of Y docs — AI unavailable, extracting dates/amounts/IDs only"
         - Complete: chip "Done extracting entities" (existing auto-dismiss behavior)
         - Error: chip "Extraction failed" + tooltip line 1 "Entity extraction failed" + line 2 = error message
       - Do NOT change any existing spacing (existing `px-2.5` is preserved per UI-SPEC §Spacing exception).
       - When progress.status transitions to "complete" AND progress.fallbacks > 0, fire the sonner warning toast per D-29:
         ```
         toast.warning(`Backfill complete. ${progress.fallbacks} of ${progress.total} docs used pattern extraction only. Retry after network is healthy.`, { duration: 8000 });
         ```
         When fallbacks == 0, do NOT fire the toast (silent success per UI-SPEC §Backfill Completion Toast Case A).
    3. In `client/components/layout/AiNoProviderBanner.tsx`:
       - Add a second `<p>` below the existing copy paragraph with the D-34 sub-copy:
         `<p className="text-xs text-text-tertiary mt-1">Connect AI to extract people, organizations, and topic tags from your docs. Dates, amounts, and IDs work without AI.</p>`
       - Keep the existing dismiss button + 44px touch target.
    4. In `client/components/ai/AiProvidersSection.tsx` (Phase 7 component — Plan 08 extends its behavior slightly):
       - Detect when the active provider changes via a `useEffect(() => { if (previous !== null && previous !== active) toast.info(...) }, [active])` pattern; provider display name lookup driven by activeProvider (map "anthropic" → "Claude Haiku 4.5" using the default model per D-11, or the current settings.extractionModel display name if the user has set one).
       - Toast copy per D-30: `toast.info(`Provider switched. New extractions use ${modelDisplayName}. Run 'Re-extract entities' for consistent labels across all docs.`, { duration: 6000 })`.
       - Do not fire on initial mount (previous starts as `undefined`, so the `previous !== null` guard suffices — use `useRef` to hold previous instead of state to avoid re-renders).
    5. Add a small vitest for the completion-with-fallbacks toast: use `sonner`'s test harness or spy on `toast.warning` via `vi.spyOn`, fire a mock progress event with `status="complete"`, `fallbacks=3`, `total=10`, assert `toast.warning` called with the exact string.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/cortex && bunx tsc --noEmit 2>&1 | head -20 && bunx --bun vitest run client/components/layout/BackfillIndicator client/components/ai 2>&1 | tail -30</automated>
  </verify>
  <done>SettingsPage renders ExtractionSettings inside AI tab; BackfillIndicator has two-pass vs Pass-1-only tooltip variants; completion-with-fallbacks fires sonner warning toast per D-29; AiNoProviderBanner has D-34 sub-copy; AiProvidersSection fires D-30 info toast on provider change (not on initial mount); tsc --noEmit clean; new tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
The Settings → AI Extraction Settings section is now live. Every user-facing D-decision in the Settings cluster is implemented:
- D-11 model defaults per provider
- D-22 Re-extract button with cost estimate tooltip
- D-29 backfill completion sonner toast when fallbacks > 0
- D-30 provider-switch info toast (on AiProvidersSection)
- D-33 Use-LLM toggle (default on when provider connected)
- D-34 AiNoProviderBanner sub-copy extension
- UI-SPEC §4 BackfillIndicator tooltip variants (two-pass vs Pass-1-only)
  </what-built>
  <how-to-verify>
1. Run `bun dev` OR `cargo tauri dev`, open the app
2. Navigate to Settings → AI & Models tab
3. Verify: below the existing AI Providers cards, a new "Entity Extraction" section appears with section heading + description, then three rows (dropdown, toggle, button)
4. If a provider is connected (from Phase 7): confirm the model dropdown shows the expected D-11 default for that provider (Haiku 4.5 for Anthropic, gpt-5-mini for OpenAI, gemini-2.5-flash for Gemini, or user-entered model for Ollama)
5. Toggle "Use LLM for entity extraction" off — Re-extract button becomes disabled (opacity-50) and dropdown stays enabled
6. Toggle back on — Re-extract button becomes enabled (btn-primary accent fill)
7. Hover Re-extract button → tooltip shows "Est: $X.XX across N docs on {model}" (for priced providers) OR "Est: free (local model) across N docs" for Ollama OR "No documents to re-extract" when the corpus is empty
8. Click Re-extract → sonner toast "Backfill started. Progress appears in the top bar." appears
9. TopBar `BackfillIndicator` chip appears with "Extracting entities X/Y"; hover to see tooltip variant (two-pass or Pass-1-only depending on provider state)
10. Let backfill complete OR simulate a partial fallback by disconnecting the provider mid-backfill (Settings → AI → Disconnect a card). After completion:
    - Case A (all Pass 2 succeeded): TopBar chip auto-dismisses, no toast
    - Case B (some fell back): sonner warning toast "Backfill complete. {X} of {Y} docs used pattern extraction only. Retry after network is healthy."
11. Change the active provider in AI Providers → sonner info toast "Provider switched. New extractions use {model}. Run 'Re-extract entities' for consistent labels across all docs."
12. Disconnect ALL providers → AiNoProviderBanner should appear at the top of AppShell (Phase 7 behavior); confirm it now has the second paragraph "Connect AI to extract people, organizations, and topic tags from your docs. Dates, amounts, and IDs work without AI."
13. Reconnect any provider → banner dismisses on session; ExtractionSettings section becomes fully enabled again
  </how-to-verify>
  <resume-signal>Type "approved" if all 13 steps pass; describe any deviation from the copy in UI-SPEC §Copywriting or any incorrect disabled state to file corrective edits before Plan 08 runs.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click Re-extract → backend | Frontend fires trigger_entity_backfill IPC; backend Plan 06 owns the actual backfill |
| tooltip cost estimate | Static pricing constants — become stale if provider changes prices |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-21 | Information Disclosure | Cost estimate misleading user into thinking a run is cheaper/more expensive than actual | accept | Static tooltip constants clearly labeled "Est:"; user makes an informed guess. Live cost tracking deferred (v1.2) |
| T-08-22 | Denial of Service | Rapid Re-extract clicks | mitigate | Button disables while isBackfillRunning is true (from useBackfillStore); backend Plan 06 mitigation covers residual double-spawn |
| T-08-23 | Tampering | Model dropdown accepts a model ID the provider doesn't recognize | accept | Provider rejects at HTTP layer; frontend surfaces the error via useTriggerEntityBackfill error path (sonner toast) |
</threat_model>

<verification>
- `bunx tsc --noEmit` clean
- `bunx --bun vitest run client/components/ai/ExtractionSettings client/components/layout/BackfillIndicator client/components/layout/AiNoProviderBanner` — all suites green
- `grep "Est:" client/components/ai/ExtractionSettings.tsx` shows the tooltip string template
- `grep "Connect AI to extract" client/components/layout/AiNoProviderBanner.tsx` finds the D-34 sub-copy
- `grep "Two-pass entity extraction\|Pattern extraction (Pass 1)" client/components/layout/BackfillIndicator.tsx | wc -l` ≥ 2
- Human checkpoint approved (13-step walk-through)
</verification>

<success_criteria>
1. ExtractionSettings component + tests exist and pass
2. SettingsPage AI tab mounts ExtractionSettings below AiProvidersSection
3. BackfillIndicator has two-pass vs Pass-1-only tooltip variants
4. BackfillIndicator fires sonner warning toast on completion when fallbacks > 0 (D-29)
5. AiNoProviderBanner has D-34 sub-copy
6. AiProvidersSection fires sonner info toast on provider switch (D-30) — not on initial mount
7. Cost estimate tooltip populates for priced providers, shows free variant for Ollama
8. Human checkpoint approved
</success_criteria>

<output>
Create `.planning/phases/08-llm-entity-extraction/08-07-SUMMARY.md` when done. Include: final ExtractionSettings.tsx file, screenshots of each of the 13 verification steps (or a narrative if screenshots impractical), model-switch vs provider-switch toast decision rationale, and any deviation from UI-SPEC copy.
</output>

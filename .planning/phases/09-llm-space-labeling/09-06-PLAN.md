---
phase: 09-llm-space-labeling
plan: 06
type: execute
wave: 4
depends_on: [09-04, 09-05]
files_modified:
  - client/components/spaces/SpaceCard.tsx
  - client/components/spaces/EntityHintChip.tsx
  - client/components/spaces/SpaceLabelingIndicator.tsx
  - client/pages/SpacesPage.tsx
  - client/components/AppShell.tsx
  - client/lib/format.ts
autonomous: true
requirements: [LLML-02, LLML-05]

must_haves:
  truths:
    - "SpaceCard is a standalone module at client/components/spaces/SpaceCard.tsx (extracted from SpacesPage inline definition)"
    - "SpaceCard shows a Skeleton shimmer + 'Generating label…' when space.labelStatus === 'generating' (D-14, LLML-05)"
    - "SpaceCard wraps in a Radix Tooltip that shows the description (truncated to 100 chars) when space.description is non-empty (D-16, LLML-02)"
    - "SpaceCard shows a Lock icon in the header row when space.userLocked === true (D-15)"
    - "SpaceCard shows an EntityHintChip below sample files when space.canonicalEntityHint is non-null AND labelStatus !== 'generating' (D-17)"
    - "EntityHintChip uses outline-only styling (bg-transparent, border-border-secondary, rounded-full) — visually distinct from Phase 8 topic/tag chips"
    - "SpacesPage mounts SpaceLabelingIndicator in the header when useSpaceLabelingStore.isActive is true"
    - "AppShell mounts useSpaceLabelingProgress once at the top level (mirrors useBackfillProgress mounting)"
    - "SpacesPage.tsx no longer defines SpaceCard inline"
  artifacts:
    - path: "client/components/spaces/SpaceCard.tsx"
      provides: "Named export SpaceCard with all Phase 9 states (shimmer, tooltip, lock, entity hint)"
      exports: ["SpaceCard"]
      min_lines: 100
    - path: "client/components/spaces/EntityHintChip.tsx"
      provides: "EntityHintChip component with entity-class icon map"
      exports: ["EntityHintChip"]
      min_lines: 40
    - path: "client/components/spaces/SpaceLabelingIndicator.tsx"
      provides: "Progress chip with labeling/complete/error visual states"
      exports: ["SpaceLabelingIndicator"]
      min_lines: 50
    - path: "client/pages/SpacesPage.tsx"
      provides: "Uses SpaceCard from new file; mounts SpaceLabelingIndicator"
      contains: "SpaceLabelingIndicator"
    - path: "client/components/AppShell.tsx"
      provides: "useSpaceLabelingProgress() mounted once"
      contains: "useSpaceLabelingProgress"
  key_links:
    - from: "SpaceCard"
      to: "space.labelStatus"
      via: "shimmer render branch"
      pattern: "labelStatus.*generating"
    - from: "SpaceCard"
      to: "shadcn Tooltip"
      via: "TooltipTrigger asChild + Link"
      pattern: "TooltipTrigger"
    - from: "SpacesPage"
      to: "useSpaceLabelingStore"
      via: "conditional SpaceLabelingIndicator render"
      pattern: "useSpaceLabelingStore"
---

<objective>
Extract `SpaceCard` from `SpacesPage.tsx` into `client/components/spaces/SpaceCard.tsx` and extend it with all Phase 9 UI states per 09-UI-SPEC.md §§1-4: shimmer state (D-14), description tooltip (D-16), lock icon (D-15), canonical entity hint chip (D-17). Add `SpaceLabelingIndicator` for batch progress (D-14 / §8) and mount `useSpaceLabelingProgress` at AppShell.

Purpose: Delivers LLML-02 (description tooltip) and LLML-05 (background-navigable app with placeholder state). Also lands the first-ever component under `client/components/spaces/` — a directory Phase 9 creates and Phase 10+ will populate further.

Output: New `client/components/spaces/` directory with three components (`SpaceCard`, `EntityHintChip`, `SpaceLabelingIndicator`), updated `SpacesPage.tsx` importing from that directory, `AppShell.tsx` with a single `useSpaceLabelingProgress()` call.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-llm-space-labeling/09-UI-SPEC.md
@.planning/phases/09-llm-space-labeling/09-CONTEXT.md
@client/pages/SpacesPage.tsx
@client/components/AppShell.tsx
@client/lib/types.ts
@client/lib/icons.ts
@client/components/ui/skeleton.tsx
@client/components/ui/tooltip.tsx

<interfaces>
Current inline SpaceCard lives in client/pages/SpacesPage.tsx lines 41-77 (the extraction source).

Entity class icon map per 09-UI-SPEC.md §4 (must be identical to Phase 8's EntityChip map):
Person → Users, text-purple-400
Organization → Building2, text-amber-400
Location → MapPin, text-red-400
Date → Calendar, text-blue-400
Amount → DollarSign, text-green-400
Email → Mail, text-cyan-400
Phone → Phone, text-teal-400
Identifier → Fingerprint, text-orange-400
(default) → Fingerprint, text-orange-400

shadcn primitives (all pre-installed per 09-UI-SPEC.md §Registry Safety): Skeleton, Tooltip + TooltipProvider + TooltipTrigger + TooltipContent.

`formatRelativeTime` is currently inline in SpacesPage.tsx lines 13-21 (and duplicated in SpaceDetailPage.tsx lines 10-18). Extract into `client/lib/format.ts` so both SpaceCard and Plan 07's SpaceDetailPage can import it. If a `format.ts` already exists, add the export alongside existing helpers — do not overwrite.

Plan 05 hooks/store to consume:
- `useSpaceLabelingProgress()` — mounts the Tauri event listener; call once in AppShell
- `useSpaceLabelingStore()` — Zustand store with { isActive, processed, total, status, lastError }
- `useSpaceLabels()` — read cache (Plan 07 uses)
- `useRelabelSpace()`, `useRenameSpace()`, `useClearSpaceOverride()` — mutations (Plan 07 uses; not needed here)

Grid-aligned spacing (09-UI-SPEC.md §Spacing Revision Notes): px-2 py-1 (chip), px-3 py-1 (indicator), gap-2 (icon-to-text). Do NOT use px-2.5 or py-0.5 or gap-1.5.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract SpaceCard + create EntityHintChip + format helper</name>
  <files>client/components/spaces/SpaceCard.tsx, client/components/spaces/EntityHintChip.tsx, client/lib/format.ts</files>
  <action>
Create the `client/components/spaces/` directory (parent exists per Phase 4).

Create `client/lib/format.ts` if missing, or extend if present, adding an exported `formatRelativeTime(iso: string): string` (verbatim body from SpacesPage.tsx lines 13-21). Update SpacesPage.tsx to import from `../lib/format` in Task 3 — don't do that here.

Create `client/components/spaces/EntityHintChip.tsx` per 09-UI-SPEC.md §4. Component props: `{ hint: string }`. Split `hint` on the first `": "` to derive `className` + `value`. Look up an ICON_MAP keyed by lowercase className. Default to Fingerprint / text-orange-400 for unmapped classes. Render an inline-flex `<div role="note" aria-label={"Space organized around " + hint}>` with:
- `bg-transparent border border-border-secondary rounded-full`
- `px-2 py-1` (grid-aligned per §Spacing Revision Notes — NOT `py-0.5`)
- `max-w-[160px]`
- Icon at size 10, strokeWidth 1.5, color per map
- `<span className="text-xs text-text-tertiary truncate">{hint}</span>`

Component is a non-interactive `<div>` — NOT a `<button>` or `<Link>` (per §4 "EntityHintChip is non-interactive"). Phase 11 may upgrade later.

Create `client/components/spaces/SpaceCard.tsx`. Start from the inline definition in SpacesPage.tsx and add all Phase 9 states. Requirements:

1. Named export `export function SpaceCard({ space }: { space: Space })` — no default export (matches CLAUDE.md convention).

2. Local helper `truncateAt100(text: string): string` returning `text.length > 100 ? text.slice(0, 100) + "…" : text`. JavaScript truncation, NOT CSS `truncate` class (per §2).

3. Wrap the outer `<Link>` in `<TooltipProvider><Tooltip><TooltipTrigger asChild>{link}</TooltipTrigger>{space.description ? <TooltipContent side="bottom" sideOffset={4} className="max-w-[240px] text-xs leading-relaxed">{truncateAt100(space.description)}</TooltipContent> : null}</Tooltip></TooltipProvider>` per §2.

4. Shimmer branch — when `space.labelStatus === "generating"`, replace the label + count text with:
   - `<Skeleton className="h-5 w-36" />` (label region)
   - `<Skeleton className="h-4 w-24" />` (count region)
   - `<span className="text-xs text-text-tertiary mt-1">Generating label…</span>`
   Preserve the sample files list, the icon, and the header row structure. Do NOT render EntityHintChip. TooltipContent's conditional render already guards on `space.description`.

5. Ready branch — when labelStatus is anything other than 'generating' (including undefined per 09-UI-SPEC.md §Data Type Extensions backwards compatibility), render label + count as prose (unchanged from inline behavior).

6. Header row extension: wrap the timestamp span in `<div className="flex items-center gap-1">` and render `<Lock size={12} className="text-text-tertiary" aria-label="Label locked by user" />` before the timestamp when `space.userLocked === true`. Per §3.

7. Entity hint chip: inside the outer `space-y-3` container, below the sample files div, render `{space.canonicalEntityHint && space.labelStatus !== "generating" && <div className="pt-1"><EntityHintChip hint={space.canonicalEntityHint} /></div>}` per §4.

8. Imports: `Space` from `@/lib/types`, `formatRelativeTime` from `@/lib/format`, `resolveIcon` from `@/lib/icons`, `EntityHintChip` from `./EntityHintChip`, `Skeleton` from `@/components/ui/skeleton`, `Tooltip`/`TooltipContent`/`TooltipProvider`/`TooltipTrigger` from `@/components/ui/tooltip`, `Lock`/`FileText` from `lucide-react`, `Link` from `react-router-dom`.

9. Preserve all existing behavior (border-l-4 with space.color, card padding, hover states).

Include a co-located test file `client/components/spaces/SpaceCard.test.tsx` covering the SpaceCard state matrix from 09-UI-SPEC.md §Interaction States (four rows). Use `@testing-library/react` + `vitest` (mirror any existing `.test.tsx` in client/components — grep for `render(` in `client/**/*.test.tsx`). Wrap tests in a `<MemoryRouter>` (react-router requirement for `<Link>`). Assertions:
- `labelStatus: 'generating'` → `Generating label…` text visible, Lock icon absent, EntityHintChip absent, TooltipContent not rendered.
- `userLocked: true` → Lock icon visible with aria-label `"Label locked by user"`.
- `canonicalEntityHint: "Person: Alex Doe"` → EntityHintChip renders `"Person: Alex Doe"` text.
- `description: "..."` → hovering the card (via `userEvent.hover`) reveals a TooltipContent element containing the (possibly-truncated) description.
  </action>
  <verify>
    <automated>bunx tsc --noEmit -p client/tsconfig.json 2>&1 | head -5 ; bun test client/components/spaces/SpaceCard.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <done>SpaceCard extracted with 4 state branches and unit tests passing; EntityHintChip renders per §4 spec with grid-aligned padding; formatRelativeTime lives in client/lib/format.ts; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: SpaceLabelingIndicator + AppShell mount</name>
  <files>client/components/spaces/SpaceLabelingIndicator.tsx, client/components/AppShell.tsx</files>
  <action>
Create `client/components/spaces/SpaceLabelingIndicator.tsx` per 09-UI-SPEC.md §8. Named export `SpaceLabelingIndicator`. Consumes `useSpaceLabelingStore()`.

Return `null` when `status === "idle"`.

For `status === "labeling"`: render
`<div className="flex items-center gap-2 px-3 py-1 rounded-full bg-accent-primary/10 text-accent-primary text-xs font-medium"><Loader2 size={12} className="animate-spin" strokeWidth={1.5} />Labeling spaces {processed}/{total}</div>`

For `status === "complete"`: render the same shape with `bg-success/10 text-success` (no spinner, text `"Labels ready"`), and auto-dismiss after 3s using a local `useEffect` that calls `useSpaceLabelingStore.getState().clear()` after `setTimeout(clear, 3000)`. Clean up the timeout on unmount.

For `status === "error"`: render `bg-error/10 text-error`, text `"Labeling failed"`, no auto-dismiss.

Grid-aligned spacing (§Spacing Revision Notes): `px-3 py-1` and `gap-2` — do NOT use `px-2.5` or `gap-1.5`.

Icon-only choice per §8: `Loader2` for labeling state, no icon for complete/error text-only variants (matches §8 States table).

Update `client/components/AppShell.tsx` — mount the event listener at the top of the component (before returning JSX), matching the existing `useBackfillProgress()` call (grep for it):
```typescript
useBackfillProgress();
useSpaceLabelingProgress();
```
Import from `../hooks/useSpaceLabelingProgress`. Do NOT change any other AppShell behavior.
  </action>
  <verify>
    <automated>bunx tsc --noEmit -p client/tsconfig.json 2>&1 | head -5</automated>
  </verify>
  <done>SpaceLabelingIndicator exports with 3 visual states + auto-dismiss on complete; AppShell mounts useSpaceLabelingProgress once; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire SpacesPage to new SpaceCard + SpaceLabelingIndicator</name>
  <files>client/pages/SpacesPage.tsx</files>
  <action>
Update `client/pages/SpacesPage.tsx`:

1. DELETE the inline `SpaceCard` function definition (lines 41-77) and the inline `formatRelativeTime` (lines 13-21).

2. Import `{ SpaceCard } from "@/components/spaces/SpaceCard"` and `{ SpaceLabelingIndicator } from "@/components/spaces/SpaceLabelingIndicator"` and `{ formatRelativeTime } from "@/lib/format"`.

3. SpaceRow (lines 79-98) still uses `formatRelativeTime` — its inline definition is now sourced from the same import. Keep SpaceRow as-is.

4. In the page header (line ~146, the `flex items-center justify-between` div), mount SpaceLabelingIndicator on the RIGHT side of the header per 09-UI-SPEC.md §8 "SpacesPage header, right side". Add it as the FIRST child inside the existing `<div className="flex items-center gap-2">` action bar (line ~154), so it appears to the left of the Re-cluster button:
```tsx
<SpaceLabelingIndicator />
<button onClick={handleRecluster} ...>...</button>
```

5. Remove now-unused imports (e.g., `FileText` if only SpaceCard used it — verify with tsc).

6. Do NOT change SpaceRow, do NOT change SkeletonGrid, do NOT change routing, do NOT change useSpaces / useReclusterSpaces calls.

7. Update the existing SpaceRow implementation to also show a Lock icon when `space.userLocked === true` in list mode. Add a `<Lock size={12} className="text-text-tertiary" />` element before the docs-count span. This keeps grid and list views consistent for D-15.
  </action>
  <verify>
    <automated>bunx tsc --noEmit -p client/tsconfig.json 2>&1 | head -5 ; grep -c "function SpaceCard" client/pages/SpacesPage.tsx | awk '$1==0'</automated>
  </verify>
  <done>SpacesPage.tsx no longer defines SpaceCard inline; imports SpaceCard + SpaceLabelingIndicator from the new files; SpaceLabelingIndicator sits in the header action bar; SpaceRow renders Lock icon for user_locked spaces; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust backend → Tauri event → client hook | space-labeling-progress payload deserialization |
| shadcn Tooltip → DOM | Description string renders into TooltipContent — potentially attacker-controlled if LLM output is manipulated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08 | Tampering | Malicious LLM output rendered in tooltip | mitigate | React text nodes escape HTML by default; description is rendered as text, not innerHTML; truncateAt100 caps length before render |
| T-09-09 | Denial of Service | UI stuck in "Generating…" if backend crashes | mitigate | Plan 04 pitfall #5 emits terminal `error` progress event; SpaceLabelingIndicator surfaces `Labeling failed` chip until user dismisses |
</threat_model>

<verification>
- `bunx tsc --noEmit -p client/tsconfig.json` clean.
- `bun test client/components/spaces/SpaceCard.test.tsx` — all 4 state tests pass.
- `grep -c 'function SpaceCard' client/pages/SpacesPage.tsx` returns 0 (no inline definition remains).
- `grep -c 'SpaceLabelingIndicator' client/pages/SpacesPage.tsx` returns ≥ 1.
- `grep -c 'useSpaceLabelingProgress' client/components/AppShell.tsx` returns 1.
- `grep -c 'py-0.5\|px-2.5\|gap-1.5' client/components/spaces/*.tsx` returns 0 (no sub-grid values, per §Spacing Revision Notes).
</verification>

<success_criteria>
1. LLML-02: Description tooltip renders on card hover when `space.description` is non-empty (verified via unit test).
2. LLML-05: Shimmer + "Generating label…" placeholder visible when `space.labelStatus === 'generating'`; SpacesPage remains navigable during batch (no modal/blocker).
3. D-15 Lock icon renders in grid AND list view when `space.userLocked === true`.
4. D-17 EntityHintChip renders below sample files when `space.canonicalEntityHint` is set AND not generating.
5. All new components use grid-aligned spacing (no `py-0.5`, `px-2.5`, `gap-1.5`).
6. SpaceLabelingIndicator auto-dismisses `complete` state after 3s; `error` state persists until store cleared.
</success_criteria>

<output>
Create `.planning/phases/09-llm-space-labeling/09-06-SUMMARY.md` when done. Include:
- List of new files created under client/components/spaces/.
- Confirmation that SpacesPage.tsx inline SpaceCard is gone.
- Test count for SpaceCard.test.tsx.
- Any places where `formatRelativeTime` still lives outside `client/lib/format.ts` (there should be one — SpaceDetailPage — which Plan 07 will fix).
</output>

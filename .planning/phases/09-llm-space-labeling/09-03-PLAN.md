---
phase: 09-llm-space-labeling
plan: 03
type: execute
wave: 2
depends_on: [09-02]
files_modified:
  - src-tauri/src/spaces/llm_labeler.rs
  - src-tauri/src/spaces/mod.rs
autonomous: true
requirements: [LLML-01, LLML-02, LLML-04]

must_haves:
  truths:
    - "SPACE_LABEL_PROMPT is a compile-time constant with 6 few-shot examples matching Phase 8 corpus (D-03)"
    - "label_cluster() returns {label, description} parsed from LLM JSON response"
    - "Collision retry appends 'Avoid: [X, Y]' to user message and re-invokes ai_request (D-13)"
    - "try_bootstrap_from_nearest() returns Some(label) when nearest space centroid cosine >= 0.75 (D-11 replacement — pure Rust, no crate)"
    - "compute_canonical_entity_hint() returns None when top entity < 20% of doc count (D-18)"
    - "SpaceLabelingProgress serde emits camelCase field names (spaceId, processed, total, etc.)"
  artifacts:
    - path: "src-tauri/src/spaces/llm_labeler.rs"
      provides: "SPACE_LABEL_PROMPT, label_cluster(), collision helpers, try_bootstrap_from_nearest(), compute_canonical_entity_hint(), SpaceLabelingProgress"
      exports: ["SPACE_LABEL_PROMPT", "SpaceLabel", "label_cluster", "label_with_avoid_list", "try_bootstrap_from_nearest", "compute_canonical_entity_hint", "SpaceLabelingProgress", "resolve_collisions"]
      min_lines: 180
    - path: "src-tauri/src/spaces/mod.rs"
      provides: "Module declaration for llm_labeler"
      contains: "pub mod llm_labeler"
  key_links:
    - from: "label_cluster()"
      to: "crate::ai::retry::ai_request_with_retry"
      via: "AIServiceRequest with SPACE_LABEL_PROMPT + temperature=0.3"
      pattern: "ai_request_with_retry"
    - from: "label_cluster()"
      to: "crate::pipeline::pass2_llm_refiner::strip_json_fences"
      via: "response.content passes through strip_json_fences before serde_json::from_str"
      pattern: "strip_json_fences"
    - from: "try_bootstrap_from_nearest()"
      to: "crate::spaces::clustering::cosine_similarity"
      via: "pairwise similarity to labeled centroids"
      pattern: "cosine_similarity"
---

<objective>
Build `spaces/llm_labeler.rs` — the LLM call that generates a `{label, description}` pair per cluster (D-01..D-04), the collision-resolution helpers (D-13), the pure-Rust domain-expansion bootstrap that replaces the ruvector-domain-expansion crate per 09-RESEARCH.md critical finding (D-11 replacement), the canonical-entity-hint calculator (D-17/D-18), and the `SpaceLabelingProgress` event payload struct (D-14).

Purpose: Delivers LLML-01 (LLM label), LLML-02 (description), LLML-04 (collision resolution), and Success Criterion 7 (domain-expansion bootstrap). Everything in this plan is a pure function or an isolated LLM call — integration with `SpaceManager::recluster` happens in Plan 04.

Output: `spaces/llm_labeler.rs` with SPACE_LABEL_PROMPT, `label_cluster`, `label_with_avoid_list`, `resolve_collisions`, `try_bootstrap_from_nearest`, `compute_canonical_entity_hint`, `SpaceLabel`, `SpaceLabelingProgress` — plus unit tests for prompt schema, JSON parse, collision detection, bootstrap threshold, entity-hint threshold, and progress event serde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-llm-space-labeling/09-CONTEXT.md
@.planning/phases/09-llm-space-labeling/09-RESEARCH.md
@src-tauri/src/ai/service.rs
@src-tauri/src/ai/retry.rs
@src-tauri/src/pipeline/pass2_llm_refiner.rs
@src-tauri/src/spaces/clustering.rs

<interfaces>
From `src-tauri/src/ai/service.rs`:
```rust
pub struct AIServiceRequest {
    pub system_prompt: String,
    pub messages: Vec<ServiceMessage>,
    pub max_tokens: Option<u32>,
    pub temperature: Option<f64>,
    pub response_format: Option<String>,
}
pub struct ServiceMessage { pub role: String, pub content: String }
pub struct AIServiceResponse { pub content: String, pub model: String, pub input_tokens: Option<u64>, pub output_tokens: Option<u64> }
```

From `src-tauri/src/ai/retry.rs`:
```rust
pub async fn ai_request_with_retry(auth: &AuthState, req: AIServiceRequest, max_retries: u8) -> Result<AIServiceResponse, String>;
```

From `src-tauri/src/pipeline/pass2_llm_refiner.rs`:
```rust
pub fn strip_json_fences(s: &str) -> String; // idempotent — handles ```json / ``` / <think> blocks
```

From `src-tauri/src/spaces/clustering.rs`:
```rust
pub fn cosine_similarity(a: &[f32], b: &[f32]) -> f32; // already pub
```

Entity summary input format (D-01): `"Person: 12, Organization: 8, Amount: 5, Date: 42, Identifier: 3"` — top-5 classes by count, sorted count-descending.

CRITICAL — do NOT add `ruvector-cluster` or `ruvector-domain-expansion` to Cargo.toml. 09-RESEARCH.md verified these crates do not provide HDBSCAN or centroid-based label transfer. Implement bootstrap as pure `cosine_similarity` iteration per the "Domain expansion bootstrap (D-11 replacement)" example.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SPACE_LABEL_PROMPT + label_cluster() + label_with_avoid_list()</name>
  <files>src-tauri/src/spaces/llm_labeler.rs, src-tauri/src/spaces/mod.rs</files>
  <behavior>
    - `SPACE_LABEL_PROMPT` contains all 6 few-shot examples from D-03 (Property Tax Records, Kids School Docs, Health Insurance Claims, Investment Statements, Vehicle Registration, Identity Docs) — one grep test per exemplar.
    - `SPACE_LABEL_PROMPT` explicitly forbids generic labels ("Documents", "Files") — grep test.
    - `SPACE_LABEL_PROMPT` mandates JSON-only output (`Output ONLY valid JSON`) — grep test.
    - `SpaceLabel` deserializes `{"label":"Property Tax Records","description":"Municipal tax documents."}` into fields label + description.
    - `strip_json_fences` is invoked before serde parse (mock/wrap ai_request_with_retry so a ````json\n{...}\n```` response still parses).
    - Empty doc_titles + empty entity_summary still produces a well-formed AIServiceRequest (no panic; content non-empty).
    - `label_with_avoid_list(avoid: &["Work Docs", "Property"])` builds a user_content string containing the substring `IMPORTANT: Avoid these labels already in use: Work Docs, Property`.
    - `label_with_avoid_list(avoid: &[])` produces user_content WITHOUT the "IMPORTANT: Avoid" substring (empty avoid list → no injection).
  </behavior>
  <action>
Create `src-tauri/src/spaces/llm_labeler.rs`. Add `pub mod llm_labeler;` to `src-tauri/src/spaces/mod.rs` (below `pub mod label_cache;` from Plan 02).

Follow 09-RESEARCH.md §Code Examples "LLM labeler call" verbatim for the module skeleton. Key symbols to expose:

- `pub const SPACE_LABEL_PROMPT: &str` — verbatim from D-03: 2-4 word labels rule, ≤ 25 word description rule, JSON-only rule, all 6 few-shot examples, forbid generic labels.
- `pub const LABEL_TEMPERATURE: f64 = 0.3` (D-04).
- `pub const MAX_LABEL_RETRIES: u8 = 3` — passed to `ai_request_with_retry`.
- `pub struct SpaceLabel { pub label: String, pub description: String }` derives Debug/Clone/Serialize/Deserialize.
- `pub async fn label_cluster(auth: &AuthState, model: &str, doc_titles: &[String], entity_summary: &str, top_topics: &[String], top_tags: &[String], doc_count: usize) -> Result<SpaceLabel, String>` — first-attempt call with NO avoid list.
- `pub async fn label_with_avoid_list(auth: &AuthState, model: &str, doc_titles: &[String], entity_summary: &str, top_topics: &[String], top_tags: &[String], doc_count: usize, avoid: &[String]) -> Result<SpaceLabel, String>` — retry variant used by `resolve_collisions` (Task 2). Both share a private `build_user_content()` helper.

Sanitize inputs per T-09-01 mitigation: cap each doc title at 100 chars via `title.chars().take(100).collect::<String>()`, strip ASCII control characters (`c.is_control()`) before pushing into the prompt. Apply the same clamp to entity_summary and each topic/tag string. Comment the sanitizer with a reference to threat T-09-01.

`build_user_content` shape per D-01:
```
Cluster size: {N} documents

Top document titles:
1. {title1}
2. {title2}
...
20. {title20}

Entity summary: {entity_summary}
Top topics: {topic1, topic2, topic3}
Top tags: {tag1, tag2, ...}
```
When `avoid` is non-empty, append: `\n\nIMPORTANT: Avoid these labels already in use: {joined-by-comma}`.

`AIServiceRequest`: system_prompt = SPACE_LABEL_PROMPT, messages = [{role:"user", content:user_content}], max_tokens = Some(256), temperature = Some(LABEL_TEMPERATURE), response_format = None.

Parse: `let stripped = strip_json_fences(&response.content); serde_json::from_str::<SpaceLabel>(&stripped).map_err(|e| format!("LLM JSON parse failed: {}", e))`.

Do NOT introduce a new HTTP client, do NOT re-implement retry, do NOT touch ai/service.rs.

Write `#[cfg(test)]` mod. Since ai_request_with_retry needs a real AuthState, the LLM-network tests are stubbed via a small `#[cfg(test)]` re-implementation: extract `build_user_content(...)` as a `pub(crate) fn` so tests can call it directly and assert prompt shape without hitting the network. Prompt-schema tests use raw `SPACE_LABEL_PROMPT` grep. Serde test uses a hand-built JSON string through `strip_json_fences` → `serde_json::from_str::<SpaceLabel>`.
  </action>
  <verify>
    <automated>cargo test -p cortex --lib spaces::llm_labeler::tests::prompt -- --nocapture</automated>
  </verify>
  <done>SPACE_LABEL_PROMPT compiles with 6 grep-verified exemplars; `label_cluster`/`label_with_avoid_list` exposed; `SpaceLabel` deserializes fence-wrapped JSON; input sanitizer strips control chars + caps to 100 chars; avoid-list injection is conditional.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Collision resolution + domain expansion bootstrap + canonical entity hint + progress event</name>
  <files>src-tauri/src/spaces/llm_labeler.rs</files>
  <behavior>
    - `resolve_collisions` sees a set of proposed labels, groups by case-insensitive+whitespace-normalized string (per RESEARCH.md Open Question #3 recommendation), and returns which space_ids need a retry.
    - `resolve_collisions` retries at most 2 times per space (D-13); after that, it appends the top NON-shared entity as a suffix.
    - The suffix format is `" — {EntityValue}"` (em-dash + space + entity value only, no class prefix), so `"Work Docs" + top entity "Freelance"` becomes `"Work Docs — Freelance"` per D-13.
    - `try_bootstrap_from_nearest` returns `Some(label)` when best cosine similarity ≥ 0.75 (D-11 threshold), else `None`.
    - `try_bootstrap_from_nearest` returns `None` on an empty candidate list (guard against `f32::NEG_INFINITY` best-sim staying uninitialized).
    - `compute_canonical_entity_hint` returns `Some("Person: Alex Doe")` when Person=15 dominates 20 docs (75% > 20% threshold).
    - `compute_canonical_entity_hint` returns `None` when top entity count / doc_count < 0.20 (D-18: hint scope guard).
    - `compute_canonical_entity_hint` returns `None` on an empty entity map (no dominant entity).
    - `SpaceLabelingProgress` serialization emits `spaceId`, `processed`, `total`, `label`, `error` (camelCase — matches D-14 event payload).
    - `SpaceLabelingProgress` derives Clone so backend can construct once and emit repeatedly if needed.
  </behavior>
  <action>
Extend `src-tauri/src/spaces/llm_labeler.rs` with three additional public items and one struct — all pure functions (no I/O, no LLM calls except through Task 1's helpers).

`pub fn resolve_collisions(proposed: &[(String, String)]) -> Vec<CollisionAction>` where the input is `(space_id, label)` pairs and the output enum is:
```rust
pub enum CollisionAction {
    Keep { space_id: String },
    Retry { space_id: String, avoid: Vec<String> },
    Suffix { space_id: String, suffix_entity_value: String },
}
```
Actually SIMPLIFY: return `Vec<(String /*space_id*/, ResolvedLabel)>` where `ResolvedLabel` is a tagged enum:
```rust
pub enum ResolvedLabel {
    Keep(String),                    // final label
    RetryWithAvoid(Vec<String>),     // avoid list to feed label_with_avoid_list
}
```
Collision definition (per 09-RESEARCH.md Open Question #3): normalize both sides to `label.trim().to_lowercase()` before comparison. Store and return the original-case label unchanged. `resolve_collisions` fires only ONE round of retries per call — the caller (Plan 04's manager loop) invokes it a second time to check if retry-labels also collided.

Add a separate helper `pub fn apply_suffix_fallback(base: &str, suffix_entity_value: &str) -> String` that returns `format!("{} — {}", base, suffix_entity_value)`. Plan 04 calls this after 2 retry rounds fail (D-13 "auto-suffix with the 2nd distinguishing entity").

`pub fn try_bootstrap_from_nearest(new_centroid: &[f32], labeled_spaces: &[(String /*space_id*/, String /*label*/, Vec<f32> /*centroid*/)]) -> Option<String>` — per 09-RESEARCH.md §Code Examples "Domain expansion bootstrap":
- Initialize `best_sim = f32::NEG_INFINITY`, `best_label = None`.
- Iterate the slice, calling `crate::spaces::clustering::cosine_similarity(new_centroid, centroid)`.
- Track max similarity and its label.
- Return `Some(label)` iff `best_sim >= 0.75`, else `None`.
- On empty `labeled_spaces`, return `None` immediately (do not compute).

`pub fn compute_canonical_entity_hint(entity_counts: &HashMap<String /*class_value*/, usize>, doc_count: usize) -> Option<String>` per D-17 / D-18:
- Input key format: `"ClassName: value"` (e.g., `"Person: Alex Doe"`) — Plan 04's caller aggregates entities into this shape.
- Return `Some(top_key)` when the highest-count entry has count ≥ ⌈doc_count / 5⌉ (i.e., ≥ 20% per D-18).
- Ties broken by lexicographic order of the key (deterministic).
- Empty map → None.
- doc_count == 0 → None.

`#[derive(Debug, Clone, Serialize, Deserialize)]` + `#[serde(rename_all = "camelCase")]`:
```rust
pub struct SpaceLabelingProgress {
    pub space_id: String,
    pub status: String,           // "labeling" | "complete" | "error"
    pub processed: usize,
    pub total: usize,
    pub label: Option<String>,
    pub error: Option<String>,
}
```
Do NOT change the field names, do NOT drop `Clone` — Plan 04 emits per-space and needs to reuse instances.

Write unit tests for each behavior. Bootstrap tests use small handcrafted centroids (e.g., `[1.0, 0.0]` vs `[0.9, 0.44]` → cosine ~0.90 → transferred label; `[1.0, 0.0]` vs `[0.0, 1.0]` → cosine 0 → None). Progress event test uses `serde_json::to_value` and asserts the resulting `Value` contains `"spaceId"` as a top-level key (camelCase confirmation).
  </action>
  <verify>
    <automated>cargo test -p cortex --lib spaces::llm_labeler::tests</automated>
  </verify>
  <done>`resolve_collisions`, `apply_suffix_fallback`, `try_bootstrap_from_nearest`, `compute_canonical_entity_hint`, `SpaceLabelingProgress` all exported and unit-tested; bootstrap threshold at exactly 0.75 verified; entity hint threshold at exactly 20% verified; progress event serde emits camelCase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust backend → LLM Provider API | doc titles + entity strings become prompt input (untrusted → provider) |
| LLM Provider API → Rust backend | Response content parsed as JSON — schema violation risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | Prompt injection via document titles | mitigate | Sanitizer in `build_user_content` caps each title at 100 chars and strips control characters before prompt assembly |
| T-09-02 | Tampering | LLM response schema | mitigate | `strip_json_fences` + `serde_json::from_str::<SpaceLabel>` rejects any response missing `label` or `description` |
| T-09-03 | Denial of Service | Unbounded LLM cost via retry loop | mitigate | `ai_request_with_retry` bounded by `MAX_LABEL_RETRIES = 3`; collision retries capped at 2 per D-13; Plan 04 layers semaphore(8) on top |
| T-09-04 | Information Disclosure | canonical_entity_hint reveals PII | accept | Hint is derived from entities already surfaced in DocumentPage sidebar (Phase 8); no new disclosure surface |
</threat_model>

<verification>
- `cargo test -p cortex --lib spaces::llm_labeler::tests` — all tests pass.
- `cargo check -p cortex` clean.
- `grep -c 'Property Tax Records' src-tauri/src/spaces/llm_labeler.rs` returns ≥ 1 (few-shot exemplar).
- `grep -c 'Kids School Docs' src-tauri/src/spaces/llm_labeler.rs` returns ≥ 1.
- `grep -c 'Health Insurance Claims' src-tauri/src/spaces/llm_labeler.rs` returns ≥ 1.
- `grep -c 'Investment Statements' src-tauri/src/spaces/llm_labeler.rs` returns ≥ 1.
- `grep -c 'Vehicle Registration' src-tauri/src/spaces/llm_labeler.rs` returns ≥ 1.
- `grep -c 'Identity Docs' src-tauri/src/spaces/llm_labeler.rs` returns ≥ 1.
- `grep -c 'Output ONLY valid JSON' src-tauri/src/spaces/llm_labeler.rs` returns ≥ 1.
- `grep -c 'ruvector_cluster\|ruvector_domain_expansion' src-tauri/src/spaces/llm_labeler.rs` returns 0 (confirms no phantom crate calls).
</verification>

<success_criteria>
1. LLML-01: `label_cluster` returns a `SpaceLabel` with 2-4 word label + 1-sentence description (verified by prompt structure test).
2. LLML-02: `SpaceLabel.description` field is present and populated (round-trip test).
3. LLML-04: `resolve_collisions` + `apply_suffix_fallback` implement D-13's retry-then-suffix cascade.
4. SC7: `try_bootstrap_from_nearest` implements D-11 bootstrap purely in Rust, threshold at exactly 0.75.
5. D-17/D-18: `compute_canonical_entity_hint` returns None on <20% dominance.
6. D-14: `SpaceLabelingProgress` serde emits camelCase field names.
7. T-09-01 mitigated by input sanitizer.
</success_criteria>

<output>
Create `.planning/phases/09-llm-space-labeling/09-03-SUMMARY.md` when done. Include:
- Total public symbols exported from llm_labeler.rs (should be ≥ 10).
- Test count.
- Any deviation from D-13's collision cascade (retry twice, then suffix).
- Note: this plan intentionally does NOT touch Cargo.toml. Confirm no `ruvector-cluster` / `ruvector-domain-expansion` dependency was added (09-RESEARCH.md critical finding).
</output>

---
phase: 09-llm-space-labeling
plan: 04
type: execute
wave: 3
depends_on: [09-01, 09-02, 09-03]
files_modified:
  - src-tauri/src/spaces/manager.rs
  - src-tauri/src/spaces/naming.rs
  - src-tauri/src/commands/spaces.rs
  - src-tauri/src/commands/mod.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements: [LLML-01, LLML-03, LLML-04, LLML-05]

must_haves:
  truths:
    - "SpaceManager::recluster reads the SpaceLabelCache, skips clusters whose Jaccard distance from cached fingerprint is ≤ 20% (D-06 / LLML-03)"
    - "New clusters first try try_bootstrap_from_nearest (D-11 replacement) before invoking label_cluster (SC7)"
    - "SpaceManager applies collision resolution across the whole batch — no two spaces share a case-insensitive label (LLML-04)"
    - "SpaceManager emits `space-labeling-progress` events per space during the batch (D-14 / LLML-05)"
    - "user_locked spaces are NEVER re-labeled — recluster preserves their name/description untouched (D-15)"
    - "space_labels.json cache is populated + saved at end of every recluster (D-07)"
    - "naming.rs is used as fallback only when ai_request returns an error after retries (pitfall #2)"
    - "4 new IPC commands are registered: get_space_labels, rename_space_label, clear_space_override, trigger_relabel"
    - "SpaceLabelCache lives in AppState behind a Mutex (pitfall #4 / T-09-03 mitigation)"
  artifacts:
    - path: "src-tauri/src/spaces/manager.rs"
      provides: "Recluster loop wired to LLM labeler + cache + progress emission"
      contains: "label_cluster|SpaceLabelCache"
    - path: "src-tauri/src/spaces/naming.rs"
      provides: "Rule-based fallback with #[allow(dead_code)] or explicit fallback-only marker"
      contains: "fallback"
    - path: "src-tauri/src/commands/spaces.rs"
      provides: "4 new IPC commands + progress emit call sites"
      exports: ["get_space_labels", "rename_space_label", "clear_space_override", "trigger_relabel"]
    - path: "src-tauri/src/lib.rs"
      provides: "AppState registers Arc<Mutex<SpaceLabelCache>>; new commands wired into invoke_handler"
      contains: "SpaceLabelCache"
  key_links:
    - from: "SpaceManager::recluster"
      to: "spaces::llm_labeler::label_cluster"
      via: "async labeling loop with semaphore(8)"
      pattern: "label_cluster"
    - from: "SpaceManager::recluster"
      to: "app_handle.emit(\"space-labeling-progress\", ...)"
      via: "per-space progress emission"
      pattern: "space-labeling-progress"
    - from: "commands/spaces.rs::rename_space_label"
      to: "SpaceLabelCache::insert with user_locked=true"
      via: "cache.save on write"
      pattern: "user_locked = true"
    - from: "SpaceManager::recluster"
      to: "SpaceLabelCache::is_user_locked"
      via: "skip labeling when locked (D-15)"
      pattern: "is_user_locked"
---

<objective>
Wire the Wave-1/2 primitives into the recluster path: `SpaceManager::recluster` now consults the `SpaceLabelCache`, computes Jaccard distance, calls `try_bootstrap_from_nearest` for new clusters, invokes `label_cluster` when the LLM must run, applies collision resolution across the whole batch, emits `space-labeling-progress` events, and persists the cache. Register 4 new IPC commands so the frontend can rename, clear, regenerate, and read labels.

Purpose: This is the plan that turns the assembly of Wave 1/2 primitives into user-visible behavior — LLML-03 (cache skip), LLML-04 (collision-free), LLML-05 (background + navigable), and LLML-01/02 wire-up. Also handles the SC6 reality: RESEARCH.md confirms `ruvector-cluster` does not provide HDBSCAN, so k-means stays; this plan documents the deviation in the recluster doc comment.

Output: Updated `spaces/manager.rs` recluster loop, 4 new IPC commands in `commands/spaces.rs`, `AppState` extension for the cache, `naming.rs` marked as fallback-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-llm-space-labeling/09-CONTEXT.md
@.planning/phases/09-llm-space-labeling/09-RESEARCH.md
@.planning/phases/09-llm-space-labeling/09-01-PLAN.md
@.planning/phases/09-llm-space-labeling/09-02-PLAN.md
@.planning/phases/09-llm-space-labeling/09-03-PLAN.md
@src-tauri/src/spaces/manager.rs
@src-tauri/src/spaces/naming.rs
@src-tauri/src/spaces/clustering.rs
@src-tauri/src/commands/spaces.rs
@src-tauri/src/lib.rs

<interfaces>
From Plan 02 (spaces/fingerprint.rs):
```rust
pub fn membership_fingerprint(doc_ids: &[String]) -> String;
pub fn jaccard_distance(old: &HashSet<String>, new: &HashSet<String>) -> f32;
```

From Plan 02 (spaces/label_cache.rs):
```rust
pub struct SpaceLabelEntry { pub fingerprint, pub label, pub description, pub canonical_entity_hint: Option<String>, pub generated_at: String, pub user_locked: bool }
pub struct SpaceLabelCache { pub labels: HashMap<String, SpaceLabelEntry> }
impl SpaceLabelCache {
    pub fn load(&Path) -> Self;
    pub fn save(&self, &Path) -> std::io::Result<()>;
    pub fn get(&self, space_id: &str) -> Option<&SpaceLabelEntry>;
    pub fn insert(&mut self, String, SpaceLabelEntry);
    pub fn remove(&mut self, space_id: &str);
    pub fn is_user_locked(&self, space_id: &str) -> bool;
}
```

From Plan 03 (spaces/llm_labeler.rs):
```rust
pub const SPACE_LABEL_PROMPT: &str; pub const LABEL_TEMPERATURE: f64;
pub struct SpaceLabel { pub label: String, pub description: String }
pub async fn label_cluster(auth, model, doc_titles, entity_summary, top_topics, top_tags, doc_count) -> Result<SpaceLabel, String>;
pub async fn label_with_avoid_list(auth, ..., avoid: &[String]) -> Result<SpaceLabel, String>;
pub fn resolve_collisions(proposed: &[(String, String)]) -> Vec<(String, ResolvedLabel)>; // ResolvedLabel::Keep(String) | RetryWithAvoid(Vec<String>)
pub fn apply_suffix_fallback(base: &str, suffix_entity_value: &str) -> String;
pub fn try_bootstrap_from_nearest(new_centroid, labeled_spaces: &[(String, String, Vec<f32>)]) -> Option<String>;
pub fn compute_canonical_entity_hint(entity_counts: &HashMap<String, usize>, doc_count) -> Option<String>;
pub struct SpaceLabelingProgress { space_id, status, processed, total, label, error }
```

Current SpaceManager::recluster loop (src-tauri/src/spaces/manager.rs lines 53-217) — key points:
- Iterates `result.clusters`, calls `name_space(&cluster_metadata, i)` → `(name, icon, color)`.
- Applies domain expansion (lines 126-157) at cosine ≥ 0.6 → inherits icon+color.
- Appends `" (N)"` for duplicate names (lines 161-167).
- Builds Space struct and updates internal state.

`AppState` in src-tauri/src/lib.rs registers Arc<Mutex<>> primitives via `.manage()`. Add `label_cache: Arc<tokio::sync::Mutex<SpaceLabelCache>>` alongside `space_manager`.

Concurrency: use `tokio::sync::Semaphore::new(8)` for LLM calls (D-15 discretion, mirrors Phase 8 pattern in pass2_llm_refiner.rs).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire SpaceManager::recluster to consume label cache + LLM labeler</name>
  <files>src-tauri/src/spaces/manager.rs, src-tauri/src/spaces/naming.rs, src-tauri/src/lib.rs</files>
  <behavior>
    - `recluster` now takes an additional `app_handle: &tauri::AppHandle`, `auth: &AuthState`, `model: &str`, `cache: &mut SpaceLabelCache` (or an Arc<Mutex<>>) — signature update.
    - For each cluster: compute `fingerprint = membership_fingerprint(&cluster.doc_ids)`.
    - Lookup `cache.get(&cluster.id)`; if present, compute `jaccard = jaccard_distance(&cached_doc_ids, &new_doc_ids)` from the cached membership snapshot and skip labeling when `jaccard <= 0.20` AND fingerprints differ (per D-06 "> 0.20 → re-label").
    - For clusters flagged `user_locked=true` in cache, ALWAYS reuse cached label + description regardless of Jaccard (D-15).
    - For NEW clusters (no cache entry for space_id): call `try_bootstrap_from_nearest(new_centroid, &labeled_centroids_from_cache)`; if `Some(label)`, use that transferred label AND still call `label_cluster` ONLY IF description is also required — actually per D-11 the bootstrap covers BOTH label and description; on a bootstrap hit, reuse the source space's description via cache lookup.
    - LLM call path: build `entity_summary` (top-5 classes by count), `top_topics` (top-3 from Phase 8 topic field), `top_tags` (top-10 from Phase 8 tags), `doc_titles` (top-20 nearest centroid), `doc_count`. Call `label_cluster` under a `tokio::sync::Semaphore::new(8)` gate.
    - On LLM error after retries, fall back to `name_space(&cluster_metadata, i)` from naming.rs (pitfall #2). Log a warning.
    - After all LLM calls settle: run `resolve_collisions` on the proposed labels; for `RetryWithAvoid(avoid)`, invoke `label_with_avoid_list` (bounded to 2 retries per space per D-13); if still colliding, use `apply_suffix_fallback(base, top_non_shared_entity)`.
    - After collision resolution: build `canonical_entity_hint` via `compute_canonical_entity_hint(entity_counts, doc_count)` per space.
    - Persist each finalized entry into the cache with `SpaceLabelEntry { fingerprint, label, description, canonical_entity_hint, generated_at: now_iso, user_locked: preserved-from-existing-entry }` and call `cache.save(&app_data_dir)` at the end.
    - Emit `SpaceLabelingProgress { status: "labeling", processed: i, total, ... }` before each LLM call and `status: "complete"` after each success; `status: "error"` on failure. Wrap the whole batch in `catch_unwind` or a Result guard so a panic still emits a terminal `"error"` progress event (pitfall #5 mitigation).
    - Lazy garbage collect (D-08): remove cache entries whose space_id is not present in the new cluster set at end of recluster.
    - Populate the returned `Space.label`/`Space.name` from cache label (D-15 lock respected).
    - Populate `Space.description`, `Space.canonical_entity_hint`, `Space.user_locked` from cache.
    - `Space.label_status` is `Some("ready".to_string())` on final return (during the LLM call the SpaceManager holds an internal `labeling_in_progress: HashSet<String>` — see task 2).

    Unit tests (no real LLM):
    - Reuse cached label when Jaccard ≤ 20% (mock `SpaceLabelCache` with a known entry).
    - Always reuse cached label when `user_locked=true` regardless of Jaccard (proves D-15 lock).
    - Fingerprint change but Jaccard 0.15 → skip LLM (proves D-06 threshold).
    - Collision resolution: two clusters propose "Work Docs" → one gets suffix from top non-shared entity.
    - GC: deleted space_id is removed from cache after recluster.
  </behavior>
  <action>
Update `spaces/manager.rs`:

1. Change `SpaceManager::recluster` signature to accept `(&mut self, engine: &CortexEngine, auth: &AuthState, model: &str, app_handle: &tauri::AppHandle, cache: &mut SpaceLabelCache, app_data_dir: &Path)`. Return `Result<Vec<Space>, AppError>` as before. The old `naming.rs` call gets wrapped as fallback. Since this is now async (LLM calls), change to `pub async fn recluster(...)`.

2. Wrap the existing k-means clustering path (lines 92-93 currently `let k = auto_detect_k(...); let result = cluster_documents(...);`) unchanged. Add a comment above it referencing 09-RESEARCH.md §"CRITICAL: ruvector Crate Audit" — the SC6 note that `ruvector-cluster` does not provide HDBSCAN and k-means remains authoritative. This documents the deviation for future readers without inventing scope reduction.

3. Add an in-memory `labeling_in_progress: Arc<tokio::sync::Mutex<HashSet<String>>>` field to `SpaceManager` (or a separate AppState field) so `get_spaces` IPC (Plan 05 → Plan 07) can surface `label_status='generating'` mid-batch. Reset at batch end.

4. Extract a private helper `async fn label_or_fallback(&self, auth, model, cluster, cluster_metadata, i) -> (SpaceLabel, bool /*was_fallback*/)` that: attempts `label_cluster`; on `Err`, invokes `name_space(&cluster_metadata, i)` and wraps the returned name+ derived-icon+color into a `SpaceLabel { label, description: format!("Documents grouped by heuristic (LLM unavailable)") }`. Emits a `status: "error"` progress event when fallback fires.

5. Add `Semaphore` cap of 8 via `let sem = Arc::new(Semaphore::new(8))` around the labeling loop; use `futures::future::join_all` or `tokio::spawn` per cluster with permits.

6. Delete the current line-104-105 `name_counts` counter + `" (N)"` suffix loop — collision resolution now goes through `resolve_collisions` + `apply_suffix_fallback` (D-13).

7. Delete the OLD domain expansion block at lines 126-157 (icon/color-only) — the new domain expansion transfers LABELS via `try_bootstrap_from_nearest`, not icons/colors. Icons/colors continue to be derived from `name_space` heuristics on fallback path only; for LLM-labeled paths, use icon/color inheritance from nearest cached space if a bootstrap fires. Keep the visual UX (icon+color) stable across reclusters by preferring cached entry's icon+color when it exists.

Update `spaces/naming.rs`: add a top-of-file comment: `// Fallback naming: invoked only when ai_request errors after retries (Phase 9 wiring). Full deletion deferred to Phase 12+ per RESEARCH pitfall #2.` No behavior change to `name_space`.

Update `lib.rs`:
- Add `let label_cache = Arc::new(tokio::sync::Mutex::new(spaces::label_cache::SpaceLabelCache::load(&app_data)));` next to the existing SpaceManager init (line ~87).
- Register with `.manage(label_cache.clone())`.
- Extend `AppState` (in src-tauri/src/state.rs — grep for the struct if separate) with `pub label_cache: Arc<tokio::sync::Mutex<SpaceLabelCache>>` and `pub labeling_in_progress: Arc<tokio::sync::Mutex<HashSet<String>>>`.

Add unit tests for the new logic. Since recluster now needs an AuthState + AppHandle, split logic into a `plan_labeling_operations(...)` pure function that takes the cluster set + current cache and RETURNS a plan (which clusters to skip, which to bootstrap, which to LLM-label). Test that pure function directly. The async labeling wrapper can be smoke-tested in Plan 08's checkpoint.
  </action>
  <verify>
    <automated>cargo test -p cortex --lib spaces::manager::tests</automated>
  </verify>
  <done>SpaceManager::recluster signature updated; cache read/write wired; user_locked skip enforced; collision resolution goes through Plan 03 helpers; naming.rs marked as fallback; AppState registers label_cache + labeling_in_progress; `plan_labeling_operations` pure function has unit tests covering skip/bootstrap/label/lock scenarios.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register 4 new IPC commands + update recluster_spaces call site</name>
  <files>src-tauri/src/commands/spaces.rs, src-tauri/src/commands/mod.rs, src-tauri/src/lib.rs</files>
  <behavior>
    - `get_space_labels(state)` returns `HashMap<String, SpaceLabelEntry>` (the full cache view) — allows debugging + advanced UI.
    - `rename_space_label(space_id, new_label, state)` writes `{ label: new_label, user_locked: true, ...preserve rest }` to cache, saves to disk, returns updated SpaceLabelEntry. Preserves existing description + canonical_entity_hint.
    - `clear_space_override(space_id, state)` sets `user_locked: false`, saves. Does NOT delete label/description (D-15 "Clear override" — allows next recluster to re-label). Returns the updated entry.
    - `trigger_relabel(space_id, state)` for a single space: reads its current cluster centroid + doc_ids from SpaceManager, invokes `label_cluster` (skipping user_locked check server-side — command errors with `SpaceLocked` if space is user_locked; Plan 07 handles the toast). Emits the same `space-labeling-progress` event stream so UI reacts.
    - Existing `recluster_spaces` command updates its call to `space_guard.recluster(engine, auth, model, app_handle, cache, app_data_dir)`.
    - All 4 new commands are registered in `invoke_handler![...]` in lib.rs.

    Unit test where possible (IPC commands are hard to unit-test end-to-end — focus on the cache mutations they wrap):
    - `rename_space_label` sets `user_locked=true` and preserves description + canonical_entity_hint.
    - `clear_space_override` sets `user_locked=false` without deleting label.
    - `trigger_relabel` on a user_locked space returns `AppError::SpaceLocked` (or the equivalent variant defined in this plan).
  </behavior>
  <action>
Update `src-tauri/src/commands/spaces.rs`:

1. Add `use super::super::spaces::label_cache::{SpaceLabelCache, SpaceLabelEntry};` and imports for the new state fields.

2. Update `#[tauri::command] pub async fn recluster_spaces` to accept `app_handle: tauri::AppHandle` and pull `cache` + `auth` + `model` from state. Call the async `space_guard.recluster(...)` with new args. Wrap the whole call in `spawn_blocking` cannot be used for async; instead run inside `tokio::spawn` if needed, but simplest: `recluster_spaces` becomes `pub async fn` (not `spawn_blocking`) since the LLM call inside is async.

3. Add 4 new commands with the signatures below. All use `#[tauri::command]`, all take `state: State<'_, AppState>`, all return `Result<T, AppError>`. Convention mirrors existing `move_document_to_space`.

```
#[tauri::command] pub async fn get_space_labels(state: State<'_, AppState>) -> Result<HashMap<String, SpaceLabelEntry>, AppError>;
#[tauri::command] pub async fn rename_space_label(space_id: String, new_label: String, state: State<'_, AppState>) -> Result<SpaceLabelEntry, AppError>;
#[tauri::command] pub async fn clear_space_override(space_id: String, state: State<'_, AppState>) -> Result<SpaceLabelEntry, AppError>;
#[tauri::command] pub async fn trigger_relabel(space_id: String, state: State<'_, AppState>, app_handle: tauri::AppHandle) -> Result<Space, AppError>;
```

`rename_space_label` behavior (T-09-06 mitigation — pitfall #5 server-side enforcement of user_locked):
- Lock cache mutex, get existing entry (or default), update `label` + `user_locked = true`, preserve other fields, save to disk, return updated entry.
- Also update the in-memory SpaceManager's Space.name so `get_spaces` reflects the change immediately (no wait for next recluster).

`clear_space_override` behavior:
- Lock cache mutex, get existing entry, set `user_locked = false`, save to disk, return updated entry.

`trigger_relabel` behavior:
- Lock cache mutex, if entry exists and `user_locked == true`, return `Err(AppError::SpaceLocked(space_id))` (add this variant to `error.rs` if missing).
- Otherwise: pull SpaceManager's cluster centroid + doc_ids for the space, build the LLM inputs (entity summary, topics, tags, titles) via the same helpers the recluster loop uses (extract them into a shared `pub(crate) fn build_llm_inputs_from_metadata(...)` module-level function in manager.rs so this command reuses them), call `label_cluster`, save to cache, update in-memory Space, emit progress events, return updated Space.

Update `src-tauri/src/commands/mod.rs` — export the 4 new commands.

Update `src-tauri/src/lib.rs` — extend `.invoke_handler(tauri::generate_handler![...])` with the 4 new command names.

Add `AppError::SpaceLocked(String)` variant to `src-tauri/src/error.rs` if not present. Reuse existing serde_json error mapping.

Write cache-mutation unit tests for rename/clear/trigger without the actual LLM call (use a mock AuthState or gate behind `#[cfg(test)]` bypass). Cache tests are enough for this plan; end-to-end IPC testing happens in Plan 08's UX checkpoint.
  </action>
  <verify>
    <automated>cargo test -p cortex --lib commands::spaces::tests</automated>
  </verify>
  <done>4 IPC commands registered in `invoke_handler!`; `rename_space_label` sets `user_locked=true` and persists; `clear_space_override` sets `user_locked=false` without deleting label; `trigger_relabel` errors with `SpaceLocked` on locked spaces; existing `recluster_spaces` signature works with the new async recluster; `cargo build --release -p cortex` clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client (React) → IPC (Tauri) | user_locked flag mutations cross here — must be enforced server-side |
| Rust backend → LLM Provider API | doc titles/entities become prompt input |
| Rust backend → app_data_dir filesystem | space_labels.json write path (Mutex-guarded) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Elevation of Privilege | Malicious frontend bypasses user_locked | mitigate | `trigger_relabel` checks `cache.is_user_locked(space_id)` server-side before calling `label_cluster` — pitfall #5 mitigation |
| T-09-02 | Denial of Service | Unbounded LLM cost via recluster spam | mitigate | Semaphore(8) cap on parallel LLM calls; fingerprint cache short-circuits unchanged clusters (D-05/D-06); collision retries capped at 2 (D-13) |
| T-09-03 | Tampering | space_labels.json race on concurrent recluster + rename | mitigate | Cache wrapped in `Arc<tokio::sync::Mutex<>>` in AppState; all reads/writes take the same lock |
| T-09-04 | Denial of Service | Panic in labeling task leaves UI stuck in "Generating…" | mitigate | Wrap batch in Result guard; emit terminal `status: "error"` progress event on any error path (pitfall #5) |
| T-09-05 | Information Disclosure | Frontend sees other users' labels | accept | Single-user desktop app; no cross-user isolation required |
</threat_model>

<verification>
- `cargo test -p cortex --lib spaces::manager::tests` — all tests pass.
- `cargo test -p cortex --lib commands::spaces::tests` — all tests pass.
- `cargo build --release -p cortex` clean.
- `grep -c 'space-labeling-progress' src-tauri/src/spaces/manager.rs` returns ≥ 1.
- `grep -c 'is_user_locked' src-tauri/src/spaces/manager.rs` returns ≥ 1.
- `grep -c 'label_cluster' src-tauri/src/spaces/manager.rs` returns ≥ 1.
- `grep -v '^//' src-tauri/src/spaces/manager.rs | grep -c 'name_space' | awk '$1>0 {print}'` — verifies name_space is still referenced (fallback path).
- `grep -c 'get_space_labels\|rename_space_label\|clear_space_override\|trigger_relabel' src-tauri/src/lib.rs` returns 4 (all commands registered).
</verification>

<success_criteria>
1. LLML-01: Recluster invokes `label_cluster` for clusters not in cache and not user_locked.
2. LLML-03: Recluster skips LLM for clusters whose Jaccard ≤ 20% from cached fingerprint.
3. LLML-04: Recluster runs `resolve_collisions` + `apply_suffix_fallback` across the batch.
4. LLML-05: Recluster emits `space-labeling-progress` events per cluster (labeling/complete/error).
5. D-15 lock: `rename_space_label` sets `user_locked=true`; `trigger_relabel` errors on locked spaces; recluster preserves locked spaces untouched.
6. SC7: `try_bootstrap_from_nearest` fires for new clusters before LLM call.
7. SC6 deviation: manager.rs recluster docs note k-means retained because ruvector-cluster does not provide HDBSCAN (per 09-RESEARCH.md verified finding).
8. pitfall #5: batch always emits a terminal progress event; UI never gets stuck.
</success_criteria>

<output>
Create `.planning/phases/09-llm-space-labeling/09-04-SUMMARY.md` when done. Include:
- Which manager.rs lines were rewritten vs preserved.
- The exact SC6 deviation note as written in the recluster doc comment.
- Test counts.
- The complete list of registered commands in `invoke_handler![...]`.
- Confirmation that `Cargo.toml` was NOT modified (no ruvector-cluster / ruvector-domain-expansion added).
</output>

---
phase: 09-llm-space-labeling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/types.rs
  - client/lib/types.ts
  - client/lib/mock-data.ts
autonomous: true
requirements: [LLML-01, LLML-02, LLML-05]

must_haves:
  truths:
    - "Rust Space struct exposes description, user_locked, canonical_entity_hint, label_status fields"
    - "TypeScript Space interface mirrors the four new fields with camelCase names"
    - "Existing IPC calls returning Space[] still deserialize on frontends built before Phase 9 (backwards compatibility)"
  artifacts:
    - path: "src-tauri/src/types.rs"
      provides: "Space struct with 4 new fields, all #[serde(default)]"
      contains: "pub description: Option<String>"
    - path: "client/lib/types.ts"
      provides: "Space interface with 4 new optional fields"
      contains: "description?: string"
    - path: "client/lib/mock-data.ts"
      provides: "Sample mockSpaces demonstrating new fields"
      contains: "labelStatus"
  key_links:
    - from: "src-tauri/src/types.rs::Space"
      to: "client/lib/types.ts::Space"
      via: "serde camelCase rename"
      pattern: "userLocked|canonicalEntityHint|labelStatus"
---

<objective>
Extend the shared Space type with four new fields required by Phase 9 (`description`, `user_locked`, `canonical_entity_hint`, `label_status`). Mirror the Rust struct in TypeScript so IPC responses deserialize correctly, and seed mock data so frontend development in mock mode exercises the new UI paths.

Purpose: These fields are prerequisites for every downstream plan — Plan 02 stores them in the label cache, Plan 04 emits them from `SpaceManager`, and Plans 05-07 render them in the UI. Landing the type extension first lets Waves 2+ compile.

Output: Extended `Space` struct in `types.rs`, extended `Space` interface in `client/lib/types.ts`, mock spaces demonstrating each new field state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-llm-space-labeling/09-CONTEXT.md
@.planning/phases/09-llm-space-labeling/09-UI-SPEC.md
@.planning/phases/09-llm-space-labeling/09-RESEARCH.md
@src-tauri/src/types.rs
@client/lib/types.ts
@client/lib/mock-data.ts

<interfaces>
Current Rust Space struct (src-tauri/src/types.rs lines 168-180):
```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct Space {
    pub id: String,
    pub name: String,
    pub icon: String,
    pub color: String,
    pub document_count: u32,
    pub last_updated: String,
    pub sub_spaces: Vec<Space>,
    pub parent_id: Option<String>,
    pub sample_files: Vec<String>,
}
```

Current TS Space interface (client/lib/types.ts lines 117-127):
```typescript
export interface Space {
  id: string;
  name: string;
  icon: string;
  color: string;
  documentCount: number;
  lastUpdated: string;
  subSpaces: Space[];
  parentId?: string;
  sampleFiles: string[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Rust Space struct with Phase 9 fields</name>
  <files>src-tauri/src/types.rs</files>
  <action>
Add four new fields to `pub struct Space` after `sample_files`, per D-17 / D-18 and the "Space struct extension" block in 09-RESEARCH.md Code Examples: `description: Option<String>`, `user_locked: bool`, `canonical_entity_hint: Option<String>`, `label_status: Option<String>` (enum-shaped as "ready" | "generating" — a String today so a future migration can widen it without breaking serde). Annotate every new field with `#[serde(default)]` so pre-Phase-9 serialized Space values (e.g. from cached SpaceManager state) still deserialize cleanly (pitfall #3 in 09-RESEARCH.md). Keep the existing `#[serde(rename_all = "camelCase")]` attribute on the struct — do not rewrite existing fields. Do not touch `SpaceGraphNode`/`SpaceGraphEdge`/`SpaceGraph`.

Verify with `cargo check -p cortex` before moving on. Add a unit test in the existing `#[cfg(test)]` mod (create the mod if none exists at end of file) that constructs a Space with only the pre-Phase-9 fields via `serde_json::from_str` on a JSON literal missing the four new keys, and asserts the four new fields deserialize to their defaults (`None`, `false`, `None`, `None`).
  </action>
  <verify>
    <automated>cargo test -p cortex --lib types::tests::space_deserialize_backwards_compat -- --nocapture</automated>
  </verify>
  <done>Space struct has 4 new fields, all `#[serde(default)]`; backwards-compat deserialize test passes; `cargo check -p cortex` clean.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror Space extension in TypeScript + mock data</name>
  <files>client/lib/types.ts, client/lib/mock-data.ts</files>
  <action>
In `client/lib/types.ts`, extend the `Space` interface (currently lines 117-127) with four new optional fields matching the Rust struct after serde camelCase conversion:
- `description?: string`
- `userLocked?: boolean`
- `canonicalEntityHint?: string`
- `labelStatus?: 'ready' | 'generating'`

Keep field ordering (existing fields first, then Phase 9 additions), keep the file's existing export style, do not rewrite other interfaces.

In `client/lib/mock-data.ts`, extend `mockSpaces` so that:
- At least one mock space carries a `description` (used for tooltip verification in Plan 06).
- At least one mock space has `userLocked: true` (drives lock icon in Plan 06/07).
- At least one mock space has `canonicalEntityHint: "Person: Alex Doe"` (drives entity hint chip in Plan 06).
- At least one mock space has `labelStatus: 'generating'` (drives shimmer state in Plan 06).

Use a mixture across the existing mock entries — do NOT invent new mock spaces. Preserve mockSpaces ordering. This is the ONLY place mock-data.ts is modified in Phase 9; downstream plans must not re-edit this file (per D-14 discretion).

Frontend backwards-compat: any consumer treating `labelStatus` as absent must render 'ready' — see 09-UI-SPEC.md §"Data Type Extensions".
  </action>
  <verify>
    <automated>bun run --cwd . tsc --noEmit -p client/tsconfig.json 2>&1 | grep -v "^$" | head -20 ; test $? -eq 0 || (echo "typecheck failed"; exit 1)</automated>
  </verify>
  <done>`Space` interface has 4 new optional fields; at least one mock demonstrates each new field state; `tsc --noEmit` clean; existing pages compile without changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust ↔ IPC bridge | Serde camelCase rename crosses here — new fields must not confuse existing consumers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | Space serde compatibility | mitigate | `#[serde(default)]` on all 4 new fields prevents deserialization failure on legacy payloads (pitfall #3 in 09-RESEARCH.md) |
| T-09-02 | Information Disclosure | canonical_entity_hint payload | accept | Entity hint is same trust level as existing entity chips already surfaced; no PII escalation |
</threat_model>

<verification>
- `cargo check -p cortex` passes with the extended struct.
- `cargo test -p cortex --lib types::tests::space_deserialize_backwards_compat` passes.
- `tsc --noEmit -p client/tsconfig.json` passes.
- `grep -c 'description?: string' client/lib/types.ts` returns ≥ 1.
- Legacy JSON without the 4 new fields still deserializes into a valid Space with defaulted values.
</verification>

<success_criteria>
1. Rust `Space` struct has `description`, `user_locked`, `canonical_entity_hint`, `label_status`, all optional/`#[serde(default)]`.
2. TS `Space` interface mirrors the four fields in camelCase.
3. `mockSpaces` covers each of the four new field states across (at least) 4 entries.
4. Legacy Space payloads deserialize successfully (proven by the round-trip test).
</success_criteria>

<output>
Create `.planning/phases/09-llm-space-labeling/09-01-SUMMARY.md` when done, noting: the four fields added to each side, whether `#[serde(default)]` covered any pre-existing serialized state issues, and the specific mockSpaces indices that now demonstrate the new field states (for Plan 06's UI verification).
</output>

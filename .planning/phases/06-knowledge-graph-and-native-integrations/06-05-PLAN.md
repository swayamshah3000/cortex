---
phase: 06-knowledge-graph-and-native-integrations
plan: 05
type: execute
wave: 3
depends_on: [06-03, 06-04]
files_modified:
  - client/components/preview/FilePreview.tsx
  - client/components/preview/PdfPreview.tsx
  - client/components/preview/ImagePreview.tsx
  - client/components/preview/TextPreview.tsx
  - client/components/preview/MarkdownPreview.tsx
  - client/components/preview/SizeGuardCard.tsx
  - client/components/preview/UnsupportedPreview.tsx
  - client/hooks/usePreview.ts
  - client/hooks/useTauri.ts
  - client/pages/DocumentPage.tsx
  - client/global.css
autonomous: true
requirements: [PAGE-13, UX-06]
user_setup: []
tags: [file-preview, asset-protocol, pdf, markdown, react-markdown, document-page]
must_haves:
  truths:
    - "PDF documents render in-app via WebView's native PDF viewer (iframe + convertFileSrc) with zoom/scroll/find from the browser-provided UI"
    - "PNG/JPG documents render via img with convertFileSrc(path) as src"
    - "Plain text and CSV render in a monospace pre block populated by read_document_text IPC"
    - "Markdown renders via react-markdown + remark-gfm (NO rehype-raw, NO custom urlTransform — safe by default)"
    - "Size guard: PDFs > 50 MB, images > 20 MB, text/markdown > 5 MB show a SizeGuardCard with Load preview + Open in default app buttons"
    - "DocumentPage's existing 200-char excerpt block is replaced with FilePreview"
    - "DocumentPage header gains Open in default app (primary) + Reveal in Finder (secondary) buttons; the dead Open-in-Finder placeholder is removed"
    - "Entity chips on DocumentPage become clickable Links navigating to /entities/{canonicalId} (fallback to encodeURIComponent(value) when canonicalId is null)"
    - "FilePreview dispatcher switches over doc.docType: pdf | png/jpg | md | txt/csv | default to UnsupportedPreview"
    - "usePreview hook fetches read_document_text via React Query for text/markdown/csv types only"
  artifacts:
    - path: "client/components/preview/FilePreview.tsx"
      provides: "Dispatcher by docType"
      contains: "FilePreview"
    - path: "client/components/preview/PdfPreview.tsx"
      provides: "iframe + convertFileSrc + size guard"
      contains: "convertFileSrc"
    - path: "client/components/preview/MarkdownPreview.tsx"
      provides: "react-markdown + remark-gfm rendering"
      contains: "ReactMarkdown"
    - path: "client/hooks/usePreview.ts"
      provides: "React Query hook for text/markdown content fetch"
      contains: "usePreview"
    - path: "client/pages/DocumentPage.tsx"
      provides: "Slots FilePreview in place of excerpt; adds Open/Reveal header buttons; entity chips become Links"
      contains: "FilePreview"
  key_links:
    - from: "client/components/preview/PdfPreview.tsx"
      to: "@tauri-apps/api/core::convertFileSrc"
      via: "asset protocol URL"
      pattern: "convertFileSrc"
    - from: "client/components/preview/MarkdownPreview.tsx"
      to: "react-markdown"
      via: "ReactMarkdown component"
      pattern: "from \"react-markdown\""
    - from: "client/hooks/usePreview.ts"
      to: "read_document_text IPC"
      via: "tauriInvoke DocumentTextPreview"
      pattern: "read_document_text"
    - from: "client/pages/DocumentPage.tsx"
      to: "FilePreview"
      via: "replaces excerpt block"
      pattern: "FilePreview"
---

<objective>
Deliver in-app file preview (PAGE-13) and finish the Open-in-OS surfaces (UX-06 DocumentPage header buttons). After this plan, opening any indexed PDF / image / text / markdown document in DocumentPage shows the actual file content, not a 200-char excerpt; and Open in default app / Reveal in Finder are reachable from the document detail header in addition to the context menu wired in Plan 04.

Purpose:
- PAGE-13 — D-13 PDF via asset protocol, D-14 image/text/markdown renderers, D-15 size guard, D-16 usePreview hook
- UX-06 — D-18 DocumentPage header buttons (the context-menu portion already lives in Plan 04)
- Entity chips on DocumentPage become Links so Plan 06's /entities/:id route has its primary nav source

Output:
- 7 new preview components under client/components/preview/
- usePreview hook + read_document_text queryKey in useTauri.ts
- DocumentPage modifications: header buttons + FilePreview slot + clickable entity chips
- All renderers respect size guards
- Markdown XSS-safe by default
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-knowledge-graph-and-native-integrations/06-CONTEXT.md
@.planning/phases/06-knowledge-graph-and-native-integrations/06-RESEARCH.md
@.planning/phases/06-knowledge-graph-and-native-integrations/06-PATTERNS.md
@.planning/phases/06-knowledge-graph-and-native-integrations/06-UI-SPEC.md
@.planning/phases/06-knowledge-graph-and-native-integrations/06-03-SUMMARY.md
@.planning/phases/06-knowledge-graph-and-native-integrations/06-04-SUMMARY.md
@CLAUDE.md
@client/pages/DocumentPage.tsx
@client/hooks/useTauri.ts
@client/lib/types.ts
@client/lib/tauri.ts
@client/components/documents/DocumentContextMenu.tsx
@client/global.css

<interfaces>
Key types/utilities required by this plan:

From Plan 04 (client/lib/types.ts):
- interface DocumentTextPreview { text: string | null; truncated: boolean; size: number; }
- Document.extractedEntities[] elements include optional canonicalId?: string
- Document.docType union type "pdf" | "docx" | "txt" | "png" | "jpg" | "xlsx" | "csv" | "md" | "other"

From Plan 03 (Rust IPC):
- read_document_text command (doc_id, max_bytes) returning DocumentTextPreview; server-side 5 MB hard cap

From Plan 01 (npm deps):
- react-markdown ^10.1.0 (default export ReactMarkdown)
- remark-gfm ^4.0.1
- @tauri-apps/api/core::convertFileSrc(path) returns asset://localhost/... URL
- @tauri-apps/plugin-opener::openPath, revealItemInDir

From client/components/documents/DocumentContextMenu.tsx (Plan 04 — reuse for the toast pattern):
- Already implements openPath/revealItemInDir wrappers with toast on failure. DocumentPage's header buttons mirror the same toast.error copy.

From client/hooks/useTauri.ts (existing — extend in place):
- queryKeys factory; useDocument(id) pattern at lines 135-146 is the analog for usePreview.
- tauriInvoke generic helper with browser-mode fallback signature.

Size limits (from D-15 + UI-SPEC):
- PDF_SIZE_LIMIT = 50 * 1024 * 1024 (50 MB)
- IMAGE_SIZE_LIMIT = 20 * 1024 * 1024 (20 MB)
- TEXT_SIZE_LIMIT = 5 * 1024 * 1024 (5 MB)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 7 preview components + usePreview hook + queryKeys.documentText</name>
  <files>client/components/preview/FilePreview.tsx, client/components/preview/PdfPreview.tsx, client/components/preview/ImagePreview.tsx, client/components/preview/TextPreview.tsx, client/components/preview/MarkdownPreview.tsx, client/components/preview/SizeGuardCard.tsx, client/components/preview/UnsupportedPreview.tsx, client/hooks/usePreview.ts, client/hooks/useTauri.ts, client/global.css</files>
  <read_first>
    - client/hooks/useTauri.ts (existing queryKeys factory lines 45-60 + useDocument pattern lines 135-146)
    - client/lib/types.ts (Document, DocumentTextPreview)
    - client/lib/tauri.ts (isTauri, tauriInvoke signatures)
    - client/components/documents/DocumentContextMenu.tsx (toast.error copy + revealLabel approach to mirror)
    - client/global.css (check whether @plugin "@tailwindcss/typography" is already enabled — if not, this plan adds it once)
    - .planning/phases/06-knowledge-graph-and-native-integrations/06-RESEARCH.md §Pattern 2 (asset protocol), §Code Examples > Example 4 (PdfPreview iframe + size guard), §Example 5 (MarkdownPreview with GFM), §File Preview Architecture (per-renderer notes lines 1157-1190), §Markdown Pipeline (XSS posture)
    - .planning/phases/06-knowledge-graph-and-native-integrations/06-UI-SPEC.md §Per-component visual spec for FilePreview, PdfPreview, ImagePreview, TextPreview, MarkdownPreview, SizeGuardCard, UnsupportedPreview (lines 537-617)
    - .planning/phases/06-knowledge-graph-and-native-integrations/06-PATTERNS.md §FilePreview, §PdfPreview/ImagePreview/TextPreview/MarkdownPreview, §SizeGuardCard, §UnsupportedPreview, §client/hooks/usePreview.ts, §Shared Pattern E (Browser-dev fallback)
  </read_first>
  <behavior>
    - Test 1: FilePreview dispatcher — given doc.docType="pdf", renders PdfPreview; "png" or "jpg" renders ImagePreview; "md" renders MarkdownPreview; "txt" or "csv" renders TextPreview; "xlsx" or "docx" renders UnsupportedPreview.
    - Test 2: PdfPreview — when doc.size > 50_000_000 AND forceLoad=false, renders SizeGuardCard (not the iframe). Clicking the Load preview CTA flips forceLoad to true and renders the iframe.
    - Test 3: ImagePreview — when doc.size > 20_000_000 AND not forced, renders SizeGuardCard; otherwise renders an img with convertFileSrc(doc.path) as src.
    - Test 4: TextPreview — calls usePreview(doc.id); on isLoading=true shows centered Loader2 + caption "Reading file…"; on success renders text in a pre; on isError shows AlertCircle + caption "Could not read file" + Retry button calling refetch.
    - Test 5: MarkdownPreview XSS — given input "# Hello\n\n<script>alert(1)</script>", the rendered DOM contains heading "Hello" AND the literal text "<script>alert(1)</script>" as plain text; container.querySelector('script') returns null (no executable script element).
    - Test 6: SizeGuardCard — primary CTA invokes onLoad; secondary invokes onOpenExternal.
    - Test 7: UnsupportedPreview — primary CTA invokes openPath(doc.path); secondary invokes revealItemInDir(doc.path). Body copy includes the file extension parsed from doc.name.
    - Test 8: usePreview(docId) dispatches React Query for ["documents", docId, "text"] and invokes tauriInvoke read_document_text with { docId, maxBytes: 5_242_880 }. Verified via mock tauriInvoke.
    - Test 9 (Browser-dev fallback per Shared Pattern E): When isTauri() returns false, PdfPreview renders an UnsupportedPreview-style placeholder; ImagePreview likewise. No call to convertFileSrc made in browser mode.
  </behavior>
  <action>
Implement 7 preview components under client/components/preview/. Each is a default-named export with prop signature defined in UI-SPEC. Each must use ONLY existing design tokens (bg-bg-primary, text-text-primary, accent-primary, border-border-secondary, etc.) and existing shadcn primitives (no new color hexes).

FilePreview.tsx — dispatcher per UI-SPEC §FilePreview spec. Switch over doc.docType per the case mapping in interfaces above. Default to UnsupportedPreview.

PdfPreview.tsx — props { doc: Document }. Local state forceLoad: boolean (default false). Constants: PDF_SIZE_LIMIT = 50 * 1024 * 1024. Browser-dev guard: if !isTauri() return UnsupportedPreview (or a simple placeholder div with copy "PDF preview requires the desktop app."). Size guard: if doc.size > PDF_SIZE_LIMIT && !forceLoad return SizeGuardCard with sizeMB rounded, onLoad=() => setForceLoad(true), onOpenExternal=() => handleOpenExternal(doc.path). Otherwise render a div with classes "h-full w-full bg-bg-primary" containing an iframe with src=convertFileSrc(doc.path), className "w-full h-full border-0", title=doc.name. handleOpenExternal wraps openPath in try/catch with toast.error copy from UI-SPEC §Error state copy ("Could not open file. Open it manually from the file manager.").

ImagePreview.tsx — analogous structure. IMAGE_SIZE_LIMIT = 20 * 1024 * 1024. On img onError, swap to UnsupportedPreview with copy "Could not display image" per UI-SPEC §States. Wrapper "h-full w-full flex items-center justify-center bg-bg-primary p-6"; img has classes "max-w-full max-h-full object-contain rounded-md shadow-md".

TextPreview.tsx — calls const { data, isLoading, isError, refetch } = usePreview(doc.id). Browser fallback: render a placeholder when !isTauri(). Loading state: centered Loader2 16 with animate-spin + caption "Reading file…" in text-text-secondary. Error state: AlertCircle 24 in text-red-400 + caption "Could not read file" + a Retry button invoking refetch. Empty state (data.text === ""): FileText 24 in text-text-tertiary + caption "Empty file". Success: pre block with classes "font-mono text-sm whitespace-pre-wrap p-6 text-text-primary leading-relaxed" containing data.text. TEXT_SIZE_LIMIT = 5 * 1024 * 1024 — if doc.size exceeds, render SizeGuardCard before calling usePreview (defer the network request).

MarkdownPreview.tsx — same usePreview + size guard pattern. On success: render a div with classes "prose prose-invert dark:prose-invert max-w-none p-6" containing ReactMarkdown with remarkPlugins={[remarkGfm]} and data.text as the child. CRITICAL: do NOT add rehype-raw plugin. Do NOT add custom urlTransform prop. Default react-markdown HTML-escape behavior is what blocks XSS — keep it. Imports: ReactMarkdown from "react-markdown" (default export); remarkGfm from "remark-gfm".

In client/global.css: read first; if line "@plugin \"@tailwindcss/typography\"" is NOT present, add it once after the existing @import lines and before the @theme block. The Tailwind v4 CSS-first config syntax requires this plugin directive for prose classes to render. This is the only global.css edit allowed in this plan.

SizeGuardCard.tsx — props { sizeMB: number; onLoad: () => void; onOpenExternal: () => void }. Outer wrapper classes "h-full w-full flex items-center justify-center bg-bg-primary p-6". Inner card wrapper classes "card p-8 max-w-md text-center space-y-4". FileWarning icon 32 in text-amber-400. Heading "Large file" with section-header class. Body "This file is {sizeMB} MB. Preview may take a moment to load." in text-text-secondary text-sm. Primary button "Load preview" with btn-primary classes invoking onLoad. Secondary button "Open in default app" with btn-secondary classes invoking onOpenExternal.

UnsupportedPreview.tsx — props { doc: Document }. FileQuestion icon 32 in text-text-tertiary. Heading "Preview not supported". Body "Cortex does not preview .{ext} files yet. Open in the default app to view." where ext is parsed from doc.name (last dot segment, lowercased). Primary CTA "Open in default app" invoking openPath(doc.path) wrapped in try/catch + toast.error. Secondary CTA with revealLabel() text invoking revealItemInDir(doc.path) wrapped likewise.

revealLabel() helper: if navigator.userAgent contains "Mac" return "Reveal in Finder", else return "Show in file manager". If client/components/documents/DocumentContextMenu.tsx already exports this helper (verify by reading), import it; else duplicate inline at the top of UnsupportedPreview.tsx.

client/hooks/usePreview.ts — implement per PATTERNS.md §client/hooks/usePreview.ts. Imports: useQuery from @tanstack/react-query, tauriInvoke from @/lib/tauri, queryKeys from "./useTauri", DocumentTextPreview from @/lib/types. Single export usePreview(documentId: string) returning useQuery({ queryKey: queryKeys.documentText(documentId), queryFn: () => tauriInvoke<DocumentTextPreview>("read_document_text", { docId: documentId, maxBytes: 5 * 1024 * 1024 }, () => ({ text: "(mock preview text)", truncated: false, size: 100 })), enabled: Boolean(documentId) }).

client/hooks/useTauri.ts — add a single queryKey to the queryKeys factory: `documentText: (id: string) => ["documents", id, "text"] as const`. Preserve every existing key byte-for-byte. This is the ONLY change in useTauri.ts in this plan; Plan 06 ships the entity-specific keys + hooks.

Tests: create one test file per component under client/components/preview/*.test.tsx and one for client/hooks/usePreview.test.tsx. Tests cover the behaviors above. Mock @tauri-apps/api/core (convertFileSrc), @tauri-apps/plugin-opener (openPath, revealItemInDir), @/lib/tauri (isTauri), and the tauriInvoke generic. Follow the existing test setup conventions from client/pages/WatchedPage.test.tsx (Plan 04).
  </action>
  <verify>
    <automated>bun run test --run client/components/preview client/hooks/usePreview 2>&1 | tail -60; bun run tsc --noEmit 2>&1 | head -40; for f in FilePreview PdfPreview ImagePreview TextPreview MarkdownPreview SizeGuardCard UnsupportedPreview; do test -f client/components/preview/$f.tsx && echo OK_$f || echo FAIL_$f; done; test -f client/hooks/usePreview.ts && echo OK_usePreview_hook || echo FAIL_usePreview_hook; grep -q convertFileSrc client/components/preview/PdfPreview.tsx && echo OK_pdf_convertFileSrc || echo FAIL_pdf_convertFileSrc; grep -q remarkGfm client/components/preview/MarkdownPreview.tsx && echo OK_gfm || echo FAIL_gfm; grep -E 'rehype-raw|rehypeRaw|dangerouslySetInnerHTML' client/components/preview/MarkdownPreview.tsx >/dev/null && echo FAIL_rehype_raw_present || echo OK_no_rehype_raw; grep -q read_document_text client/hooks/usePreview.ts && echo OK_ipc_name || echo FAIL_ipc_name; grep -q 'documentText:' client/hooks/useTauri.ts && echo OK_querykey_added || echo FAIL_querykey_missing; grep -q '@plugin "@tailwindcss/typography"' client/global.css && echo OK_typography_enabled || echo NOTE_typography_check_manually</automated>
  </verify>
  <acceptance_criteria>
    - All 7 preview files exist at the paths listed in files_modified
    - client/hooks/usePreview.ts contains literal substrings useQuery, read_document_text, DocumentTextPreview
    - client/hooks/useTauri.ts queryKeys factory contains literal substring `documentText:` (with the trailing colon)
    - client/components/preview/PdfPreview.tsx contains substrings convertFileSrc, the constant 50 * 1024 * 1024 (or 50_000_000), the literal iframe element tag opener, and forceLoad
    - client/components/preview/ImagePreview.tsx contains substrings convertFileSrc, 20 * 1024 * 1024 (or 20_000_000), and an img tag
    - client/components/preview/MarkdownPreview.tsx contains substrings from "react-markdown" and remarkGfm. The substrings rehype-raw, rehypeRaw, dangerouslySetInnerHTML must NOT appear (grep -E count = 0). This enforces the XSS posture.
    - client/components/preview/TextPreview.tsx contains substring usePreview and renders a pre element
    - client/components/preview/SizeGuardCard.tsx contains substrings "Load preview", "Open in default app", FileWarning
    - client/components/preview/UnsupportedPreview.tsx contains substrings openPath, revealItemInDir, "Preview not supported"
    - bun run test --run client/components/preview client/hooks/usePreview exits 0
    - The MarkdownPreview XSS regression test (Test 5) passes — container.querySelector('script') is null after rendering script-tag input
    - bun run tsc --noEmit exits 0
  </acceptance_criteria>
  <done>All 7 preview components implemented per UI-SPEC; usePreview hook fetches text content; markdown is XSS-safe by default; size guards in place for PDF/image/text/markdown; browser-dev fallback gracefully degrades.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DocumentPage — replace excerpt with FilePreview, add Open/Reveal header buttons, make entity chips clickable Links</name>
  <files>client/pages/DocumentPage.tsx</files>
  <read_first>
    - client/pages/DocumentPage.tsx (entire file — note lines 43-58 entityTypeIcon(), lines 121-130 title block, lines 133-144 excerpt block to REPLACE, lines 146-155 dead Open-in-Finder placeholder to DELETE, lines 259-274 inline entity rendering to REPLACE with chip-as-Link cluster)
    - client/components/preview/FilePreview.tsx (created in Task 1 — what we slot in)
    - client/components/documents/DocumentContextMenu.tsx (Plan 04 — for the openPath/revealItemInDir toast pattern and revealLabel helper to mirror or import)
    - .planning/phases/06-knowledge-graph-and-native-integrations/06-UI-SPEC.md §Route 3: /document/:id (MODIFIED) — full spec for layout, header buttons, entity chips, Document header preview area diagram
    - .planning/phases/06-knowledge-graph-and-native-integrations/06-PATTERNS.md §client/pages/DocumentPage.tsx (all four edit points)
  </read_first>
  <behavior>
    - Test 1: Clicking the "Open in default app" button in DocumentPage header calls openPath(doc.path) (mock plugin-opener). Failure path shows toast.error with copy "Could not open file. Open it manually from the file manager.".
    - Test 2: Clicking "Reveal in Finder" calls revealItemInDir(doc.path). Label changes to "Show in file manager" when navigator.userAgent does not contain "Mac".
    - Test 3: The excerpt block (the existing div with class "rounded-lg bg-bg-secondary border border-border-primary p-4" containing "Content Preview" heading and the 200-char excerpt) is no longer in the DOM; in its place a FilePreview component renders for the loaded doc.
    - Test 4: Entity chips render as Link elements with to=/entities/{canonicalId}. Each chip shows the entity-type icon mapping per UI-SPEC §Entity-type color palette (Organization uses Building2 with amber-400; Email uses Mail with cyan-400; Date/Amount/Person/Location colors unchanged).
    - Test 5: When canonicalId is null/undefined for an entity (backfill not yet run), the chip falls back to to=/entities/{encodeURIComponent(entity.value)}. Route still resolves; Plan 06 EntityDetailPage handles unknown-id state.
    - Test 6: The dead Open-in-Finder placeholder button (lines 146-155 of original DocumentPage.tsx, including the comment "Open in Finder placeholder" if present) is removed from the file.
  </behavior>
  <action>
Make four surgical edits in client/pages/DocumentPage.tsx per PATTERNS.md §client/pages/DocumentPage.tsx.

Edit 1 — REPLACE the excerpt block (current lines ~133-144 — the wrapping div containing the "Content Preview" heading and {doc.excerpt} text) with a single FilePreview element. The wrapping div with classes rounded-lg, bg-bg-secondary, border, border-border-primary, p-4 is removed because FilePreview owns its own frame per UI-SPEC §Per-renderer notes. The section heading "Content Preview" is removed per UI-SPEC §Route 3 item 1 ("the preview IS the content"). Add the import at the top: from @/components/preview/FilePreview, named import FilePreview.

Edit 2 — DELETE the dead Open-in-Finder placeholder (current lines ~146-155 — a no-op button described in PATTERNS.md as the "Existing Open in Finder placeholder"). Remove the entire JSX block. If a stale handler const (e.g., handleOpenInFinder = () => {}) exists, remove it too.

Edit 3 — ADD Open / Reveal header buttons in the preview-pane header area, positioned AFTER the path display and BEFORE the FilePreview slot per UI-SPEC §Document header preview area diagram. The two-button group is a flex container with gap-2. Primary button "Open in default app" uses btn-primary styling matching WatchedPage.tsx Add Folder CTA (inline-flex, items-center, gap-1.5, px-3, py-1.5, bg-accent-primary, text-white, rounded-lg, hover:bg-accent-hover, transition-colors, text-sm, font-medium); icon ExternalLink size 14. Secondary button (revealLabel() result) uses ghost/secondary styling (inline-flex, items-center, gap-1.5, px-3, py-1.5, border, border-border-primary, text-text-secondary, rounded-lg, hover:bg-bg-tertiary, transition-colors, text-sm, font-medium); icon FolderOpen size 14.

handleOpen async function: if !isTauri() return; try { await openPath(doc.path) } catch { toast.error("Could not open file. Open it manually from the file manager.") }. Mirrors DocumentContextMenu.tsx pattern.

handleReveal async function: same shape with revealItemInDir + toast copy "Could not reveal file in {Finder/file manager}." (use revealLabel() output to choose suffix). 

revealLabel(): if navigator.userAgent contains "Mac" return "Reveal in Finder" else "Show in file manager". If DocumentContextMenu.tsx exports this helper from Plan 04, import it; otherwise duplicate inline. Acceptable duplication for a single line.

Add imports at the top: openPath, revealItemInDir from @tauri-apps/plugin-opener; ExternalLink, FolderOpen from lucide-react; toast from sonner; isTauri from @/lib/tauri.

Edit 4 — REPLACE entity chip rendering (current lines ~259-274 — the iteration `doc.extractedEntities?.map(...)` producing inline rows with entityTypeIcon + label + value). REPLACE the map body so each entity renders as a Link with these attributes: to=`/entities/${e.canonicalId ?? encodeURIComponent(e.value)}`, aria-label=`Entity: ${e.value}, ${e.entityType}`, className uses the entity-chip token set per UI-SPEC §EntityChip (inline-flex, items-center, gap-1.5, px-2.5, py-1, rounded-full, border, border-border-secondary, bg-bg-tertiary, hover:bg-accent-subtle, focus-visible:ring-2, focus-visible:ring-accent-primary, focus-visible:ring-offset-2, focus-visible:outline-none, transition-colors). Inside the Link: entityTypeIcon(e.entityType) followed by a span containing e.value with classes text-sm, text-text-primary, truncate, max-w-[160px]. Wrap the mapped Links in an outer div with classes "flex flex-wrap gap-2".

Update the LOCAL entityTypeIcon() function (current lines ~43-58) per UI-SPEC §Entity-type color palette:
- organization case: change the icon component from Users to Building2 (color stays text-amber-400).
- ADD a new email case returning Mail icon with className text-cyan-400 (size 14).
- Keep existing date (Calendar text-blue-400), amount (DollarSign text-green-400), person (Users text-purple-400), location (MapPin text-red-400) mappings byte-for-byte.
- Update lucide-react imports: add Building2 and Mail to the import list at the top.

Plan 06 will extract entityTypeIcon to a shared EntityChip component; for THIS plan keep the helper inline in DocumentPage.tsx to avoid creating a coupling on Plan 06.

Create a test file client/pages/DocumentPage.test.tsx covering Tests 1-6 above. Mock @tauri-apps/plugin-opener (openPath, revealItemInDir), @/lib/tauri (isTauri), react-router (useNavigate or wrap with MemoryRouter), the useDocument hook output (return a fixture Document). Use the existing test patterns from client/pages/WatchedPage.test.tsx as the template.
  </action>
  <verify>
    <automated>bun run test --run client/pages/DocumentPage 2>&1 | tail -40; bun run tsc --noEmit 2>&1 | head -40; grep -q FilePreview client/pages/DocumentPage.tsx && echo OK_filepreview_slotted || echo FAIL_filepreview_missing; grep -q doc.excerpt client/pages/DocumentPage.tsx && echo FAIL_excerpt_present || echo OK_excerpt_gone; grep -q 'Content Preview' client/pages/DocumentPage.tsx && echo FAIL_content_preview_heading || echo OK_heading_gone; grep -q openPath client/pages/DocumentPage.tsx && echo OK_open_wired || echo FAIL_open_missing; grep -q revealItemInDir client/pages/DocumentPage.tsx && echo OK_reveal_wired || echo FAIL_reveal_missing; grep -q '/entities/' client/pages/DocumentPage.tsx && echo OK_entity_links || echo FAIL_no_entity_links; grep -q Building2 client/pages/DocumentPage.tsx && echo OK_org_icon || echo FAIL_org_icon; grep -q '"email"' client/pages/DocumentPage.tsx && echo OK_email_case || echo FAIL_email_case; grep -c 'Open in Finder placeholder' client/pages/DocumentPage.tsx | awk '{ if ($1 == 0) print "OK_placeholder_gone"; else print "FAIL_placeholder_still_present_" $1 }'</automated>
  </verify>
  <acceptance_criteria>
    - client/pages/DocumentPage.tsx contains literal substring FilePreview (used as a JSX element)
    - client/pages/DocumentPage.tsx does NOT contain literal substring doc.excerpt anywhere (the excerpt rendering is gone — grep -c == 0)
    - client/pages/DocumentPage.tsx does NOT contain literal substring "Content Preview" (the section heading is gone — grep -c == 0)
    - client/pages/DocumentPage.tsx contains literal substrings openPath and revealItemInDir (header buttons wired)
    - client/pages/DocumentPage.tsx contains literal substring /entities/ used inside a to= attribute (entity chips as Links)
    - client/pages/DocumentPage.tsx contains literal substring Building2 in the entityTypeIcon function body
    - client/pages/DocumentPage.tsx entityTypeIcon function contains a case for "email" (grep substring `"email"`)
    - client/pages/DocumentPage.tsx does NOT contain literal substring "Open in Finder placeholder" (grep -c == 0)
    - client/pages/DocumentPage.test.tsx exists with the 6 tests above
    - bun run test --run client/pages/DocumentPage exits 0
    - bun run tsc --noEmit exits 0
  </acceptance_criteria>
  <done>DocumentPage shows real preview, exposes Open in default app and Reveal in Finder in the header, and turns entity chips into clickable Links pointing to /entities/:id. The dead placeholder button is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem -> asset protocol | convertFileSrc(path) exposes arbitrary local files to the renderer; scope ["**"] from Plan 01 allows any path the user has indexed |
| read_document_text IPC -> file read | doc_id resolves to a path stored in metadata; backend reads the file via std::fs::read with a 5 MB cap |
| react-markdown -> rendered DOM | Untrusted markdown content (from indexed user files) flows into the markdown renderer; XSS risk if HTML escaping bypassed |
| DocumentPage onClick -> opener plugin | openPath/revealItemInDir invoked from header buttons; receives doc.path from server-validated metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-MD-XSS | Tampering / Injection | MarkdownPreview rendering arbitrary user markdown via react-markdown | mitigate | Default react-markdown HTML-escape posture; NO rehype-raw added; NO custom urlTransform; Test 5 regression asserts script tag is escaped to text. Acceptance criterion enforces no rehype-raw substring. |
| T-06-MD-URL | Tampering | react-markdown URL handling for [link](javascript:...) attacks | mitigate | react-markdown default urlTransform blocks javascript: URLs (per RESEARCH §Markdown Pipeline XSS posture point 2). We do not override urlTransform. |
| T-06-ASSET-LARGE | Denial of Service | Loading a 1 GB PDF via iframe asset URL crashes WebView | mitigate | PdfPreview SizeGuardCard at 50 MB (UI-SPEC); user must explicitly opt-in via Load preview. Same for ImagePreview at 20 MB. |
| T-06-TEXT-SIZE | Denial of Service | read_document_text on a multi-GB log file exhausts memory | mitigate | Frontend gate: TextPreview shows SizeGuardCard above 5 MB. Backend (Plan 03) enforces hard 5 MB cap regardless of caller-supplied maxBytes — defense in depth. |
| T-06-ASSET-PATH | Information Disclosure | asset:// URL leaks file path via dev tools network panel | accept | User indexed these paths intentionally; not a meaningful disclosure vs the already-visible doc.path field on DocumentPage. Documented. |
| T-06-IFRAME-NAV | Tampering | A PDF could attempt window.location navigation within the iframe | accept | WebView native PDF viewer is sandboxed; CSP frame-src 'self' asset: restricts cross-origin loads. Documented. |
| T-06-OPEN-FROM-HEADER | Elevation of Privilege | openPath from DocumentPage header receives doc.path from useDocument query | mitigate | doc.path originated from server-side document metadata indexed from user-watched folders. Same trust boundary as Plan 04's context menu. |
| T-06-CSP-PROSE-INLINE | Tampering | @tailwindcss/typography may inject inline styles for prose classes; current CSP allows style-src 'unsafe-inline' | accept | RESEARCH §Pattern 2 documents this — required for Tailwind + react-markdown inline styling. Accepted trade-off. |
</threat_model>

<verification>
- bun run test --run exits 0 across all new test files
- bun run tsc --noEmit exits 0
- bun tauri dev smoke: opening a PDF, an image, a .md file, a .txt file in DocumentPage each shows the appropriate renderer; clicking Open in default app and Reveal in Finder fire OS handlers
- A markdown file containing a script tag renders the literal text, NOT an executable script element
</verification>

<success_criteria>
- PAGE-13 — D-13 PDF iframe via asset protocol, D-14 image/text/markdown renderers, D-15 size guard, D-16 usePreview hook all implemented
- UX-06 — DocumentPage header now has Open in default app + Reveal in Finder (UX-06 fully complete combined with Plan 04 context menu)
- Entity chips clickable as Links to /entities/:id; pre-backfill fallback uses encodeURIComponent(value)
- Markdown XSS-safe; size guards prevent WebView freezes
- DocumentPage no longer renders the 200-char excerpt block
</success_criteria>

<output>
Create .planning/phases/06-knowledge-graph-and-native-integrations/06-05-SUMMARY.md when done. Include:
- Confirmation that the PDF iframe rendered correctly in dev (smoke test result)
- Whether @tailwindcss/typography needed to be enabled in global.css
- Confirmation that bun run test --run shows 0 failures
- Note on T-06-MD-XSS acceptance — XSS regression test output
</output>

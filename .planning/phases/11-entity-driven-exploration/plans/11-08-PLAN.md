---
phase: 11-entity-driven-exploration
plan: 08
type: execute
wave: 4
depends_on: [11-01, 11-06, 11-07]
files_modified:
  - client/pages/EntityDetailPage11.tsx
  - client/App.tsx
autonomous: true
requirements: [ENEX-01]

must_haves:
  truths:
    - "Navigating to /entity/Person/Alex%20Shah renders a Phase 11 EntityDetailPage backed by get_entity_page_data"
    - "Page shows: header (class icon + value + alias count badge + doc count), aliases section, paginated docs (20/page), top-10 co-occurring entity chips"
    - "Pagination via ?page=N URL param; Previous / Next ghost buttons hidden when totalDocCount <= 20"
    - "Empty state per D-18 renders when totalDocCount === 0 with links to /watched and /settings"
    - "Loading state renders a header skeleton + 5 doc-row skeletons"
    - "IPC error surfaces an inline retry state and a sonner error toast"
    - "Phase 6 route /entities/:id remains registered — Phase 11 route /entity/:class/:value is additive per Open Q 2"
  artifacts:
    - path: "client/pages/EntityDetailPage11.tsx"
      provides: "New Phase 11 entity detail page component"
      contains: "useEntityPageData"
      min_lines: 120
    - path: "client/App.tsx"
      provides: "/entity/:class/:value route registered (Phase 6 /entities/:id preserved)"
      contains: "/entity/:class/:value"
  key_links:
    - from: "client/pages/EntityDetailPage11.tsx"
      to: "client/hooks/useTauri.ts::useEntityPageData"
      via: "class + value + page params drive the IPC call"
      pattern: "useEntityPageData"
    - from: "client/pages/EntityDetailPage11.tsx"
      to: "client/components/entities/EntityChip.tsx"
      via: "co-occurring entities rendered as EntityChips with dual-nav (left-click filters, right-click drills into another entity page)"
      pattern: "EntityChip"
---

<objective>
Ship the Phase 11 `/entity/:class/:value` route and its `EntityDetailPage` per UI-SPEC §6 + §7. Header + aliases + paginated docs + co-occurring entities; loading, empty (D-18), and error states covered. Named `EntityDetailPage11.tsx` per 11-RESEARCH.md project structure so the existing Phase 6 `EntityDetailPage.tsx` (route `/entities/:id`, canonical-id-keyed) survives untouched per Open Q 2 resolution.

Purpose: Complete ENEX-01 — right-click on any EntityChip lands here.
Output: One new page component + one route registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-entity-driven-exploration/11-CONTEXT.md
@.planning/phases/11-entity-driven-exploration/11-UI-SPEC.md
@.planning/phases/11-entity-driven-exploration/11-RESEARCH.md

@client/App.tsx
@client/pages/EntityDetailPage.tsx
@client/pages/DocumentPage.tsx
@client/components/entities/EntityChip.tsx
@client/components/ui/skeleton.tsx

<interfaces>
<!-- Route registration site (VERIFIED: client/App.tsx lines 51-52): -->
<Route path="/entities" element={<EntitiesPage />} />
<Route path="/entities/:id" element={<EntityDetailPage />} />
// Phase 11 adds AFTER these lines:
// <Route path="/entity/:class/:value" element={<EntityDetailPage11 />} />

<!-- New hook available from Plan 11-07: -->
useEntityPageData(cls, value, page) -> UseQueryResult<EntityPageData>
// Data shape (Plan 11-01):
interface EntityPageData {
  canonical: CanonicalEntity;
  documents: Document[];
  totalDocumentCount: number;
  coOccurringEntities: RelatedEntityRef[];  // { class, value, coDocCount }
  page: number;
  pageSize: number;
}

<!-- Class icon map: reuse getIconForClass from EntityChip if exported, otherwise inline a switch. -->
<!-- Phase 8 8-class list: Person, Organization, Location, Date, Amount, Email, Phone, Identifier. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EntityDetailPage11.tsx + register /entity/:class/:value route</name>
  <files>client/pages/EntityDetailPage11.tsx, client/App.tsx</files>
  <action>
Create `client/pages/EntityDetailPage11.tsx` implementing UI-SPEC §6 and §7. Structure:

Imports:
- `useParams, useSearchParams, Link` from `react-router-dom`
- `useEntityPageData` from `../hooks/useTauri`
- `EntityChip` from `../components/entities/EntityChip`
- `Skeleton` from `../components/ui/skeleton`
- `Badge` from `../components/ui/badge`
- Lucide icons: `FileText, Folder, Settings, ChevronLeft, ChevronRight`, plus the Phase 8 class icons (Users, Building2, MapPin, Calendar, DollarSign, Mail, Phone, Fingerprint, Tag) for the header icon
- `toast` from `sonner`
- helpers `cn`, `formatRelativeTime`, `shortenPath` from existing lib/util modules (if `shortenPath`/`formatRelativeTime` do not exist yet, inline minimal versions — grep existing pages for prior usage first)

Component skeleton:
```
export default function EntityDetailPage11() {
  const { class: cls = "", value = "" } = useParams();
  const [searchParams, setSearchParams] = useSearchParams();
  const page = Math.max(0, parseInt(searchParams.get("page") ?? "0", 10) || 0);

  const decodedClass = decodeURIComponent(cls);
  const decodedValue = decodeURIComponent(value);

  const { data, isLoading, isError, error } = useEntityPageData(decodedClass, decodedValue, page);

  // Fire toast.error on error via useEffect on [isError, error]
  // Render:
  //   - Loading: header skeleton + 5 row skeletons
  //   - Error: inline error state + retry hint
  //   - Loaded, totalDocumentCount === 0: empty state per §7
  //   - Loaded, totalDocumentCount > 0: full page (header + aliases + docs + co-occurring)
}
```

Header (per UI-SPEC §6):
- 48x48 accent-subtle rounded square containing the class icon at 24px
- `<h1 className="page-title text-text-primary">{decodedValue}</h1>`
- If `canonical.aliases.length > 1` (canonical_name always in aliases, so > 1 means real aliases exist), render `<Badge variant="outline" className="text-xs text-text-tertiary">+{aliases.length - 1} alias(es)</Badge>` — pluralize per aria/copy rules in UI-SPEC.
- Sub-line: `<p className="text-sm text-text-secondary"><span className="capitalize">{decodedClass}</span> · {totalDocumentCount} document(s)</p>`.

Aliases section (only if aliases.length > 1):
- Section label `<div className="text-xs text-text-tertiary uppercase tracking-wider">Also known as</div>`
- Flex-wrap row of alias chips. Each chip: `<span className="inline-flex items-center bg-transparent border border-border-secondary rounded-full px-2 py-1 text-xs text-text-tertiary">{alias}</span>`. Non-interactive per UI-SPEC §6.

Documents section:
- Section header `<h2 className="text-lg font-semibold text-text-primary">Documents ({totalDocumentCount})</h2>`
- Map over `documents` array — each `<Link to={\`/document/${doc.id}\`} className="flex items-center gap-3 px-4 py-3 rounded-lg hover:bg-bg-secondary transition-colors border border-transparent hover:border-border-primary">` with FileText icon (16px), name (`text-sm font-medium truncate`), path (`text-xs text-text-tertiary font-mono truncate mt-1`), modified date on the right (`text-xs text-text-tertiary`).
- Pagination (hidden when `totalDocumentCount <= pageSize`): two ghost buttons "Previous" (disabled at page 0) and "Next" (disabled at last page). Center label `Page {page + 1} of {Math.ceil(totalDocumentCount / pageSize)}`. Buttons update `?page=N` via `setSearchParams`.

Co-occurring entities section:
- Section header `<h2 className="text-lg font-semibold text-text-primary">Related Entities</h2>`
- Flex-wrap row of `<EntityChip>` — for each `RelatedEntityRef` construct a minimal `entity` prop: `{ value: ref.value, entityType: ref.class.toLowerCase(), class: ref.class }`. Left-click filters `/search?entity=...` (EntityChip handles this from Plan 05); right-click drills into another EntityDetailPage11. Do NOT render `coDocCount` in the chip; UI-SPEC does not require it in the chip itself.

Loading state per UI-SPEC §6:
- Header skeleton: `<div className="flex items-center gap-4 animate-pulse"><Skeleton className="h-12 w-12 rounded-lg" /><div className="space-y-2"><Skeleton className="h-7 w-48" /><Skeleton className="h-4 w-32" /></div></div>`
- Doc list skeleton: `{Array.from({length:5}).map((_,i) => <Skeleton key={i} className="h-14 w-full rounded-lg" />)}`

Empty state per UI-SPEC §7:
- Centered layout at `min-h-[40vh]` with 64x64 bg-bg-secondary rounded-full containing the class icon at 32px
- Heading (18px semibold): `No documents mention {decodedClass}: {decodedValue}`
- Body (14px secondary, max-w-[360px]): `Try syncing your folders or connecting more provider data.`
- Two links: `/watched` "Manage watched folders" (with Folder icon 14px), `/settings` "Settings" (with Settings icon 14px), separated by a `text-text-tertiary` `·`

Error state:
- Full-page centered: heading "Could not load entity", body `{error?.message}. Try again.`, retry ghost button that calls `refetch` (from useQuery result — destructure it). Fire `toast.error(\`Could not load entity data. ${error.message}\`, {duration: 6000})` once on transition into error state.

Register the route: in `client/App.tsx`, add
```
import EntityDetailPage11 from "./pages/EntityDetailPage11";
...
<Route path="/entity/:class/:value" element={<EntityDetailPage11 />} />
```
INSIDE the AppShell parent `<Route element={<AppShell />}>` block. Place AFTER the existing `/entities/:id` route. Do NOT remove or reorder the Phase 6 route.

Add a basic Vitest test `client/pages/EntityDetailPage11.test.tsx`:
- Loading state: renders skeletons when `isLoading=true` (mock the hook via `vi.mock("../hooks/useTauri")`).
- Empty state: `totalDocumentCount=0` renders the "No documents" heading and links.
- Full state: `totalDocumentCount=3, documents.length=3` renders 3 document Links and the aliases section.

Pass acceptance means the route lands on the page, IPC error surfaces UI, and empty-state copy matches D-18 verbatim.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/cortex &amp;&amp; bunx tsc --noEmit -p tsconfig.json 2>&amp;1 | tail -10 &amp;&amp; bun test --run client/pages/EntityDetailPage11.test.tsx 2>&amp;1 | tail -20 &amp;&amp; rtk proxy rg "path=\"/entity/:class/:value\"" client/App.tsx -c &amp;&amp; rtk proxy rg "path=\"/entities/:id\"" client/App.tsx -c</automated>
  </verify>
  <done>EntityDetailPage11 exists with header + aliases + docs + co-occurring sections + loading/error/empty variants; App.tsx registers /entity/:class/:value while preserving /entities/:id; tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL -> get_entity_page_data | class and value are user-controllable path params |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-26 | XSS | class/value rendered as text in header + copy | mitigate | React JSX escapes text nodes; decoded via decodeURIComponent, no innerHTML |
| T-11-27 | Broken route | Missing class or value param crashes page | mitigate | useParams destructures with default empty strings; useEntityPageData `enabled` guard skips when either is falsy — page renders loading state harmlessly |
| T-11-28 | Info leakage | Aliases exposed to anyone with URL access | accept | Local desktop app; no remote user; single-user threat model per REQUIREMENTS.md |
</threat_model>

<verification>
- `bunx tsc --noEmit` exits 0.
- `bun test --run client/pages/EntityDetailPage11.test.tsx` all pass.
- `rg -c 'path="/entity/:class/:value"' client/App.tsx` == 1.
- `rg -c 'path="/entities/:id"' client/App.tsx` == 1 (Phase 6 route intact).
- `rg -c "useEntityPageData" client/pages/EntityDetailPage11.tsx` >= 1.
- Manual smoke: navigate to `/entity/Person/Test%20User`; loading skeleton renders briefly, then empty state (no docs) with correct copy.
</verification>

<success_criteria>
- Route `/entity/:class/:value` renders `EntityDetailPage11`.
- Page structure matches UI-SPEC §6: header, aliases, docs (paginated 20/page), co-occurring entities (top 10 as interactive EntityChips).
- Empty state matches D-18 copy verbatim and links to /watched + /settings.
- Loading and error states covered.
- Phase 6 `/entities/:id` route preserved (Open Q 2 resolution).
- Vitest coverage for loading + empty + full render paths.
</success_criteria>

<output>
Create `.planning/phases/11-entity-driven-exploration/11-08-SUMMARY.md` when done.
</output>

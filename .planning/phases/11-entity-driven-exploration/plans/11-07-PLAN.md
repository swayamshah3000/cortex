---
phase: 11-entity-driven-exploration
plan: 07
type: execute
wave: 4
depends_on: [11-01, 11-03, 11-04, 11-05, 11-06]
files_modified:
  - client/hooks/useTauri.ts
  - client/pages/SearchPage.tsx
  - client/components/search/ScoreBadge.tsx
  - client/components/search/EntityFilterPill.tsx
  - client/components/search/EntityFilterBar.tsx
  - client/components/search/SaveSearchDialog.tsx
autonomous: true
requirements: [ENEX-01, ENEX-02]

must_haves:
  truths:
    - "SearchPage reads ?entity= URL params and populates SearchFilters.entityFilters — backend filters activate"
    - "Multi-entity URL params (?entity=A&entity=B) render as removable EntityFilterPill chips atop results with AND semantics"
    - "Removing a pill drops the corresponding entity param from the URL (SearchParams-driven, not local state)"
    - "'Save this search' button opens SaveSearchDialog, name required, submits via save_search IPC, sonner success toast on completion"
    - "React Query hooks: useSavedSearches, useSaveSearch, useDeleteSavedSearch, useSavedSearchCounts, useRelatedDocsScored, useEntityPageData all present and typed"
    - "ScoreBadge extracted to standalone component and consumed by SearchPage's existing result rows without visual regression"
  artifacts:
    - path: "client/hooks/useTauri.ts"
      provides: "6 new React Query hooks for Phase 11 IPCs"
      contains: "useSavedSearches"
    - path: "client/pages/SearchPage.tsx"
      provides: "URL-param entity filter, EntityFilterBar, Save button + SaveSearchDialog wiring, ScoreBadge import"
      contains: "useSearchParams"
    - path: "client/components/search/ScoreBadge.tsx"
      provides: "Standalone score badge (percentage, semantic color ranges — extracted from SearchPage)"
      contains: "export function ScoreBadge"
    - path: "client/components/search/EntityFilterPill.tsx"
      provides: "Removable accent pill for one entity filter"
      contains: "onRemove"
    - path: "client/components/search/EntityFilterBar.tsx"
      provides: "Row of EntityFilterPills + Clear all button"
      contains: "Clear all"
    - path: "client/components/search/SaveSearchDialog.tsx"
      provides: "shadcn Dialog for naming and saving current search"
      contains: "Save search"
  key_links:
    - from: "client/pages/SearchPage.tsx"
      to: "client/hooks/useTauri.ts::useDocumentSearch"
      via: "SearchFilters.entityFilters derived from URL params flows into search IPC"
      pattern: "entityFilters"
    - from: "client/components/search/SaveSearchDialog.tsx"
      to: "client/hooks/useTauri.ts::useSaveSearch"
      via: "Dialog submit calls mutation.mutateAsync + toast"
      pattern: "useSaveSearch"
    - from: "client/components/search/EntityFilterBar.tsx"
      to: "react-router-dom useSearchParams"
      via: "removeFilter mutates URL params directly (routing = source of truth)"
      pattern: "useSearchParams"
---

<objective>
Wire the frontend search experience for Phase 11: URL-param-driven entity filters on SearchPage, removable filter pill bar, the "Save this search" flow (button + shadcn Dialog + sonner toast), and the six React Query hooks that talk to the Wave 2/3 Rust IPCs. Extract the inline `ScoreBadge` from SearchPage into a standalone component so Plan 09's DocumentPage Related panel can reuse it.

Purpose: Deliver ENEX-01 UI (entity chip -> filtered SearchPage) and ENEX-02 UI (save current search). Backend IPCs from Plans 03/04/06 stand ready; this plan connects them.
Output: Six new hooks, extended SearchPage, three new search components + ScoreBadge extraction.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-entity-driven-exploration/11-CONTEXT.md
@.planning/phases/11-entity-driven-exploration/11-UI-SPEC.md
@.planning/phases/11-entity-driven-exploration/11-RESEARCH.md

@client/hooks/useTauri.ts
@client/pages/SearchPage.tsx
@client/lib/types.ts
@client/components/ui/dialog.tsx

<interfaces>
<!-- From Plan 11-01 (queryKeys already extended, VERIFIED post-Wave-1): -->
queryKeys.savedSearches;              // ["saved-searches"]
queryKeys.savedSearchCounts(ids);     // ["saved-searches", "counts", sortedJoined]
queryKeys.entityPage(cls, value, p);  // ["entity-page", cls, value, p]
queryKeys.relatedDocsScored(docId);   // ["documents", docId, "related-scored"]

<!-- From Plan 11-04 (IPC surface, VERIFIED): -->
IPC "get_saved_searches" -> Vec<SavedSearch>
IPC "save_search"({name, query, filters: SavedSearchFilters}) -> SavedSearch
IPC "delete_saved_search"({id}) -> void
IPC "get_saved_search_counts"({ids: string[]}) -> Record<string, number>

<!-- From Plan 11-06 (IPC surface, VERIFIED): -->
IPC "get_related_docs_scored"({docId, topN}) -> RelatedDocScored[]
IPC "get_entity_page_data"({class, value, page}) -> EntityPageData

<!-- Existing SearchPage state hooks (VERIFIED: client/pages/SearchPage.tsx lines 172-183): -->
const [query, setQuery] = useState("");
const [selectedDocType, setSelectedDocType] = useState<string | undefined>();
const [selectedSpaceId, setSelectedSpaceId] = useState<string | undefined>();
const debouncedQuery = useDebouncedValue(query, 150);
const filters: SearchFilters = useMemo(() => ({ docType, spaceId }), [selectedDocType, selectedSpaceId]);
const { data: results } = useDocumentSearch(debouncedQuery, filters);

<!-- Inline ScoreBadge in SearchPage.tsx lines 64-77 (to extract): -->
function ScoreBadge({ score }: { score: number }) {
  const pct = Math.round(score * 100);
  const color = pct >= 80 ? "text-green-400 bg-green-400/10" : pct >= 50 ? "text-amber-400 bg-amber-400/10" : "text-text-tertiary bg-bg-tertiary";
  return <span className={cn("text-xs font-mono px-2 py-0.5 rounded", color)}>{pct}%</span>;
}

<!-- sonner + shadcn Dialog usage (VERIFIED: client/components/ui/dialog.tsx exists; sonner in package.json 1.7.4): -->
import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter } from "@/components/ui/dialog";
import { toast } from "sonner";
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 6 React Query hooks to useTauri.ts</name>
  <files>client/hooks/useTauri.ts</files>
  <action>
Append to `client/hooks/useTauri.ts`. Import `SavedSearch`, `SavedSearchFilters`, `RelatedDocScored`, `EntityPageData` from `../lib/types` at the top (add to the existing import block, do not create a new one).

Add the following hooks. Every hook uses the existing `tauriInvoke` helper with a mock fallback that matches the shape (return `[]` for lists, `{}` for maps, `undefined` for void mutations). Follow the existing exemplar pattern from `useSpaceLabels` / `useRenameSpace` verbatim.

Hook list to add (mirror the style of `useSpaceLabels`, `useRenameSpace`, `useSpaceDocuments` already present in the file):
- `useSavedSearches()` — GET, key `queryKeys.savedSearches`, IPC `get_saved_searches`, mock `[]`, staleTime 30_000.
- `useSaveSearch()` — mutation, IPC `save_search` with req `{name, query, filters}`; on success invalidate `queryKeys.savedSearches` AND the count key prefix `["saved-searches", "counts"]` (pitfall #4 in 11-RESEARCH.md).
- `useDeleteSavedSearch()` — mutation, IPC `delete_saved_search` with `{id}`; on success invalidate the same two keys.
- `useSavedSearchCounts(ids)` — GET, key `queryKeys.savedSearchCounts(ids)`, IPC `get_saved_search_counts` with `{ids}`, staleTime 30_000 (D-08 TTL), enabled only when `ids.length > 0`.
- `useRelatedDocsScored(docId, topN=5)` — GET, key `queryKeys.relatedDocsScored(docId)`, IPC `get_related_docs_scored` with `{docId, topN}`, staleTime 5*60_000 (D-13 TTL), enabled when `docId` truthy.
- `useEntityPageData(cls, value, page=0)` — GET, key `queryKeys.entityPage(cls, value, page)`, IPC `get_entity_page_data` with `{class: cls, value, page}`, enabled when `cls && value`.

For `useEntityPageData`: the Rust param is named `class`; passing `class: cls` in a JS object literal is legal (`class` is not reserved as an object property key). Tauri v2 auto-serializes camelCase to snake_case where needed.

Mock fallbacks:
- `useSaveSearch` mock returns a SavedSearch with id `ss-mock-${Date.now()}`, echoing input, `docCountCache: 0`.
- `useEntityPageData` mock returns `{ canonical: {id:"mock", canonicalName:value, entityType:cls, aliases:[value], documentCount:0}, documents:[], totalDocumentCount:0, coOccurringEntities:[], page, pageSize:20 }`.

No new queryKeys entries — Plan 01 already added all four keys used above.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/cortex &amp;&amp; bunx tsc --noEmit -p tsconfig.json 2>&amp;1 | tail -10 &amp;&amp; rtk proxy rg "export function (useSavedSearches|useSaveSearch|useDeleteSavedSearch|useSavedSearchCounts|useRelatedDocsScored|useEntityPageData)" client/hooks/useTauri.ts -c</automated>
  </verify>
  <done>Six named exports added; tsc clean; each hook uses tauriInvoke with typed generic and a mock fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Extract ScoreBadge + create EntityFilterPill/EntityFilterBar/SaveSearchDialog</name>
  <files>client/components/search/ScoreBadge.tsx, client/components/search/EntityFilterPill.tsx, client/components/search/EntityFilterBar.tsx, client/components/search/SaveSearchDialog.tsx</files>
  <action>
Create four new component files.

(A) `client/components/search/ScoreBadge.tsx` — extract verbatim from `SearchPage.tsx` lines 64-77. Export as `ScoreBadge`. Add JSDoc noting the semantic color thresholds (>=80% green, >=50% amber, <50% neutral) so future edits do not lose the meaning. No visual change; no signature change.

(B) `client/components/search/EntityFilterPill.tsx` — per UI-SPEC §2. Props: `{ encodedParam: string; onRemove: (encodedParam: string) => void }`. Parse `encodedParam` by splitting on the first `:` to get `cls` + `value`. Render an inline-flex pill with `bg-accent-subtle text-accent-primary border-accent-primary/20`, a Phase 8 class icon at 10px (inline a small switch on cls returning the same icons used by EntityChip — do not import from EntityChip to keep the coupling loose), truncated `{cls}: {value}` text with `max-w-[140px]`, and a remove `<button type="button">` with:
- Lucide `X size={10}`
- aria-label `Remove ${cls}: ${value} filter`
- `style={{ minWidth: 44, minHeight: 44 }}` (44x44 touch target — UI-SPEC §2)
- className `ml-1 rounded-full p-1 hover:bg-accent-primary/20 focus-visible:ring-1 focus-visible:ring-accent-primary`

Button `onClick` calls `onRemove(encodedParam)`.

(C) `client/components/search/EntityFilterBar.tsx` — per UI-SPEC §2 wrapper. Props: `{ activeEntityFilters: string[]; onRemove: (encodedParam: string) => void; onClearAll: () => void }`. Return `null` when `activeEntityFilters.length === 0`. Otherwise render a `flex flex-wrap items-center gap-2` container with:
- `<span className="text-xs text-text-tertiary">Filtering by:</span>`
- one `<EntityFilterPill>` per active filter (key = encoded string)
- when `activeEntityFilters.length > 1`, a `<button type="button" className="text-xs text-text-tertiary hover:text-text-secondary transition-colors" onClick={onClearAll}>Clear all</button>`

(D) `client/components/search/SaveSearchDialog.tsx` — per UI-SPEC §3. Props: `{ open: boolean; onOpenChange: (open: boolean) => void; query: string; filters: SavedSearchFilters }`.

Imports: shadcn `Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter` from `@/components/ui/dialog`; `Input` from `@/components/ui/input`; `Label` from `@/components/ui/label`; `Button` from `@/components/ui/button`; `toast` from `sonner`; `Loader2` from `lucide-react`; `useSaveSearch` from `@/hooks/useTauri`; local state `useState`, `useEffect`.

Internal state: `const [name, setName] = useState("");`. Reset to empty string when `open` transitions false->true via a `useEffect` on `[open]`.

Submit handler: guard `!name.trim()`; call `saveSearchMutation.mutateAsync({name: name.trim(), query, filters})`; on success `toast.success(\`Search saved as "${saved.name}". Find it in the sidebar.\`, {duration: 4000})`, reset name, `onOpenChange(false)`; on error `toast.error(\`Failed to save search. ${err.message}\`, {duration: 6000})`.

Render exactly per UI-SPEC §3:
- `<DialogContent className="sm:max-w-[400px]">`
- DialogHeader with DialogTitle "Save search" and DialogDescription "Name this search to find it quickly in the sidebar."
- Body: `<div className="space-y-4 py-4">` -> label + Input. Input `id="search-name"`, `placeholder="e.g. Property Tax 2024"`, `autoFocus`, className includes `focus-visible:ring-accent-primary`, `onKeyDown={(e) => e.key === "Enter" && handleSave()}`.
- DialogFooter: two buttons — ghost "Keep searching" calls `onOpenChange(false)`; primary `className="btn-primary"` "Save search" `disabled={!name.trim() || saveSearchMutation.isPending}`. When pending, render an inline `<Loader2 size={14} className="animate-spin mr-2" />` before the button text.

Do NOT modify `SearchPage.tsx` in this task — Task 3 handles integration.

Verify build cleanly with tsc.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/cortex &amp;&amp; bunx tsc --noEmit -p tsconfig.json 2>&amp;1 | tail -10 &amp;&amp; ls client/components/search/ScoreBadge.tsx client/components/search/EntityFilterPill.tsx client/components/search/EntityFilterBar.tsx client/components/search/SaveSearchDialog.tsx 2>&amp;1 | grep -c tsx</automated>
  </verify>
  <done>Four new component files exist; tsc clean; SaveSearchDialog invokes useSaveSearch and sonner toasts; EntityFilterPill has 44x44 touch target on remove button; EntityFilterBar returns null when empty.</done>
</task>

<task type="auto">
  <name>Task 3: Wire SearchPage URL params + EntityFilterBar + Save button + Dialog</name>
  <files>client/pages/SearchPage.tsx</files>
  <action>
Edit `client/pages/SearchPage.tsx`:

(1) Delete the inline `ScoreBadge` function (lines 64-77) and replace all internal usages with an import: `import { ScoreBadge } from "../components/search/ScoreBadge";`. This is behavior-preserving.

(2) Add imports:
- `import { useSearchParams } from "react-router-dom";`
- `import { Bookmark } from "lucide-react";`
- `import { Button } from "../components/ui/button";`
- `import { EntityFilterBar } from "../components/search/EntityFilterBar";`
- `import { SaveSearchDialog } from "../components/search/SaveSearchDialog";`
- `import type { EntityClassFilter } from "../lib/types";`

(3) Inside the `SearchPage` component, after existing state:
- `const [searchParams, setSearchParams] = useSearchParams();`
- `const rawEntityParams = searchParams.getAll("entity");` — an array of strings like `["Person:Alex Doe", "Location:AlphaComplex"]`.
- A `useMemo` that parses each raw string into `EntityClassFilter` by splitting on the FIRST `:` (`indexOf`, then `slice(0, idx)` + `slice(idx + 1)`). Discard entries that lack a `:`.
- `removeEntityParam(encoded)` — mutates URL via `setSearchParams((prev) => { ... prev.delete("entity"); rest.forEach((v) => prev.append("entity", v)); return prev; })`.
- `clearAllEntityParams()` — `setSearchParams((prev) => { prev.delete("entity"); return prev; })`.
- `const [saveDialogOpen, setSaveDialogOpen] = useState(false);`

(4) Extend the existing `filters: SearchFilters = useMemo(...)` to add `entityFilters: entityFilters.length > 0 ? entityFilters : undefined,`. Include `entityFilters` in the dependency array.

(5) In the header row, replace the current `<div><h1>Search</h1><p>...</p></div>` with a `flex items-center justify-between` container: left side is the existing title + description; right side is a `<Button variant="secondary" size="sm" disabled={!debouncedQuery && entityFilters.length === 0} onClick={() => setSaveDialogOpen(true)}>` containing `<Bookmark size={14} />` and `Save this search`.

(6) Immediately after `<TopicFilterBar ... />`, insert `<EntityFilterBar activeEntityFilters={rawEntityParams} onRemove={removeEntityParam} onClearAll={clearAllEntityParams} />`.

(7) At the bottom of the JSX tree (sibling to `ResizablePanelGroup`, still inside the outermost wrapper), mount:
`<SaveSearchDialog open={saveDialogOpen} onOpenChange={setSaveDialogOpen} query={debouncedQuery} filters={{ entities: rawEntityParams, topic: selectedTopic, docType: selectedDocType, spaceId: selectedSpaceId }} />`

Do NOT touch the topic filter, result rendering loop, or preview panel — Phase 11 does not modify those.

(8) Create or extend `client/pages/SearchPage.test.tsx` with tests:
- URL `?entity=Person:Bob` -> exactly one EntityFilterPill rendered with text containing `Person: Bob`.
- Clicking the pill's X removes the entity param from the URL (assert via `useLocation().search` in a probe route).
- Save button is disabled when both query and rawEntityParams are empty.
- Save button becomes enabled once `?entity=Person:Bob` is present.

Use `MemoryRouter` with initialEntries; wrap in `QueryClientProvider` (mirror any existing test that uses `useDocumentSearch`).
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/cortex &amp;&amp; bunx tsc --noEmit -p tsconfig.json 2>&amp;1 | tail -10 &amp;&amp; bun test --run client/pages/SearchPage.test.tsx 2>&amp;1 | tail -20 &amp;&amp; rtk proxy rg "EntityFilterBar|SaveSearchDialog|useSearchParams" client/pages/SearchPage.tsx -c</automated>
  </verify>
  <done>SearchPage renders EntityFilterBar + Save button; URL-driven entity filters flow into the search IPC; SaveSearchDialog mounts and successfully submits via useSaveSearch; existing ScoreBadge usages behave identically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL -> SearchFilters | URL is user-controllable via manual edit or shared link; entity filter payload crosses to Rust IPC |
| Dialog -> save_search | User-supplied search name crosses IPC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-22 | Tampering | Malformed `?entity=` param (no colon) crashes parser | mitigate | `indexOf(":") === -1` -> discard; test covers this |
| T-11-23 | XSS | Entity value rendered as text | mitigate | React JSX escapes text nodes by default; no `dangerouslySetInnerHTML` used |
| T-11-24 | Injection | Long entity value bypasses truncate | accept | `max-w-[140px] truncate` visually caps; no security impact |
| T-11-25 | UX bug | Save button enabled with empty query but active entity filter | accept | Deliberate per D-09: entity-only filters are valid searches to save |
</threat_model>

<verification>
- `bunx tsc --noEmit -p tsconfig.json` exits 0.
- `bun test --run client/pages/SearchPage.test.tsx` all pass.
- `rg -c "useSavedSearches|useSaveSearch|useSavedSearchCounts|useRelatedDocsScored|useEntityPageData|useDeleteSavedSearch" client/hooks/useTauri.ts` >= 6.
- `rg -c "ScoreBadge" client/pages/SearchPage.tsx` — >= 1 (import), and 0 hits for `function ScoreBadge` (extracted).
- `rg -c "useSearchParams" client/pages/SearchPage.tsx` >= 1.
- Manual smoke: `bun dev`, navigate to `/search?entity=Person:Bob`, one accent-tinted pill renders with an X remove button.
</verification>

<success_criteria>
- Six hooks landed with correct query keys, TTLs, and cache-invalidation onSuccess.
- Four new components created + typed.
- ScoreBadge extracted without visual regression.
- SearchPage honors URL params as source of truth for entity filters (D-01) — back button and shareability preserved.
- Save button is `variant="secondary"` per UI-SPEC §3 — accent fill only appears inside the Dialog on the primary Save action.
- Dialog uses sonner for success/error toasts per D-09.
- Vitest coverage for URL-driven filter rendering + removal.
</success_criteria>

<output>
Create `.planning/phases/11-entity-driven-exploration/11-07-SUMMARY.md` when done.
</output>

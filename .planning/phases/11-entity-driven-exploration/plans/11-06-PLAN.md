---
phase: 11-entity-driven-exploration
plan: 06
type: execute
wave: 3
depends_on: [11-01, 11-04]
files_modified:
  - src-tauri/src/commands/documents.rs
  - src-tauri/src/commands/entities.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements: [ENEX-01, ENEX-03]

must_haves:
  truths:
    - "get_related_docs_scored returns up to 5 documents ranked by 0.6*cosine + 0.4*jaccard, filtered by score >= 0.3, sorted desc"
    - "Jaccard is computed over {class}:{value} pair sets (not canonical_id sets) per D-11"
    - "get_related_docs_scored skips the target document itself"
    - "get_entity_page_data returns canonical + paginated docs (20/page) + co-occurring entities (top 10) per D-16"
    - "get_entity_page_data class/value lookup uses lowercase-insensitive alias_index resolution"
    - "Empty co-occurrence returns empty list (never panics)"
    - "Both commands registered in lib.rs invoke_handler"
  artifacts:
    - path: "src-tauri/src/commands/documents.rs"
      provides: "get_related_docs_scored IPC command"
      contains: "get_related_docs_scored"
    - path: "src-tauri/src/commands/entities.rs"
      provides: "get_entity_page_data IPC command"
      contains: "get_entity_page_data"
    - path: "src-tauri/src/lib.rs"
      provides: "Two new IPC registrations in invoke_handler"
      contains: "get_related_docs_scored"
  key_links:
    - from: "src-tauri/src/commands/documents.rs::get_related_docs_scored"
      to: "src-tauri/src/graph/entity_store.rs::EntityStore"
      via: "Reads doc metadata inside spawn_blocking to build Jaccard sets"
      pattern: "extracted_entities"
    - from: "src-tauri/src/commands/entities.rs::get_entity_page_data"
      to: "src-tauri/src/graph/entity_store.rs::EntityStore::doc_index"
      via: "Resolves class:value → canonical_id → doc_ids → docs + co-occurrence aggregate"
      pattern: "doc_index"
---

<objective>
Implement the two remaining Rust IPCs for Phase 11:

1. `get_related_docs_scored(doc_id, top_n)` — Pattern 3 in 11-RESEARCH.md. Hybrid ranking = `0.6*cosine + 0.4*jaccard` over `{class}:{value}` entity sets. Top 5 default; score floor 0.3 (D-12). Powers DocumentPage Related panel (ENEX-03).
2. `get_entity_page_data(class, value, page)` — Pattern 4 in 11-RESEARCH.md. Aggregates canonical + paginated docs (20/page) + top-10 co-occurring entities from `EntityStore`. Powers EntityDetailPage (ENEX-01 nav target).

Purpose: Complete the backend surface for Phase 11 UI. Wave 3 sits after Plan 04's `AppState` wiring lands so both commands can rely on the `saved_search_store` field ordering + the module registration pattern; more importantly, both commands add lines to `lib.rs::invoke_handler` which Plan 04 also modified — sequencing prevents merge conflict.
Output: New IPC commands + tests + lib.rs registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-entity-driven-exploration/11-CONTEXT.md
@.planning/phases/11-entity-driven-exploration/11-RESEARCH.md

@src-tauri/src/commands/documents.rs
@src-tauri/src/commands/entities.rs
@src-tauri/src/graph/entity_store.rs
@src-tauri/src/search/query.rs

<interfaces>
<!-- From EntityStore (VERIFIED: src-tauri/src/graph/entity_store.rs): -->
pub fn get_canonical(&self, canonical_id: &str) -> Option<CanonicalEntity>;
pub fn related_entities(&self, canonical_id: &str, min_co_occurrence: u32, limit: usize, engine: &CortexEngine) -> Result<Vec<RelatedEntity>, AppError>;
pub cosine(&[f32], &[f32]) -> f32;  // free function in same file

<!-- From search/query.rs (VERIFIED): -->
pub fn build_document_from_metadata(doc_id: &str, metadata: &HashMap<String, serde_json::Value>) -> Document;

<!-- Existing get_documents_for_entity pattern (VERIFIED: commands/entities.rs lines 59-114): -->
// Locks entity_store std::sync::Mutex INSIDE spawn_blocking (pitfall #1 mitigation)
// Then locks engine tokio::sync::Mutex via .blocking_lock() also INSIDE spawn_blocking
// Then reads collection metadata via collection_arc.read()

<!-- New Plan 11-01 types (VERIFIED: Wave 1 output): -->
pub struct RelatedDocScored { document: Document, score: f64, cosine_score: f64, jaccard_score: f64, snippet: Option<String> }
pub struct EntityPageData { canonical: CanonicalEntity, documents: Vec<Document>, total_document_count: u32, co_occurring_entities: Vec<RelatedEntityRef>, page: u32, page_size: u32 }
pub struct RelatedEntityRef { class: String, value: String, co_doc_count: u32 }

<!-- HNSW search API (used elsewhere in query.rs — grep for `.search(search_query)` for exact call pattern): -->
// let search_query = ruvector_core::types::SearchQuery { vector, k, filter, ef_search };
// col.db.search(search_query) → Vec<SearchResult>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement get_related_docs_scored in commands/documents.rs</name>
  <files>src-tauri/src/commands/documents.rs</files>
  <behavior>
    - Test A (pure cosine score): seeded engine with target doc and two neighbors sharing zero entities; both neighbors have cosine 0.9 and 0.7 respectively — final scores = 0.54 and 0.42; both pass 0.3 floor and return in that order.
    - Test B (pure Jaccard boost): two neighbors with identical cosine 0.5; neighbor-1 shares 2 entities with target (Jaccard=1.0) and neighbor-2 shares 0 → neighbor-1 wins by margin `0.4 * 1.0 = 0.4`. Assert order.
    - Test C (score floor 0.3): neighbor with cosine 0.4 (contributes 0.24) and no entity overlap → total 0.24 → filtered out. Assert not in result.
    - Test D (top_n=5 truncation): seed 10 neighbors above threshold; assert result length is exactly 5 and sorted descending.
    - Test E (target self-exclusion): HNSW returns target as top match; result does not include target doc_id.
    - Test F (empty entity metadata): target and neighbors have no `extracted_entities` metadata → Jaccard=0 for all; cosine-only scoring applies; no panic.
  </behavior>
  <action>
Append `pub async fn get_related_docs_scored(doc_id: String, top_n: Option<usize>, state: State<'_, AppState>) -> Result<Vec<RelatedDocScored>, AppError>` to `src-tauri/src/commands/documents.rs`.

Implementation (mirror Pattern 3 in 11-RESEARCH.md verbatim, adapted to the codebase's actual `AppState` field names — `state.engine`, `state.entity_store`):

- Extract `let engine = state.engine.clone(); let entity_store = state.entity_store.clone(); let n = top_n.unwrap_or(5);`
- Enter `tokio::task::spawn_blocking(move || { ... }).await??` block. Everything below is inside the closure.
- `let engine_guard = engine.blocking_lock();` — engine is tokio Mutex; use blocking_lock inside spawn_blocking (mirror `commands::entities::get_documents_for_entity`).
- `let _store_guard = entity_store.lock().map_err(|e| AppError::Internal(e.to_string()))?;` — held only if needed; the target/neighbor entity sets come from doc metadata, so this guard may be unnecessary. Prefer NOT to hold `entity_store` if the entity sets are derived from `extracted_entities` metadata (which is the case in Pattern 3). Comment inline: "// entity_store lock not required — entity sets sourced from doc metadata".
- Fetch the target doc's `VectorEntry` from `documents_384` collection. Extract `target_vec = entry.vector.clone()` and build `target_entity_set: HashSet<String>` by iterating `metadata["extracted_entities"]` and collecting `format!("{}:{}", class, value)` for each entity where BOTH `class` and `value` are present. If Phase 6 entities are present with only `entityType` (lowercase), use the capitalized class map via a small `capitalize_class` helper — mirror the logic from Plan 03's Phase-6/8 bridge (pitfall #5).
- Run HNSW search with `k=20` and no filter (raw cosine neighbors).
- For each raw result: filter out `r.id == doc_id` (test E). Build neighbor entity set from `r.metadata["extracted_entities"]` the same way. Compute Jaccard = `|target ∩ neighbor| / |target ∪ neighbor|`; both empty → Jaccard 0.
- Convert raw HNSW score to cosine: `let cosine = (1.0 - r.score as f64).clamp(0.0, 1.0);` — check whether `r.score` is a distance (1 - cosine) or already cosine. Grep the existing pattern in `search/query.rs` to confirm (the same conversion appears near `build_document_from_metadata`). Use the SAME conversion the codebase already uses; do not reinvent.
- Composite: `let score = 0.6 * cosine + 0.4 * jaccard;` Filter out `score < 0.3`.
- Build `Document` via `crate::search::query::build_document_from_metadata(&r.id, meta)`.
- Compute `snippet`: pull the first entity-overlap value from `target_entity_set ∩ neighbor_entity_set` and slice ~120 chars around its first occurrence in the neighbor's text metadata (if `metadata["text"]` or `metadata["excerpt"]` present). When no overlap exists, `snippet = None`. This is a simple heuristic — do NOT over-engineer. If neither field exists, `snippet = None`.
- Collect into `Vec<RelatedDocScored>`, `.sort_by(|a, b| b.score.partial_cmp(&a.score).unwrap_or(Ordering::Equal))`, `.truncate(n)`, return.

Add unit tests A–F using an in-memory `CortexEngine` and manually crafted `VectorEntry` records with `extracted_entities` metadata. Follow the fixture style used in `commands::entities::tests::make_seeded_store` for AppState-adjacent tests. Where a full spawn_blocking test is heavy, extract the scoring math into a small pure helper `compute_composite_score(cosine: f64, jaccard: f64) -> f64` and test the helper directly (this simplifies tests A/B/C to arithmetic assertions without a live engine).

Do NOT modify or delete the existing `get_related_documents` (Phase 3 graph-edge-based) — the new command is additive. Per Assumption A1 in 11-RESEARCH.md, both coexist; DocumentPage will switch from the old one to the new one in Plan 09.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/cortex/src-tauri && cargo test --lib commands::documents::tests::test_(related_scored|composite_score) 2>&amp;1 | tail -20 &amp;&amp; rtk proxy rg "pub async fn get_related_docs_scored" src/commands/documents.rs -c</automated>
  </verify>
  <done>get_related_docs_scored implemented; helper `compute_composite_score` unit-tested; tests A/B/C/D pass; existing `get_related_documents` untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement get_entity_page_data in commands/entities.rs</name>
  <files>src-tauri/src/commands/entities.rs</files>
  <behavior>
    - Test A (class+value resolves to canonical): seeded store with canonical "cid-p1" at ("alex doe", "person") mapped to docs {"d1","d2","d3"}. Call with `class="Person", value="Alex Doe", page=0`. Assert `canonical.canonical_name == "Alex Doe"`, `total_document_count == 3`, `page == 0`, `page_size == 20`.
    - Test B (case-insensitive resolution): call with `value="ALEX DOE"` — same canonical returned.
    - Test C (Phase 6/8 class fallback): store has ("acme corp","organization"); call with `class="Organization"` (Phase 8 capitalized) → resolves.
    - Test D (unknown entity returns NotFound): call with an unregistered class/value pair; assert `Err(AppError::NotFound(...))`.
    - Test E (pagination): 25 docs across page 0 → 20 returned, `total_document_count==25`; page 1 → 5 returned.
    - Test F (co-occurring entities): seed a co-occurring canonical "cid-p2" appearing in 2 of the 3 target docs; call returns co_occurring_entities with `[{class:"Person", value:"Bob", co_doc_count:2}]`.
    - Test G (empty co-occurrence returns empty list): entity with docs but no other entities → co_occurring_entities is empty; no panic.
  </behavior>
  <action>
Append `pub async fn get_entity_page_data(class: String, value: String, page: Option<u32>, state: State<'_, AppState>) -> Result<EntityPageData, AppError>` to `src-tauri/src/commands/entities.rs`.

Semantics per Pattern 4 in 11-RESEARCH.md:

- Constants: `const PAGE_SIZE: u32 = 20;` — per D-16.
- Inside `spawn_blocking`:
  - Lock entity_store std::sync::Mutex. Attempt alias_index lookup with `(value.to_lowercase(), class.clone())`; if miss, retry with `(value.to_lowercase(), class.to_lowercase())` — Phase 6/8 bridge (pitfall #5). If both miss → `Err(AppError::NotFound(format!("entity not found: {}:{}", class, value)))`.
  - Fetch `CanonicalEntity` via `store.get_canonical(&canonical_id)` (Option). If missing (should not happen given alias_index hit, but defensive), return NotFound.
  - Fetch full `doc_index[canonical_id]` and sort deterministically (e.g. `.into_iter().collect::<Vec<_>>()` then `.sort()`). Deterministic order = stable pagination across page reloads.
  - `total_document_count = sorted_ids.len() as u32`.
  - `page_start = page.unwrap_or(0) as usize * PAGE_SIZE as usize`.
  - Slice `sorted_ids[page_start .. page_start + PAGE_SIZE]` (guard bounds).
  - For each id in the slice, look up its `VectorEntry.metadata` from the `documents_384` collection and call `build_document_from_metadata(id, metadata)` — mirror `get_documents_for_entity`.
  - Compute co-occurring entities: iterate ALL doc_ids (not just page slice — the co-occurrence signal must aggregate across the full doc set) via the collection, accumulate a `HashMap<(String, String), u32>` keyed by `(class, value)` counting entities OTHER than the target. Use the SAME class-normalization helper as Task 1 (both use `capitalize_class`). Sort desc by count, truncate to 10, map to `Vec<RelatedEntityRef>` with `co_doc_count`.
  - Assemble and return `EntityPageData { canonical, documents, total_document_count, co_occurring_entities, page: page.unwrap_or(0), page_size: PAGE_SIZE }`.

Follow the mutex discipline of `get_documents_for_entity` (drop entity_store guard before entering the engine lock section if you can — collect doc_ids first, then release entity_store guard, then engine.blocking_lock; but if the collection-read section needs entity_store to resolve co-occurring canonical → alias, hold both).

Add unit tests A–G. Where full engine wiring is heavy, factor out `aggregate_co_occurrence(docs_metadata: &[serde_json::Value], target_class: &str, target_value: &str) -> Vec<RelatedEntityRef>` into a pure helper. Tests F/G can drive the pure helper directly.

Do NOT modify `get_documents_for_entity` or `get_related_entities` — Phase 6 IPCs stay untouched. The Phase 6 `/entities/:id` route still uses the old IPCs per Open Q 2 resolution.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/cortex/src-tauri && cargo test --lib commands::entities::tests::test_(entity_page|aggregate_co_occurrence) 2>&amp;1 | tail -20 &amp;&amp; rtk proxy rg "pub async fn get_entity_page_data" src/commands/entities.rs -c</automated>
  </verify>
  <done>get_entity_page_data implemented; helper aggregate_co_occurrence unit-tested; tests A/B/C/D/E/F/G pass; Phase 6 IPCs untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Register both commands in lib.rs invoke_handler</name>
  <files>src-tauri/src/lib.rs</files>
  <action>
Append these two lines to the `invoke_handler(tauri::generate_handler![ ... ])` list in `src-tauri/src/lib.rs`, immediately after the Phase 11 saved-searches block that Plan 04 added:

```
// Phase 11 — Related docs + entity page (ENEX-01, ENEX-03)
commands::documents::get_related_docs_scored,
commands::entities::get_entity_page_data,
```

No other change to lib.rs. Run `cargo build --manifest-path src-tauri/Cargo.toml` to prove IPC types are correct and reachable.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/cortex/src-tauri && cargo build 2>&amp;1 | tail -15 &amp;&amp; rtk proxy rg "get_related_docs_scored|get_entity_page_data" src/lib.rs -c</automated>
  </verify>
  <done>cargo build clean; both IPC names appear once each in lib.rs invoke_handler.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend → get_related_docs_scored | doc_id is user-controlled URL param |
| frontend → get_entity_page_data | class + value are user-controlled URL params |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-17 | Input Validation | Unknown doc_id in get_related_docs_scored | mitigate | Return AppError::NotFound; do not silently return empty results (that would mask indexing bugs) |
| T-11-18 | Input Validation | Unknown class+value in get_entity_page_data | mitigate | Return AppError::NotFound; UI (Plan 08) renders empty-state per D-18 when frontend catches NotFound |
| T-11-19 | Deadlock | entity_store std::sync::Mutex + engine tokio Mutex held incorrectly | mitigate | All locks acquired inside spawn_blocking; entity_store lock dropped before any await (pitfall #1) |
| T-11-20 | DoS | Entity with 100,000 mentions triggers massive co-occurrence scan | accept | Personal-corpus scale; if this becomes a hotspot in real usage, Phase 13 (Cypher graph) accelerates via ruvector-graph |
| T-11-21 | Score manipulation | Malicious extracted_entities metadata (crafted to inflate Jaccard) | accept | Metadata originates from local LLM extraction, not remote user input; low-value target |
</threat_model>

<verification>
- `cargo test --lib commands::documents::` and `cargo test --lib commands::entities::` — all new tests pass; pre-existing tests remain green.
- `cargo build --manifest-path src-tauri/Cargo.toml` exits 0.
- `rg -c "get_related_docs_scored" src/lib.rs` == 1.
- `rg -c "get_entity_page_data" src/lib.rs` == 1.
- `rg -c "0\.6 \* cosine" src/commands/documents.rs` >= 1 (weights explicit in code).
- `rg -c "0\.3" src/commands/documents.rs` >= 1 (score floor explicit).
</verification>

<success_criteria>
- `get_related_docs_scored` implements exact `0.6*cosine + 0.4*jaccard` formula, top_n default 5, score floor 0.3, self-exclusion, snippet extraction (best-effort).
- `get_entity_page_data` implements class/value → canonical resolution (with Phase 6/8 bridge), 20/page pagination, top-10 co-occurring entities aggregated across all target docs.
- Both commands registered in lib.rs invoke_handler.
- Unit tests cover scoring math, pagination bounds, co-occurrence aggregation, empty-set edge cases.
- Phase 6 IPCs unchanged; new commands are additive.
</success_criteria>

<output>
Create `.planning/phases/11-entity-driven-exploration/11-06-SUMMARY.md` when done.
</output>

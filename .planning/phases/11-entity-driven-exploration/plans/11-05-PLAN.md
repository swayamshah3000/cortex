---
phase: 11-entity-driven-exploration
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - client/components/entities/EntityChip.tsx
  - client/components/entities/EntityChip.test.tsx
autonomous: true
requirements: [ENEX-01]

must_haves:
  truths:
    - "Left-clicking an EntityChip navigates to /search?entity={class}:{value} (URL-encoded)"
    - "Right-clicking (onContextMenu) an EntityChip navigates to /entity/{class}/{value} (URL-encoded) and preventDefault suppresses the native context menu"
    - "isActive prop renders the chip with accent styling (bg-accent-subtle text-accent-primary border-accent-primary/20)"
    - "aria-label communicates dual navigation: 'Filter by {class}: {value}. Right-click for entity detail page.'"
    - "Existing prop shape (entity: {value, entityType, canonicalId?, class?, subclass?, confidence?}) still accepted — Phase 6 callers unchanged"
  artifacts:
    - path: "client/components/entities/EntityChip.tsx"
      provides: "Dual-navigation EntityChip with optional isActive styling"
      contains: "onContextMenu"
    - path: "client/components/entities/EntityChip.test.tsx"
      provides: "Vitest coverage of left-click, right-click, isActive, aria-label"
      contains: "onContextMenu"
      min_lines: 40
  key_links:
    - from: "client/components/entities/EntityChip.tsx"
      to: "react-router-dom useNavigate"
      via: "navigate() called on both onClick and onContextMenu"
      pattern: "useNavigate"
    - from: "client/components/entities/EntityChip.tsx"
      to: "URL param format {class}:{value}"
      via: "encodeURIComponent for both navigation targets"
      pattern: "encodeURIComponent"
---

<objective>
Refactor `EntityChip` from a single-click `<Link>` (Phase 6 behavior) into a dual-navigation `<button>` per D-02, D-03, D-17 and the UI-SPEC §1 control specification. Left click → `/search?entity={class}:{value}` (filters SearchPage). Right click / long-press / `onContextMenu` → `/entity/{class}/{value}` (Phase 11 EntityDetailPage — Plan 08 creates that route). Add optional `isActive` prop to render accent-tinted styling when the chip's `{class}:{value}` matches an active filter param on SearchPage.

Purpose: Delivers ENEX-01 UI contract. This plan is self-contained — no backend dependency, so it lives in Wave 1 alongside the type foundation and store module.
Output: Updated `EntityChip.tsx` + `EntityChip.test.tsx` with dual-navigation coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-entity-driven-exploration/11-CONTEXT.md
@.planning/phases/11-entity-driven-exploration/11-UI-SPEC.md
@.planning/phases/11-entity-driven-exploration/11-RESEARCH.md

@client/components/entities/EntityChip.tsx

<interfaces>
<!-- Existing EntityChip prop shape (VERIFIED: client/components/entities/EntityChip.tsx lines 89-99): -->
interface EntityChipProps {
  entity: {
    value: string;
    entityType: string;         // legacy Phase 6 lowercase string; e.g. "person"
    canonicalId?: string;
    class?: string;              // Phase 8 capitalized; e.g. "Person"
    subclass?: string;
    confidence?: number;
  };
}

<!-- Existing mapLegacyEntityTypeToClass helper (VERIFIED, lines 50-77): -->
// Maps entityType ("person", "organization", ...) → class ("Person", "Organization", ...).
// Fallback used when Phase 6 entities lack a class field.

<!-- Current render (VERIFIED, lines 102-119) returns a <Link to={`/entities/${canonicalId ?? encodeURIComponent(value)}`}> -->
<!-- Phase 11 replaces this with a <button> that intercepts click + contextmenu. -->

<!-- URL-SPEC §1 control spec (from 11-UI-SPEC.md): -->
// className default:  "inline-flex items-center gap-2 px-2 py-1 rounded-full border border-border-secondary bg-bg-tertiary hover:bg-accent-subtle focus-visible:ring-2 focus-visible:ring-accent-primary focus-visible:ring-offset-2 focus-visible:outline-none transition-colors cursor-pointer"
// className active:   swap bg-bg-tertiary → bg-accent-subtle, border-border-secondary → border-accent-primary/20, text-text-primary → text-accent-primary
// Padding correction: existing uses px-2.5 (sub-grid); phase 11 uses px-2 (8px, sm token) per UI-SPEC §1
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor EntityChip to dual-navigation button + isActive styling + tests</name>
  <files>client/components/entities/EntityChip.tsx, client/components/entities/EntityChip.test.tsx</files>
  <behavior>
    - Test 1 (left click): `render(<EntityChip entity={{value:"Alex Doe", entityType:"person"}} />)` inside a `MemoryRouter`, wire a Routes → `<Route path="/search" element={<span>SEARCH:{'{'}useLocation().search{'}'}</span>} />` component; user event `click` on the chip; assert the routed component text contains `?entity=Person%3AAlex%20Shah`. Confirms class fallback from entityType + URL-encoding of `:` as `%3A` and space as `%20`.
    - Test 2 (right click / onContextMenu): same setup with a `/entity/:class/:value` route rendering `<span>ENTITY:{'{'}params.class{'}'}/{'{'}params.value{'}'}</span>`; use `fireEvent.contextMenu` on the chip; assert the routed span shows `ENTITY:Person/Alex Doe` (params get URL-decoded by React Router). Also assert native context-menu-open was suppressed via `preventDefault`.
    - Test 3 (isActive styling): render with `isActive={true}`; query the button by role; assert its `className` contains `bg-accent-subtle`, `text-accent-primary`, and `border-accent-primary/20`.
    - Test 4 (default styling): render with `isActive={false}` (or omit prop); assert className contains `bg-bg-tertiary` and does NOT contain `text-accent-primary`.
    - Test 5 (Phase 8 explicit class): render with `entity={{value:"AlphaComplex", entityType:"organization", class:"Location"}}` — the URL param must use `Location:AlphaComplex`, not the entityType fallback.
    - Test 6 (aria-label): assert the button has an aria-label matching `Filter by Person: Alex Doe. Right-click for entity detail page.` (or the exact string used in the component).
  </behavior>
  <action>
Refactor `client/components/entities/EntityChip.tsx`:

(1) Change the import: replace `import { Link } from "react-router-dom";` with `import { useNavigate } from "react-router-dom";`.

(2) Extend the `EntityChipProps` interface with `isActive?: boolean;` (default false when omitted).

(3) Replace the `<Link>` render (lines 102-119) with a `<button>` per UI-SPEC §1. Use `resolvedClass` (existing logic — prefer `entity.class`, fall back to `mapLegacyEntityTypeToClass(entity.entityType)`). If both are unavailable, fall back to the raw `entity.entityType` string as a last resort (never navigate with `undefined`).

Handlers:
```
const navigate = useNavigate();
const paramClass = resolvedClass ?? entity.entityType;
const handleClick = () => {
  navigate(`/search?entity=${encodeURIComponent(`${paramClass}:${entity.value}`)}`);
};
const handleContextMenu = (e: React.MouseEvent) => {
  e.preventDefault();
  navigate(`/entity/${encodeURIComponent(paramClass)}/${encodeURIComponent(entity.value)}`);
};
```

Use `type="button"` on the `<button>` to prevent any accidental form-submit context.

className: composed via `cn(...)` from `client/lib/utils`. Base classes exactly per UI-SPEC §1 (see `<interfaces>` block). When `isActive`, swap to accent variant. Do NOT introduce hex values or new tokens.

Padding correction per UI-SPEC §1: use `px-2` (8px sm token), not the current `px-2.5`. This is intentional — the UI-SPEC calls out the existing sub-grid deviation.

aria-label: `Filter by ${paramClass}: ${entity.value}. Right-click for entity detail page.`

Keep `showSubclassBadge` logic (Identifier subclass rendering) unchanged.

(4) Create `client/components/entities/EntityChip.test.tsx` with the six tests from `<behavior>`. Use `@testing-library/react` + `@testing-library/user-event` + `MemoryRouter` from `react-router-dom`. Follow the same testing style as any existing `.test.tsx` (grep `client/components/*.test.tsx` for one to mirror imports).

(5) Optional pitfall #7 mitigation (from 11-RESEARCH.md): touch-based long-press support is out of scope for Phase 11 (desktop-first). onContextMenu handles right-click on macOS/Linux/Windows. Document this in a `// TODO(Phase 11 v2)` comment referencing pitfall #7 — no implementation now.

Run tests: `bun test --run client/components/entities/EntityChip.test.tsx`.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/cortex &amp;&amp; bun test --run client/components/entities/EntityChip.test.tsx 2>&amp;1 | tail -25</automated>
  </verify>
  <done>Six tests pass; EntityChip renders as `<button type="button">`; useNavigate used for both left-click and onContextMenu; isActive prop drives accent styling; grep confirms `encodeURIComponent` at both navigate call sites.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| entity value → URL | Untrusted entity value strings become path/query segments |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-14 | Tampering | Special chars in entity value break URL routing | mitigate | `encodeURIComponent` at both navigate call sites (test 1 asserts encoding) |
| T-11-15 | UI Confusion | Right-click may open native context menu instead of navigate | mitigate | `e.preventDefault()` inside onContextMenu; test 2 asserts routed span appears (native menu would prevent that) |
| T-11-16 | Regression | Existing DocumentPage renders lose navigation to /entities/:canonicalId | accept | Deliberate: Phase 11 replaces /entities/:canonicalId (Phase 6 route) with /entity/{class}/{value} (Phase 11 route). Phase 6 route stays registered per Open Q 2 resolution; users reach it only via the direct URL, not via chip clicks. |
</threat_model>

<verification>
- `bun test --run client/components/entities/EntityChip.test.tsx` — 6/6 tests pass.
- `rg -c "onContextMenu" client/components/entities/EntityChip.tsx` >= 1.
- `rg -c "useNavigate" client/components/entities/EntityChip.tsx` >= 1.
- `rg -c 'to=".*/entities/' client/components/entities/EntityChip.tsx` — 0 hits (no leftover Link).
- Manual smoke (post-integration in Plan 07): opening any DocumentPage, left-click an entity chip → SearchPage URL contains `?entity=...`.
</verification>

<success_criteria>
- EntityChip refactored to `<button>` with useNavigate-driven dual navigation.
- URL params use `{class}:{value}` for search and `/entity/{class}/{value}` for detail page; both encoded.
- `isActive?: boolean` prop drives accent styling.
- 6 unit tests exist and pass.
- Padding corrected to grid-aligned `px-2`.
- Phase 6 backward compat preserved: chips lacking Phase 8 class field fall back through mapLegacyEntityTypeToClass, then raw entityType.
</success_criteria>

<output>
Create `.planning/phases/11-entity-driven-exploration/11-05-SUMMARY.md` when done.
</output>

---
phase: 11-entity-driven-exploration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/saved_searches/mod.rs
  - src-tauri/src/saved_searches/store.rs
autonomous: true
requirements: [ENEX-02, ENEX-04]

must_haves:
  truths:
    - "SavedSearchStore round-trips: entries written by save() reload identically via load() including all filter fields"
    - "SavedSearchStore::load() returns Default::default() on missing/malformed JSON — never panics"
    - "Adding a second saved search does not lose the first (append semantics + file overwrite)"
    - "Malformed JSON file yields empty store, matching SpaceLabelCache resilience"
  artifacts:
    - path: "src-tauri/src/saved_searches/mod.rs"
      provides: "pub mod store; declares the saved_searches feature module"
      contains: "pub mod store"
    - path: "src-tauri/src/saved_searches/store.rs"
      provides: "SavedSearchStore struct + load/save/insert/remove/get_all accessors + full test suite"
      contains: "pub struct SavedSearchStore"
      min_lines: 100
  key_links:
    - from: "src-tauri/src/saved_searches/store.rs"
      to: "app_data_dir/saved_searches.json"
      via: "std::fs::write / std::fs::read_to_string on {app_data_dir}/saved_searches.json"
      pattern: "saved_searches\\.json"
    - from: "src-tauri/src/saved_searches/store.rs"
      to: "src-tauri/src/types.rs::SavedSearch"
      via: "SavedSearchStore.saved_searches: Vec<SavedSearch>"
      pattern: "Vec<SavedSearch>"
---

<objective>
Create the `saved_searches` Rust module with a JSON-sidecar `SavedSearchStore` that mirrors the exact shape of Phase 9's `SpaceLabelCache` — same load/save resilience, same silent-default-on-error contract, same `Arc<tokio::sync::Mutex<T>>` wrapping convention (mutex applied by Plan 04 at the `AppState` layer, not here).

Purpose: Ship a self-contained, unit-tested persistence layer for saved searches before any IPC command depends on it. Plan 04 will wrap this store in `Arc<Mutex<>>` and expose CRUD IPCs; Plan 03 does not touch this file.
Output: `src-tauri/src/saved_searches/mod.rs` + `src-tauri/src/saved_searches/store.rs` with 6+ passing unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-entity-driven-exploration/11-CONTEXT.md
@.planning/phases/11-entity-driven-exploration/11-RESEARCH.md

@src-tauri/src/spaces/label_cache.rs

<interfaces>
<!-- Canonical mirror pattern. Plan 02 replicates this file's structure for saved_searches. -->

From src-tauri/src/spaces/label_cache.rs:
pub struct SpaceLabelCache { pub labels: HashMap<String, SpaceLabelEntry> }
impl SpaceLabelCache {
  pub fn load(app_data_dir: &Path) -> Self  // returns Default on any error
  pub fn save(&self, app_data_dir: &Path) -> std::io::Result<()>
  pub fn get(&self, space_id: &str) -> Option<&SpaceLabelEntry>
  pub fn insert(&mut self, space_id: String, entry: SpaceLabelEntry)
  pub fn remove(&mut self, space_id: &str)
  pub fn is_user_locked(&self, space_id: &str) -> bool
}

From Plan 11-01, src-tauri/src/types.rs (created in Wave 1):
pub struct SavedSearch { id, name, query, filters, created_at, doc_count_cache }
pub struct SavedSearchFilters { entities, topic, doc_type, space_id, date_from, date_to, tags }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create saved_searches module + SavedSearchStore with round-trip tests</name>
  <files>src-tauri/src/saved_searches/mod.rs, src-tauri/src/saved_searches/store.rs</files>
  <behavior>
    - Test 1 (empty default): `SavedSearchStore::default()` produces `saved_searches: Vec::new()`.
    - Test 2 (load-missing): `SavedSearchStore::load(nonexistent_path)` returns empty default — never panics.
    - Test 3 (roundtrip preserves all fields): insert two SavedSearch entries with distinct filters (one entity-only filter, one entity+topic+dateFrom), save, reload, assert every field matches (id, name, query, filters.entities, filters.topic, filters.doc_type, filters.date_from, created_at, doc_count_cache).
    - Test 4 (malformed JSON returns empty default): pre-write garbage to `saved_searches.json`, call `load()`, assert empty result — no panic.
    - Test 5 (save overwrites): call `save()` twice with different content, `load()` returns the second content.
    - Test 6 (file at correct path): after save, assert `{tmp_dir}/saved_searches.json` exists (not a subdirectory, not a differently-named file).
  </behavior>
  <action>
Create `src-tauri/src/saved_searches/mod.rs` containing a single line: `pub mod store;`. This makes `saved_searches::store` importable. Plan 04 will add `pub mod commands;` to this file.

Create `src-tauri/src/saved_searches/store.rs` implementing `SavedSearchStore` as an exact structural mirror of `src-tauri/src/spaces/label_cache.rs`. Concretely:

- File-scoped doc comment describes the sidecar path `{app_data_dir}/saved_searches.json`, D-05 provenance, and thread-safety note (wrap in `Arc<tokio::sync::Mutex<>>` at AppState — done by Plan 04, not here).
- Import `use crate::types::SavedSearch;` and `use serde::{Deserialize, Serialize};`, `use std::path::Path;`.
- Struct `pub struct SavedSearchStore` with `#[derive(Debug, Clone, Serialize, Deserialize, Default)]` and a single public field `pub saved_searches: Vec<SavedSearch>`. Use camelCase serde (`#[serde(rename_all = "camelCase")]`) so the JSON key is `savedSearches` — matches D-06 shape.
- `impl SavedSearchStore` block with the following methods, all synchronous, all mirroring `SpaceLabelCache` semantics:
  - `pub fn load(app_data_dir: &Path) -> Self` — reads `{app_data_dir}/saved_searches.json`; returns `Default::default()` on any I/O or JSON parse error. Never panics. Same 3-line pattern as `SpaceLabelCache::load`.
  - `pub fn save(&self, app_data_dir: &Path) -> std::io::Result<()>` — writes `{app_data_dir}/saved_searches.json` via `serde_json::to_string_pretty` + `std::fs::write`. Same 5-line pattern as `SpaceLabelCache::save`.
  - `pub fn get(&self, id: &str) -> Option<&SavedSearch>` — linear scan of `saved_searches`.
  - `pub fn insert(&mut self, search: SavedSearch)` — pushes if id absent, replaces in-place if id already present. This matches CRUD semantics needed by Plan 04's `save_search` command.
  - `pub fn remove(&mut self, id: &str) -> bool` — retains all searches with a different id; returns true if a removal happened.
  - `pub fn all(&self) -> &[SavedSearch]` — returns the full slice; used by `get_saved_searches` in Plan 04.

Add `#[cfg(test)] mod tests` with the six tests listed under `<behavior>`. Use `tempfile::TempDir` for filesystem tests (dep already in Cargo.toml — verify with `rg tempfile src-tauri/Cargo.toml`; if missing, add it — but it should already be present since `label_cache.rs` uses it).

Register the module: append `pub mod saved_searches;` to `src-tauri/src/lib.rs` at the top-level module list (near existing `pub mod spaces;`). This is the ONLY line Plan 02 adds to `lib.rs`; Plan 04 will add IPC registration.

Do NOT add commands, IPC handlers, or AppState wiring — that is Plan 04's scope. This plan produces only the persistence primitive + tests.

Run `cargo test --manifest-path src-tauri/Cargo.toml --lib saved_searches::` to prove all six tests pass.
  </action>
  <verify>
    <automated>cd /Users/gshah/work/apps/cortex/src-tauri && cargo test --lib saved_searches:: 2>&amp;1 | tail -20</automated>
  </verify>
  <done>File `src-tauri/src/saved_searches/store.rs` exists with `SavedSearchStore` + 6+ tests; `cargo test --lib saved_searches::` shows all tests passing; `lib.rs` contains `pub mod saved_searches;`; `cargo check` succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → in-memory struct | `saved_searches.json` on disk is user-controllable if the app_data_dir is writable by another process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Tampering | Malformed `saved_searches.json` on disk crashes SavedSearchStore::load | mitigate | `.ok().and_then(...).unwrap_or_default()` chain — mirror of SpaceLabelCache::load (test 4 asserts this) |
| T-11-05 | DoS | Extremely large saved_searches.json exhausts memory | accept | Personal-corpus scale (< 1000 saved searches realistic); no upper bound needed at v1 |
| T-11-06 | Corruption | Torn write during save from concurrent AppState mutations | mitigate | Plan 04 wraps store in `Arc<tokio::sync::Mutex<>>`; std::fs::write is atomic replace on POSIX. Documented in file-level comment. |
</threat_model>

<verification>
- `cargo test --manifest-path src-tauri/Cargo.toml --lib saved_searches::` — all 6 tests pass.
- `rg -c "pub struct SavedSearchStore" src-tauri/src/saved_searches/store.rs` returns 1.
- `rg -c "pub mod saved_searches;" src-tauri/src/lib.rs` returns 1.
- No warnings introduced: `cargo check --manifest-path src-tauri/Cargo.toml 2>&1 | grep -c warning` does not increase for the new file.
</verification>

<success_criteria>
- `src-tauri/src/saved_searches/{mod.rs, store.rs}` created.
- `SavedSearchStore` implements load/save/get/insert/remove/all with `SpaceLabelCache`-identical resilience semantics.
- 6+ unit tests present and all passing.
- Module registered in `lib.rs` via `pub mod saved_searches;`.
- No IPC command code in this plan (deferred to Plan 04).
</success_criteria>

<output>
Create `.planning/phases/11-entity-driven-exploration/11-02-SUMMARY.md` when done.
</output>
